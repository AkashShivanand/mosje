# Archived — superseded NHAPOA run artifacts

Kept for provenance, not part of the live pipeline. The current source-of-truth is
`../build_final_report.py` + `../sync_data.py`; coverage/mapping live in `../out/`.

- `COVERAGE.md`, `INVENTORY.md` — Phase-A capture ledger/checklist (now `out/coverage-ledger.json`).
- `author.py`, `journey_extract.py` — one-off curation/capture drivers (now the engine + build_final_report.py).
- `rg-design-frames.json` — one-off Phase-0 input.
- `NHA-9-VERIFICATION.md` — a portal-BUILD verification doc for `apps/portals/nhapoa` (not a design-QC artifact); misfiled here.
