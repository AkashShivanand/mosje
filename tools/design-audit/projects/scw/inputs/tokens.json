{
  "_note": "SOURCE CAVEAT: the Figma desktop-tethered MCP (get_variable_defs) could not resolve SCW's design nodes in this session (the file's SCW page wasn't loaded in the active Figma desktop tab — repeated 'node not found'/timeout errors, see scw-port-report.md). These values were instead sampled from computed CSS across the live SCW build (scw-user-uat.mosje.in: home, login, our-services) — i.e. the portal's OWN observed navy/orange/neutral palette, cross-checked against the shape already used in projects/_template/inputs/tokens.example.json (which itself uses #003366 as its example navy — a strong signal it was modelled on SCW). A human should re-run Phase 0 with the Figma desktop app on the SCW page loaded to replace this with true design-variable-sourced tokens; until then this is a reasonable interim baseline, not a design-system export.",
  "colors": ["#003366", "#ffffff", "#ff671f", "#1f2937", "#374151", "#6b7280", "#9ca3af", "#e5e7eb", "#198754", "#e5eff9", "#f9fafb"],
  "radii": [0, 4, 6, 8, 16, 9999],
  "fontSizes": [10, 11, 12, 14, 16, 18, 20, 24, 30, 48],
  "fontFamilies": ["Noto Sans"]
}
