# design-qc assertion failures (last run)

Every pin must fall inside its element box inside the crop inside the capture.
Misses below mean a wrong match or a too-short capture — fix before shipping.

_No failures — all pins asserted inside their element + crop._