# Dependencies
node_modules/

# Independent app repos — each has its own git history, versioned separately
/dosje/
/portals/

# Raw source material — never commit (21 GB of drops)
/Incoming/

# Large binary design exports — keep in Figma / Git LFS, not here
/Designs/

# Build outputs & caches
.next/
out/
*.tsbuildinfo
.turbo/

# Secrets & local environment
.env
.env.*
!.env.example
*.pem
*.key
secrets*

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/*
!.vscode/extensions.json
