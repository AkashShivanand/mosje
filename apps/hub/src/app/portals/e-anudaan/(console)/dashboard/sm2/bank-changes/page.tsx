"use client";

import { ChangeRequestDesk } from "@/components/e-anudaan/change-request-desk";

/** Bank Account Changes — the Joint Secretary, Programme Division, decides an NGO's request (inventory §36). */
export default function BankChangesPage() {
  return <ChangeRequestDesk kind="bank" />;
}
