"use client";

import { ApplicationExplorer } from "@/components/e-anudaan/application-explorer";

/** All Applications — each Programme Division grade's own path (verify bug 9); see ApplicationExplorer. */
export default function PdAllApplicationsPage() {
  return <ApplicationExplorer />;
}
