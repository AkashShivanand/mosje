import { SitePageHeader } from "@mosje/design-system";
import * as React from "react";

export function Specimen(): React.JSX.Element {
  return (
    <SitePageHeader
      eyebrow="Schemes"
      title="Scheme of Assistance for Senior Citizens"
      lead="Support for the care, dignity and welfare of older persons across the country."
    />
  );
}
