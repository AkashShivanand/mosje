import * as React from "react";
import type { Metadata } from "next";
import { Callout } from "@/components/design-system/docs-kit/index";

export const metadata: Metadata = {
  title: "Changelog",
  description:
    "Every release of the SAMAVESH design system — what was added, changed, and fixed in each version.",
};

interface ChangeEntry {
  /**
   * `Breaking` was missing until 2026-09-02, so across ninety-four releases
   * nothing was ever marked as breaking — including the retirement of the
   * `--ds-*` token contract, which is the most severe change a token system can
   * make and which a consumer could only discover by their application
   * silently losing its styles. A changelog that cannot say what broke is a
   * list of news, not a release note.
   */
  kind: "Breaking" | "Added" | "Changed" | "Fixed" | "Removed";
  text: string;
  /** Required in spirit on `Breaking`: where a consumer goes to migrate. */
  migration?: string;
}

interface Release {
  version: string;
  date: string;
  current?: boolean;
  changes: ChangeEntry[];
}

const RELEASES: Release[] = [
  {
    version: "v0.104.0",
    date: "2026-09-05",
    current: true,
    changes: [
      { kind: "Changed", text: "THE SIDEBAR IS ONE COMPONENT AGAIN, AND IT GOES THREE LEVELS DEEP. The audit of 2026-09-05 found two libraries publishing `sidebar/type-1` (the handoff used the Portal DS copy, the code documented the SAMAVESH one), an active label bound to a REMOTE variable from the other library, a hover painted in the same tint as the current page, a collapse control that was an empty 308×790 frame, and no Focused, Disabled or collapsed-group variant anywhere. The SAMAVESH sets were rebuilt IN PLACE — same keys, every instance link kept — as `Sidebar` (Mode, Menu and Footer slots, Show Control, Show Footer), `Sidebar/Item · Level 1` (Mode × Type × Open × State, 24), `Sidebar/Item · Level 2` (Placement × Type × Open × State, 16), and three new masters: `Sidebar/Item · Level 3`, `Sidebar/GroupLabel` and `Sidebar/CollapseControl`, plus `Sidebar/Flyout` for a collapsed group. Focused is a boolean, as on Tabs. Active ink is BOLDER (7.75:1 Blue, 15.15:1 Navy) with the FILLED glyph; hover is neutral; item gap is stack/4; levels 2 and 3 keep the handoff's elbow connector, with a Continues boolean instead of first/middle/last variants. The Portal DS copies are ⛔-deprecated with a pointer; the handoff screens migrate later, one at a time." },
      { kind: "Added", text: "`SidebarNav` renders three levels: `items[].children[]` are level-2 entries and `children[].children[]` level-3 leaves, indented under the parent's text with the elbow connector, the last entry ending the trunk. Group labels are the accessible name of a `role=\"group\"` in both modes (they were `aria-hidden`). A collapsed group is a real `<button aria-haspopup>` that opens a flyout — focus moves in, Escape returns it, a click outside or a scroll closes it (it was an `<a>` with no href that toggled a hidden list). A collapsed leaf shows its label as a DS Tooltip; a badge count becomes a dot on the icon instead of vanishing. The rail's own collapse control is optional and off by default — the masthead's toggle drives the same state — and, where shown, is a visible IconButton in a 48px row at the top with a tooltip and `aria-expanded`, replacing the hover-only strip at the foot with a `col-resize` cursor. Five children per group is the design limit and seven the ceiling; `warnOversizedGroups` says so in development. `disabled` on any level. Tokens `layout/sidebar/collapsedWidth` (88) and `layout/sidebar/flyoutWidth` (240) join `layout/sidebar/width`, and the rail binds all three." },
      { kind: "Fixed", text: "THE PORTAL ROOT ITEM LIT UP ON EVERY PAGE. `SidebarNav` matched an item by path prefix, and a portal's root (`/portals/scw`) is a prefix of every route in it, so Dashboard was marked current alongside the real page. `resolveCurrent` now picks the single longest matching href at any level; ancestors open and highlight, nothing else does. Caught by the rail's first unit test. Level-2 and level-3 rows are 44px (`target/comfortable`), matching Figma — code had 40. The Figma registry pointed the Sidebar Nav page at node 4208-740, which did not exist; it points at 4286-428 and the page links to it. A Code Connect template for `SidebarNav` exists for the first time." },
    ],
  },
  {
    version: "v0.103.0",
    date: "2026-09-04",
    current: false,
    changes: [
      { kind: "Changed", text: "MOTION IS TWELVE INTENTS ON A VALUE-NAMED LADDER, AND REDUCED MOTION IS HANDLED ONCE. The five pairs (enter · exit · emphasis · reveal · press) left components with no name for the commonest transition in the estate — a colour responding to the pointer — so seven of them paired an exit duration with an enter easing and 85 more typed a millisecond. There are now twelve: instant · hover · press · focus · enter · exit · expand · collapse · emphasis · reveal · page · loading, plus stagger (45ms step, max 8). The Tier-1 ladder beneath is value-named (0 · 50 · 100 · 150 · 200 · 250 · 300 · 400 · 500 · 700, like space and radius) and the five curves are named by behaviour in Material 3's vocabulary (linear · accelerate · decelerate · standard · emphasized) — the same four curves as before plus linear, renamed: out → decelerate, in → accelerate, inOut → standard, outStrong → emphasized. tokens.css emits ONE @media (prefers-reduced-motion: reduce) block collapsing every intent's duration to 0.01ms, so a component that binds a pair honours the preference without its own query; instant is already zero and loading is exempt because a stopped spinner reads as a frozen page. Easings are authored as DTCG cubicBezier arrays and shadows as DTCG composite objects; two custom transforms project them to byte-identical CSS, proven by the visual contract." },
      { kind: "Added", text: "LAYERING — THE Z LADDER THE ESTATE ACTUALLY RENDERS. The Tier-1 z ladder was UX4G/Bootstrap's 1000–1090 transcribed verbatim, had zero consumers, and the floating-element rule said 'there is no --sa-z-* scale yet' while components stacked at 20, 30, 60, 70, 90, 1010, 999999 and 2147483000. Fifteen Tier-2 rungs now name that reality: base 0 · raised 1 · dropdown 100 · sticky 200 · fixed 300 · overlay 400 · modal 500 · popover 600 · toast 700 · tooltip 800 · rail 1000 · launcher 1010, and three RESERVED — statutory 999999 (the UX4G panel's own value), demo 2147483000, top 2147483001. Code-only, deliberately not exported to Figma (a canvas has no z-axis), and every elevation role names its rung. 21 z-index literals in the DS were bound." },
      { kind: "Added", text: "OPACITY INTENTS, AND STROKE ADOPTED. alpha/disabled (48%) and alpha/muted (64%) replace 0.5, 0.45, 0.48, 0.6 and 0.7 layer-opacity literals across the DS — the only two whole-element opacities the estate uses, named. stroke/0…4 existed since 2026-08-18 with ZERO consumers while 110 '1px solid' literals drew every edge; 126 are now bound. Every semantic group carries a $type (chart, bg, text, border, overlay, density, layout had none) and a group description; motion, shadow and breakpoint primitives carry per-rung descriptions." },
      { kind: "Changed", text: "ONE TEMPLATE FOR EVERY FOUNDATION PAGE, EVERY TABLE GENERATED, AND A GATE WITH NO BASELINE. The census found eleven foundation pages on five different page shells, 0 of 11 with a maturity badge, tabs or a feedback bar; the Elevation page documented three of six roles with a shadow ink retired a month earlier and labelled card as a pressed state; the Motion page hard-coded the values it documented. FoundationDocPage (eyebrow · name · badge · summary · Figma link or declared absence · counted at-a-glance stats · Overview/Tokens/Accessibility tabs with NN / KEYWORD claim-titled sections · standards register · related · feedback) now renders all 19 pages, and foundations-data.generated.ts feeds every non-colour table from the build. Eight foundations that had no page have one: Brand & White-Labelling, Breakpoints, Sizing, Stroke, Layering, Opacity, Interaction States, Content & Localisation. The Foundations nav is in dependency order, not alphabetical. npm run check:foundations fails any page off the template or typing a literal it should read." },
      { kind: "Changed", text: "FIGMA VARIABLES FOLLOW A WRITTEN STANDARD. .claude/rules/figma-variables-standard.md, from Figma's own documentation: the four API-creatable types and what each binds to; the six types the API creates (a live probe showed TIMING and EASING are creatable — the bundled typings that listed four were stale — so every duration is a native TIMING variable and every curve a native EASING variable, the types Figma Motion binds directly; the 41 FLOAT/STRING copies of the first push and the 8 retired ref/z/* rungs were deleted after a full-file binding scan of all 83 pages found zero consumers); modes only on the collection whose variables ALL vary on that axis; and the five fields every push must carry — name = DTCG path, description, narrowest true scopes, codeSyntax.WEB, hiddenFromPublishing by tier. The seven motion primitives were RENAMED in place (ids and bindings kept), the eight ref/z/* Bootstrap rungs deleted after a full-file binding scan of all 83 pages found zero consumers; the exporter's stale 'seven collections … Theme' description was corrected and its dead title-case code removed." },
    ],
  },
  {
    version: "v0.102.0",
    date: "2026-09-04",
    current: false,
    changes: [
      { kind: "Added", text: "THE SCHEME TILE IS BACK, AND FIGMA LED. The Selection Card set gained a `Layout` axis \u2014 Compact (control first, 24px glyph, one line) and Detailed (a tinted 64px icon tile, a title, a fuller description, a `Meta` fact such as the target group, and the control trailing on the right) \u2014 40 variants, key unchanged, on its own page with its own Index card. Code follows it: `cardLayout=\"detailed\"` and `meta` on Checkbox, Radio and both groups, with `meta` joining `aria-describedby` so a screen reader hears the fact after the name and never as part of it" },
    ],
  },
  {
    version: "v0.101.0",
    date: "2026-09-04",
    current: false,
    changes: [
      { kind: "Changed", text: "THREE TYPE SCALES WERE IN PRODUCTION, AND NOW THERE IS ONE. The 2026-09-04 typography audit found the 21-role token scale (1,251 references), a static px \u201csmile-admin\u201d scale exposed as Tailwind `text-headline-1` etc. (242 uses, 32px on every surface against a token of 40/32 fluid, and a `text-body-3` of 13px \u2014 a size the contract bans) and the stock Tailwind size utilities (`text-*`, xs through 6xl) that nothing had ever re-bound (1,622 uses), with the type gate able to see only the first. `globals.css` now clears the stock `text-*`, `leading-*`, `tracking-*` and thin/light/extrabold/black weight namespaces and binds `text-<role>` to the tokens \u2014 size, leading, tracking AND weight from one class, fluid and surface-aware \u2014 and every call site in the estate was migrated" },
      { kind: "Changed", text: "THE SCALE IS RE-CUT FOR RHYTHM. Every size sits on the 16-step ramp 12\u00b714\u00b716\u00b718\u00b720\u00b722\u00b724\u00b728\u00b732\u00b736\u00b740\u00b748\u00b756\u00b764\u00b772\u00b780, every line height on the 4px grid, and leading rises as size falls \u2014 display 1.10 \u2192 1.20, headline 1.20 \u2192 1.50, body 1.5 on both surfaces \u2014 where headline-3 used to sit at 1.14 beside headline-2 at 1.25. Title, Body and Label are now IDENTICAL on the website and in portals; only Display and Headline differ by surface. The 13px and 15px stops the Portal ramp carried are gone. Display tracking is negative on both surfaces from one em rule per rung; the new `caps` tracking (+0.06em) belongs to label-3, which is now the 12px uppercase overline role. Title moved from 500 to 600 so a title and its body differ by two weight steps, not one" },
      { kind: "Added", text: "`Heading` AND `Text` \u2014 the primitives the estate never had. A Heading's `level` is the outline and is required; its role defaults from the level and `variant` departs from it. A Text is a body, label or title role with `measure`, `flow` (the paragraph-spacing tokens finally have a consumer), `numeric` and `lang`. `SectionTitle` composes them and moved from headline-5 to headline-4, because the DS section heading was smaller than what every page hand-rolled instead" },
      { kind: "Changed", text: "NOTHING RENDERS BELOW 12PX. label-3 moved from 11 to 12 (UX4G 3.0 \u00a72 calls 12 the minimum usable size); the 9px and 10px literals in charts, the app switcher, the media gallery and three portals are roles now. Asserted by `type-scale.test.mjs` alongside the ramp, the grid, the 1.2\u20131.5 leading band for every non-display role, the monotonic ratio and the loaded weights" },
      { kind: "Fixed", text: "THE DEVANAGARI FONT WAS PRELOADED ON EVERY ENGLISH PAGE \u2014 99 KB, the largest asset in a 148 KB preload set, on a service built for low-bandwidth readers \u2014 because a `<link rel=\"preload\">` does not consult `unicode-range`. The Devanagari subset is a second `next/font` loader with `preload: false`, placed behind the Latin face in every stack, so a Hindi page fetches it on first paint of Hindi and an English page never does" },
      { kind: "Fixed", text: "FOUR PORTALS PINNED THE ROOT FONT SIZE IN PX. nmba, nhapoa, tg and scw declared `html { font-size: calc(16px * var(--font-scale)) }`, which overrode a raised browser default \u2014 the one preference the rem scale exists to honour \u2014 and the `[data-fontscale]` attribute that would have varied it was never set anywhere. Removed; the DS accessibility bar's stepper is the one mechanism" },
      { kind: "Fixed", text: "HINDI WITHOUT `lang=\"hi\"`: 16 of the 20 files carrying Devanagari had no language attribute, so screen readers voiced Hindi with an English engine and the Devanagari face binding never applied. Marked, and the type gate now reports a file that writes Devanagari without the attribute" },
      { kind: "Added", text: "`docs/audit/typography-deviation-register.md` \u2014 the register `standards-precedence.md` requires and typography never had. Headline sizes one step above DBIM \u00a74's 36/24/20, display leading below DBIM's 1.2 floor, and no 18px body are recorded decisions with the reason each; the typography page's standards panel no longer claims the scale is \u201cthe DBIM type scale as implemented\u201d" },
      { kind: "Changed", text: "THE MEASURE IS A TOKEN. `--sa-container-measure` (36rem \u2248 68 characters, in rem so a raised default font size keeps the character count) replaces the 72ch literal on `.ds-prose` and the four `max-w-prose` call sites; `max-w-measure` and `Text measure` bind it. The container ladder kept widening to 1376px while body stayed 16px, a 130-character line" },
      { kind: "Removed", text: "THE FLAT TYPE RAMP AND THE SHIPPED UX4G PARITY SHEET ARE GONE. `font.size.11…48` and `font.lineHeight.16…56` — the pre-role primitives — had no consumer in any component, page or text style; their twelve Figma variables were bound only by eight Footer text nodes, which now sit on the Body/body-3 and Body/body-1 styles, and the twelve were deleted after the scan. Their one code reader was the `--ux4g-*` parity stylesheet, which nothing in the estate ever imported: `@mosje/design-system/ux4g.css` and its package export are removed, the two UX4G palette modes that only existed inside it (`ux4g`, `ux4gdeep`) go with it, and the mapping is now a build output of `tools/ux4g-conformance/` read only by `measure.mjs`, pointed at `ref/size/*`. The Type collection holds 92 variables, byte-equal to the payload" },
      { kind: "Removed", text: "FORTY LIBRARY-ONLY VARIABLES ARE GONE FROM FIGMA. The ten `deprecated/type/*` shadows of the fluid scale, the two retired `ref/font/family/heading|body`, twenty-five Tier-1 colour orphans the build never defined (`ref/color/ink/*`, `ref/color/stroke/*`, the six `*/source` keys, `color/text/disabled`, a duplicate `focus/ring`, `ref/color/badge/beta`), the two `_deprecated/layout/container/*` and `border/brand/primary/hover`. Deleting a variable strands every node bound to it, so first every page, style and alias was scanned (82 pages, 59,027 nodes, 13,310 text ranges): 17 of the 44 still had consumers \u2014 244 brand-blue fills, 25 brand-ink text fills, old slate strokes, 20 text ranges on the retired heading family \u2014 and 312 nodes were rebound to the value-matched semantic token before anything was removed. The four `ref/viewport/*` widths stay: 14 documentation frames are drawn at them and nothing in code models a viewport width. Every type/* variable now carries its reasoning in its description \u2014 the size, the ratio, the tracking rule and the standard \u2014 mirrored from the token source" },
      { kind: "Changed", text: "FIGMA TABLET SAMPLES ARE WHOLE PIXELS. The Tablet type mode stored the fluid curve at 768px to two decimals \u2014 display-1 at 57.74px, its leading 63.51 \u2014 which is a size no designer would author and off the grid the documentation promises. Rounded at export; the library's Type collection was re-pushed with the re-cut values and the value checksums re-recorded" },
      { kind: "Changed", text: "DEVANAGARI LEADING IS PER ROLE, DERIVED, AND BINDABLE IN FIGMA. Hindi had ONE line height \u2014 `leading/devanagari`, a unitless 1.7 on every role \u2014 which undid the scale (a 40px Hindi headline at 68px leading against 48 Latin) and which Figma read as 1.7px, so nothing in the library could bind it. Every role now carries `lhDevanagari` beside its `lh`: the Latin leading plus a fifth of the size, rounded UP to the 4px grid, derived by `build/devanagari-leading.mjs` from `ref/font/lineHeight/devanagariOffset` (0.2) and never typed. Body-1 is 16/28, headline-1 40/56, display-1 80/104 \u2014 same size, more room, the heading still a heading. It reaches CSS as `--sa-type-<role>-lhDevanagari`, `<Text lang=\"hi\">` and `<Heading lang=\"hi\">` take it, and Figma gets `type/<tier>/<n>/lhDevanagari` as a pixel value per mode with a LINE_HEIGHT scope \u2014 the Typography page\u2019s five bilingual specimens are bound to it. `leading/devanagari` survives as body-1\u2019s alias; the offset is scoped to nothing so it cannot be bound as 0.2px. `type-scale.test.mjs` asserts the grid, the 1.25\u20131.80 band and the rhythm" },
    ],
  },
  {
    version: "v0.100.0",
    date: "2026-09-04",
    current: false,
    changes: [
      { kind: "Added", text: "CHECKBOX AND RADIO REBUILT TO THE INDUSTRY CEILING. Read against Material 3, Carbon, Polaris, Atlassian, GOV.UK, USWDS, Spectrum, Fluent, Radix and Primer, the two selection atoms lacked a size scale, an uncontrolled form, an error state, a description, read-only, a required marker, label placement, a hidden label, a card variant on Checkbox, a group-level disabled, a select-all, a conditional reveal, an exclusive option, reduced motion, a forced-colours checked state and a touch target. Every one of those is present now, on one shared markup, so the two components cannot drift from each other again" },
      { kind: "Added", text: "A SIZE SCALE \u2014 `sm` 16 \u00b7 `md` 20 \u00b7 `lg` 24 for the box \u2014 and a HIT AREA that follows the target ladder rather than the box: 24 \u00b7 44 \u00b7 48. The real, visually hidden input IS the target, centred on the box, so a citizen tapping 12px beside a default checkbox still hits it. The box was a hardcoded 18px on no scale, and its hit area was 18px; the default control now meets WCAG 2.2 SC 2.5.8 on its own and UX4G\u2019s 44\u00d744 touch recommendation" },
      { kind: "Added", text: "`description`, LINKED THROUGH `aria-describedby` AND OUTSIDE THE LABEL. The card variant\u2019s description sat inside the `<label>`, so a screen reader read a paragraph as the option\u2019s NAME. It is a sibling now, on both atoms and in both variants, which is what WCAG 1.3.1 and UX4G \u00a76 ask for" },
      { kind: "Added", text: "`error` ON CHECKBOX, `invalid` ON BOTH. A lone declaration box had no way to say it was wrong; the message renders after the control with `role=\"alert\"`, sets `aria-invalid` and joins `aria-describedby`. Radio deliberately has no `error`: the error belongs to the QUESTION, i.e. the group, which paints every circle" },
      { kind: "Added", text: "`readOnly` THAT IS NOT `disabled`. Native `readonly` has no effect on a checkbox or radio, so the change is refused by hand \u2014 click, Space and the arrow keys \u2014 while the tab stop, the value and the ink all stay. A disabled control leaves the form and tells the reader they did something wrong; a read-only one tells them the value is recorded" },
      { kind: "Added", text: "`variant=\"card\"` ON CHECKBOX, WITH AN `icon` ON BOTH. The whole tile is the target: the input stretches to the tile\u2019s edges, so a tap on the description lands on the control. `labelPlacement=\"start\"`, `hideLabel`, `required` (marker plus native attribute), `defaultChecked` and `onCheckedChange` complete the API" },
      { kind: "Added", text: "THE GROUPS GROW UP. `RadioGroup` and `CheckboxGroup` take `defaultValue`, `disabled` (a native `<fieldset disabled>`), `readOnly`, `size`, `labelPlacement`, `hideLegend` and `forwardRef`; each option takes a `description`, an `icon`, a `reveal` shown only while it is selected (GOV.UK\u2019s conditional reveal, always in the DOM so `aria-controls` resolves) and, on a checkbox group, `exclusive` \u2014 \u201cnone of the above\u201d after an \u201cor\u201d divider, clearing the others and cleared by them. `CheckboxGroup` gains `selectAll` (checked when every enabled option is, indeterminate when some are) and `name`, which it never posted, so a plain form submit carried nothing" },
      { kind: "Fixed", text: "`aria-invalid` AND `aria-required` WERE SET ON A PLAIN `<fieldset>`, which is role `group` and permits neither \u2014 axe\u2019s `aria-allowed-attr` fired the first time a group rendered an error, and no docs specimen ever did. `RadioGroup` is `role=\"radiogroup\"` now, the one role on a fieldset that permits all three; a checkbox group has no ARIA host for \u201cat least one of these\u201d, so each box carries its own `aria-invalid` and \u201crequired\u201d reaches the reader through the legend" },
      { kind: "Fixed", text: "`aria-checked` IS NO LONGER SET ON THE NATIVE CHECKBOX. ARIA in HTML prohibits it, and the docs page\u2019s claim that the DOM `indeterminate` property \u201cis not exposed to assistive technology\u201d was false in every current engine. The attribute existed to drive a stylesheet selector; that reads `data-state` now, which is also rendered on the server so the dash paints before hydration" },
      { kind: "Fixed", text: "`CheckboxGroup` ACCEPTED `variant` AND IGNORED IT, dropped every option\u2019s `description`, and the `PortalLoginTemplate` passed `hint:` to an option whose field is `description`, so the per-method explanation under the sign-in selector was silently discarded. `.ds-control-group.is-invalid` was applied and styled nowhere; it draws the GOV.UK error bar now" },
      { kind: "Changed", text: "THE BORDER IS 2px, UP FROM A RAW 1.5px. A 1.5px edge anti-aliases to a lighter, thinner line at 1\u00d7 density, and that rendered edge is what WCAG 1.4.11 measures a control\u2019s boundary against. 2px is what GOV.UK, USWDS, Polaris and Material draw. Six `control/selection/*` token families replace every raw length; twelve raw transition durations became the motion tokens and vanish under `prefers-reduced-motion`; disabled is painted in tokens rather than `opacity: .5`; forced colours paints the checked fill and the radio dot in `Highlight` instead of leaving the state to the glyph" },
      { kind: "Added", text: "THE FIGMA LIBRARY FOLLOWS THE CODE, IN PLACE. The Checkbox set (15:664) went from Type x State (15) to Size x Checked x State (45) and the Radio set (18:791) from Selected x State (10) to 30, both keys preserved, each with Label, Description, Show Label, Show Description, Invalid and Required properties, a control cell bound to target/*, a focus ring bound to focus/*, and a library Icon glyph. The kebab-case radio-card that depended on a set from another file is `Selection Card` (20 variants, Control = Radio | Checkbox); `Checkbox Group` and `Radio Group` exist for the first time, with exposed item instances, an error bar, a select-all row and an OR divider. Both pages carry a Documentation frame and a Component record, sit in numbered sections, and read Ready on the Index. Twelve control/selection/* variables reached the library; the value-parity record was re-read after the push" },
      { kind: "Added", text: "FIVE TEST FILES WHERE THERE WERE NONE. Three render specs assert every ARIA relationship named on the documentation pages; a pure-logic test covers option-order emission, the exclusive rule and select-all; a stylesheet test fails the build on any raw px or duration returning to the selection stylesheets. The accessibility checklists on both pages are VERIFIED with the test that proves each row" },
    ],
  },
  {
    version: "v0.99.0",
    date: "2026-09-04",
    current: false,
    changes: [
      { kind: "Changed", text: "EVERY TRANSLUCENT TOKEN IS A COLOUR REFERENCE PLUS AN OPACITY REFERENCE. Figma variables can now alias a colour while keeping a separate opacity, and that opacity can be a number variable — so the 48 overlay tiers, the modal scrim, the inverse rules, the code-specimen chrome and all 84 inverse button states are authored as `{base}` + `{alpha.N}` instead of 136 rgba() literals. In CSS a tier resolves as `color-mix(in srgb, var(base) calc(var(alpha step) * 100%), transparent)`, so it follows its base through every brand island; the Figma payload states the alias-with-opacity binding and the exporter writes the composited fallback where the Plugin API cannot yet write it. What this corrected: Navy's scrim was the Blue neutral (#1E2124 for #1E2024) and every wash under a DBIM mode was a Blue-brand literal. The opacity scale was rationalised to ONE thirteen-step ladder (0 · 4 · 8 · 16 · 24 · 32 · 40 · 48 · 64 · 72 · 80 · 88 · 100) from UX4G's fourteen plus ten estate one-offs — six had zero consumers, eight snapped (the titlebar label rose from 4.52:1 to 4.97:1; nothing fell below its floor). Figma reads an opacity-bound number as a PERCENTAGE, so ref/opacity/* is projected ×100; the library's old 0–1 values were a factor of 100 wrong for the purpose. THE LIBRARY'S SCOPES NOW FOLLOW THE AGREED RULE: a designer is offered the Tier-2 alias and only the alias — 109 ref/* primitives that were offered as gaps, radii and fills are offered nowhere, twelve motion tokens left every picker, each alias carries the scope of the property it is for, and the two radii that sat in the Color collection moved to Radius with their 72 bindings re-pointed. The scope is part of the token payload, so a push re-asserts it." },
      { kind: "Changed", text: "THE COLOUR SYSTEM READ AS DULL BECAUSE IT WAS TWO RUNGS TOO DARK. Every status ink sat at rung 700 (7.8\u201311.7:1, L* 33\u201344) where USWDS, GOV.UK, Carbon and Primer sit at L* 48\u201357; a dark colour has little chroma to give, so success was a black-green and warning a brown. `text|icon|border/status/*/base` is now rung 600 and `bolder` is 700 \u2014 until now base was 700 and bolder 600, so the louder name was the LIGHTER colour. The legacy `--sa-color-status-*` aliases moved one rung with them" },
      { kind: "Fixed", text: "INDIA GREEN WAS STARVING ITS OWN TINTS. Anchored at rung 500 with L* 46, it dragged the success ladder a full rung darker than every sibling (600 was L* 39 against 49 elsewhere), and the chroma arc tapered so steeply that success/100 held 17% of the chroma sRGB allows at that lightness \u2014 a sage grey called \u201csuccess\u201d, and the reason every success alert looked like a grey panel with a green stripe. The anchor now sits at 600, the rung its lightness says (the rule navy and saffron already followed), so India Green ITSELF is the success ink and fill (6.72:1) and 500 is a live mid-green; the tint exponent in ramp.mjs is 0.5 (was 0.85). The anchor hex is unchanged" },
      { kind: "Changed", text: "THE FILL LADDER NO LONGER CLAIMS 3:1 FOR `subtle` AND `bold`. Those rungs are tonal fills \u2014 a chip, a tinted row, a plate \u2014 and their readability is the measured `on/*` ink, which is AA in every brand. The page-contrast claim was what SC 1.4.11 asks of a CONTROL boundary, and it put sixteen \u201cshortfalls\u201d on the ledger that were the claim and nothing else; re-anchoring India Green would have made green the seventeenth. The ledger is now empty, and a new entry means a colour to fix" },
      { kind: "Changed", text: "INFO IS CYAN-TEAL (hue 220, `#0B86A2` at 500), NOT THE BRAND BLUE. It was `#1a73e8`, three degrees and dE 0.5 from gov-blue: an info banner, an info badge and a primary button were one colour, and the toast\u2019s info variant was literally painted with primaryScale/50. Now 35 degrees from primary and 39 from the chart\u2019s teal; the union survives only in the DBIM conformance modes, where DBIM\u2019s own palette maps info to DBIM Blue" },
      { kind: "Changed", text: "THE ERROR RED ROTATED 4.7 DEGREES TOWARD CRIMSON (hue 28.7 \u2192 24, `#EC4E4F` at 400) at identical lightness and chroma. At 29 it was a coral 12 degrees from India Saffron, the pair the separation ledger has carried as a known defect since it existed; Carbon, Primer and Material all sit on this side of orange" },
      { kind: "Fixed", text: "THE ALERT PAINTED ITS GROUNDS WITH FOUR HAND-MIXED PERCENTAGES OF THE INK (6%, 7%, 14%, 6% over white), so the warning alert was khaki and none of the four matched the toast beside it. All four now read `bg/status/*/base`. Its warning glyph was `--sa-color-brand-yellow` (#FFD323) \u2014 1.44:1 on white, a BETA-badge colour \u2014 and now takes the warning ink like every other variant" },
      { kind: "Fixed", text: "THE SOLID WARNING BADGE WAS DARK TEXT ON A BROWN, 2.08:1. Amber has no dark rung that is still amber, so it is the one family whose solid chip takes the `bold` rung (300, `#E09C1D`) with its measured dark ink, 6.9:1 \u2014 how USWDS treats the same problem" },
      { kind: "Fixed", text: "`text/brand/primary/base` MEASURED 4.07:1 ON THE PAGE GROUND every `<body>` carries, and it is a TEXT token. It is now rung 600 (`#005EB9`, 5.57:1 on the ground, 6.36:1 on white), the rung the link already moved to; the key colour `#0373DF` remains on `icon/brand/primary/base` and every fill" },
      { kind: "Fixed", text: "A DISABLED BUTTON\u2019S LABEL COULD NOT BE READ: `text/neutral/disabled` was a 48% wash that composited to 1.83:1 on the disabled fill, and that fill was the same colour as `border/neutral/base`, so a disabled input had no edge. The ink is opaque neutralScale/400 (3.06:1 on white, 2.68:1 on the fill) and the fill is neutralScale/100. WCAG exempts disabled controls; this is a floor the estate sets for itself" },
      { kind: "Fixed", text: "THE RESTING FORM-CONTROL BORDER WAS 2.68:1 ON THE MUTED PAGE GROUND \u2014 a 1.4.11 failure on the surface most forms sit on, hidden by a 3.06:1 hairline pass on white. `border/neutral/bolder/default` is neutralScale/500 (4.65:1 white, 4.07:1 muted); hover steps to 600" },
      { kind: "Fixed", text: "THE DIVERGING CHART SCALE WAS RED AGAINST GREEN, AND VANISHED UNDER DEUTERANOPIA \u2014 dE 4.1 at the strong ends, 1.7 at the soft ends, so a diverging map showed no direction to about one man in twelve. The positive wing is the sequential BLUE now (`chart/seq/100, 400, 800`), lightness-symmetric with the red wing (64.3 / 64.4), which is why every published diverging scheme is red\u2013blue or purple\u2013green" },
      { kind: "Changed", text: "THE CATEGORICAL CHART PALETTE WAS RE-CUT by simulated annealing over all twelve slots against six constraints at once: chroma >= 0.10 on every slot (four were grey), >= 3:1 on white and on the muted page, dE >= 8 from every status ink (the brown cat/2 sat dE 4 from the warning ink and read as \u201cwarning\u201d), adjacent slots dE >= 15, every pair among 1\u20139 dE >= 12 in ordinary vision, and the colour-blind ratchet on all 36 pairs. Ratchets tightened: CVD worst pair 8.0 \u2192 8.2, ordinary worst 11.7 \u2192 13.4, chroma deficit 4 \u2192 0, lightness-band deficit 4 \u2192 2. Nine colour-blind-safe slots cannot all sit inside the lightness band \u2014 measured, not assumed \u2014 so two remain one point below it. Slot 1 is still gov-blue" },
      { kind: "Fixed", text: "THE SEQUENTIAL RAMP\u2019S 400\u2192500 STEP WAS HALF ITS NEIGHBOURS (5.0 L* against 7\u20139.7) because 500 is pinned to gov-blue and the rest were placed by eye. Now an even 7.6\u20137.8 L* ladder from 95.7 to 25.9; 500 is still exactly #0373DF. `chart/trend/up|down` moved to rung 600 to match the status inks" },
      { kind: "Fixed", text: "THE SHARED FOCUS RING WAS STILL A 48% WASH. `--sa-focus-ring` composited to `#86bcf0`, 2.01:1 on white, and 53 outline rules across chips, search, controls and the auth stack read it \u2014 the 2026-09-03 fix had corrected forms.css alone. The token now aliases the brand key colour (solid, 4.64:1 white, 4.07:1 muted; Navy and the DBIM modes follow), so every consumer moved at once" },
      { kind: "Changed", text: "FOUR PORTALS HAD RE-DRAWN INDIA SAFFRON AS `#EC6A1F` and PM-AJAY carried the retired orange `#F97316` and two private reds; all seven now alias the estate tokens. Every raw hex in component, portal and website CSS was inventoried (audit \u00a715): the rest are campaign colours, specimen swatches, mask stops and logo marks" },
      { kind: "Added", text: "THE COLOUR PAGE NOW SHOWS ITS EVIDENCE. Three new sections on both surfaces \u2014 18 Three contexts, one palette (real Alert, Badge, Button and Input specimens for a citizen form, an administrative dashboard and an analytics portal); 19 What a colour-blind reader sees (every status ink, solid fill, tint, chart series and diverging end rendered under protanopia, deuteranopia and tritanopia, with the worst-pair \u0394E per set); 20 Ten modes, one set of measurements (eleven load-bearing roles resolved and measured inside every brand, DBIM and UX4G mode). Section 07 is now the full pairing matrix a component uses, each pair rendered live with its ratio and the class it earns; section 15 lists all 46 text, icon and border roles on both grounds; section 03 shows OKLCH lightness per rung. A sticky on-page navigation, numbered sections and a measured-class pill replace the old flat list. Every figure is generated from tokens.css; the page still holds no hand-written literal, and scripts/check-color-docs.mjs gates the 22-section list against the Figma frame" },
      { kind: "Changed", text: "THE LIBRARY HOLDS THE NEW COLOURS. Palette (57 rungs) and Color (73 values, 80 measured-contrast descriptions) were pushed to the SAMAVESH Figma library through the Plugin API and read back; `reference/figma-live.json` records the library checksums and the 94 contrast sentences it now publishes, and both parity gates are green. The Colour \u2014 Documentation frame carries the three new sections, and its sentences about info, the focus ring, the rung ledger and the chart floor were corrected in the same pass. The full four-lens audit, with every measurement, is docs/audit/2026-09-04-colour-system-audit-and-redesign.md" },
    ],
  },
  {
    version: "v0.98.0",
    date: "2026-09-03",
    current: false,
    changes: [
      { kind: "Added", text: "THE FIELD STACK NOW CARRIES ALL EIGHT STATES UX4G PUBLISHES. Read against the UX4G 3.0 Figma library (file 0v5NnMKFKC3foZ85bxPp6c), its `Input - Text Field` master ships Default, Hover, Focused, Error, Disabled, Success, Warning and Read-only. We shipped five. `status=\"error\" | \"warning\" | \"success\"` now covers the three that were missing, on Input, Textarea, Select and the message beneath them. `warning` and `success` deliberately do NOT set `aria-invalid` \u2014 a warning that blocks is an error wearing the wrong colour. The legacy `invalid` boolean still works everywhere, including the object `FormField` spreads onto its control" },
      { kind: "Added", text: "READ-ONLY IS A REAL `readonly`, NOT A DRESSED-UP `disabled`. It keeps its tab stop, its value stays selectable so a citizen can copy an application number out of it, and a screen reader says read-only. Most systems style `disabled` and call it read-only, which takes the field out of the tab order and out of the submitted form, and tells the reader they got something wrong" },
      { kind: "Added", text: "A SIZE SCALE \u2014 `sm` 40 \u00b7 `md` 44 \u00b7 `lg` 48 \u00b7 `xl` 56 \u2014 on Input, Textarea, Select, AadhaarInput and PanInput, and on `FormField` so one prop sets whatever is inside it. UX4G\u2019s 32px S is deliberately not offered: 44px is WCAG 2.2\u2019s Level AAA target size (2.5.5), and the default a developer gets without choosing should be the accessible one" },
      { kind: "Fixed", text: "`md` AND `lg` RENDERED AT THE SAME HEIGHT, AND `min-height` WAS NOT WHAT DECIDED EITHER. At the base padding a 16/24 line plus 24px of padding plus 2px of border is 50px, so the stated 44 and 48 were both 50 and indistinguishable. Found by measuring a rendered matrix in a browser, not by looking at it. Each step now sets a padding that leaves `min-height` in charge, and the four sizes measure exactly 40/44/48/56. The default field is 6px shorter than it was" },
      { kind: "Fixed", text: "THE FOCUS RING FAILED WCAG 2.2 1.4.11 ON EVERY FIELD IN THE ESTATE. It was `0 0 0 3px rgba(3, 115, 223, 0.48)`, which flattens on white to #86bcf0 \u2014 2.01:1 against a required 3:1. It is now a solid 3px outline in `--sa-color-action-primary-default` at a 2px offset: 4.64:1 on white, darker in the navy and dbim modes. An outline rather than a box-shadow, because a shadow is not painted at all in Windows High Contrast Mode, so a keyboard user in forced colours previously had no focus indicator on any form field" },
      { kind: "Fixed", text: "A SERVER-RENDERED VALIDATION ERROR WAS ANNOUNCED ON PAGE LOAD. `FormField` put `role=\"alert\"` on its message unconditionally, so a page returning eight errors announced all eight out of reading order, detached from their fields. Two live regions now sit on the page from the first paint holding nothing, and fill only when the message changes away from the one the field was born with. The first guard used a ref set inside the effect and did NOT work \u2014 React double-invokes effects in development and a remount replays them, so the second pass announced anyway; verified by reading the live regions of a rendered page with seven messages present" },
      { kind: "Added", text: "`prefix` AND `suffix` \u2014 fixed text inside the field\u2019s border for a currency symbol, a country code, a unit. Each is `aria-hidden`, because a screen reader announcing \u201crupee sign\u201d in the middle of a value is noise, and its meaning reaches the reader through `prefixLabel` / `suffixLabel`, which join the field\u2019s description. Adorned fields are now ONE bordered flex row rather than icons overlaid on a padded input \u2014 a text affix\u2019s width depends on its content, and two mechanisms would have disagreed about where the border is" },
      { kind: "Added", text: "`CharacterCount`, which counts GRAPHEME CLUSTERS. `\"\u0928\u092e\u0938\u094d\u0924\u0947\".length` is 6 where a reader counts 3, and `\"\ud83d\udc4d\ud83c\udffd\".length` is 4 where a reader counts 1. It is silent until three quarters of the limit, debounced, and escalates from a polite region to an assertive one once past the limit \u2014 two separate elements, because swapping `aria-live` on one node is honoured inconsistently. Do NOT also set `maxLength`: the browser counts UTF-16 units and would cut a Devanagari word mid-cluster" },
      { kind: "Added", text: "`FieldPolicyProvider` AND `RequiredFieldsLegend`. Whether a form marks its mandatory fields or its optional ones is a FORM-level decision, not a field-level one \u2014 putting it on the field lets one form mark half its fields each way, and the unmarked ones then read as a third category. UX4G publishes the legend as its own component in the Form Field Group; here it reads the same policy the marks read, so the key and the marks cannot disagree. Mark the minority: most scheme applications here are almost entirely mandatory, so `necessity=\"optional\"` is usually right" },
      { kind: "Added", text: "EVERY STRING THE FIELD STACK SPEAKS IS NOW OVERRIDABLE, from one provider at the root of a portal. GIGW 3.0 requires Hindi alongside English on a Government of India property, and a design system that hard-codes its own sentences is reusable in exactly one language. The three count strings are FUNCTIONS rather than templates with placeholders, because pluralisation is not the same shape in every language and `{n} characters remaining` cannot express Hindi\u2019s agreement rules. Overrides merge over the English defaults and are inherited by nested providers" },
      { kind: "Added", text: "`autoComplete` IS TYPED to the HTML autofill field names. WCAG 2.2 1.3.5 (Identify Input Purpose, AA) is the criterion most often claimed and least often checked, because on every framework `autoComplete` is a plain string \u2014 `\"firstname\"` compiles, does nothing, and passes review. Here it fails the build; `\"given-name\"` compiles" },
      { kind: "Added", text: "`labelHelp` \u2014 a disclosure beside the label, not a tooltip. A tooltip cannot be opened by touch, cannot be read at leisure, and vanishes the moment the reader moves to answer the question it was explaining. Its target is rendered even while shut, hidden with the `hidden` attribute, so `aria-controls` always resolves" },
      { kind: "Added", text: "`pending` \u2014 a spinner and `aria-busy` while a value is checked against something. It deliberately does not disable the field: a reader who wants to correct a value should not have to wait for a request they cannot see" },
      { kind: "Added", text: "`FormField` IS ASSEMBLED FROM EXPORTED PARTS \u2014 `FieldLabel`, `FieldHint`, `FieldMessage`, `FieldHelp`, `FieldHelpToggle` and `useFieldIds` \u2014 for the screen that needs a different arrangement. Ids are DERIVED from one `useId`, not registered through context and an effect: a compound component that discovers its children after hydration cannot compose `aria-describedby` on the server\u2019s first paint, so a screen reader reaching the control first hears nothing" },
      { kind: "Added", text: "`classNames` per part, `data-part` / `data-status` / `data-size` on every element, and `orientation=\"inline\"` for dense review tables. The attributes exist so a consumer never has to write a selector against a `.ds-field__*` class, which is an implementation detail that breaks silently when the internals move" },
      { kind: "Changed", text: "THE FIELD LABEL IS 16px AND THE HINT AND MESSAGE ARE 14px, up from 14 and 12. UX4G\u2019s two sources disagree by 4px on both rows \u2014 its written guidance names Label/XL 16 and Body/S 14, its Figma master binds Label/M 12 and Body/XS 12 \u2014 and where a standard contradicts itself the estate takes the larger. 12px is what that same guidance reserves for captions, legal text and timestamps; it was being used for the instruction and the error, which are the two strings a struggling reader most needs" },
      { kind: "Fixed", text: "`.ds-sr-only` WAS USED BY DatePicker, Combobox AND Chip AND DEFINED BY NONE OF THEIR STYLESHEETS. Each component\u2019s CSS is its own root, so their \u201c(required)\u201d text and Combobox\u2019s result count rendered VISIBLE on any page that did not happen to load one of the nine stylesheets that did define it. Now defined in all four" },
      { kind: "Added", text: "Devanagari leading of 1.8 (UX4G \u00a72.8) on any field inside `:lang(hi|mr|ne|sa)`, applied by RE-BINDING the leading tokens for the subtree rather than by overriding `line-height` \u2014 which would have unpaired every size from its leading and put two unbound numbers in the estate\u2019s strictest stylesheet" },
      { kind: "Added", text: "`invalid` ON Combobox, DatePicker, RadioGroup, CheckboxGroup AND DeclarationCheckbox. Those five ask for `error` while `FormField` hands over `invalid`, so `<FormField>{(c) => <Combobox {...c} />}</FormField>` silently lost the error state. Nothing in the estate did it, but nothing stopped it either. Prefer `error` \u2014 a field marked wrong with nothing said about it tells the reader only that they are stuck" },
    ],
  },
  {
    version: "v0.97.0",
    date: "2026-09-03",
    changes: [
      { kind: "Fixed", text: "THE LOGIN FORM WAS A DRAWING OF THE DESIGN RATHER THAN AN ASSEMBLY OF IT. `PortalLoginTemplate` imported ZERO components from its own design system \u2014 8 raw inputs, 8 raw labels, 6 raw buttons, a raw select, and 77 arbitrary-value Tailwind classes wrapping `var(--sa-*)`, which is a token reference no token gate can see and no brand mode can re-bind. Every field height, focus ring and disabled tone was a separately written value with nothing keeping it in step with the design. It now renders Alert, Button, FormField, Input, OtpInput, PasswordInput, RadioGroup, Select and Tabs; the arbitrary-value count is 0" },
      { kind: "Added", text: "`BotCheck` \u2014 THE REPLACEMENT FOR A CAPTCHA, AND DELIBERATELY WITHOUT AN AUDIO MODE. Measured, the audio alternative a text captcha ships with is solved by bots over 85% of the time while only 31.2% of challenges get three-person agreement among people, and a blind citizen takes 65s against 9.8s for the visual form \u2014 so it is harder for the people it is for and easier for the software it exists to stop. The default mode is `invisible`: the server decides from a self-hosted proof-of-work token, a honeypot and rate limiting, and the citizen sees nothing unless it fails. `helpHref` is REQUIRED, because a proof-of-work or reputation check has no accessible workaround of its own and a blocked citizen otherwise has no way to identify themselves as a person" },
      { kind: "Added", text: "`useBotCheck` \u2014 THE CHECK NOW ACTUALLY RUNS, in the browser, for the prototype. It mints a random challenge and hashes `challenge + nonce` with SHA-256 through the Web Crypto API until the digest clears twelve leading zero bits: real elapsed time, a real receipt, and a status that moves idle \u2192 verifying \u2192 verified because work genuinely happened. Measured at 3,542 hashes in 47 ms. The hashes are issued in batches of 512, because awaiting `crypto.subtle.digest` one at a time paid its overhead thousands of times and took 16.6 SECONDS for the same work. What is stubbed is the server\u2019s half \u2014 the challenge is minted client-side, so a token proves work was done but not that we asked for it \u2014 and the documentation names the four things (issue, verify, burn, expire) that move server-side, and what breaks without each" },
      { kind: "Changed", text: "`CaptchaField` IS DEPRECATED. It can only draw a distorted-characters test, which WCAG 2.2 3.3.8 forbids without an alternative, and the alternative it would ship with makes it worse. Use `BotCheck`" },
      { kind: "Fixed", text: "`ConsentLine` AND `AccountPrompt` WERE EXPORTED BY THE SYSTEM AND RENDERED BY NOTHING. The consent sentence GIGW requires existed only in the Figma drawing of the login page, not on the page. Both are now rendered by the template" },
      { kind: "Changed", text: "`AccountPrompt` STOPPED DRAWING A SECOND FULL-WIDTH BUTTON UNDER THE PRIMARY ACTION. A single registration route is now a link on a rule with the question centred on it, which is how the reference draws it; two genuinely different applicants (SCW registers a Volunteer AND a SAGE Organisation) still get buttons, because at that point the reader is choosing between them rather than being offered one route" },
      { kind: "Changed", text: "THE ROLE TABS WERE DRAWN WITH INLINE `style` OBJECTS, which no token gate can see and no brand mode can re-bind, and as a full pill where the reference draws a rounded rectangle. Both are now token-bound classes. They remain real `<a href>`s rather than the DS `Tabs` component: `TabDef` has no `href`, and adopting it would trade working middle-click and copy-link-address for a shared implementation" },
      { kind: "Added", text: "`\"darpan\"` IS BACK IN `PortalAuthMode`, and the removal that took it out was unsound. That audit reasoned \u201ca full read of the handoff found no DARPAN screen in any portal\u201d \u2014 but DARPAN belongs to E-Anudaan, and E-Anudaan has NO login screen in the handoff at all, so the audit could not have found one either way. Its wizard says the organisation record is \u201cPre-filled from your login / NGO-Darpan\u201d. `\"aadhaar\"` stays out" },
      { kind: "Fixed", text: "The recovery link\u2019s required marker wrapped onto a line of its own \u2014 \u201cPassword / Forgot Password?\u201d with a lone red star underneath. `FormField` renders its marker after whatever label it is given, so a full-width flex row pushed it off the line" },
    ],
  },
  {
    version: "v0.96.0",
    date: "2026-09-02",
    changes: [
      { kind: "Changed", text: "THE DIGILOCKER CARD IS A PER-ROLE HANDOFF, NOT A LOGIN METHOD AND NOT AN AUDIENCE RULE. It was modelled first as a fourth `PortalAuthMode` and then as “everyone who is not an officer”; the Handoff supports neither. It carries the card on SMILE-Transgender’s Citizen frames and on neither Admin nor Garima Greh, so one portal disagrees with itself across two tabs and only a per-role switch can say so. It is now `PortalRoleTab.digilocker`, and it renders only when `links.digilockerHref` is also set — a handoff button with nowhere to go is worse than no button. The “or sign in with credentials” divider belongs to the card: no card, no divider" },
      { kind: "Changed", text: "THE SECURITY CAPTCHA MOVED TO THE ROLE FOR THE SAME REASON, resolving `role.captcha` then `config.captcha` then off. The same portal asks a Garima Greh organisation for a code and asks its own citizen for none, which a portal-wide boolean can express only by imposing one answer on both. The fallback is `??` and never `||`, so a role setting `captcha: false` opts OUT of a portal default rather than being read as unset. It stays off by default: a captcha is a cognitive function test, and WCAG 2.2 3.3.8 Accessible Authentication (AA) forbids one with no alternative, so switching it on is a commitment to provide that alternative and to say which in the same change" },
      { kind: "Changed", text: "`SSOButton` RENDERS AN ANCHOR WHEN GIVEN AN `href`. A handoff to a government identity provider is a navigation, not a form submission, and it had been a `<button>` in every case. Without an `href` it still renders a button, for a caller running the redirect itself. It also takes the provider’s mark — `markSrc` as an image path, `mark` as a node — and falls back to a Material Symbols glyph, which is a complete card rather than a broken one" },
      { kind: "Fixed", text: "THE LOGIN TEMPLATE WAS HAND-ROLLING TWO COMPONENTS THE SYSTEM ALREADY SHIPPED. Its DigiLocker call to action and its labelled divider were written inline rather than imported, which is the failure the design-system-first rule exists to catch: the estate ends up with two versions of one thing and no way to fix both at once. Both are now `SSOButton` and `AuthDivider`" },
      { kind: "Added", text: "The DigiLocker mark ships at `/design-system/digilocker-mark.png`. `brandAssets.digilockerLogoSrc` still has no default and deliberately so — every portal mounts under its own `basePath`, so a default path would resolve to nothing on most of them" },
    ],
  },
  {
    version: "v0.95.0",
    date: "2026-09-02",
    changes: [
      { kind: "Added", text: "`ComponentDocPage` — one template carrying the whole documentation shape. Ninety-nine component pages each declared their own heading and prose styles, and with them 161 unbound line heights and 107 width literals across nine different measures. The measure is now one token-bound value in one stylesheet." },
      { kind: "Added", text: "Props tables are generated from the TypeScript type checker (`npm run build:props`, gated by `npm run check:props`). Of twelve components audited against their implementations, one table matched: `ChartCard` documented a prop named `action` where the prop is `actions`, `AppShell` marked two optional props required, and `BarChart` documented two of eleven, hiding its entire multi-series form. A generated table cannot drift from the interface it describes." },
      { kind: "Added", text: "An illustration system — a stated drawn language, nine primitives, fourteen scenes and three rendered tiers, every stroke bound to one of four ink layers so a drawing follows the brand it is rendered under. It depicts no people: the Department serves Scheduled Castes, senior citizens, persons with disabilities and transgender persons, and a depicted person tells every citizen who is not that person the page is not for them." },
      { kind: "Added", text: "Every chart can now report loading, empty, error and filtered-to-nothing, resolved once in `ChartFrame` and forwarded by all fifteen. Six charts previously drew convincing-looking nothing — the three India maps rendered a complete grey map from an empty feed and announced \u201cacross 0 states\u201d." },
      { kind: "Added", text: "Twenty-one component frames recovered into `FIGMA_NODES` from the Code Connect templates, which already published them. Fifty-three pages carried a link labelled \u201cFigma Component Spec\u201d that landed on the file root." },
      { kind: "Fixed", text: "The documentation tab strip could not be operated with a keyboard. Roving `tabIndex` was implemented without arrow-key handling, so a reader could reach the selected tab and no key selected another — leaving the Code and Accessibility panels of every component page, including every props table and accessibility checklist, reachable only with a mouse. WCAG 2.1.1, on roughly ninety-five pages." },
      { kind: "Fixed", text: "The accessibility checklist could not express a failure. Every row rendered a fixed tick against a named success criterion, on seventy-four pages, with no way to say a criterion had not been verified. Rows now carry a status that defaults to unverified, and a field naming the evidence." },
      { kind: "Fixed", text: "`Lightbox` claimed in its own documentation that focus was trapped while open. It was not, and there was no focus restore either — so `aria-modal` told assistive technology the page behind was inert while Tab walked straight into it. Both are now implemented." },
      { kind: "Fixed", text: "A checklist row for a GIGW criterion was labelled \u201cWCAG GIGW\u201d, attributing an Indian government standard to the W3C." },
      { kind: "Fixed", text: "Eight advisories in shipped dependencies, one critical, reduced to one moderate. `npm audit` and `lint:css` now run in continuous integration; neither did before, so the estate-wide colour-token rule executed only when a person typed it." },
      { kind: "Changed", text: "The categorical chart palette gained three measurements it never had: an OKLCH lightness band, a chroma floor, and separation in ordinary vision. The last is the one that mattered — the ramp was solved so far for colour-vision deficiency that two slots ended up 11.7 apart unsimulated, against a floor of 15. All three are ratcheted at today's figures; the palette itself is unchanged, because a replacement that satisfied the new checks failed three of the existing ones." },
      { kind: "Changed", text: "The package declares `sideEffects`, and eleven stylesheets that carried no cascade-layer order statement now do — including the one defining `.sa-container`, the estate's content-width contract." },
    ],
  },
  {
    version: "v0.94.0",
    date: "2026-09-01",
    changes: [
      { kind: "Added", text: "`Breadcrumb` — THE SYSTEM EXPORTED NONE, so every surface that needed a trail drew its own. Two hand-rolled copies were live and they were not the same kind of thing: the website\u2019s is NAVIGATION (real links to ancestor pages) and PM-AJAY\u2019s map rail is DRILL STATE (buttons that pop a view that has no URL). One component now takes both — a crumb with `href` is a link, a crumb with `onSelect` is a button, and the last crumb is where you are and is never interactive" },
      { kind: "Fixed", text: "THE WEBSITE TRAIL CLAIMED TWO CURRENT PAGES ON 64 PAGES. A crumb rendered as plain text for two quite different reasons — it is the page you are on, or it is a mega-menu section with no route behind it (\u201cDepartment\u201d, \u201cDocuments\u201d, \u201cConnect\u201d, \u201cAssociated Organisations\u201d) — and the old markup stamped `aria-current=\u201cpage\u201d` on both. `aria-current` marks exactly one thing, and two is worse than none because the wrong one comes first. A crumb with neither an href nor a handler is now a labelled section: not a link, not current" },
      { kind: "Added", text: "A FOURTH CASE THE HAND-ROLLED VERSIONS BOTH LACKED: the current crumb ellipsises instead of overflowing. PM-AJAY\u2019s rail is 304px and \u201cIndia \u203a Andaman and Nicobar Islands\u201d is wider than it, and `wrap={false}` holds a fixed-width rail to one line so its height does not change every time the reader drills in" },
      { kind: "Added", text: "`linkAs` TAKES `next/link`, so the website trail keeps soft navigation and prefetch rather than dropping to full page loads — the same escape hatch `SiteFooter` and `Ticker` already use. It is safe to pass from a server component because the file does not claim the client boundary, which is exactly why `Pagination` does not either" },
      { kind: "Changed", text: "SEPARATORS ARE `chevron_right` GLYPHS, NOT A TYPED \u201c\u203a\u201d, and they are aria-hidden — so the trail is announced as a list of names rather than punctuated with \u201cchevron right\u201d seven times. Crumbs clear 24px vertically (WCAG 2.2 AA \u00a72.5.8); link ink is 4.6:1 at rest and deepens one rung on hover, because the base rung over the hover wash measures 4.07:1, under the floor" },
    ],
  },
  {
    version: "v0.93.0",
    date: "2026-09-01",
    changes: [
      { kind: "Fixed", text: "`Chip`'s NEUTRAL SELECTED STATE MEASURED 1.66:1 AND READ AS DISABLED. It filled with `bg/neutral/bold` and white ink — white on a light grey — so the selected pills looked quieter than the unselected ones beside them and the state hierarchy was inverted. A WCAG 1.4.3 failure, and the design failure is the one a reader hits first. Solid ink at 18.94:1 now: a selected state has to be the loudest thing in its own group, and being NEUTRAL rather than brand is what keeps it from competing with the chart keys. Quiet is a matter of hue, not of contrast" },
    ],
  },
  {
    version: "v0.92.0",
    date: "2026-09-01",
    changes: [
      { kind: "Added", text: "TYPOGRAPHY FINALLY HAS A GATE, AND IT WAS THE LAST FAMILY WITHOUT ONE. Colour has stylelint plus six contract tests; spacing, radius and icons each have a per-file ratchet. Font size \u2014 21 roles, 73 custom properties, the most visible thing on a government page \u2014 had nothing, and `check:ds-linkage` said so out loud every time it passed: \u201cevery fill, stroke, padding, gap and radius resolves through the design system\u201d. Font size is not in that sentence, and was not in its property table" },
      { kind: "Added", text: "`check:type-linkage` COVERS SIZE, LEADING, TRACKING AND FAMILY \u2014 including UNITLESS leading ratios, which no px-grep can ever see \u2014 across CSS, Tailwind arbitrary values and React style objects. It reads the ramp from `primitive.json` so it cannot hold a stale copy, and it classifies an off-ramp SIZE separately from an unbound one, because a size the 21-role scale cannot express needs a DESIGN decision rather than a binding. A ratchet, per file: new debt fails, and debt that SHRINKS without the baseline being lowered fails too, so the backlog can only go down. All four failure modes were exercised by deliberately breaking them" },
      { kind: "Added", text: "WHAT IT FOUND, MEASURED: 562 literal font sizes, 224 of them off the 15-step ramp \u2014 9, 10, 12.5, 13, 15, 17, 18, 19, 21, 26, 30, 34, 36, 38, 42, 44, 50, 62px, with 13px alone appearing 71 times. 100 raw letter-spacings against 10 tracking tokens. A second, complete, hand-maintained type scale sitting in `globals.css` under `@theme`, several of whose values (44px, 26px, -0.02em) the ramp cannot express at all. And a component shipping a `body-4` size token, which has never existed \u2014 the body ramp has three steps \u2014 CSS drops an undefined `var()` in silence, so that text rendered at whatever it inherited and nothing said a word" },
      { kind: "Added", text: "ONE PARSER FOR BOTH LINKAGE GATES. `tools/ds-linkage/regions.mjs` now holds the region extraction, the comment blanking and the `ds-exempt` vocabulary, so the two gates ask DIFFERENT questions of the SAME regions of the SAME files without a second copy of the machinery between them. This estate has a long record of what a hand-maintained second copy does \u2014 the colour documentation page, the changelog itself, the 168 hand-typed type numbers \u2014 and the second gate was one commit away from starting another" },
      { kind: "Changed", text: "ds-linkage NOW COVERS THE WHOLE ESTATE, AND THE ESTATE IS AT ZERO. Only `e-anudaan` of eight portals had ever been in scope and the website was in nothing at all, so a gate reporting a clean estate was measuring the wrong estate. All 23 scopes are gated and every one of the 867 findings they exposed is cleared \u2014 693 token bindings and 76 role-named palette entries. pm-ajay alone went 489 \u2192 0" },
      { kind: "Changed", text: "LENGTHS BIND BY PROPERTY AND LAYOUT AXIS, the same rule the Figma side already used: `padding*` \u2192 padding, a vertical gap or margin \u2192 stack, a horizontal one \u2192 inline, `border-radius` \u2192 shape, with a bare `gap` taking its axis from its own rule\u2019s `flex-direction`. The ladder steps by 4 above 8, so 10 \u00b7 14 \u00b7 18 \u00b7 22 \u00b7 28 are exact TIES; they resolve DOWN, floored at the smallest non-zero rung. Down tightens, which is recoverable by eye; up inflates every tie by 2px, and on a dense admin dashboard with fixed-width rails that is the direction that wraps. The floor is what stops a 1px inset rounding to nothing \u2014 0 is the ABSENCE of a value, never a rounding of one" },
      { kind: "Changed", text: "COLOURS ROUTE THROUGH EACH PORTAL\u2019S OWN PALETTE FIRST, and not one rendered colour moved. 60 loose literals in `pm-ajay.css` alone became 32 role-named entries \u2014 `--pm-row-hover`, `--pm-track`, `--pm-on-navy-88` \u2014 so a rule now names what a colour is FOR instead of restating six digits. A literal was only ever replaced by a variable already holding that exact value, which is why the whole colour pass is pixel-identical by construction" },
      { kind: "Added", text: "A `portal-palette` EXEMPTION CATEGORY, because a per-portal palette is a DECISION and not a linkage failure. Of pm-ajay\u2019s 58 palette entries exactly TWO resolve to a `--sa-*` value \u2014 both `#003366`, and both are now bound to `--sa-color-brand-navy`, which is defined once and does not vary by mode. The other 56 are a Gov-India-navy ramp over Tailwind\u2019s gray scale with no estate equivalent, so binding them would RECOLOUR the portal rather than link it. Declared in each stylesheet, and registered as divergence 9 in `docs/guidelines/README.md`. It covers DEFINITIONS only \u2014 a repeated hex inside a rule still fails the gate" },
      { kind: "Fixed", text: "ds-linkage MIS-ATTRIBUTED THE PROPERTY FOR EVERY BARE NUMERIC, which had been quietly mis-sorting real findings. A `BARE_NUMERIC` match starts at the property NAME, so the 60-character lookback landed on the PREVIOUS declaration: `gap: 8, height: 44` reported `height` as a gated `gap`, and would just as happily have reported a real `padding` as an advisory `color`. Reading `m[1]` instead surfaced 96 genuine findings \u2014 12 of them in the design-system docs, a scope that had been reporting clean" },
      { kind: "Fixed", text: "`--json` WAS TRUNCATED WHENEVER IT WAS PIPED. `process.exit()` does not flush a large stdout write to a pipe, so a consumer reading the findings got a JSON document cut off mid-string. Found by piping it into a diff script, and the same defect had already bitten the new type gate first. Both now set `exitCode` and let the process end" },
      { kind: "Changed", text: "FIGMA PARITY IS NOW A MEASUREMENT, NOT A RECORD. The Type collection\u2019s checksum has only ever been compared against a manually recorded observation; all 115 variables were read LIVE and diffed against the built payload, and against the emitted `clamp()`s evaluated at 360 / 768 / 1280px on both surfaces. 438 of 438 name-by-mode pairs are identical \u2014 including the fluid TABLET midpoints, which are samples of a linear interpolation and can only match if the whole curve does (`headline-1` at Website \u00b7 Tablet: Figma 33.31999969, CSS 33.32). The 2026-08-11 finding that `ref/font/family/mono` held a reverted webfont is resolved" },
      { kind: "Fixed", text: "THE TEN `deprecated/type/*` DESCRIPTIONS IN FIGMA POINTED DESIGNERS AT TWO DEAD THINGS: `ref/font/role/*`, a Tier-1 path hidden from publishing since 2026-08-26 so it cannot be bound even by accident, and `var(--ds-type-*)`, a prefix the code no longer ships. Anyone following them copied a name that resolves to nothing. They now name the Tier-2 `type/<role>/<n>/*` path and `var(--sa-type-*)`, and the five `/leading` variables no longer claim to alias the RAW SIZE steps. Written fresh rather than round-tripped, so no entity was escaped twice, and read back to confirm" },
    ],
  },
  {
    version: "v0.91.0",
    date: "2026-09-01",
    changes: [
      { kind: "Fixed", text: "THE INDIA MAPS FRAMED 39% OCEAN. `INDIA_STATES_VIEWBOX` was `0 0 800 560`, but every path in the file lives between x=161 and x=647 — India measures 486x550 and is very nearly square. Framed at 1.43:1 the map was width-bound in every consumer: a 646px column drew it 435px tall and left a 221px band of white inside PM-AJAY's panel. The window is the land plus a margin now, `153 -17 502 567`, and the same map draws about 1.6x larger in the same box. Nothing else moved — the projection, the paths and `INDIA_STATE_BOXES` share one coordinate space and a viewBox only chooses which part of it is shown. `IndiaMap` and `IndiaBubbleMap` follow automatically" },
      { kind: "Fixed", text: "`IndiaPointMap` HARDCODED 800 AND 560 IN TWO PLACES — the aspect it holds a zoomed state to, and the divisor that keeps marks optically constant. Both now read the national viewBox, so the unzoomed map is exactly 1 and a future reframing cannot leave a stale constant behind making every mark 1.6x too large" },
      { kind: "Added", text: "`Chip` GAINS `tone=\"neutral\"`. For a FILTER THAT SITS BESIDE SOMETHING LOUDER — a row of type filters next to a chart's own legend, where three brand-blue pills outshout the two keys they belong to and the eye reads the filter before the thing being filtered. A filled neutral rather than a tint: at `sm` a tinted selected state and an unselected one differ by too little to read across a control bar" },
    ],
  },
  {
    version: "v0.90.0",
    date: "2026-09-01",
    changes: [
      { kind: "Fixed", text: "`Search` AT `size=\"sm\"` CHANGED THE BOX AND NOT THE TYPE, WHICH IS HALF A SIZE. It set a 40px shell and left the input at `body-1`, so a field sized for a card rail carried 16px text — out of proportion with the 12px and 14px beside it, and wide enough that a placeholder the field had room for clipped anyway. The type steps with the box now: `sm` takes `body-2`, `md` and `lg` are unchanged" },
    ],
  },
  {
    version: "v0.89.0",
    date: "2026-09-01",
    changes: [
      { kind: "Fixed", text: "A LEGEND KEY WAS AS CLOSE TO ITS LABEL AS THE LABEL WAS TO ITS OWN TOTAL. An entry is three things in a row — key, label, value — and a `ramp` key is itself three more: a low end, the strip, a high end. At one uniform gap they read as one run, so PM-AJAY's villages key rendered `1 [▓▓▓] 387  Adarsh Gram villages  19,768` with nothing saying which number belonged to which. A reader taking \"1 to 387\" as a qualifier on \"Adarsh Gram villages\" was reading it exactly as the spacing told them to. Three graded steps now — 4 inside the key, 12 from key to label, 8 from label to value — because proximity is the only thing grouping any of it. Same defect as the dots that once floated between two labels" },
    ],
  },
  {
    version: "v0.88.0",
    date: "2026-09-01",
    changes: [
      { kind: "Changed", text: "AN INTERACTIVE `Legend` NOW LOOKS LIKE ONE, WHICH REVERSES AN EARLIER CALL. It was deliberately quiet — bare text with a hover ground — on the reasoning that a row of buttons would compete with the chart it labels. That reasoning cost the feature: a reader who cannot see that a key is pressable does not press it, and \"hover to discover it\" is not an answer on a touchscreen. Each entry is a bordered pill in `Chip`'s language now, at `Chip`'s own 26px, because a `Chip` is usually what sits beside it doing the same job" },
      { kind: "Changed", text: "A SWITCHED-OFF SERIES SAYS SO THREE WAYS. Its pill empties and its border goes dashed, its `solid` key goes HOLLOW — the switched-off checkbox convention, and the one cue that survives a greyscale print — and `aria-pressed` reports it. A hollow key also keeps its identity: a reader can still see WHICH series is off, which a uniformly greyed key cannot say. A `ramp` and `dots` fade instead, having no single shape to empty" },
      { kind: "Added", text: "`Pagination` GAINS `size`. `md` is sized for a page-level pager with the full width to sit in; put the same control in a card and it wraps — PM-AJAY's coverage rail is 19rem and `md` with word-labelled steps asked for 267px of it. `sm` is 32px, still past the 24×24 minimum target (SC 2.5.8), and drops the step labels to icons at EVERY width rather than only below 480px: a card pager sits directly beside the list it pages, so a chevron has a visible referent that a pager stranded at the foot of a document does not. The WORDS stay in the accessibility tree, so a screen reader still hears \"Previous\", never \"chevron left\"" },
      { kind: "Fixed", text: "PM-AJAY'S COVERAGE RAIL WAS HAND-ROLLING A SEARCH FIELD OUT OF `Input` — a text input, a magnifier passed as `leftIcon`, and a pill radius of its own. `Search` has existed all along, with `sm` at 40px, and brings the three things the hand-rolled version quietly did without: a real `type=\"search\"`, a clear button once there is something to clear, and `dir=\"auto\"` so an English placeholder does not clip from its HEAD when the estate is running in Urdu. Nothing was added to the design system for this one — the component was simply not being used" },
      { kind: "Changed", text: "`ProvenanceChip` IS A `Badge` NOW, NOT A PILL OF ITS OWN. A small status pill with a leading dot is precisely what `Badge` is, and the hand-rolled copy had drifted: uppercase at weight 700 with letter-spacing, the loudest typographic treatment available, on a component whose own docstring calls it a footnote. Status maps live → `success`, part-illustrative → `warning`, illustrative → `neutral` — illustrative is not an alarm, so it does not read as one. What is left in the app is what belongs to the app: the wording, the mapping, and the single gate that decides whether a mark is drawn at all" },
    ],
  },
  {
    version: "v0.87.0",
    date: "2026-09-01",
    changes: [
      { kind: "Added", text: "`Chip` GAINS `count` — AND IT IS A PROP BECAUSE TWO CALLERS WERE ALREADY DOING IT BY HAND, DIFFERENTLY. `DocumentLibrary` wrote `{group} ({count})` into the chip's children; PM-AJAY's coverage map appended a muted `<span>`. One idea, two typographic answers, and one of them inside the design system. It now renders once — muted, `tabular-nums`, and OUTSIDE the label, so a screen reader hears \"Guidelines, 2 documents\" rather than \"Guidelines open bracket two close bracket\". `countLabel` names the unit; pass a string when the figure needs Indian grouping" },
      { kind: "Added", text: "`Chip` GAINS `size`, matching `Badge`, which has had one all along. `sm` is 26px against `md`'s 32 and drops the type one step rather than shrinking the padding alone, so the pill stays in proportion to what is in it. It exists for a DENSE FILTER ROW: PM-AJAY's map needed two legend keys plus three type filters in an 798px bar, they wanted 816px, and eighteen pixels are not worth a second row of chrome above a map. Still past the 24px minimum target (SC 2.5.8), and not a licence to fit more chips into a space that is simply too small" },
      { kind: "Added", text: "`Legend` CAN NOW BE THE SWITCH FOR WHAT IT KEYS. Pass `onToggle` and every entry becomes a control — `role=\"button\"`, `aria-pressed`, Enter and Space — with `item.on` saying which series are drawn. Charts whose series can be switched on and off are ubiquitous and the estate had none: every one of them hand-rolled a row of buttons beside a legend that could not do it, PM-AJAY's map being the latest. The capability arrives with the handler, exactly as `Chip`'s `onSelectedChange` and `Pagination`'s `onPageChange` do, rather than through a flag someone can forget to pair with it" },
      { kind: "Changed", text: "`Legend`'s `aria-hidden` IS NOW CONDITIONAL, and the condition is the whole point. A decorative legend stays hidden — the real values reach assistive tech through `ChartFrame`'s screen-reader table, and a second numberless recital of the same labels helps nobody. An INTERACTIVE legend may never be hidden, because a control that changes the chart has to be reachable. Passing `onToggle` removes it and adds an `aria-label`" },
      { kind: "Added", text: "`Legend` GAINS `item.swatch` — `solid` · `ramp` · `dots`. A key should be drawn the way its series is drawn, and a solid square could only ever key a solid mark. `ramp` builds the sequential scale from `colors` and takes `scale` for its two ends, because a gradient with no numbers on it says only \"more is darker\", which the reader could already see. `dots` is for a series that is itself a GROUP of marks: PM-AJAY's hostels are one layer rendered in three colours by type, and one square would have keyed a colour the map never uses" },
      { kind: "Changed", text: "AN OFF SERIES DIMS ITS KEY AND REPORTS `aria-pressed=\"false\"`. State on a legend is never carried by colour alone (WCAG 1.4.1) — which matters more here than usual, because the thing whose colour would carry it IS the colour key" },
      { kind: "Fixed", text: "`DocumentLibrary` migrated to `count`, so the design system stops shipping the hand-formatted version of a pattern it now supports" },
    ],
  },
  {
    version: "v0.86.0",
    date: "2026-09-01",
    changes: [
      { kind: "Added", text: "`IndiaPointMap` — REAL COORDINATES, WHICH THE ESTATE HAD NO WAY TO DRAW. `IndiaMap` shades a state and `IndiaBubbleMap` puts a circle at its centroid; both take a value already aggregated to a region, so both discard coordinates by construction. That is right for a rate and wrong for a list of PLACES. PM-AJAY's 19,768 Adarsh Gram villages are not a per-state quantity — they are a belt across West Bengal, Bihar and northern Tamil Nadu, with 44% of the whole programme in the first two states, and twenty-four circles at twenty-four centroids cannot say so" },
      { kind: "Added", text: "THREE MARKS, BECAUSE DENSITY AND IDENTITY ARE DIFFERENT QUESTIONS. `bins` is a hex density field for points too many to tell apart — what would ruin that answer is overplotting, not resolution. `bubbles` is proportional circles at named units for a zoomed grain. `pins` is one mark per record when the records are few enough to be individuals. PM-AJAY combines two of them on one canvas: 19,283 villages as density and 195 hostels as pins, because those are genuinely different kinds of thing and one mark flatters neither" },
      { kind: "Added", text: "~1,000 HEXES IN TEN DOM NODES. Bins are grouped by colour bucket and each bucket is emitted as ONE `<path>`; the cursor is resolved against the lattice arithmetically by inverting the axial transform, so hovering names the cell under the pointer with nothing individually hit-tested. A hex is deliberately NOT keyboard-reachable — a density cell is not an entity, it has no identity to land on, and tabbing through a thousand would be hostile. The screen-reader table carries the named rows instead" },
      { kind: "Added", text: "`projectIndia`, `repairIndiaCoordinate`, `binIndiaPoints`, `isOnIndianLand`. The projection is the SAME Mercator the outlines were baked with, restated for runtime — verified by projecting eleven cities and confirming each lands inside its own state's polygon, because a point projected differently from the coastline beside it lands in the sea" },
      { kind: "Fixed", text: "GOVERNMENT POINT FEEDS TRANSPOSE LATITUDE AND LONGITUDE, and 155 of PM-AJAY's 19,971 records do — an Assam hostel published at `lat 90.71, lon 26.47` is standing in the Pacific. `repairIndiaCoordinate` swaps them back, and the swap is safe here ONLY because India's latitude band (6–37.6) and longitude band (68–97.5) do not overlap: at most one reading can be inside the country, and a check across the whole feed finds zero ambiguous rows. For a country straddling the equator this function would be guessing, and it says so" },
      { kind: "Fixed", text: "THE SECOND CLASS OF BAD COORDINATE, WHICH NO RANGE CHECK CAN SEE. Seventy records pass every bounds test and put a village in the Arabian Sea. Drawn, they were pale hexes adrift off Gujarat that a reader takes for a bug in the map. `isOnIndianLand` catches them with a point-in-polygon against the baked outlines, dilated by a 6-unit (~25 km) tolerance — required, not optional, because the outline is simplified TopoJSON and a strict test condemned 58 genuine Konkan and Sundarbans cells as bad data. The tolerance sits where the measured distribution has its gap: 58 cells lie within 6 units of land and the next one out is at 9" },
      { kind: "Changed", text: "THE COVERAGE LINE IS ON THE PAGE, NOT IN A LOG. 19,478 of 19,971 records are drawn; 423 carry no usable coordinate and 70 sit outside India; 152 were reversed and are drawn corrected. Every one of them is still COUNTED — the totals are the department's own — because a map that silently omits 493 places while printing 19,768 underneath is telling the reader it drew them all" },
      { kind: "Fixed", text: "A ZOOMED CHART PAINTED OUTSIDE ITS CARD. `.ds-chart__svg` is `overflow: visible` so an edge mark is not sheared by a pixel — right for every chart whose viewBox always frames its own data, and wrong for a map that narrows its viewBox to zoom. Framing on Tamil Nadu washed a pale grey landmass across the section's own heading. `.ds-chart__svg--clip` fixes it in the CHART, because a consumer cannot be expected to know which charts overflow" },
      { kind: "Fixed", text: "MUTED STATES ARE NOW INERT, NOT MERELY FADED. While the map was framed on one state the other 35 kept their `tabIndex` and their click handler, so a keyboard user had 35 stops on invisible off-screen buttons before reaching anything real, and a stray click on the sliver of Kerala still in frame threw away the view they had chosen" },
      { kind: "Added", text: "`interactivePins` — the same judgement one scale up. A pin earns a keyboard stop when a reader could plausibly be hunting for that particular one. PM-AJAY's hostels are individuals inside a state and 195 indistinguishable dots across the country, where leaving them focusable put 195 stops between the heading and the ranked list that is the actual route to any of them. National tab stops: 231 → 36" },
      { kind: "Added", text: "`SitePageHeader` and its halo are now in `design.md`, which they were missing from — and `reservesOverlap` is documented in its story. Both gates had been reporting the gap since the header shipped" },
    ],
  },
  {
    version: "v0.85.0",
    date: "2026-08-31",
    changes: [
      { kind: "Fixed", text: "THE HALO LOOP WAS PERIODIC BUT NOT SEAMLESS, and the audit had to separate the two. Sampling all three discs at t and t+2s returns byte-identical state, so the loop repeats exactly — that part was already true. But a disc's wrap (scale 1.365 teleporting back to scale 1) was hidden by a claim that turned out to be false on most of the estate: that `scale(1)` is behind the portrait, so a disc reappearing there at 0.48 cannot be seen" },
      { kind: "Fixed", text: "THE PORTRAIT IS NOT OPAQUE. `PageHero` draws a mark or the State Emblem as `object-contain` on a `bg-white/10` circle — 90% transparent — and only a PHOTOGRAPH gets `object-cover` and fills it. All 23 root organisation pages take the transparent path because none carries a featured photograph. Measured on Dr. Ambedkar Foundation: the innermost disc sits at edge 171.8 at full opacity against a portrait radius of 170, plainly visible through the circle. A fresh disc appeared there instantly every two seconds — a 13-point step in composite alpha, behind the emblem, on a government landing page" },
      { kind: "Changed", text: "THE LOOP IS NOW A CROSSFADE. A disc is born at opacity 0 and dies at opacity 0, and — the part that matters — the fade-in and fade-out are EXACT COMPLEMENTS OFFSET BY THE STAGGER: both run over 30% of the cycle (in at 3.33→33.33%, out at 70→100%) and the discs are two thirds of a cycle apart, so the one fading in and the one fading out are always in their fade at the same time with slopes that cancel. A(p) + C(p+⅔) = 0.48 for every p. The dying disc hands its opacity to the newborn exactly" },
      { kind: "Changed", text: "MEASURED AFTER THE CHANGE: total ink across the loop is 0.9600 → 0.9601, constant to four decimals; the composite behind the portrait went from a 13-point INSTANT step to a smooth 0.6997 → 0.7296 ripple with a largest frame-to-frame change of 0.0041; speed is unchanged at 0.0608/s with a spread of 0.0024 across fourteen intervals; and the state at t still equals the state at t+2s. The residual ripple is the non-linearity of alpha compositing — `1 − ∏(1 − a)` is not linear, so a constant SUM is not a constant COMPOSITE — and at 0.0041 it is imperceptible and, crucially, continuous" },
      { kind: "Changed", text: "FIXED IN THE ANIMATION, NOT THE PORTRAIT. Making `PageHero`'s circle opaque would have worked for today's markup and broken again the next time a consumer renders something translucent inside the halo. An animation should not depend on what is drawn on top of it" },
    ],
  },
  {
    version: "v0.84.0",
    date: "2026-08-31",
    changes: [
      { kind: "Changed", text: "ONE SPEED, START TO FINISH — AND THE DESIGN'S OWN SAMPLES WERE NOT IT. The published states step +64, +48, +40, which is a DECELERATING curve, and the previous pass declared all three as keyframes so linear interpolation would reproduce it faithfully. Faithful to the drawing, wrong for the motion: each wave lunged away from the portrait and then crawled, so the waves bunched towards the outside and the flow visibly changed pace. Water does not do that — a wave leaving a still surface travels at one speed, and every wave travels at the same one" },
      { kind: "Changed", text: "`scale` IS NOW INTERPOLATED LINEARLY ACROSS THE WHOLE CYCLE AND NOWHERE ELSE. Because radius is 170 × scale, a scale linear in time IS a radius linear in time — constant radial velocity. Measured on the running page at 300ms intervals: 0.0606 · 0.0621 · 0.0603 · 0.0604 · 0.0608 · 0.0618 · 0.0605 · 0.0608 · 0.0606 · 0.0608 · 0.0604 · 0.0607 · 0.0620 scale/second — constant at 0.0608, which is exactly 0.365 ÷ 6s. The spread is `setTimeout` jitter in the sampler, not the animation. The three states remain the source for how FAR a disc travels; they are no longer the source for how it gets there" },
      { kind: "Changed", text: "AND IT IS ALWAYS THERE, BY CONSTRUCTION. Opacity holds flat at 0.48 for the first 70% and only then releases, so a disc is a steady presence for almost its whole life rather than an event with a peak. Measured across five moments the innermost disc is always between scale 1.004 and 1.100 at full opacity, and the three sit exactly 0.1217 apart — a third of the travel, permanently. The field just outside the portrait is covered continuously without depending on a chosen fade curve" },
      { kind: "Removed", text: "THE `opacity: 1` AT BIRTH. It was the component's value for a disc still hidden behind the image — but our dark rim is thinner than the reference's (10px on a 340px portrait against 8px on a 416px one), so ours emerged at ~0.75 and fell to 0.48: a darkening pulse at the rim every two seconds. Flat from the start removes it and costs nothing, because at `scale(1)` the disc is behind the portrait either way" },
    ],
  },
  {
    version: "v0.83.0",
    date: "2026-08-31",
    changes: [
      { kind: "Fixed", text: "THE HALO WAS NEVER A RING. Every version up to here drew ANNULI — `box-shadow: 0 0 0 <n>px`, which paints a band and leaves its interior untouched. The component (`Image Container with Halo Effect/lg`, node 99:1334) is a stack of filled circles with a solid #036 fill and a layer opacity — not a stroke among them. The concentric bands a reader sees are not outlines at all: they are WHERE TRANSLUCENT DISCS OVERLAP, and each band's edge is simply where one more disc stops contributing" },
      { kind: "Fixed", text: "WHICH IS WHY NO AMOUNT OF RETIMING COULD FIX IT. A ring leaves a gap behind it, so the flow needs enough rings to keep that gap covered and the eye reads a procession of separate objects — hence the escalation to five. A disc covers everything inside it, so THREE suffice and the field between the portrait and the outer edge is never empty for an instant" },
      { kind: "Changed", text: "THE COMPONENT'S THREE STATES ARE THE KEYFRAMES, not three designs. Read by DISC IDENTITY rather than z-order, the layer names track individual discs moving through the frames: disc `0` goes 416 → 480 → 528, disc `1` goes 480 → 528 → 568-at-zero, and a disc `3` is born at 416 in State 2. So one disc's life is 416 → 480 → 528 → 568 while its opacity runs 1 → .48 → .48 → 0, one is born every step, and three are alive at once — scale 1 → 1.154 → 1.269 → 1.365 against the 416px image" },
      { kind: "Changed", text: "IT IS BORN OPAQUE AND THE BIRTH IS INVISIBLE. A disc starts at exactly the portrait's size, BEHIND the portrait, so its opaque first instant is occluded — nothing appears out of nothing because the appearing happens where nobody can look. That removes the need for a fade-in entirely, which is what finally makes the loop seamless; and the extra density just past the picture's edge, on its way down to .48, is what keeps the rim crisp. It ends at zero, largest and faintest, dissolving into a band it already matches" },
      { kind: "Changed", text: "`linear`, BECAUSE THE EASING IS IN THE SAMPLES. The published sizes step +64, +48, +40 — decelerating — and those samples are declared as keyframes, so linear interpolation between them reproduces the designer's curve exactly. Putting `ease-out` on top would ease an already-eased set of values twice, which is how an animation ends up lurching at its start and stalling at its end" },
      { kind: "Fixed", text: "THE FILL IS SOLID NOW, with the translucency carried by the animated `opacity` as the component does it. Baking the alpha into the colour AND animating opacity multiplies the two and lands at ~22% — less than half the published density" },
    ],
  },
  {
    version: "v0.82.0",
    date: "2026-08-31",
    changes: [
      { kind: "Fixed", text: "THE HALO WAS NEVER RUNNING, AND THREE PASSES OF TUNING HAD NO EFFECT. `.sa-siteheader__pulse` sets `position: absolute; z-index: 0`; `.sa-siteheader__halo > *` sets `position: relative; z-index: 1` to lift the portrait — and the pulse is a CHILD, so it matched both. Equal specificity (0,1,0), so the later rule won. `getComputedStyle` on the shipped page returned `position: relative` and `z-index: 1` on the element the stylesheet spends forty lines describing: an in-flow grid item taking its own row in a `place-items: center` grid, drawing a shadow around a ZERO-SIZED box, above the picture rather than behind it" },
      { kind: "Fixed", text: "THE LESSON IS THE DIAGNOSIS, NOT THE FIX. Each earlier pass verified by LOOKING at a small screenshot rather than asking the browser what it had computed, so three rounds of durations, easings and opacity envelopes were tuned against a component rendering none of them. The first measurement found it in one call. The lift is now scoped with `:not(.sa-siteheader__pulse)` so the rules cannot tie — deliberately NOT fixed by moving a block up the file, because source order is not a contract and the next reorder would reintroduce it silently" },
      { kind: "Changed", text: "FIVE RINGS, NOT THREE, AND A 12px BAND RATHER THAN 28. With the bug fixed the motion was visible for the first time, and three rings on a 9s loop emit one every 3s — which leaves a visible PAUSE at the portrait's rim between one ring leaving and the next appearing, and the eye locks onto a pause. Five on an 8s loop emit every 1.6s, and because the delays are exact fifths there is always a ring within a fifth of a cycle of the rim BY CONSTRUCTION rather than by luck: measured live at 1.076 / 1.160 / 1.244 / 1.328 / 1.412, evenly spread across the track" },
      { kind: "Changed", text: "THE BAND NARROWED AND THE TRAVEL WIDENED TOGETHER (×1.29 → ×1.42), and they are one decision: five rings ~15px apart need a band narrower than the gap between them or they merge into a single moving wash and stop reading as rings at all. Five rings need five boxes and an element has two pseudo-elements, so the halo's own ::before/::after now carry two of them alongside the `__pulse` span and its two" },
      { kind: "Changed", text: "THE FADES ARE ASYMMETRIC ON PURPOSE — 10% in, 32% out. A ring appears at the rim where the eye already is, so it has to arrive too quickly to be watched arriving; it leaves at the band's outer reaches, where a long dissolve is invisible. Symmetrical fades made every birth a small event, which is the thing that made it read as a pulse rather than as flow" },
      { kind: "Changed", text: "REDUCED MOTION PARKS TWO AND HIDES THREE. The handoff draws three rings in total — the static inner one plus a mid and an outer band — and the other three exist only to make the loop read as continuous, which is exactly what this media query switches off. Parking all five would draw five concentric outlines the design does not have" },
    ],
  },
  {
    version: "v0.81.0",
    date: "2026-08-31",
    changes: [
      { kind: "Added", text: "`IndiaBubbleMap` — ONE CIRCLE PER STATE, AREA PROPORTIONAL TO THE VALUE, and it exists because `IndiaMap` is the wrong instrument for a count. A choropleth gives each state as much ink as it has LAND, so a map of counts reports “big state” as “big number”: PM-AJAY's Rajasthan (1,493 villages) and Delhi (1) differ 1,493× in the data and roughly 250× in area, and when those two effects fight, area wins. Use the choropleth for a RATE — literacy, coverage, a percentage, anything that genuinely belongs to the whole territory — and the bubble map for a count" },
      { kind: "Added", text: "AREA, NOT RADIUS: `r ∝ √v`. Scaling the radius by the value squares the difference the eye receives — a 4× count would draw 16× the ink — which is the most common defect in published bubble maps. Circles paint largest-first so small ones stay visible and clickable inside their neighbours; each is keyboard-reachable and announced, and the frame emits the full screen-reader table" },
      { kind: "Added", text: "STATE CENTROIDS ARE DERIVED FROM THE PATHS, NOT STORED BESIDE THEM. A committed table of 36 centroids is a second copy of geometry that no build checks — and the geometry is itself generated, so the day someone regenerates it a stored table starts placing circles in the sea. It takes the centroid of each state's LARGEST closed ring rather than averaging all of them, because averaging puts Andaman and Nicobar's circle in open water between its island groups" },
      { kind: "Changed", text: "THE FACT STRIP WAS MEASURED AGAINST THE HANDOFF AND WAS WRONG IN SIX PLACES. Its shadow was `elevation-raised` (4px/6px) against the published `Shadows/shadow-lg`, which is `elevation-modal` (12px/16px) offset for offset and alpha for alpha — so a card that straddles a banner sat flat against the page. Bound by RESOLVED VALUE, per the linkage rule: the rung is named “modal” and the thing is a card, and that is not a licence to pick a shadow that is visibly wrong" },
      { kind: "Fixed", text: "THE VALUE AND ITS OWN LABEL WERE 12px APART. The handoff nests them in a `Copy` frame at `gap: 0` — two 24px lines, flush, reading as one block — but a flat 12px gap across all three children put that same 12 between a figure and the word that explains it, so the label read as an unrelated third line. The icon carries the spacing itself now" },
      { kind: "Fixed", text: "THE ICON TILES WERE FOUR RUNGS TOO HEAVY. The handoff paints saffron at 8%, which over white is #fef4ee; `bg/brand/secondary/base` resolves to #fff2ed, the same tint to within two points per channel. They were bound to `subtler` (#ffd4c4) — a peach tile rather than a barely-there wash, loud enough that four of them competed with the figures they label. Contrast IMPROVES with the change: 4.40:1, up from 3.67:1, on a 3:1 floor" },
      { kind: "Fixed", text: "THE VALUE AND THE LABEL SHARED ONE INK, so the pair had no tonal hierarchy at all. Both now sit one rung lighter and one rung apart — #1e2124 and #3a3d41, against the handoff's #1f2428 and #343a40 — and the cell padding became a real 12px column gap instead of 12px of padding on each cell, which was the same idea applied twice" },
      { kind: "Changed", text: "THE HALO FLOWS INSTEAD OF PULSING, and the numbers came off a kymograph of the reference recording — one row of pixels through the halo's centre, stacked over time, which turns each ring edge into a line whose slope IS its speed. Three edges drifting outward together, a new one every ~2.9s, travelling r153→r197. So: 9s per ring (exact thirds), scale 1 → 1.29, and LINEAR. It ran 6s to 1.4 on an ease-out — too fast, too far, and front-loaded, which is why it read as a repeating pulse" },
      { kind: "Changed", text: "AND THE OPACITY NOW HOLDS FLAT THROUGH THE MIDDLE, which is the difference between flow and a beat. A peak-then-decay curve gives every ring a visible moment of maximum brightness, and three of those a cycle IS a pulse however smooth the travel is. Rising to 0.55, holding, then releasing to 0 means a ring is simply present for most of its life and the eye finds nothing to lock onto" },
    ],
  },
  {
    version: "v0.80.0",
    date: "2026-08-31",
    changes: [
      { kind: "Fixed", text: "EVERY BAND ON EVERY ORGANISATION PAGE WAS THE SAME GREY, and nothing said so. The page wrapper carried `bg/neutral/subtler` — added so the fact card had a surface to lift off — while bands alternate by tinting only the EVEN ones and leaving the odd ones transparent. A transparent band over a grey wrapper is a grey band, so tinted and untinted measured identically: the alternation the template computes so carefully was still being computed and had nothing left to express. The wrapper is white now, and the card's surface was never the wrapper's job — the strip paints its own grey and the first band is always the tinted one, so the grey runs unbroken underneath it" },
      { kind: "Fixed", text: "AND `transparent` WAS NOT THE FIX, which cost a second pass. `body` is itself `#eef0f3` across this site, so a transparent wrapper still showed grey and the repair looked like no repair at all. An alternation needs a white the page OWNS, not the absence of a colour" },
      { kind: "Changed", text: "THE FACT CARD OVERLAPS THE BANNER BY 64, NOT 40 — measured off the handoff rather than judged: the blue band is 572 tall and `Org at a Glance` starts at 508. Forty was the card's own top padding, which looked deliberate and was never measured; at that depth the card rests against the band instead of straddling it. THREE rules carry that one number — the card's pull, the band's reservation and the surface's inset — and a comment in each now names the other two, because moving one alone either paints grey over the band or leaves a strip of banner under the card" },
      { kind: "Changed", text: "THE HALO PULSES THREE RINGS INSTEAD OF TWO, a third of a period apart rather than a half — the handoff's halo is a photograph plus three rings, one opaque hugging the image and two translucent. They still share ONE track: animating each on its own radius makes one arrive exactly where the next sits at rest, and the edges beat against each other, which is the defect the previous two passes existed to remove" },
      { kind: "Added", text: "A `sa-siteheader__pulse` SPAN, because an element has only two pseudo-elements and three rings need three boxes. It carries the first ring on itself and the other two on its own ::before and ::after. Purely decorative — the media column is already `aria-hidden` and the span is absolutely positioned, so it adds nothing to layout and nothing to the accessibility tree. Its `z-index: 0` is load-bearing: the halo lifts the portrait to 1, and without it a later sibling would paint over the picture" },
      { kind: "Changed", text: "THE LOOP SLOWED 5s → 6s AND THE PEAK DIMMED 0.85 → 0.7. Three rings on a 5s loop emit every 1.67s, which reads as a pulse to watch rather than as ambience; and three overlapping rings put half again as much ink on the band as two, so the peak comes down to keep the total weight where it was" },
    ],
  },
  {
    version: "v0.79.0",
    date: "2026-08-31",
    changes: [
      { kind: "Changed", text: "THE PAGE HEADER’S HALO NOW MOVES, EXCEPT FOR THE RING THAT HOLDS THE PICTURE. It was three static rings with one ripple leaving the outermost; it is now ONE static ring — the dark inner outline — with two rings travelling out from its edge, half a cycle apart. The innermost stays because it is the only ring doing structural work: it separates a photograph from a blue gradient at the picture’s lower edge, where the band is darkest, and a portrait whose own boundary breathes reads as a rendering fault rather than as motion" },
      { kind: "Fixed", text: "THE OBVIOUS WAY TO ANIMATE RINGS 2 AND 3 REBUILDS THE DEFECT THEY WERE FIXED FOR. Expanded on their OWN radii, ring 2 leaving 40 arrives exactly where ring 3 sits at rest, and the two edges cross and beat — which is the seven-edge collision the previous pass removed. Both rings therefore share ONE track, offset by half the period, so nothing ever coincides. The plaque’s old “denser near, fainter far” gradient survives as a property of travel rather than of three drawn outlines" },
      { kind: "Changed", text: "REDUCED MOTION NOW RESTORES THE DRAWING THE HANDOFF PUBLISHED rather than simply stopping. The two travelling rings freeze at the radii the static mid and outer bands occupied, so a reader who asked for less movement gets the three-ring plaque — not a bare disc with a hole where the animation was" },
      { kind: "Fixed", text: "THE FACT CARD NEVER ACTUALLY OVERLAPPED THE BANNER, and a comment in the stylesheet asserted that it did. The wrapper has no padding or border, so the card’s −40 margin COLLAPSED THROUGH it and moved the wrapper’s own top edge up 40 — taking the grey surface with it and painting over the band’s last 40 points. The banner just looked 40 shorter and the card sat flush on grey. The surface moved to a pseudo-element inset by the same 40, so the blue now shows behind the card’s top edge as the handoff draws it. Nothing detected this because nothing overlapped WRONGLY — the overlap merely never happened" },
    ],
  },
  {
    version: "v0.78.0",
    date: "2026-08-31",
    changes: [
      { kind: "Changed", text: "THE BANNER NOW DEFAULTS TO THE HOMEPAGE ONLY. It defaulted to every page, which put an 80px identity band above every scheme page, organisation profile and press release on the site. It is an ENTRY POINT: it earns its space where a reader is deciding where to go, and costs 80px everywhere they have already decided. The three-way placement setting at /admin/portals is unchanged and still applies without a redeploy \u2014 widening it is one click" },
      { kind: "Fixed", text: "PARKED COULD LAST FOREVER, WHICH MADE THE BANNER PERMANENTLY PINNED BY ACCIDENT. It ended only on `focusout`, so a reader who pressed Escape and then simply SCROLLED kept a band pinned to the top of every screen for the rest of the session \u2014 the exact state this component deliberately does not have. It now also ends on the next scroll and on a pointer-down outside. The third was needed because a click on empty page space moves focus to `<body>` and fires `focusout` with a NULL relatedTarget, which is indistinguishable from a window blur and is correctly ignored" },
      { kind: "Fixed", text: "A CONTROLLED PARENT COULD LEAVE THE BAND SLIDING AWAY WITH AN OPEN PANEL. `handleToggle` cleared the exit phase, but a parent that sets `isOpen` back to true without going through it did not \u2014 so the band could be mid-exit with its drawer open. An effect on `open` itself now covers every route in, and both phase rules carry `:not(--open)` as a second line" },
      { kind: "Fixed", text: "`Node.contains` THROWS ON A NON-NODE \u2014 proven, not assumed: `band.contains(window)` raises a TypeError. Real pointer events always carry an Element so a user cannot trigger it, but a synthetic or retargeted event can, and a thrown listener would leave the band parked forever with no error anyone sees. Both outside-click handlers now guard with `instanceof Node`" },
      { kind: "Changed", text: "REDUCED MOTION PARKS INSTEAD OF JUMPING. It used to release straight to flow, which is a 1,400px teleport \u2014 not travel, but not gentle either. It now holds the band still and releases on the reader\u2019s own scroll, which masks it. \u201cFewer and gentler\u201d, not \u201cswap the animation for a jump\u201d" },
      { kind: "Fixed", text: "THE FOOTER LINK\u2019S UNDERLINE RAN UNDER ITS ARROW. `text-decoration` inherits into every child, so setting it on the anchor drew a stray dash after the text \u2014 the same defect the AccessibilityBar\u2019s launch glyph has already had once. The underline is on the words only" },
      { kind: "Removed", text: "THE PORTAL CARD\u2019S RESTING SHADOW. The card already has a saffron rule doing the separating, and a shadow beneath a border is two boundary treatments for one edge \u2014 the handoff reference carries none either, putting one on SELECTED only. The deeper reason is hierarchy: if every card is elevated, elevation says nothing. Reserving it for hover and selected buys two states a signal they could not otherwise have, and press now removes it entirely so the card reads as going DOWN rather than merely shrinking" },
      { kind: "Added", text: "AN EMPTY STATE, because the list is DERIVED and can therefore be empty \u2014 a registry with nothing marked live, a failed read, a filter matching nothing. It rendered a heading, an empty list and a link, which reads as a broken page rather than an answer. One sentence, no illustration: a graphic for an empty list is decoration on a failure. The footer link stays, because it is the route that still works" },
      { kind: "Changed", text: "THE FIGMA MASTERS WERE BROUGHT IN LINE, not left behind: the Portal Card set lost its resting shadow and gained one on selected; the SAMAVESH Banner\u2019s drawer heading is left-aligned at Title/title-1 across all three open variants; the footer carries the new copy in India Green, bound, and its parent auto-layout moved to the left axis \u2014 text alignment governs lines inside a box, not where the box sits, which is why left-aligning the text alone left it reading as a third axis. Both master descriptions rewritten as agent briefings" },
      { kind: "Fixed", text: "EVERY ORGANISATION MARK WAS RENDERING SOFT, and the arithmetic says why: the tile draws the mark at 46 CSS px, which on a 2\u00d7 display is 92 DEVICE pixels and on a 3\u00d7 display 138 \u2014 against files that were 96px. That is 1.04\u00d7 headroom at 2\u00d7 and less than one pixel per pixel at 3\u00d7. All seventeen are re-pulled from the library\u2019s own source artwork, which every variant turned out to hold at identity transform \u2014 so the raw image IS what the library displays, nothing was cropped, and each was mapped to its slug by its exact pixel dimensions rather than by guessing. Capped at 256px and NEVER upscaled, so a mark keeps its native size where the source is smaller" },
      { kind: "Changed", text: "AND THE SET GOT SMALLER WHILE GETTING SHARPER. Re-encoding with palette quantisation at the same resolution cut 1,106 KB to 350 KB \u2014 68% \u2014 so 2.7\u00d7 the pixels costs 62 KB more than the blurry set did, not four times as much. Each file was only kept if the re-encode was actually smaller. Bandwidth matters on a government estate; \u201chigh resolution\u201d is not a licence to ship a megabyte of logos" },
      { kind: "Fixed", text: "NCSC WAS BRIEFLY OVERWRITTEN WITH PM-AJAY\u2019S ARTWORK during that re-pull, by a shell loop that assumed bash word-splitting: zsh does not split an unquoted variable, so a sixteen-entry mapping collapsed into ONE iteration pointing at the wrong file. Caught by reading the loop\u2019s own output \u2014 it printed one line where sixteen were expected, and the dimensions it reported belonged to another organisation. Rewritten with a real array" },
      { kind: "Added", text: "NCBC IS RECORDED AS THE ONE MARK THAT CANNOT BE IMPROVED. Every other mark now carries 2.2\u20132.8\u00d7 headroom over what a 2\u00d7 display needs; NCBC has 1.09\u00d7, because 100\u00d7100 is all the LIBRARY holds. The shipped file is already its full source resolution, so this needs new artwork from the organisation rather than a better export. Logged as an open item on the Brand component record" },
      { kind: "Added", text: "AN EMBLEM VARIANT, so a portal with no bespoke mark can be drawn honestly. Code has always carried ORG_LOGO_FALLBACK \u2014 the State Emblem \u2014 for an unknown slug; the library had no counterpart, so e-Utthaan and e-Anudaan sat in the SAMAVESH Banner\u2019s drawer wearing the NCSC crest. The set is 18 variants now, and that is BETTER parity rather than worse: seventeen organisations plus a fallback, mirroring ORG_LOGOS plus ORG_LOGO_FALLBACK exactly. Cloned from DAF so the artwork is the library\u2019s own" },
      { kind: "Fixed", text: "JRF\u2019S MASTER REBUILT. One rectangle carried THREE image fills \u2014 the correct Babu Jagjivan Ram portrait, in FILL mode and switched OFF, underneath two hand-cropped copies \u2014 on a 56\u00d758 rectangle offset a pixel above its frame. It is now a single visible FIT fill on a 56\u00d756 rectangle at the origin, like every sibling, using the original artwork that was there all along" },
      { kind: "Fixed", text: "A CLAIM OF OURS THAT WAS SIMPLY WRONG: the documentation said JRF\u2019s content \u201crenders 216px tall inside a 56px frame\u201d. Its `absoluteRenderBounds` was always 56\u00d756. The 216 came from screenshotting a VARIANT INSIDE ITS SET, which returns the height of the set\u2019s column \u2014 an artefact of the read, not a property of the node. Corrected on the web page, in the Figma documentation and in the component record" },
      { kind: "Fixed", text: "THE MASTERS WENT INTO THE SECTIONS THAT ALREADY EXISTED FOR THEM, not a new one. A section titled \u201cOrganisation logos\u201d was already on the Brand page holding a heading, a caption and an empty slot; \u201cEmblems, logos & misc marks\u201d had an empty \u201cSample brand lockup\u201d slot with a caption describing exactly the placeholder that belonged in it. A section was added alongside them anyway, duplicating both. Removed; org-logo and Logo now sit in their own slots, and with the invented section gone the numbering starts at 1 rather than the orphaned 2 it inherited from Iconography" },
      { kind: "Fixed", text: "THREE MARKS IN CODE DISAGREED WITH THE FIGMA LIBRARY, and matching NAMES had hidden it \u2014 the registry and the library both held the same seventeen slugs, so every count and every parity claim read green while the ARTWORK differed. Fingerprinting each variant\u2019s original image bytes through the Plugin API against the shipped PNG found it: JRF shipped the STATE EMBLEM while the library held Babu Jagjivan Ram\u2019s portrait; DAIC shipped a portrait roundel against the library\u2019s building; NSKFDC shipped a 96\u00d719 sliver that rendered as an all-but-blank tile. All three re-exported from the library at 96px. Code and Figma now agree on exactly which FOUR organisations share the State Emblem \u2014 NCSK, DAF, DWBDNC, SCW \u2014 and JRF is no longer one of them" },
      { kind: "Fixed", text: "ONE-LINE PORTAL CARDS SHOWED A RAGGED GAP UNDER THEIR TEXT. The card carried `min-height: 96px`, which floored the CARD but not its CONTENT, so with `justify-content: flex-start` the slack fell below the last line in different amounts \u2014 29px under a one-line name, 17px under a two-line one, side by side in the same row. The NAME now reserves two lines instead, so every compact tile is 98px with 16px under its text whichever it has. It is a floor, not a cap: a three-line name still grows, and grid row-stretch still applies when a row-partner is genuinely taller" },
      { kind: "Fixed", text: "THE ARTWORK NOTE NAMED THE WRONG COMPONENT. Both the web page and the Figma documentation said \u201cDAIC carries two stacked image fills\u201d. DAIC has ONE. It is JRF that carries THREE fills on a single rectangle \u2014 one hidden, two composited \u2014 inside a 56px frame whose content renders 216px tall, which is why exporting that variant does not give a square mark. Corrected in both places, and it is now an OPEN item on the new component record rather than a line of prose nobody owns" },
      { kind: "Added", text: "THE THREE COMPONENT RECORD FRAMES THE HOUSE STYLE REQUIRES, and a correction: an earlier note here claimed no page in the library had one. Button, Chatbot and Radius all do \u2014 the claim was true when it was written and was never re-checked. Brand, Portal Card and SAMAVESH Banner now have theirs, carrying only forward-looking work: the two drawer cards that cannot be drawn honestly until the set has an Emblem variant, the master that needs rebuilding, the parity gaps between each master and its props, and where every number came from" },
      { kind: "Changed", text: "ALL THREE FIGMA PAGES RESTRUCTURED TO THE PAGE CONVENTION. The masters sat LOOSE at the top-left, unlabelled and outside any section, and Brand\u2019s numbered sections began at \u201c2 \u00b7\u201d because the section-1 that would have held them was never made. Each page now reads documentation \u2192 record in the left column, with the published sets in a numbered section 1 in a second column at x=1840 \u2014 matching Buttons, Chatbot and Radius. Brand\u2019s existing sections 2 and 3 keep their numbers, which are now correct rather than orphaned" },
      { kind: "Changed", text: "EVERY MARK IN THE FIGMA CATALOGUE IS LABELLED WITH ITS REGISTRY SLUG, as the web page already did. Seventeen crests in a row with no captions told a designer what exists but not what to type; `daic` and `dwbdnc` are not guessable from the artwork" },
      { kind: "Fixed", text: "`org` COULD NEVER TAKE EFFECT ON A PORTAL CARD \u2014 a documented prop that was dead on arrival. `PortalCard` passes `path={path ?? href}` and `href` is REQUIRED, so the derived path was ALWAYS defined; `OrgLogo` tested `path !== undefined` first, so the `org` branch was unreachable. Every card carrying a slug rather than a portal route silently rendered the State Emblem \u2014 visible in this component\u2019s own documentation specimen, where NCSC showed the emblem while `ncsc.png` sat in the registry. An explicit slug now beats a derived path. Proved both directions before and after, and pinned in `org-logo-resolution.test.ts` plus a Figma parity claim" },
      { kind: "Fixed", text: "THE ORG LOGO PAGE CONTRADICTED ITSELF ON ITS OWN HEADLINE NUMBER \u2014 \u201cSixteen marks\u201d in the header, \u201cSeventeen, in registry order\u201d two paragraphs below. The registry holds SEVENTEEN and so does the Figma set, verified one for one through the Plugin API; the header was the stale half. `pm-ajay` had been added without the count following it" },
      { kind: "Fixed", text: "ORG LOGO SAT UNDER \u201cNAVIGATION\u201d IN THE DOCS SIDEBAR while its code folder, its route and its Figma page all read \u201cbrand\u201d, so every reader arrived at a breadcrumb that disagreed with the URL. A Brand group now exists, placed after Foundations to mirror where the Brand page sits in the library. The component\u2019s own status badge said \u201cStable\u201d on all three new pages while the sidebar said \u201cNew\u201d \u2014 they now agree" },
      { kind: "Added", text: "DOCUMENTATION FRAMES IN FIGMA FOR ALL THREE NEW COMPONENTS, which had none: `SAMAVESH Banner \u2014 Documentation` (7 sections), `Portal Card \u2014 Documentation` (5, with all four variants live) and `Brand \u2014 Documentation` (3, with all seventeen marks live). Built to the house grammar \u2014 1680px, hero with six COUNTED stats, numbered sections, 880px prose measure. Every fill, radius and text node is bound: 0 raw fills, 0 raw radii, 0 unstyled text, nothing below the 11px floor" },
      { kind: "Added", text: "SEVEN FIGMA PARITY CLAIMS, so those pages cannot rot silently. `claims.json` had no entry for any of these components and was passing VACUOUSLY. The gate earned its keep immediately: it rejected one of the new claims because the regex matched `box-shadow` inside the base rule\u2019s `transition` list \u2014 the claim was wrong, not the code. The suite is 26 claims and 69 assertions" },
      { kind: "Fixed", text: "A TEST THAT TESTED A COPY OF ITSELF. `org-logo-resolution.test.ts` mirrors the resolver rather than importing it, because the component is a client module and the suite runs in bare node \u2014 and the mirror was unchecked, so the file passed 6/6 with the precedence bug reintroduced. It now reads the component\u2019s source and fails when the mirror drifts, which is what makes every other assertion in it mean anything" },
      { kind: "Changed", text: "`grid-template-rows` STAYS, AND THE REASON IS NOW RECORDED. It is a layout animation \u2014 eight cards and eight images re-laid-out every frame, the most expensive thing in the component. The obvious fix, transform + opacity (which this panel can afford because it is absolutely positioned and pushes nothing), was implemented and REVERTED: `--open`\u2019s transform and opacity did not take effect while `visibility` from the same rule did, and the stylesheet is not readable from the page to find what overrode them. A rewrite of a panel\u2019s show/hide that cannot be verified is worse than a measured cost that works" },
    ],
  },
  {
    version: "v0.77.0",
    date: "2026-08-31",
    current: false,
    changes: [
      { kind: "Fixed", text: "ESCAPE LEFT KEYBOARD FOCUS 1,380px OFF THE TOP OF THE SCREEN \u2014 a regression the exit animation introduced one release earlier. Escape closes the panel and returns focus to the toggle, which is what every disclosure does; but the toggle lives INSIDE the band, and the band was translating up and out with it. Measured: `document.activeElement` was the toggle at `top: -1380`, with no visible focus indicator anywhere on the page [WCAG 2.4.7]. Guarding the animation alone did not fix it, because the teleport sat underneath the animation. Closing while pinned now has THREE phases \u2014 idle, PARKED, leaving \u2014 and the middle one exists entirely for the keyboard: while focus is inside, the band stays pinned at full opacity with the reader\u2019s anchor visible on it, and leaves on `focusout`, by which time their attention has already moved. A modal can return focus to its trigger because the trigger does not move; ours does, and that is the whole difference" },
      { kind: "Fixed", text: "THE DRAWER\u2019S FOOTER LINK FAILED AA ON ITS OWN GROUND. `--sa-color-action-primary-default` is #0373df, which measures 4.24:1 on the drawer\u2019s #fff2ed \u2014 below the 4.5:1 that 14px text owes, and it had shipped. Its HOVER state passed at 6.54, so only the resting state failed: the state a reader spends all their time in. The blue was chosen against white and the drawer is not white, which is the same mistake the whole saffron investigation was about. It is now India Green at 6.14:1" },
      { kind: "Changed", text: "THE PANEL HAD THREE BRAND HUES IN 400px \u2014 a green heading, saffron cards and a blue link \u2014 so nothing was dominant. Green is already this estate\u2019s affirmative colour here (the Explore control, the selected check), so the footer link takes it: green is now the panel\u2019s SYSTEM colour and saffron its CONTENT colour. Two roles instead of three accidents" },
      { kind: "Changed", text: "THE DRAWER HEADING IS LEFT-ALIGNED AND SMALLER, and both were wrong for the same reason. Centred, it answered to the container\u2019s midpoint while every card beneath started at its left edge \u2014 two alignment axes, so nothing lined up with anything. Centred text is right for a short self-contained block that owns its space; it is wrong as a label for a left-aligned list, and the giveaway is that nothing beneath shares its axis. At `headline-2` it also rendered 32px, LOUDER THAN THE BAND\u2019S OWN WORDMARK, on a panel whose point is the eight portals underneath. `title-1` at 22px reads as the section header it is. The filter row and the footer moved to the same axis" },
      { kind: "Fixed", text: "EVERY CARD IN A ROW PUT ITS LOGO AT A DIFFERENT HEIGHT. Two causes, one level apart: the head row centred the mark against names that wrap to one, two or three lines, and the card itself centred its whole content inside the height the tallest card in the row had stretched it to. A four-across grid therefore showed four logos on four baselines. Both now start at the top; the slack falls below the text, where a ragged edge is normal. Measured: all four logos and all four codes on one line per row" },
      { kind: "Added", text: "PRESS FEEDBACK ON THE PORTAL CARD, and hover is now gated behind `(hover: hover) and (pointer: fine)`. The card is the primary tap target of the entire pattern and had NEITHER: the DS Button has had `scale(0.97)` since it shipped while the thing people actually tap had nothing, and the ungated hover STUCK on touchscreens \u2014 the card stayed lifted with a raised shadow after the tap until you tapped elsewhere" },
      { kind: "Added", text: "`motion/reveal` AND `motion/press` TOKENS, plus the `outStrong` easing primitive \u2014 because the design system had motion tokens and this pattern bound NONE of them, hand-typing 150/180/200/220/260/300/380ms across four curves. One of those literals, `cubic-bezier(0.4, 0, 1, 1)`, was character-identical to `--sa-motion-exit-easing`: a literal that merely equals a token, which this estate\u2019s own linkage rule calls a defect. Binding to the existing pairs alone would have been a DOWNGRADE \u2014 the built-in decelerate curve is too weak to read as intentional on a large surface \u2014 so the strong curve was ADDED rather than the good one deleted, per standards-precedence. Pushed to the Figma library FIRST and read back before the value record was refreshed, because that record must never assert a state the library is not in" },
      { kind: "Fixed", text: "THE CARD\u2019S TWO LINES HAD NO SIZE HIERARCHY. `title-3` and `body-2` both resolve to 14px, so the token names promised a hierarchy the scale does not deliver and the code separated from the name on weight and colour alone. The code is what a reader scans for, so it takes `body-1` at 16px" },
      { kind: "Fixed", text: "THE CARD STAGGER RAN ON THE WAY OUT TOO. A `transition-delay` set on the open state still applies when that state is removed, so the last card was queued at 256 + 250 = 506ms of fade-out against a 150ms band exit \u2014 the panel was still dissolving after the bar carrying it had gone. Enter staggers at 45ms steps; exit has no delay and takes the exit duration" },
      { kind: "Changed", text: "THE FOOTER COPY SAID SOMETHING UNTRUE FOR THE THIRD TIME. \u201cBrowse every portal, including those in development\u201d stopped being true when the directory went live-only; \u201cSearch and compare every portal\u201d was never true, because nothing on /portals compares anything. Both surfaces show the same live portals, so ANY copy promising more there is false. What IS true is who the second route is for \u2014 a citizen has arrived where they are going, an officer needs the directory to reach the portal they sign into. It now reads \u201cAre you an officer or administrator? Find your portal\u201d, with the question as quiet body text and the link as the action" },
    ],
  },
  {
    version: "v0.76.0",
    date: "2026-08-31",
    current: false,
    changes: [
      { kind: "Fixed", text: "SAMBAL RENDERED AS THE STATE EMBLEM, AND THE REASON WAS IN THE LIBRARY. Its `org-logo` variant had a device \u2014 cupped hands round the Ashoka Chakra, SAMBAL / \u0938\u0902\u092c\u0932 beneath \u2014 sitting UNDER a 74-node strapline, one TEXT node per character, inside a 56px box. Illegible at any size, and it meant nobody had ever exported the mark, so every surface fell back to the emblem and nothing said why. The strapline is removed, the device scaled to the 48px inset its 16 siblings use, exported, and registered against `/portals/nhapoa`" },
      { kind: "Changed", text: "THE CARD\u2019S BORDER AND ITS CODE NOW SIT ON DIFFERENT SAFFRON STEPS, which is what makes it lighter WITHOUT failing. They have different jobs: the border is the card\u2019s only boundary so WCAG 1.4.11 asks 3:1, while the code is 16px BOLD \u2014 not \u201clarge\u201d, which starts at 18.66px bold \u2014 so 1.4.3 asks 4.5:1. One token for both let the stricter govern both. Measured on white / on the pale drawer: 400 `#ff671f` 2.91 / 2.66, 500 `#e1560f` 3.79 / 3.46, 600 `#c34700` 4.97 / 4.54, 700 (the old single token) 6.60 / 6.02. Border takes 500, code and CTA take 600. THE FIGMA REFERENCE USES 400 FOR BOTH and cannot be copied \u2014 its border fails non-text contrast and its code fails text contrast" },
      { kind: "Changed", text: "RADIUS 16 \u2192 20 AND `selected` NOW WASHES saffronLight \u2192 white, matching the handoff\u2019s change-portal side sheet. The wash is subtle by design: a tinted card would compete with the saffron code for the same attention, and the ink\u2019s ground where the text actually sits stays effectively white" },
      { kind: "Added", text: "THE BAND CONDENSES WHILE PINNED, AND THE SUBLINE SURVIVES IT. Padding 12\u21926, badge 52\u219240, wordmark and subline one type step each, the Explore control tighter \u2014 80px \u2192 60px, measured. This is affordable here and was NOT when the band pinned permanently: shrinking something always present forces you to drop the subline, shrinking something present only while the reader is choosing a portal does not. Reducing scale is a different act from deleting content, and only the second one was wrong. Driven by `:root[data-sa-header-condensed]`, so band and masthead condense on one scroll with one set of thresholds" },
      { kind: "Fixed", text: "CLOSING THE PANEL WHILE PINNED MADE THE BAND VANISH RATHER THAN LEAVE. Dropping `--open` reverts it to `relative` in the same frame, which returns it to a flow position a thousand pixels above the fold \u2014 not a slide, an absence, and an 80px bar disappearing out of the top of the viewport reads as a rendering fault. `--closing` holds it pinned for 260ms and translates it up and out along the path it would have taken had the reader scrolled it away, then releases it once it is already off-screen. Guarded twice: only when ACTUALLY pinned, measured from the element\u2019s own rect against its resolved `top` rather than re-derived from scroll position; and never under `prefers-reduced-motion`. Reopening mid-exit cancels it" },
      { kind: "Changed", text: "THE MARKS MOVED TO A `Brand` PAGE IN THE FIGMA LIBRARY, off Iconography \u2014 a departmental crest is a brand asset, not an icon. The Figma page, the code folder (`components/brand/`) and the docs route now all read the same word. A move preserves the component key, so every instance and every Code Connect mapping followed; the two mark-documentation sections went with it and Iconography is left holding icons only" },
      { kind: "Fixed", text: "THE MARK REGISTRY RETURNED NOTHING ON THE SERVER. `ORG_LOGOS` and its resolvers sat in the `\u201cuse client\u201d` component file, so a server component importing them got a client-reference proxy: `Object.keys()` returned an empty array, the documentation page\u2019s catalogue of every mark rendered as nothing, and no error appeared anywhere. Found by counting tiles on the page. Split into `org-logo-registry.ts`, which carries no directive; the component re-exports so callers still see one module" },
      { kind: "Added", text: "THREE GAPS IN THE ARTWORK ARE NOW RECORDED rather than rediscovered. NCSK, DAF, DWBDNC and SCW share ONE image hash \u2014 the State Emblem, a correct fallback, but if any of them has its own crest it has never been supplied. DAIC carries two stacked image fills where every sibling has one. Both sit in the `org-logo` description and on the documentation page, where a maintainer will find them" },
    ],
  },
  {
    version: "v0.75.0",
    date: "2026-08-31",
    current: false,
    changes: [
      // org-logo-exempt(prose): the entry NAMES the two directories the marks used to
      // live in. Quoting a path in documentation is not consuming it.
      { kind: "Added", text: "`OrgLogo` \u2014 EVERY ORGANISATION MARK, AND THE ONLY PLACE A MARK\u2019S PATH IS WRITTEN. The same 16 files sat in TWO byte-identical public directories (`/design-system/org-logos/` and `/website/images/org-logos/`) while `organisation-details.ts` reached into THREE different roots for the same class of asset, one of them used by a single organisation. Nothing reconciled them, so a mark replaced in one place stayed stale in the others and nobody could say where \u201cthe others\u201d were. It also carries the estate\u2019s own marks \u2014 SAMAVESH and the State Emblem in both inks \u2014 which were string literals at every point of use. design.md had specified this component, as `<OrgLogo org=\u201c\u2026\u201d />`, marked \u201cwhen built\u201d" },
      { kind: "Added", text: "`npm run check:org-logos` \u2014 A PER-FILE RATCHET, because the rule is otherwise cultural and this estate already knows what a rule with no gate is worth. 99 literals across 48 files are frozen as declared debt: a new file that writes one FAILS, a baselined file that grows FAILS, and one that SHRINKS also fails \u2014 telling you to re-baseline, so one surface\u2019s cleanup cannot be spent silently on another\u2019s regression. Exercised by breaking it, per the rule that a check nobody has watched fail cannot be trusted. It caught its own first improvement" },
      { kind: "Added", text: "`PortalCard` HAS TWO VARIANTS, AND `detailed` IS WHAT THE `/portals` DIRECTORY NOW RENDERS \u2014 closing the duplication this component was extracted for and had not closed. The directory kept its own `portals-gw__card`, so three cards existed for one object. `compact` is for a reader FINDING a portal they already know (the banner drawer, the change-portal side sheet); `detailed` is for one CHOOSING (the directory). Rule, ground, tile, saffron code and every shared measurement are identical \u2014 pick by which the reader is doing, never by how much space is going spare" },
      { kind: "Added", text: "`selected` ON `PortalCard`, for the change-portal side sheet on a login page: a heavier rule and a filled check, never a fill \u2014 a tinted card would compete with the saffron code for the same attention. It sets `aria-current` and carries a visually-hidden \u201cCurrent portal\u201d, because a green tick alone is colour and shape carrying meaning [WCAG 1.4.1], and its padding subtracts the extra border pixel so selecting a card does not shift the grid" },
      { kind: "Fixed", text: "THE SAME PORTAL READ TWO DIFFERENT WAYS IN TWO DIRECTORIES. `PORTAL_LABELS` lived inside the SAMAVESH banner, so the banner showed \u201cPM-AJAY / Pradhan Mantri Anusuchit Jaati Abhyuday Yojana\u201d while `/portals` \u2014 with no access to it \u2014 fell back to a derived two-letter code and the registry\u2019s own name, and showed \u201cPM / PM-AJAY\u201d and \u201cEU / E-Utthan Admin\u201d. That last one leaks an internal admin label to citizens. It now lives beside the registry with `portalLabel()`, and search matches the citizen-facing pair too \u2014 typing \u201cSAMBAL\u201d found nothing, because the registry calls it \u201cNational Action Plan for Older Persons\u201d" },
      { kind: "Fixed", text: "EVERY CATEGORY TAB ON `/portals` READ 0. The tab list was a hand-typed copy of a vocabulary that had since moved to the design\u2019s five, so nothing matched and selecting any tab emptied the grid. Derived from `PORTAL_CATEGORIES` now \u2014 and only categories with something in them render, the same rule the banner\u2019s chip row already followed. The \u201c4 key sectors\u201d figure beside it was a literal, also stale" },
      { kind: "Changed", text: "`/portals` LISTS LIVE PORTALS ONLY, and the status filter and both badges went with that decision rather than surviving it. \u201cLive Portal\u201d and \u201cUnder Onboarding\u201d on a page where one state is the only state is furniture that says nothing \u2014 and \u201cTotal 21 / Live 8 / Onboarding 13\u201d as a headline tells a citizen most of the department\u2019s estate does not work yet, on the page they landed on to find a service. One figure now: portals available" },
      { kind: "Removed", text: "`planned` AND `note` FROM `PortalCard`, and `href` IS NOW REQUIRED. Every surface lists live portals only, so the non-interactive \u201cIn development\u201d card has no caller \u2014 and an OPTIONAL destination is how an unbuilt portal got rendered as a link in the first place, which shipped a 404 on every page of the website. A required prop makes that impossible at build time instead of catching it at runtime" },
      { kind: "Changed", text: "THE BAND PINS ONLY WHILE ITS PANEL IS OPEN, and the subline survives every state. Pinning ALWAYS forced a condense to stay affordable \u2014 143px of an 812px viewport otherwise \u2014 and the condense cost the subline, which is the one line telling a first visitor SAMAVESH is a single access mechanism rather than a logo. Trading the component\u2019s only explanatory sentence for chrome present when nobody is using it is the wrong way round. The real defect was never WHEN it pinned; it was pinning to `--sa-header-pinned`, the resting height, which is fixed and stays fixed" },
      { kind: "Removed", text: "`--sa-band-rest` AND ITS `scroll-padding-top` RULE, WITH THE BEHAVIOUR THAT NEEDED THEM. A permanently pinned band owes the document its own share of scroll padding; one that pins only while open does not, because a closed band obscures nothing and an open panel is not a state you land an anchor in. Leaving the rule behind would have pushed every anchor and skip-link on every page 80\u201386px too low, forever, to clear a band that is not there. `data-sa-header-condensed` went too \u2014 it had no consumer left, and governance that governs nothing is worse than none" },
    ],
  },
  {
    version: "v0.74.0",
    date: "2026-08-30",
    current: false,
    changes: [
      { kind: "Fixed", text: "THE SAMAVESH BANNER\u2019S CODE CONNECT TEMPLATE TOLD AN AGENT THE OPPOSITE OF WHAT SHIPS. Its first rule read \u201cthe band\u2019s text is deep India green, NOT white \u2014 do not correct it back\u201d, written before the three-tone decision landed; the shipped default is white on saffron, deliberately, as a recorded WCAG deviation. It also named `successStrong` as the ink, which the stylesheet explicitly rejects as the WORST credible dark ink (APCA Lc 43.9, under the 45 headline floor), and its token map gave the band ink as `text-default`. That file IS what the Figma MCP server hands an agent in Dev Mode, so it was the single most load-bearing wrong sentence in the component" },
      { kind: "Fixed", text: "THE TEMPLATE MAPPED NEITHER VARIANT AXIS. The Figma set carries Tone (Light | Dark | Tint) \u00d7 State (Closed | Open) \u2014 the file\u2019s own header said so \u2014 while its property-coverage table listed neither, so both emitted `undefined`. `component-authoring.md` \u00a712a requires every property mapped or its absence explained; both axes are now exhaustive, and the six code props with no Figma property each say WHY. Its rules were also numbered 1,2,3,4,5,7,6" },
      { kind: "Fixed", text: "TEMPLATE RULE 4 STILL SAID THE DRAWER HEADING IS AN `<h2>`. It became a `<p>` naming a `<nav>` when the heading-order defect was fixed, and nothing updated the file that tells agents how to use the component" },
      { kind: "Fixed", text: "A COMMENT IN THE COMPONENT STILL DESCRIBED THE OLD SCROLL BEHAVIOUR \u2014 \u201cthe band is not sticky\u201d and \u201cpins FOR AS LONG AS THE PANEL IS OPEN\u201d \u2014 written against code that had changed hours earlier in the same branch. A stale comment beside working code is read as current" },
      { kind: "Fixed", text: "`onToggle` FIRED TWICE PER CLICK IN DEVELOPMENT. The fix for the swallowed double-tap moved the callback INSIDE `setInternalOpen`\u2019s updater; React requires updaters to be pure and StrictMode double-invokes them, so every consumer callback ran twice. A synchronous `openRef` mirror settles both halves \u2014 the second tap reads the first tap\u2019s decision, and the callback fires once, outside the updater" },
      { kind: "Fixed", text: "`PortalCard`\u2019s PLANNED BRANCH SILENTLY SWALLOWED `ref` AND EVERY PASSED PROP. An `id`, a `data-testid` or a handler vanished depending on which branch happened to run. Its `aria-label` was also inert \u2014 ARIA forbids naming an element with an implicit generic role, so screen readers dropped it; the attribute read as an accessibility affordance while doing nothing. Removed, because the code, name and \u201cIn development\u201d note are real text and announce correctly on their own" },
      { kind: "Fixed", text: "`external` OPENED A NEW TAB WITH NO WARNING OF ANY KIND. The prop\u2019s own doc told the CALLER to \u201cpair with a visible cue\u201d and handed them nothing to pair with \u2014 a warning is not a fix. The cue is now built in: an `open_in_new` glyph beside the name AND a visually-hidden \u201c(opens in a new tab)\u201d, because the glyph is `aria-hidden` and an aria-label is invisible to a sighted reader [WCAG G201]. Nobody had hit it only because no live portal is external yet" },
      { kind: "Fixed", text: "THE BANNER\u2019S EMBLEM CARRIED `alt=\u201cSAMAVESH Emblem\u201d` IMMEDIATELY BESIDE THE WORD SAMAVESH, so a screen reader announced \u201cSAMAVESH Emblem SAMAVESH\u201d. A decorative image next to the text it depicts takes an empty alt [WCAG H67]" },
      { kind: "Fixed", text: "THE DOCUMENTED PROP `plannedLabel` DOES NOT EXIST, and cannot: `liveSamaveshPortals()` filters planned portals out, so the banner never renders a planned card. It was in the docs prop table and in design.md\u2019s key-props line. `viewAllHref` and `viewAllLabel` were documented with their RETIRED defaults (`/website/samavesh-citizen-portals`, \u201cView all citizen portals\u201d) against the real `/portals` and \u201cBrowse every portal\u2026\u201d \u2014 and every Storybook story passed those same retired values, so no story showed the link that actually ships. `allLabel` and `tone` were missing from the table entirely" },
      { kind: "Changed", text: "THE CLAIM THAT `PortalCard` ENDED THE DUPLICATE-CARD PROBLEM IS WITHDRAWN, from four places that stated it as fact. Only the banner adopted it; `/portals` still renders its own `portals-gw__card` with its own stylesheet, so the extraction currently makes THREE cards for one object rather than one. The stop is deliberate \u2014 the gateway card carries an org line, a description, a category chip and a CTA row that PortalCard has no slot for, so consolidating is a design call about whether this component grows a denser variant, not a mechanical swap \u2014 but a deliberate stop recorded as a finished job is just a wrong document" },
    ],
  },
  {
    version: "v0.73.0",
    date: "2026-08-30",
    current: false,
    changes: [
      { kind: "Fixed", text: "A STRIP OF PAGE CONTENT RAN BETWEEN THE MASTHEAD AND THE SAMAVESH BAND — 89px on desktop, 155px on a phone. The band pinned to `--sa-header-pinned`, which `SiteHeader` writes ONLY while the masthead is resting, because `scroll-padding-top` has to clear the taller of its two states. So once the masthead condensed the band stayed at 154/212 while the header ended at 65/57, and the page showed through the difference. A sticky offset needs the opposite value — whichever state the masthead is in NOW — and there was no such variable to read" },
      { kind: "Added", text: "`--sa-header-stuck` — the masthead’s bottom edge once pinned, measured in whichever state it is currently in, published by the same ResizeObserver that already writes the other three. And `data-sa-header-condensed` on `:root`, so chrome pinned underneath can condense WITH the masthead instead of running a second copy of its scroll thresholds. Those thresholds are deliberately asymmetric — past 120, back under 40, so the bar cannot flutter at one exact scroll position — and a second component re-deriving that from `scrollY` is a second place to get the hysteresis wrong. Only a STICKY header publishes either: a non-sticky specimen contributes nothing to a sticky offset" },
      { kind: "Changed", text: "THE SAMAVESH BAND PINS ALWAYS, NOT ONLY WHILE ITS DRAWER IS OPEN. Pinning on open made the band’s relationship to the masthead conditional on a panel, so the way into SAMAVESH existed at one scroll depth and nowhere else. It is the estate’s single access mechanism; the door to it belongs beside the masthead. `sticky` is a new prop, on by default" },
      { kind: "Changed", text: "THE BAND CONDENSES WHEN THE MASTHEAD DOES, and that is what makes pinning it affordable. Parked at full height it would cost 86px on a phone on top of the header’s 57 — 143px, 18% of an 812px viewport, spent permanently on chrome the reader uses once. The subline, the vertical rule and 12px of padding go, the badge drops 44→32 and the wordmark one type step: 80→52 on desktop, 86→52 on a phone, and the pinned pair costs 109. The change is instant rather than tweened, matching the masthead’s own one-frame tier swap — two halves of one chrome block easing at different rates read as two things" },
      { kind: "Fixed", text: "`--sa-header-pinned` WAS BEING WRITTEN AS THE CONDENSED HEIGHT, which is precisely the one value it exists not to hold. `measure()` read `condensed` from the React value its effect closed over, and at every transition React re-renders, the condensed bar is in the DOM at its new height, and the ResizeObserver registered by the PREVIOUS effect is still the one attached — so it fired with new geometry and an old flag. That number is `scroll-padding-top`, so the frame that got it wrong left an anchor 155px short of clearing an expanded masthead: WCAG 2.4.11, and invisible unless you measure. It now reads `is-scrolled` off the element it is measuring, and a class set by the same render that produced the geometry cannot be a frame out of step with it" },
      { kind: "Added", text: "THE PINNED BAND PAYS ITS OWN SHARE OF `scroll-padding-top`. A band that no longer scrolls away obscures anchor targets exactly as the masthead does, so it publishes its RESTING height as `--sa-band-rest` and a rule at the foot of its stylesheet adds it — 302px on a phone, being 212 masthead + 86 band + 4. Both values are resting heights, since an anchor can land before the page has scrolled far enough for either to condense. Verified on three real page headings: each lands at y 302 against a band bottom of 109" },
      { kind: "Changed", text: "THE DRAWER’S HEIGHT CAP READS `--sa-header-bottom`, NOT the `--sa-header-stuck` the band pins to. The two differ by the accessibility bar’s 46px for the first 46px of scroll, and the two errors are not symmetric: a cap 46px too short only loses height the panel scrolls past anyway, while one 46px too tall puts the last rows below the fold with nothing to reach them — which is the exact defect the cap was added to close" },
      { kind: "Changed", text: "EVERY INLINE SPECIMEN PASSES `sticky={false}` — both on the documentation page and in all eleven Storybook stories. A pinned example detaches from the prose explaining it, and the three stacked tone specimens would pin to the same offset and cover each other. Same call `SiteHeader`’s own three previews already make" },
    ],
  },
  {
    version: "v0.72.0",
    date: "2026-08-30",
    current: false,
    changes: [
      { kind: "Added", text: "A LOGIN\u2019S ROLE TAB CAN BE LINKED TO. `PortalLoginTemplate` now reads `?role=<id>` on mount and keeps the URL in step as the reader switches tabs, so a login opened from a citizen surface lands on the citizen tab and one opened from an officer directory lands on the officer tab. It was NOT possible before: the tabs carried `#role-<id>` hrefs, but those were anchors \u2014 the active tab came from React state seeded by `defaultRoleId`, so every link landed on the same tab whatever it said" },
      { kind: "Added", text: "`portalLoginUrl` AND `roleFromUrl` ARE THE ONE PLACE THAT KNOWS THE PARAMETER IS CALLED `role`, so renaming it cannot leave a caller behind. They live in their own `.ts` rather than inside the `.tsx` for a blunt reason: Node runs a `.ts` under `node --test` and cannot load a `.tsx`, so putting pure string functions beside JSX is what makes them untestable. Nine cases pinned, including that an existing role is REPLACED rather than appended \u2014 `?role=a&role=b` is ambiguous and parsers disagree about which wins" },
      { kind: "Changed", text: "THE LEGACY `#role-` HASH IS STILL READ, as a fallback after the query. It was the tab anchor\u2019s original href, so any link already shared keeps landing on the right tab instead of silently opening the default one" },
      { kind: "Changed", text: "THREE BEHAVIOURS CHOSEN DELIBERATELY, all easy to get wrong. An id matching no role is IGNORED, so a stale or hand-typed link opens the default tab rather than breaking the page. The URL is rewritten with `replaceState`, NOT `pushState` \u2014 a tab is a view of one page rather than a place you travelled to, so a reader who tried three tabs would otherwise need four Back presses to leave. And the selection runs in an effect AFTER mount, costing one frame of the default tab, because reading `window` during render makes the server and the client disagree and a hydration mismatch is worse than a flicker" },
      { kind: "Added", text: "`roleId` AND `onRoleChange` for callers that already know who is arriving \u2014 a route only officers reach, or a session that resolves the role asynchronously. `deepLinkRole={false}` opts out of the URL entirely, which is what every Storybook story now does: a story rewriting the surrounding page\u2019s address on each tab click would fight the addon\u2019s routing" },
    ],
  },
  {
    version: "v0.71.0",
    date: "2026-08-30",
    current: false,
    changes: [
      { kind: "Fixed", text: "THE BANNER\u2019S FIGMA LINK WENT NOWHERE, and the reason is worth keeping. It passed a HANDOFF-file node id (7116-33784) to `figmaUrl()`, which builds URLs against the LIBRARY file \u2014 so it opened the right file at an id that does not exist in it. It was also the only docs page in the estate exposing a raw node id in its label: 52 pages read \u201cFigma Component Spec\u201d and one read \u201cFigma Component (7116:33784)\u201d. Both fixed, and the two new components are registered in FIGMA_NODES so no page has to hand-write an id again" },
      { kind: "Added", text: "PORTAL CARD HAS A DOCUMENTATION PAGE. It shipped as a component, a story, a design.md entry and a Figma component with NO page under /design-system \u2014 and no gate caught it, because `check:docs-routes` detects a DUPLICATE page for one component and cannot detect a MISSING one. That blind spot was already recorded in the follow-ups; this is the second component to fall through it" },
      { kind: "Fixed", text: "THE OPEN PANEL WAS A SCROLL TRAP. `overscroll-behavior: contain` was added to stop a flick past the end of the list dragging the band away \u2014 but the band is STICKY while open, so it cannot be dragged away; the page just moves underneath it. What `contain` actually bought was a panel covering ~65% of the viewport that swallowed every scroll once its list bottomed out, so the page stopped responding entirely. Chaining is the correct complement to a panel that deliberately does not close on scroll" },
    ],
  },
  {
    version: "v0.70.0",
    date: "2026-08-30",
    current: false,
    changes: [
      { kind: "Added", text: "THE OPEN STATE IS IN FIGMA. The SAMAVESH Banner set now carries TWO axes \u2014 Tone (Light | Dark | Tint) x State (Closed | Open), six variants \u2014 plus Title, Subline and Drawer title as text properties. The open variants hold the heading, the 4-across grid of the eight portals live today, and the footer link, so the component finally shows the thing it exists to disclose rather than only the strip that discloses it" },
      { kind: "Added", text: "PORTAL CARD IS A FIGMA COMPONENT, on its own page in the Navigation group with Status=Live | Planned and Code / Name / Note as text properties. The open banner variants are built from REAL INSTANCES of it, not redrawn rectangles \u2014 which is the point: the banner drawer and the /portals directory had already drawn two different cards for the same object once, and a Figma copy would have made a third" },
      { kind: "Changed", text: "STATUS=PLANNED IS A DIFFERENT SHAPE, NOT A DIMMED ONE. Dashed border, no fill, no elevation, and it carries the note \u2014 because in code it renders a `<div>` rather than an `<a>`, and a card that merely looked faded would still read as something you can press. A planned card is not raised, because it is not a surface you can press" },
      { kind: "Changed", text: "ZERO UNBOUND VALUES ACROSS THE WHOLE SET \u2014 27 fills, 66 spacing properties and 18 text nodes, all resolving to published variables and styles. Instance descendants are excluded because they follow their main, and the SAMAVESH seal\u2019s ring lettering is excluded because its own description declares it artwork rather than typography. The audit is the standard `component-authoring.md` asks for, run rather than assumed" },
      { kind: "Added", text: "A CODE CONNECT TEMPLATE FOR PORTAL CARD, and the banner\u2019s repointed at the two-axis set. Both account for every Figma property, and `Status` is mapped INVERTED on purpose \u2014 Status=Planned means `planned={true}` and must NOT emit an href. Neither can publish until the library itself is published, which is a human action in the Figma UI, and both say so" },
      { kind: "Changed", text: "THE FILTER ROW IS DELIBERATELY ABSENT FROM FIGMA, matching what ships. The chips render only when the portals on show span more than one category, and every portal live today is a scheme portal \u2014 so drawing them in the component would document a control nobody can see on the site. Recorded as a known gap on the set, to be drawn when the first commission or corporation goes live" },
    ],
  },
  {
    version: "v0.69.0",
    date: "2026-08-30",
    current: false,
    changes: [
      { kind: "Fixed", text: "THE BOTTOM OF THE PANEL WAS UNREACHABLE ON A PHONE, and the sticky fix from the previous release caused it. Measured at 375x812: eight stacked cards make the panel 1062px, 594px past the fold \u2014 and because the band is sticky while open and the panel hangs off it absolutely, THE PANEL PINNED TOO. Scrolling moved the band and revealed none of it, so the last three portals and the footer link could not be reached at all. The panel now caps at the space actually left below the pinned band and scrolls inside it, in `dvh` because mobile browser chrome hides and shows, with `overscroll-behavior: contain` so a flick past the end does not drag the whole band away" },
      { kind: "Fixed", text: "A FAST DOUBLE-TAP LEFT THE PANEL OPEN WHEN IT SHOULD HAVE CLOSED. `handleToggle` computed `!open` from the render closure, so two clicks landing in the same tick both read the same stale value and the second was swallowed. It toggles through the functional updater now, and reports the value it actually committed rather than the caller guessing it again. Found by clicking twice in one tick \u2014 invisible to the type checker and to every gate" },
      { kind: "Added", text: "THE COMPONENT EXISTS IN THE SAMAVESH LIBRARY NOW, on its own page under Section Templates beside Navbar and Footer, as a Tone variant set: Light | Dark | Tint. ZERO UNBOUND VALUES across 9 fills, 21 spacing properties and 6 text nodes \u2014 the seal and the Explore button are INSTANCES of the published components, never redrawn, and the seal\u2019s ring lettering is excluded because its own description declares it artwork rather than typography. Its description carries twelve numbered rules, the contrast research and the token map" },
      { kind: "Fixed", text: "THE CODE CONNECT TEMPLATE POINTED AT A SECTION OF SCREEN MOCKUPS. Node 7116:33784 in the handoff file is a SECTION, and Code Connect maps a COMPONENT \u2014 so the template could never have resolved. It now targets the library set. It is still not publishable until the library itself is published, which is a human action in the Figma UI, and it says so" },
      { kind: "Added", text: "A BEHAVIOUR SPECIFICATION, at docs/design-system/components/samavesh-banner.md. Every state, every way in and out and what each does with focus, the nine defects this component has actually shipped and what caused each, thirteen edge cases with their measured result, and the things that are deliberately absent \u2014 no focus trap, no scroll lock, no focus move on open, because it is a disclosure and not a modal. Written because \u201ctested a few things and moved on\u201d is how the two defects above shipped" },
      { kind: "Added", text: "CITIZEN PORTALS IS IN THE WEBSITE NAVIGATION, under Offerings rather than as an eighth top-level item \u2014 the row has 43px of slack and an eighth entry needs about 96, so promoting it would have dropped the inline nav to the hamburger at 1280. It closes a real wayfinding hole: /portals was reachable from the hub root and from error pages but from nowhere in the website\u2019s own navigation, which made the banner\u2019s footer link the ONLY route to it" },
      { kind: "Changed", text: "THE PANEL EASES EXPONENTIALLY AND THE CHEVRON ROTATES. `cubic-bezier(0.22, 1, 0.36, 1)` leaves the gate quickly and decelerates in, where Material\u2019s symmetric curve read as playback at a fixed rate. One chevron rotating 180\u00b0 replaces two glyphs swapping, which was a discrete jump at the midpoint of a continuous gesture. Cards settle 28ms apart, capped at eight steps so the tail of a long list does not arrive after the eye has gone looking for it \u2014 and `prefers-reduced-motion` removes the travel AND the delay, since a delay with no transition would hold them invisible for a quarter of a second" },
    ],
  },
  {
    version: "v0.68.0",
    date: "2026-08-30",
    current: false,
    changes: [
      { kind: "Fixed", text: "THE PANEL NO LONGER CLOSES WHEN YOU SCROLL, AND THE VERSION THAT DID WAS WRONG. Scrolling is how a reader LOOKS at a long list, not how they say \u201cnever mind\u201d \u2014 dismissing a panel somebody deliberately opened, to solve a layout problem, is the interface overruling the person using it. The problem it was aimed at was real: ~540px of scroll took the band, and the only visible close, off the top while a 600px panel stayed over the page. The right answer keeps the control reachable instead of cancelling the intent \u2014 the band now PINS under the masthead for as long as the panel is open, so the toggle travels with the panel it controls" },
      { kind: "Changed", text: "THE STICKY OFFSET IS READ, NOT TYPED. `--sa-header-pinned` is measured and published on :root by SiteHeader\u2019s ResizeObserver, so the band stays flush as the masthead condenses \u2014 verified live at 176px resting and 57px condensed, with a zero-pixel gap at both. A number typed here would have been wrong on the second one, and wrong again at a different font scale" },
      { kind: "Changed", text: "THE CHEVRON ROTATES; IT NO LONGER SWAPS GLYPH. `expand_more` \u2192 `expand_less` is a discrete jump at the midpoint of a continuous gesture. One chevron rotating 180\u00b0 on the drawer\u2019s own curve makes the control and the panel one movement, and survives being interrupted and reversed without a flicker" },
      { kind: "Changed", text: "THE DRAWER EASES EXPONENTIALLY INSTEAD OF SYMMETRICALLY. `cubic-bezier(0.4, 0, 0.2, 1)` is Material\u2019s standard curve and reads as playback at a fixed rate; `cubic-bezier(0.22, 1, 0.36, 1)` leaves the gate quickly and decelerates into place, which reads as something arriving under its own momentum. It is the closest single-curve approximation to the damping 0.8 / response 0.3 an OS drawer uses" },
      { kind: "Added", text: "THE CARDS SETTLE IN SEQUENCE \u2014 28ms apart, CAPPED AT EIGHT STEPS. Enough that the eye reads the grid filling in a direction; capped because an uncapped stagger over a long list makes the tail arrive after the reader has already gone looking for it. Transform and opacity only, so it stays on the compositor, and `prefers-reduced-motion` removes the travel AND the delay \u2014 a delay with no transition would hold the cards invisible for a quarter of a second" },
      { kind: "Fixed", text: "THE CODE CONNECT TEMPLATE IS MARKED AS NON-RESOLVING, BECAUSE THERE IS NO FIGMA COMPONENT TO MAP TO. Node 7116:33784 is a SECTION holding two screen mockups; the banner is artwork inside them, and a library search returns the seal, Ticker and Carousel but no banner. Code Connect maps a COMPONENT, so the template published nothing \u2014 the exact \u201cunpublishable mapping reads as a finished integration\u201d failure `design-system.md` warns about, shipped by the session that wrote it. The file now says so at the top" },
    ],
  },
  {
    version: "v0.67.0",
    date: "2026-08-30",
    current: false,
    changes: [
      { kind: "Added", text: "THE BANNER HAS THREE BAND TONES, AND THE DEFAULT KNOWINGLY FAILS WCAG 2. `tone` is `light` (white on saffron, 2.91:1 \u2014 FAILS) | `dark` (near-black on saffron, 6.50:1) | `tint` (near-black on pale saffron, 17.29:1). Shipping three is not indecision: on this ground the question has no single right answer, and pretending otherwise is what produced four weeks of wrong claims in this system\u2019s own documentation" },
      { kind: "Changed", text: "THE GROUND IS THE CONSTRAINT, NOT THE INK, AND THAT IS PROVABLE. Scanning ~700,000 colours against India Saffron #ff671f found ZERO that clear WCAG 2\u2019s 4.5:1 AND APCA\u2019s Lc 75 for the 14px subline \u2014 not one, in the entire colour space, and still none relaxed to Lc 60. For the large bold wordmark, 34,887 clear both. So the black-versus-white argument the industry has run for years cannot be won on this band; only moving the text off the saturated ground wins it, which is what `tone=\"tint\"` does" },
      { kind: "Changed", text: "WCAG 2 AND APCA RANK THE INKS IN OPPOSITE ORDERS, and the reason is measurable. WCAG 2 computes relative luminance only; the Helmholtz\u2013Kohlrausch effect makes saturated colours read far brighter than their luminance, so vivid mid-tones are systematically misjudged. On saffron APCA scores white Lc 59.8, near-black 48.9 and the brand green 43.9 \u2014 exactly inverting WCAG 2\u2019s 2.91 / 6.50 / 4.85. This is a named field problem, \u201cthe orange button problem\u201d, not a quirk of this component" },
      { kind: "Changed", text: "THE BRAND GREEN IS OUT AS THE DARK INK, and it is the counter-intuitive result worth remembering. `--sa-color-status-successStrong` looks the most on-brand and passes WCAG at 4.85:1, but its APCA Lc is 43.9 \u2014 BELOW the 45 floor for headline text, making it the worst of the credible dark inks. `tone=\"dark\"` uses `--sa-text-neutral-bolder` instead, which is the best on both measures at once" },
      { kind: "Added", text: "THE NON-CONFORMANCE IS RECORDED WHERE AN AUDITOR WILL FIND IT \u2014 entry 8 in `docs/guidelines/README.md`, beside the seven existing divergences. It states the failing ratio, the evidence for choosing it, the fact that APCA was REMOVED from WCAG 3 in 2023 and is therefore not a compliance defence, and the one-word remedy (`tone=\"tint\"`). A deviation that is written down is a decision; the same deviation undocumented is a defect waiting to be found by somebody else" },
      { kind: "Added", text: "THE RESEARCH TRAVELS WITH THE CODE. The stylesheet header, the component doc block, the `SamaveshBannerTone` type, four Storybook stories and a new Variants section on the documentation page all carry the same numbers and the same reasoning \u2014 the contrast table, the user-testing split (61% preferred white, 71% among protanopia, the monochrome participant preferring black), the colour-space scan, and why APCA cannot simply decide it. Anyone who asks \u201cwhy is the orange band failing contrast?\u201d now finds the answer at whichever surface they happen to open" },
    ],
  },
  {
    version: "v0.66.0",
    date: "2026-08-30",
    current: false,
    changes: [
      { kind: "Fixed", text: "THE HERO CAROUSEL PAINTED STRAIGHT THROUGH THE OPEN DRAWER. Its prev/next arrows and its play/pause dock are `z-20`, and the banner was `z-20` too \u2014 equal z-index falls back to DOM order, and the carousel comes later. The banner is now 30: above page content, and deliberately far below the fixed rails (ImportantLinks 1002, assistant 1010), neither of which should be covered by a panel opened at the top of the page. Found by looking at a screenshot, not by measuring" },
      { kind: "Fixed", text: "SCROLLING WITH THE PANEL OPEN LEFT A FLOATING PANEL AND NO WAY OUT. The banner is not sticky, so ~540px of scroll took the band \u2014 and the only visible close \u2014 off the top of the screen while a 616px overlay stayed open over the page. Escape and click-outside still worked, but nothing on screen SAID so. Scrolling now closes it, past an 8px threshold so momentum jitter does not. The alternative, making the band sticky under the already-sticky masthead, was rejected: two stacked bars eat ~150px of every viewport for a control used once" },
      { kind: "Added", text: "`PortalCard` IS A COMPONENT NOW, extracted because two surfaces were drawing the same object two ways \u2014 the banner drawer and the citizen-portals directory. ONLY THE BANNER HAS ADOPTED IT: `/portals` still renders its own `portals-gw__card`, so this currently makes three cards rather than one, and the consolidation is open work rather than done. It is deliberately DUMB: it takes strings. Which portals to show and whether one is built stays with the caller reading the registry, so that query does not end up in three places again. `planned` renders a `<div>`, not an `<a>` \u2014 the prop exists because linking an unbuilt portal is what shipped a 404 to citizens on every page" },
      { kind: "Changed", text: "THE EXPLORE BUTTON WEARS THE DS BUTTON INSTEAD OF IMITATING IT. It restated fill, radius, padding and shadow by hand while `Button` already ships `variant=\"success\"`; it now borrows that appearance through `buttonClasses`, which exists precisely so an element that must be a real `<button>` for other reasons does not have to fork one. The green went 6.72:1 \u2192 9.12:1 as a side effect. Its mobile full-band treatment is now scoped inside `max-width: 767px` rather than being un-set at 768 \u2014 the un-set version needed `color: unset`, which the token lint correctly refuses" },
      { kind: "Changed", text: "THE CITIZEN-PORTALS PAGE IS RETIRED AND REDIRECTS TO `/portals`. `PortalsExplorer` already lists all 21 portals from the registry with free-text search, a category filter, a live/planned filter and live-first ordering \u2014 a strict superset of the eight hand-written cards it replaces. Two directories of the same objects is the failure this estate has already had four times. A redirect rather than a delete: the path is in the search index and may be linked externally, and a 404 is a worse answer than the better page" },
      { kind: "Changed", text: "THE DRAWER\u2019S FOOTER LINK SAYS WHAT IS ON THE OTHER SIDE. \u201cView all citizen portals\u201d promised more of the same; the drawer shows LIVE portals and `/portals` shows everything including what is still being built, with search. It now reads \u201cBrowse every portal, including those in development\u201d" },
    ],
  },
  {
    version: "v0.65.0",
    date: "2026-08-30",
    current: false,
    changes: [
      { kind: "Added", text: "THE PORTAL DRAWER HAS A CATEGORY FILTER, AND IT HIDES ITSELF WHEN IT WOULD DO NOTHING. `PORTAL_CATEGORIES` is now the estate\u2019s one filter vocabulary \u2014 Commission \u00b7 Scheme Portals \u00b7 Corporations \u00b7 Training & Capacity Building \u00b7 Foundation & Autonomous Bodies, from the design \u2014 replacing four function-based names nothing consumed. The chips render only when the portals on show span MORE than one category: \u201cAll (8)\u201d beside \u201cScheme Portals (8)\u201d is two controls with a single outcome. Every portal live today is a scheme portal, so the live banner shows NO filter and will grow one by itself when a commission ships. Pinned in `portal-categories.test.ts`, which fails the day that assumption changes" },
      { kind: "Changed", text: "THE DRAWER OVERLAYS THE PAGE INSTEAD OF PUSHING IT DOWN. Opening it moved the hero and everything under it down ~300px \u2014 on a phone the content the reader was looking at left the screen \u2014 which reads as the page rebuilding itself rather than a panel opening. The Figma reference draws it over the hero for the same reason. Absolute off the band, dismissed by Escape (focus returns to the toggle) or a click outside (focus does NOT return \u2014 the pointer has already gone elsewhere and yanking it back would fight the click)" },
      { kind: "Fixed", text: "THE WHITE LINE UNDER THE ORANGE BAND WAS THE COLLAPSED DRAWER\u2019S OWN BORDER. `grid-template-rows: 0fr` zeroes the CONTENT, not the border box, so a 2px bottom rule stayed in layout while `visibility: hidden` stopped it painting \u2014 a reserved, unpainted strip showing the page background through, on every page at every width. The rule now belongs to the open state only" },
      { kind: "Changed", text: "THE BAND\u2019S INK IS DEEP INDIA GREEN, NOT NEUTRAL BLACK. Black cleared AA at 5.56:1 and was the right first move, but it reads as a browser default on a brand band. `--sa-color-status-successStrong` clears it at 4.85:1 and is already this component\u2019s palette \u2014 the Explore CTA is India Green \u2014 so the band, its rule and its button are one family instead of three. The saffron has still never moved" },
      { kind: "Added", text: "`Chip` GAINED A `tone` AXIS, rather than the banner forking its look. `brand` stays the default and the estate\u2019s selection colour; `success` paints the SELECTED state green for chips on a surface with no blue in it \u2014 the saffron-and-green drawer, where a blue pill was a third colour family on a two-family panel. Unselected is identical in both tones, so it is not a way to colour-code categories" },
      { kind: "Changed", text: "BELOW 768px THE BUTTON IS THE BAND. A 375px row cannot hold the wordmark, a three-line subline and a labelled pill, and the subline is the half that earns its place \u2014 it is what tells a first visitor that SAMAVESH is a mechanism rather than a logo. So the pill loses its chrome and its label, stretches over the whole band and leaves only its chevron. ONE control and one node in the accessibility tree; rendering a second mobile-only button would put the same action in it twice" },
      { kind: "Changed", text: "THE CITIZEN-PORTALS PAGE POINTS AT THIS ESTATE, NOT AWAY FROM IT. It carried its own hand-written list of eight portals linking to EXTERNAL production sites \u2014 a fifth independent portal list in a codebase that had already unified four \u2014 so the one \u201csee everything\u201d link on the website sent a reader off the prototype entirely. It now derives from `DEFAULT_APPS`, groups by the same five categories, links every built portal to its prototype under `/portals/*` and labels the rest" },
      { kind: "Fixed", text: "THE PORTALS GATEWAY WAS CAPPED AT 1280px IN THREE PLACES, a width on no rung of the estate ladder (1200 / 1320 / 1440). It now reads `--sa-container-page`, so it grows at 1440 and 1920 with every other content column instead of standing still" },
      { kind: "Changed", text: "THE BANNER REUSES `Chip` AND `Divider` INSTEAD OF DRAWING ITS OWN. It had hand-rolled a rule and would have hand-rolled the filter chips; both exist in the system and the design-system-first rule says to check before writing. What is still bespoke is the band, the drawer and the cards \u2014 recorded rather than quietly left" },
    ],
  },
  {
    version: "v0.64.0",
    date: "2026-08-30",
    current: false,
    changes: [
      { kind: "Fixed", text: "THE SAMAVESH BAND'S TEXT WAS WHITE ON INDIA SAFFRON AT 2.91:1, AND THREE DOCUMENTS CLAIMED IT PASSED AA \u2014 design.md, the Storybook story and this system\u2019s own component page, which asserted \u201cmeet 4.5:1+\u201d as a checklist item under 1.4.3. It failed for the 14px subline, and it failed for the title too: bold 22\u201328px is large text, which still needs 3:1. The fix moved the INK, not the brand \u2014 `--sa-color-text-default` on the SAME unchanged #ff671f is 5.56:1, so the identity paid nothing. A published false accessibility claim is worse than the failure it describes, because it tells the next builder the pattern is cleared" },
      { kind: "Fixed", text: "THE BANNER SHIPPED A 404 TO CITIZENS ON EVERY PAGE OF THE WEBSITE. Its NOS card linked `/portals/nos`, which does not exist \u2014 and `DEFAULT_APPS`, the estate\u2019s own registry, had NOS marked `planned` the entire time. The banner had copied the list and dropped the field that carried that fact. Status is now RESOLVED from the registry by matching `href` against `path`, so the two cannot disagree again; a planned portal renders as a labelled card rather than a link. `check:website-links` never saw it, because the link lives in a design-system constant rather than a website page" },
      { kind: "Fixed", text: "THE BAND WAS 16px OUT OF LINE WITH THE PAGE, AND ITS LAST PORTAL CARD RAN UNDER THE IMPORTANT LINKS RAIL. Both from one cause: a restated `max-width: 1320px`, against a `.sa-container` that owns a three-step ladder (1200 / 1320 / 1440) AND the right-wall gutter. At 1280 the page column started at x=40 and the banner\u2019s at x=24; at 1920 the page went to 1440 and the banner stayed at 1320. Adopting `.sa-container` fixed the alignment and the rail collision together \u2014 the mechanism for the second already existed and the banner simply was not using it" },
      { kind: "Fixed", text: "THE DRAWER\u2019S PORTAL CARDS WERE NOT LINKS TO A SCREEN READER. `role=\"listitem\"` sat on the anchor, and an explicit role REPLACES the implicit one \u2014 so four destinations were announced as list items with no link role between them. Now a real `<ul>` of `<li>` wrapping real `<a>`. Related: Escape now returns focus to the Explore button, because the collapse applies `visibility: hidden` and a keyboard user who was on a card was left with focus on an element nobody can see and no focus ring anywhere on the page" },
      { kind: "Changed", text: "THE EXPLORE CTA IS INDIA GREEN, WHICH IS BOTH CLOSER TO FIGMA AND MORE ACCESSIBLE THAN FIGMA. The reference draws it #198754 (4.53:1, clearing AA only just); the build drew it `--sa-color-status-success` #004220, a near-black green at 11.67:1 that was accessible but off-reference. `--sa-brand-samavesh-green` #046a38 is the reference\u2019s tone at 6.72:1. Per standards-precedence the deviation runs UPWARD and is recorded beside the rule that makes it" },
      { kind: "Changed", text: "THE BADGE WENT FROM 743 KB TO 13 KB. The master SAMAVESH mark is a traced emblem \u2014 80 paths at four decimal places \u2014 and it was `loading=\"eager\"` on every page of the website to draw a 52px circle, almost certainly the heaviest single asset on the site. The default is now a 156px raster (3\u00d7 the badge). The master SVG is untouched for consumers that render the mark large; pass `logoSrc` to reach it" },
      { kind: "Changed", text: "THE DRAWER ANIMATES ON `grid-template-rows`, NOT `max-height: 1200px`. That cap was a guess, and a guess that clips silently \u2014 pass roughly eight cards on a narrow viewport and the bottom of the drawer simply vanishes. `0fr \u2192 1fr` resolves to the content\u2019s real height. `prefers-reduced-motion` now removes the tween and the card hover lift; 26 other component files already honoured it and this one did not" },
      { kind: "Fixed", text: "THE DRAWER\u2019S `<h2>` SAT ABOVE EVERY INNER PAGE\u2019S `<h1>`. The banner is site-wide chrome and was mounted INSIDE `<main>`, so opening the drawer put \u201cChoose a portal to visit\u201d ahead of \u201cAbout Us\u201d in the document outline. It now renders between the header and `<main>` at all five call sites. Also: the drawer ground is a flat `--sa-color-brand-saffronLight`, matching the Figma render exactly \u2014 it had been a `color-mix(\u2026, white)` gradient, which was both an unbound colour keyword and a different colour from the design" },
      { kind: "Changed", text: "TYPE IN THE BANNER BINDS TO `--sa-type-*`. Twelve raw `font-size` literals \u2014 13 / 14 / 15 / 20 / 22 / 26 / 28 / 32px across six media queries \u2014 became four fluid tokens, and six of the media queries went with them. Neither `lint:css` nor any other gate had flagged them: stylelint reads the raw-hex rule and never looked at sizes, which is its own finding" },
    ],
  },
  {
    version: "v0.63.0",
    date: "2026-08-29",
    current: false,
    changes: [
      { kind: "Added", text: "FULL DOCUMENTATION FOR ALL 52 COMPONENT STUBS. Completed end-to-end design system documentation across all components (Auth, Dashboard, Data Display, Feedback, Layout, Navigation, Utilities) with live interactive specimens, strict TypeScript prop contracts, WCAG 2.2 AA & GIGW 3.0 compliance checklists, and keyboard navigation tables." },
      { kind: "Added", text: "SIDEBAR INSTANT FILTER & COLLAPSIBLE GROUP ACCORDIONS. The design system sidebar now features instant live text filtering with match counters, collapsible accordion sections by category, path-aware active states, and standardized status pill badges (Stable, Beta, Alpha, New)." },
      { kind: "Added", text: "DYNAMIC SEARCH INDEX AUTOMATION PIPELINE. Added `scripts/build-docs-search-index.mjs` and `npm run check:docs-search` gate that automatically indexes all 117 documentation pages and feeds the global ⌘K command search modal." },
      { kind: "Added", text: "BILINGUAL PLAYGROUND SANDBOXES & FEEDBACK BAR. Enhanced interactive component playgrounds with English/Hindi dual-language previews, HTML vs JSX tab modes, tokenized typography, and embedded 'Was this page helpful?' feedback bars across all doc articles." },
      { kind: "Fixed", text: "STANDARDIZED ACCESSIBILITY LANDMARK HIERARCHY. Normalized all documentation pages from nested `<main>` tags to `<article className=\"docs-article\">` with dynamic On-This-Page table of contents re-scanning and mobile bottom drawer launcher." },
    ],
  },
  {
    version: "v0.62.0",
    date: "2026-08-27",
    current: false,
    changes: [
      { kind: "Changed", text: "\u201cSTART OVER\u201d NO LONGER THROWS THE CONVERSATION AWAY. It rules the transcript off with a labelled separator \u2014 \u201cNew conversation\u201d \u2014 and greets again underneath, leaving every answer the citizen already gave scrolled up above it. A mis-tap now costs a scroll instead of five questions about caste, income and disability asked a second time. Nothing is destroyed, so there is no undo to build, no confirmation to sit through, and no snapshot to keep" },
      { kind: "Fixed", text: "IT ALSO SAT DIRECTLY UNDER SEND, AND EVERY MEASUREMENT SAID THAT WAS FINE. On a 375px phone it was 25px below Send in the SAME 32px column, with the whole of Send\u2019s width above it \u2014 the most-pressed control in the panel stacked on the rarest and the only destructive one. WCAG 2.2 \u00a72.5.8 wants 24px targets and these are 32; UX4G wants 8px between them and the gaps were 24\u201336. Nothing failed, because frequency-versus-severity adjacency is not something a checklist can see. IT IS A HEADER CONTROL NOW, and the footer is back to the composer and the disclaimer alone. Three footer arrangements were tried and each broke something measurable: hard right stacked it under Send; at the head of the disclaimer\u2019s row its 101px pushed that text 109px off the panel\u2019s left edge and rewrapped it to three lines; on its own line it cost 24px of height on a panel already tight at 375px. The premise had changed underneath all three \u2014 it appends now, so the danger that kept it out of the header (an unlabelled icon beside \u2715, on a control that used to WIPE the transcript) is gone. Order is start over, expand, \u2715 \u2014 \u2715 last, because that corner is where everyone reaches to dismiss" },
      { kind: "Fixed", text: "THE CHAT LOG NEVER ACTUALLY FOLLOWED THE CONVERSATION, AND HAD NOT FOR SOME TIME. The log carries `scroll-behavior: smooth` for hand-scrolling, and the auto-follow set `scrollTop` directly \u2014 which inherits it, turning each follow into an animation that the next render restarts. Measured: a long transcript pinned at scrollTop 540 of 1046 on the branch and 0 of 8102 on main, while the same element accepted `scrollTo({ behavior: \u201cinstant\u201d })` and landed exactly. It predates this change and was invisible only because Start over used to empty the log and leave nothing to scroll" },
      { kind: "Fixed", text: "THE HEADER GAP IS 8, NOT 12, AND THREE PIXELS IS THE WHOLE REASON. The third control left the brand 125px for a title needing 128 \u2014 enough to wrap \u201cSamajik Sahayak\u201d onto two lines and shove the Devanagari subtitle down with it. Four gaps at 8 give back 16px, so the title clears by 13. A test pins it, because a fourth control would break it silently" },
      { kind: "Changed", text: "START OVER IS AN ICON BUTTON, NOT A DS `Button` \u2014 WHICH REVERSES A DECISION THAT WAS RIGHT WHERE IT STOOD. In the footer, variant=\u201cneutral\u201d appearance=\u201ctext\u201d was hard-won: hand-rolled, that control had landed in the estate\u2019s rejection red for housekeeping. In the header the same reasoning inverts \u2014 it is one of three icons in one corner, and a third that styled itself differently would be the same failure pointing the other way. Its label lives in aria-label and title, so it is announced and revealed but not drawn. CONSEQUENCE WORTH KNOWING: `neutral` is once again a Button variant with no product consumer" },
      { kind: "Added", text: "`restartNotice` NAMES THE SEPARATOR, and `from: \u201csystem\u201d` is a third kind of turn \u2014 not a speaker, a labelled rule. Two greetings in a row read as the assistant repeating itself; a rule between them reads as a new start. It is a prop rather than a constant because this estate serves Hindi as well as English, and a hardcoded English string inside the design system is a translation defect waiting to be found by a citizen" },
      { kind: "Changed", text: "THE LABEL \u201cSTART OVER\u201d WAS REVIEWED FOR A RENAME AND KEPT. It became more true, not less \u2014 you start over, and nothing is taken away. \u201cClear chat\u201d would now say the one thing that is no longer so. A CONTROLLED consumer must do the append itself in `onEndChat`: carry the turns it is showing, add its own separator, greet again, and re-key the carried messages, because the finder derives ids from script nodes and a second run repeats them" },
    ],
  },
  {
    version: "v0.61.0",
    date: "2026-08-27",
    changes: [
      { kind: "Changed", text: "THE BUTTONS PAGE HAD EIGHT TOP-LEVEL NODES, ZERO SECTIONS AND SEVEN OVERLAPPING PAIRS \u2014 the Button set had grown to 7,828px in the migration and was sitting on top of the legacy boards, the IconButton set, and 24px of the Component record. It now follows the convention the Ticker and Chatbot pages already set: numbered sections for the published sets, a labelled scratch section for what is kept but not published, and the Documentation and Component record frames below. Zero overlaps" },
      { kind: "Fixed", text: "A FRAME NAMED \u201cIcon Buttons\u201d ACTUALLY CONTAINED THE `Link` COMPONENT SET \u2014 fifteen variants, with malformed names carrying leading and doubled spaces (`Size= Small, State=  Default`). Worse, `Link` is published in Figma and has NO CODE COUNTERPART AT ALL. Lifted into its own honestly-named section; the naming and the parity gap are recorded as open rather than quietly fixed, because a component the code does not have is a decision, not a typo. The other two frames were hand-drawn specimen boards \u2014 zero components, zero instances, one still advertising Tonal \u2014 and are parked in a scratch section rather than deleted, which is what was done with the Ticker\u2019s detached panel" },
      { kind: "Changed", text: "ICONBUTTON MIGRATED IN PLACE, 60 VARIANTS TO 45. Tonal deleted (fifteen variants, ZERO instances, and the appearance no longer exists in code). `Type` became `Sub-type` to match Button, and its value `Default` became `Text` \u2014 it rendered with no fill and no stroke, so it WAS the text appearance, wearing a name that made the quietest option sound like the normal one. All 76 instances verified healthy after the renames, which is the step that silently breaks a library if you assume instead of check. It also had no Code Connect template at all; it has one now" },
      { kind: "Fixed", text: "THE CODE CONNECT PARITY GATE WAS PASSING VACUOUSLY ON EVERY INHERITED PROP. It read only an interface\u2019s own body, so `IconButtonProps extends Omit<ButtonProps, \u2026>` \u2014 two members declared, eleven offered \u2014 reported `size` as a prop that \u201cdoes not exist\u201d. The dangerous half is the opposite case: a template emitting an inherited prop it had MISSPELLED would have been reported identically and dismissed as a false positive. It now follows `extends` and `Omit<>` through relative imports. The file\u2019s own comment records finding this same blind spot once before; this is the other half of it" },
      { kind: "Changed", text: "THE FIGMA DOCUMENTATION QUOTED FOURTEEN NUMBERS THAT WERE NO LONGER TRUE, and every replacement was re-counted through the Plugin API rather than reasoned from the change. 720 variants is 360. Radius is 360/360 on shape/8. Colour is 993 of 1,102 bindings on Tier-3 cmp/* with ZERO on Tier-1, where it was 900 of 1,956 two days ago. Padding is the one still wrong: 960 of 1,440 bound and every one to the TYPE collection" },
      { kind: "Added", text: "AND A PADDING DEFECT NOBODY HAD NAMED: 480 of the 1,440 padding slots cannot be bound at all, because the vertical padding is a raw 10px and 10 is not a rung on the space scale. Code uses 6px in the same place \u2014 so the two surfaces disagree on a value neither can currently name. Recorded rather than fixed: moving 10 to 8 or 12 changes geometry on 240 variants, which is a decision and not a rebind" },
      { kind: "Fixed", text: "THE AUDIT MEASURED EVERY BUTTON AGAINST A WHITE PAGE, AND `inverse` IS NEVER ON WHITE. Measured on a brand surface, the inverse outlined border failed WCAG 1.4.11: a flat white-alpha for every intent, 2.25:1 on primaryScale/600 and 1.91:1 on gov-blue. It cleared 3:1 on navy alone, the one brand surface anybody had checked. It paints in the portal login shell\u2019s \u201cSigning Into\u201d bar, on the design-system docs and in Storybook. So the audit was wrong in both directions and for opposite reasons: it reported a failure on a token the component does not bind, and missed one on a surface it never measured. The surface a control sits on is part of the measurement" },
      { kind: "Fixed", text: "`danger` NO LONGER LOSES ITS SIGNAL ON A BRAND SURFACE, AND IT IS THE SAME FIX. Every intent resolved the SAME white-alpha border, so all four inverse outlined buttons were identical \u2014 a token fact, not just a CSS one. Each intent now takes its own scale at rung 100, 50 on hover, white when pressed: brighter as the control is engaged, which is the correct direction on a dark ground. That makes the four distinguishable AND lifts them to 4.14\u20134.72:1 on the live bar. The LABEL stays white deliberately \u2014 a 100-rung tint as text measures 4.49:1 there, a hundredth short of the 4.5 that 1.4.3 asks for. Two jobs, two values" },
      { kind: "Added", text: "`tone=\u201cinverse\u201d` REPLACES TWO APPEARANCE WORDS, AND THAT IS WHY THE BUG EXISTED. `inverse` and `inverseOutlined` were modelled as appearances, so they could only ever have ONE look each \u2014 there was nowhere for the variant to go. As an axis that CROSSES appearance, every variant keeps its intent on either ground. The old two words still work: the Ticker\u2019s documented route-out, the login shell and two Code Connect templates all name them, and breaking those to rename a prop would be a poor trade" },
      { kind: "Removed", text: "`tonal` IS RETIRED RATHER THAN REPAIRED. Its fill and its border were the same pale wash, so the control had no edge against the page at all \u2014 1.21:1 to 1.52:1 against a 3:1 requirement \u2014 and darkening the border to fix that would simply have made it `outlined`. It had two consumers in 494 buttons; both are now outlined. The contrast gate\u2019s exemption list, which was created holding exactly these four and asserted to only ever shrink, IS NOW EMPTY" },
      { kind: "Added", text: "`loading` SETS aria-busy AND DISABLES THE CONTROL, so a form cannot be submitted twice while the first submission is in flight. It deliberately does NOT swap the label: a control that loses its name mid-action is unusable with a screen reader, so you pass \u201cSubmitting\u2026\u201d yourself. The docs used to tell consumers to wire aria-busy and disabled by hand, which meant every consumer could forget half of it" },
      { kind: "Added", text: "AN IconButton, AS A COMPONENT RATHER THAN AN `iconOnly` PROP \u2014 SO THAT aria-label CAN BE REQUIRED BY THE TYPE SYSTEM. UX4G models icon-only as a property of the button, which is a fair reading; it is not the one taken here. On an ordinary Button the accessible name arrives as `children`, and a boolean prop cannot make a DIFFERENT prop mandatory, so an unlabelled icon-only button would compile. This estate has already paid for that lesson \u2014 533 of 718 icon call sites were missing their label before Icon started hiding itself by default. A contract this easy to forget belongs in the type, not in a review checklist" },
      { kind: "Added", text: "A ButtonGroup, WHOSE LOAD-BEARING JOB IS THE SPACING NOBODY REMEMBERS. UX4G 3.0 asks for 8px between adjacent targets, and WCAG 2.2 2.5.8 lets a target smaller than 24\u00d724 be met by SPACING instead \u2014 so a row of adjacent `sm` buttons with no gap is exactly the case that fails, and a group is exactly where adjacency happens. Reaching for a bare flex div is what produces those rows. It also gives the row a role and a required name, so a screen reader announces \u201cRecord actions, group\u201d rather than reading four loose buttons. `attached` joins them into one segmented control \u2014 for ALTERNATIVES only, never for Save beside Delete" },
      { kind: "Fixed", text: "AND THEN I MADE THE SAME MISTAKE, IN THE COMMIT THAT CORRECTED IT. The first write-up of the fix above named the TICKER as the failing case, on the strength of the token value alone \u2014 a token measured where a component was meant, which is the exact error it criticises the audit for. The ticker\u2019s route-out has no border at all: `ticker.css` carries `.sa-ticker__action > :is(a, button) { border: 0 }`, which strips it inside the same cascade layer at higher specificity, so it renders as a text link. It got past a GREEN contrast gate, because that gate reads token values and button.css bindings and cannot see a third stylesheet overriding the outcome. The claim is corrected everywhere it was written, and the gate now says in its own header what it cannot see. A latent oddity this turned up and did not fix, because PR #207 is open on the ticker: the component asks for `inverseOutlined` and then removes the border that makes it outlined" },
      { kind: "Fixed", text: "THE FIGMA PAGE STILL DESCRIBED EVERY DEFECT AS OPEN, WHICH IS WORSE THAN SAYING NOTHING \u2014 A DOCUMENTATION PAGE IS BELIEVED BECAUSE IT LOOKS AUTHORITATIVE. Eleven text nodes on the Buttons page were rewritten to match what ships: the hero\u2019s \u201c5 boundary failures\u201d is 0, the 03 Size frame is no longer NAMED `open` and no longer calls the height fixed, 04 States stops saying loading is absent and stops warning against disabled link-buttons, 05 Accessibility records both the phantom fifth failure and the real one that was missed, and the Component record reads CLOSED \u00b7 CODE. Verified byte-for-byte against the recorded claims afterwards" },
      { kind: "Fixed", text: "AND ONE STALE NUMBER THAT WAS NOT THIS CHANGE\u2019S TO FIX. The hero also claimed \u201c46% colour on Tier 1 \u2014 900 of 1,956 Figma bindings reach past Tier 2\u201d, which belonged to the ref-tier cleanup of 26 August and had simply never been carried onto the page. It was re-counted live before being rewritten rather than taken on trust from design.md: 3,436 nodes walked, 2,257 colour bindings, ZERO on ref/*. It now reads 0%. Left alone deliberately: the four Sub-type=Tonal component variants, because deleting a Figma variant breaks every instance of it \u2014 that is a migration, not a documentation fix, and it stays on the open list" },
      { kind: "Changed", text: "A PIN THAT PASSED AGAINST THE VERY BUG IT WAS WRITTEN TO CATCH. The inverse test asserted the four borders were DISTINCT, and reverting a single variant to the old white-alpha still leaves four different colours \u2014 so it went green under the defect. Each border is now compared against its OWN intent token, resolved through the same computed-style path so the comparison is about colour rather than about `#c0dbff` versus `rgb(192, 219, 255)`. Distinctness is still asserted, second" },
    ],
  },
  {
    version: "v0.60.0",
    date: "2026-08-27",
    current: false,
    changes: [
      { kind: "Fixed", text: "A DISABLED LINK-BUTTON WAS AN ORDINARY LINK. `<Button href disabled>` emitted `<a disabled>`, and `disabled` is not a valid attribute on an anchor \u2014 so the browser ignored it completely: focusable, clickable, full opacity, no `aria-disabled`. It looked disabled to the person who wrote it and behaved as a live link to the person using it, which is the worst of both. The anchor now DROPS `href` and carries `aria-disabled` plus `role=\u201clink\u201d`. Dropping the href is the whole fix and it is better than swallowing clicks: an anchor without one is not focusable and not activatable by the browser\u2019s own rules, so there is no handler to get wrong and no `tabIndex` to keep in sync with the disabled state" },
      { kind: "Fixed", text: "THE LABEL WAS CLIPPED WHEN A READER SCALED TEXT TO 200% (WCAG 1.4.4). The sizes set `height`, which is a ceiling as well as a floor: an `md` button held 40px while its content needed 41, and `sm` squeezed 34 into 30. They set `min-height` plus vertical padding now, and each names its own line-height role token instead of a raw `1`. The padding is deliberately SMALLER than the amount that would fill the box \u2014 `min-height` is what makes a button 32/40/48 at 100%, and the padding is the headroom as the text grows. Sized the other way round, the button outgrows its own size ladder at the first zoom step" },
      { kind: "Fixed", text: "THE AUDIT REPORTED FIVE CONTRAST FAILURES. THERE ARE FOUR. The fifth, \u201cneutral outlined 2.15:1\u201d, measured `cmp/action/neutral/secondary/default/border` \u2014 a token the component does not bind. The border it actually paints is `#1e2124`, at 16.18:1. This is the SAME error class as the \u201c~400 button backgrounds on raw primary\u201d claim corrected on 26 August: a token was measured where a component was meant. An audit that measures tokens measures the system\u2019s intentions; only measuring the component measures what ships. The four that are real are all `tonal`, 1.21 to 1.52 against a 3:1 requirement, and `tonal` is being retired rather than darkened \u2014 it cannot gain an edge without becoming a different appearance, and it has two consumers in 494 buttons" },
      { kind: "Added", text: "NON-TEXT CONTRAST IS NOW MEASURED ON EVERY BUILD, AND IT READS THE COMPONENT RATHER THAN A LIST. `action-nontext-contrast.test.mjs` parses the variant blocks out of `button.css` and measures whatever they actually bind, in both brands \u2014 so a rebinding cannot escape it and no hand-kept table can go stale, which is precisely how the wrong number got published the first time. Its exemption list holds exactly the four tonal boundaries and is asserted to only ever shrink. All three of its failure modes were exercised by breaking them deliberately, including a simulated rebind to the 2.15:1 token, which it caught" },
      { kind: "Added", text: "THE BUTTON DOCS PAGE NOW DEMONSTRATES ITS OWN SIZE LADDER. It stated \u201c32 / 40 / 48px\u201d in prose and rendered none of them \u2014 every button on the page was `md`. A page that publishes three numbers and shows one is exactly the documentation that gets believed because it looks authoritative. It also renders a disabled link-button beside the disabled button, which is the pair that makes the fix above legible" },
      { kind: "Changed", text: "TWO PINNED FIGMA CLAIMS FLIPPED FROM ASSERTING A DEFECT TO ASSERTING ITS FIX, WHICH IS THE POINT OF PINNING THEM. The size claim asserted `min-height` was ABSENT \u2014 \u201cthe day someone fixes it, this assertion fails and the page must stop calling it open\u201d \u2014 and it duly failed. It now asserts no bare `height` survives anywhere in the file, which is the assertion that actually holds: a `min-height` on `md` means nothing if `sm` quietly keeps a fixed one. The neighbouring claim was passing for the WRONG REASON \u2014 its `height: 32px` pattern also matches `min-height: 32px` by substring \u2014 and has been tightened" },
    ],
  },
  {
    version: "v0.59.0",
    date: "2026-08-27",
    current: false,
    changes: [
      { kind: "Fixed", text: "A PHONE HAD NO TEXT RESIZE, NO ACCESSIBILITY PANEL AND NO LANGUAGE SWITCH. The accessibility bar sheds its right-hand cluster below 768px \u2014 the Figma master\u2019s own call \u2014 and its stylesheet said the controls \u201cmove into the consumer\u2019s menu\u201d. The consumer\u2019s menu is NavSheet, and NavSheet was never passed them and never rendered them. Measured at 375px: all three had offsetParent null and width 0, and the sheet held no replacement, so the estate had none of them on the device most citizens use. GIGW 3.0 requires the first two on every page and the language control is the only route into the Bhashini path. The cluster is its own component now with the step in a shared store, the sheet renders it as labelled 44px rows, and the UX4G floating button comes back on mobile as a second route \u2014 suppressed only while the sheet is open, because it anchors the same corner the sheet covers" },
      { kind: "Fixed", text: "THE ORGANISATIONS MEGA-MENU RAN 582px OFF THE BOTTOM OF THE SCREEN. Measured at 1280\u00d7720: 1100px tall, max-height none, overflow visible, with two whole categories reachable only by scrolling the page behind the panel \u2014 and not at all once the masthead is pinned, since the panel hangs off a fixed element. It is capped against a measured --sa-header-bottom now and scrolls internally. The simple dropdown takes the same ceiling: it is short today, but nothing stops a consumer passing thirty children" },
      { kind: "Fixed", text: "THE MASTHEAD SAT UNDERNEATH PAGE CONTENT. .ds-hdr was z-index 50, and Leaflet gives .leaflet-map-pane a transform and z-index 400 while .leaflet-container creates no stacking context \u2014 so that 400 competes at the ROOT. Harmless while the masthead scrolled away; fatal once pinned. It is 900 now: above content, below the floating rail (Important Links 1002, chatbot launcher 1010, statutory accessibility panel 999999). NavSheet portals to <body> rather than bidding higher, because its own z-index 1101 was scoped inside the header\u2019s stacking context and had never applied at all \u2014 the sheet was opening under the rail the whole time" },
      { kind: "Changed", text: "THE MASTHEAD IS PINNED ON EVERY VARIANT, NOT JUST PORTALS. The public website gave up 200px of chrome and then offered no navigation, no search and no identity for the length of a scheme page. Search is the fallback for navigation and GIGW 5.2 wants it in a consistent position on every page; neither survives a masthead that leaves. The accessibility bar is not pinned with it \u2014 the header sticks at a negative offset equal to that bar\u2019s height, so tier 1 scrolls away and the brand and nav rows stay. One line of CSS instead of a wrapper element, and no padding compensation, because the header stays in flow" },
      { kind: "Changed", text: "\u201cSTATE=ON SCROLL\u201d IS RETIRED FOR A REAL CONDENSE. It dropped the lockup\u2019s ministry line to take the brand row 100px to 88 \u2014 measured 146 to 134 on the live portal. Twelve pixels, 1.7% of a 720px viewport, in exchange for a class, a listener, a media-query set and a Figma variant. The three tiers now become ONE bar past 120px of scroll: 200 to 65 on desktop, 258 to 57 on a phone. The emblem holds the same left edge in both states, because it is also the go-home control and an identity mark that crosses the screen on scroll reads as a different site; the department NAME is what is given up, and printing restores the full masthead because on paper it would be gone for good. Two thresholds \u2014 120 down, 40 back \u2014 since a single threshold on a state that changes document height is a latch waiting to oscillate" },
      { kind: "Added", text: "THE NAV HANDS OVER TO THE SHEET BEFORE IT CAN OVERLAP. At 1280 the content column is 1200; after its padding, the emblem, the search button and the CTA the nav has 880px, and the seven entries measure 837 \u2014 43px of slack against the ~96 an eighth needs. Past that the entries neither wrapped nor shrank, they ran under the search button. The information architecture should not have to be measured against a masthead before it can change. Two things this had to get right, both found by measuring: the sheet trigger is excluded from BOTH sides of the comparison, since it is absent while the nav shows and present once it collapses and counting it makes the two states disagree forever; and clientWidth INCLUDES padding, 48px of room that does not exist" },
      { kind: "Fixed", text: "THE ACCESSIBILITY BAR WAS NARROWER THAN THE MASTHEAD BELOW IT. Tier 1 took container/wide \u2014 a flat 1200 that never steps \u2014 while the brand and nav rows take container/page, which runs 1200 / 1320 at 1440 / 1440 at 1920. Measured: the bar\u2019s content sat 60px narrower than the rows beneath it at 1440 and 120px at 1920, which is the same class of misalignment SiteHeader.maxWidth exists to stop between the masthead and the page, one tier higher up. AccessibilityBar gained layout=\u201cpage\u201d and SiteHeader passes it" },
      { kind: "Fixed", text: "SCROLL-PADDING WAS FOUR HAND-WRITTEN NUMBERS, AND THE MOBILE ONE WAS OUT BY 126px. 150 for portal, 72 for compact, 204 for website and 132 below 767px \u2014 against a mobile header measured at 258. That is WCAG 2.4.11 failing the moment the header is pinned, and the 768\u20131023 band had no rule at all, so it inherited the desktop 204 against a real 170. Numbers typed beside a layout that answers to viewport, font scale and content length drift as soon as any of the three moves. One rule now, from --sa-header-pinned, measured by a ResizeObserver and written only while the masthead is at its resting height, since an anchor can land before the page has scrolled far enough to condense" },
      { kind: "Fixed", text: "THE PORTAL LOST ITS SIDEBAR CONTROL ON SCROLL. MenuToggle rendered only inside the resting brand row, so an app shell gave up the control for its own navigation the moment the page moved \u2014 on the surface where the sidebar IS the navigation. Restoring the masthead for print needed a second fix underneath it: growing the header ~135px above the viewport triggers the browser\u2019s scroll anchoring, which shifts scrollY to compensate, which the scroll handler reads and condenses straight back. The handler fired, the state was set, and the condensed bar never left" },
      { kind: "Fixed", text: "FOCUS WAS DROPPED EVERY TIME THE MASTHEAD SWAPPED. The tiers are swapped rather than hidden, so a keyboard reader who had tabbed into the header and then scrolled landed back on <body>, at the top of the document. Tracked with a focusin listener rather than sampled inside the effect: sampling only sees the moments the condensed state CHANGES, and focus almost always moves between two of those, so the sample was <body> every time and the restore never once fired" },
      { kind: "Fixed", text: "THE MASTHEAD SEARCH WAS NOT A SEARCH LANDMARK, AND THE SHEET\u2019S COPY OF IT WAS DEGRADED. The field rendered as a bare div, so landmark navigation could not reach the estate\u2019s primary search box on any page. NavSheet declared its own narrowed {placeholder, onSearch} type, dropping onQueryChange, suggestions and onSuggestionSelect \u2014 autocomplete worked on desktop and silently did not on a phone \u2014 and held its own query, so whatever had been typed in the masthead vanished on opening the menu. One HeaderSearchConfig now, shared, which makes that particular drift a type error" },
      { kind: "Fixed", text: "<Search> SPREAD {...rest} LAST, so a consumer\u2019s onKeyDown REPLACED the component\u2019s own \u2014 taking out Enter-to-submit and the whole combobox arrow-key path on a component that goes to the trouble of chaining rest.onKeyDown at the end of its handler. That chaining was unreachable; onFocus and onBlur chain the same way and were broken the same way, and a consumer could overwrite role=\u201ccombobox\u201d by accident" },
      { kind: "Fixed", text: "ARROW KEYS ENTER AND TRAVERSE AN OPEN NAV PANEL, and a menu-less entry no longer closes someone else\u2019s menu. Thirty organisation rows behind thirty presses of Tab is not navigation. The hover-close timer ran unconditionally, so brushing past \u201cHome\u201d on the way to the centred organisations panel shut it 300ms later from an entry that owns nothing" },
      { kind: "Changed", text: "THE MASTHEAD PLACEHOLDER NEVER FITTED THE FIELD IT LIVES IN. \u201cSearch Schemes, Services, Documents\u201d rendered as \u201cSearch Schemes, Services, Docu\u201d at 320px desktop, 218 tablet and 343 mobile \u2014 every breakpoint. A truncated placeholder is a truncated instruction" },
      { kind: "Fixed", text: "FIVE DOCUMENTATION SURFACES DESCRIBED A MASTHEAD THAT NO LONGER EXISTS. design.md still said the brand row is \u201c100 at rest and 88 On Scroll\u201d and told authors to pass collapseOnScroll \u201conly on portal variant\u201d; its Do/Don\u2019t table still asked for a drawer that \u201cflattens the mega-menu structure\u201d, which is the opposite of the fix that gave the sheet its columns back. The web docs page carried sticky default \u201cfalse (true when variant=portal)\u201d, collapseOnScroll default \u201cfalse\u201d, and a section saying the prop tucks the accessibility bar away \u2014 something it never did. The Code Connect template and the Storybook notes said the same. navbar.md\u2019s adoption section still listed four surfaces as hand-rolling a masthead when all four had been converted and check:chrome passes across 855 files \u2014 documentation claiming work was outstanding that had in fact shipped. NHAPOA\u2019s tagline inversion is recorded as the live compromise it is rather than the open question it was" },
    ],
  },
  {
    version: "v0.58.0",
    date: "2026-08-25",
    current: false,
    changes: [
      { kind: "Fixed", text: "THE HAIRLINE BETWEEN PAUSE AND \u201cVIEW ALL\u201d ONLY EXISTED ON NARROW BARS. The desktop bar and every panel had the control and the route abutting with nothing between them, while the Figma library drew a divider \u2014 a divergence in the direction hardest to notice. It is an adjacent-sibling rule now (`.sa-ticker__nav + .sa-ticker__action`) rather than one keyed on shape or width: the nav renders only when something can move, so the hairline appears exactly when there is a control to separate from and vanishes with it. On a phone the panel has no control and correctly has no hairline. The panel also stopped rendering an EMPTY nav wrapper, which would have separated the route from nothing" },
      { kind: "Fixed", text: "THE BAR\u2019S SLIDE WAS INVISIBLE IN FIGMA. The prototype had its reactions wired but the message carried no keyframes at all, so Smart Animate crossfaded the text and the entry the component is built around could not be seen. All twelve bar variants carry TRANSLATION_X and OPACITY tracks now \u2014 12px to 0 and 0 to 1 over 240ms on the same cubic-bezier the CSS uses. One fidelity limit is recorded rather than papered over: a Figma variant cannot branch on WHICH control was pressed, so the prototype always plays the forward slide; the direction-awareness is code-only and the e2e suite is what holds it" },
      { kind: "Changed", text: "THE TICKER PARTS SECTION IS BACK ON A GRID. Deleting Ticker / Message left Ticker / Row stranded in the second column with an empty slot where the Message had been \u2014 three parts laid out as though there were still four. Control and Action share the first row, Row has the second, and every section is re-fitted to its contents" },
      { kind: "Fixed", text: "THE TICKER\u2019S MESSAGE SLID IN FROM THE RIGHT EVEN WHEN YOU PRESSED PREVIOUS. The entry offset was a fixed +2rem regardless of direction, so the motion said \u201cforward\u201d while the control said \u201cback\u201d \u2014 on a stepped component the difference between holding a position and reshuffling, and invisible in a still screenshot, which is why it survived several visual passes. `data-step` carries the direction now and the offset flips with it. Logical rather than physical: the estate runs dir=\u201crtl\u201d in Urdu, where \u201cnext\u201d travels leftward, so the sign flips again with the writing direction" },
      { kind: "Changed", text: "THE ENTRY IS 240ms AND 12px, DOWN FROM 320ms AND 32px. A citizen reading the page for a minute sees the message change a dozen times, and at that frequency the job of the movement is to say \u201cthis is new\u201d and then get out of the way \u2014 32px over a third of a second announces itself every five seconds. The Figma prototype carries the same 240ms and the same curve, and its hover states the same 150ms as the CSS, across 6 message swaps, 36 control transitions and 172 hover states" },
      { kind: "Added", text: "A MOTION SECTION ON ALL THREE DOC SURFACES, stating what moves, for how long, on which curve, and why that curve \u2014 the one contract the ticker documented nowhere. The web docs page also had TWO sections numbered 4; it now runs 1 to 9" },
      { kind: "Changed", text: "THE OUTLINED BUTTON APPEARS ONLY AT 1024px AND UP, IN BOTH SHAPES \u2014 one rule by container width rather than one per shape. The bar is that wide when full-bleed; the PANEL never is, being a column at 408px on desktop and 326px on a phone, so it always takes the text link. An outlined button costs 32px of padding plus a border, and in a 408px header already carrying a mark, the name and the pause it was the widest element competing with the component\u2019s own identity: 130px down to 53px. The outline was doing decorative work there, not affordance work" },
      { kind: "Added", text: "`labelAs` PUTS THE STRIP\u2019S NAME IN THE DOCUMENT OUTLINE. The section is labelled, so landmark navigation already reached it, but heading navigation \u2014 pressing H, one of the commonest ways people move through a page \u2014 skipped it entirely, because the name was a span. It defaults to \u201cspan\u201d because the right LEVEL depends on the page: the website\u2019s rail sits inside a section owning an h2 and passes h3, while a bar under the masthead wants h2. Guessing would skip a level, which is worse than not being a heading" },
      { kind: "Fixed", text: "THE FIGMA BAR WAS 102px AGAINST THE CODE\u2019S 72px. Its Row instance carried the PANEL\u2019s row padding \u2014 16px top and bottom, meant for spacing a list \u2014 and the Body added 16 more. Row spacing belongs to the list, not to the message, so it is stripped on the bar\u2019s instances. That was the extra space the design showed and the build never had. Ticker / Message is deleted, the controls and route are one Actions cluster, and the author\u2019s reference frame is removed now the build matches it" },
      { kind: "Fixed", text: "ONE MESSAGE LAYOUT FOR BOTH SHAPES, AND THE BAR\u2019S HAD NO STYLES AT ALL. The panel used `rowtitle`/`rowmeta`, which are specified; the bar used `title`/`description`/`more`, which carried NO CSS and were styled only by what they inherited \u2014 so the same item rendered at a different size and weight depending on which shape it was dropped into, and only the panel\u2019s second line had ever been checked for contrast. Both shapes call one renderMessage now and share one style block. In Figma the bar\u2019s Ticker / Message is replaced by a Ticker / Row instance with Show Marker off: one component, two shapes, which is what the reference frame showed all along" },
      { kind: "Changed", text: "THE BAR\u2019S HEADER HAIRLINE IS THE PUBLISHED DIVIDER COMPONENT, at Orientation=Vertical, Tone=Inverse subtle \u2014 not the rectangle it had been hand-drawn as. The component already had the tone this needed, across eight variants" },
      { kind: "Fixed", text: "THE FIGMA PANEL IS DRAWN AT THE WIDTH IT REALLY RENDERS. The vertical variants were 1440 / 834 / 390 \u2014 the VIEWPORT\u2019s width, not the panel\u2019s. It is a column component: measured off the live site it is 408 / 762 / 326, and drawing it full-bleed made the desktop specimen a fiction nobody could design against. Two earlier attempts failed because each Row instance carried a pinned inner width from the 1440 era that FILL could not override \u2014 Figma recomputes a fill-width child from a layout that had not reflowed, so every write silently reverted. resetOverrides() clears the pin, with the notice text captured first and restored after. Zero overflow at all three widths now" },
      { kind: "Fixed", text: "THE TICKER\u2019S PART MASTERS LOOKED BROKEN BECAUSE THEY WERE INVISIBLE. Control, Action, Message and Row are all inverse-inked \u2014 they only ever live on the blue bar \u2014 so on the library\u2019s default white section they rendered white on white and the page read as empty. The sections carry the strip\u2019s ground now and say so in their names. A stray DETACHED copy of the panel, floating 4,000px off to the right of everything, is parked in a labelled scratch section rather than deleted" },
      { kind: "Changed", text: "THE TICKER\u2019S DOCUMENTATION IS SPECIMENS AND MATRICES INSTEAD OF PROSE CARDS. Nine sections of text cards is the wrong shape for \u201cwhat does this do at each width\u201d \u2014 a reader comparing three breakpoints across six behaviours had to hold eighteen facts in their head to answer one question. The page embeds LIVE INSTANCES of both shapes now and answers by intersection, in two behaviour matrices that state plainly where auto-scroll exists and where it does not. `MatrixTable` was added to docs-kit for the web half rather than hand-rolled, so the same two matrices appear on both surfaces" },
      { kind: "Added", text: "`Show Date` ON THE TICKER ROW, so the library can express the optionality the component already had. The meta line is Kind plus an optional Date group carrying the separator. Fixing it surfaced an inverted master: the layer bound to `Title` held \u201cVacancy \u00b7 12 Aug 2026\u201d and the one bound to `Subtitle` held the notice, so every fresh Row instance rendered its metadata as the headline \u2014 instances had overridden their way past it, which is exactly why nobody saw it" },
      { kind: "Changed", text: "BELOW 1024px THE BAR ADOPTS THE PANEL\u2019S HEADER. Wrapping the controls under the message left the plinth a narrow square holding a mark and no name \u2014 the component losing its identity at the width where identity matters most, to save a line it then spent anyway. The plinth is a full-width header carrying the name, the pause and the route out, with the message on the ground beneath it. At narrow widths the two shapes now converge on one layout instead of diverging into three, and the name comes back" },
      { kind: "Fixed", text: "THE TICKER\u2019S FIGMA PROPERTY MODEL DID NOT MATCH ITS CODE CONNECT TEMPLATE, AND THE ONE PROPERTY THAT EXISTED WAS MISNAMED. The set\u2019s single text property was called `Title` while it actually drove the PLINTH LABEL, and the template read five more \u2014 Title, Description, Show Description, Action Label, Show Action \u2014 that had never existed on the set at all; they belong to the nested Message, Row and Action parts and were never exposed on the parent. The generated snippet therefore put the strip\u2019s own name into the first notice\u2019s headline and left the label blank. The property is `Label` now with the code default, and the template reads only what the set has" },
      { kind: "Fixed", text: "THE LATEST UPDATES RAIL GREW TO 2,936px ON A PHONE, WITH NO PAUSE CONTROL. Below `lg` the rail leaves the grid and returns to normal flow, so `height=\"fill\"` had nothing to fill against and grew to fit all 24 notices \u2014 measured at 2,016px on an 834px viewport and 2,936px on a 390px one. Worse, a list that never overflows its window is never found to be scrolling, so the control cluster was correctly removed and the citizen got a wall of headlines with no way to stop or skip it. It is the panel\u2019s own documented precondition, broken at exactly the widths nobody had measured. The rail bounds its height below `lg` now: 512px at tablet, which auto-scrolls with its pause control back, and 448px on a phone, hand-scrolled" },
      { kind: "Changed", text: "THE BAR NO LONGER DROPS ITS ONLY ROUTE OUT \u2014 IT WRAPS. `View All` was `display: none` below 1023px, the one drop in the reflow ladder that removed a DESTINATION rather than a convenience: prev/next are conveniences because the strip cycles itself, and the label is one because the accessible name carries it, but this was the only link from the strip to the archive \u2014 and the citizen on a phone was the only one who could not reach it. The message takes the full width now and the controls drop to a second line. The bar has a minimum height rather than a fixed one precisely so it can grow. WCAG 1.4.10 asks that content REORGANISE rather than be lost" },
      { kind: "Changed", text: "THE PLAY/PAUSE GLYPH IS FILLED, and it is the estate\u2019s only filled icon. At 24px on a solid brand surface the outlined `pause` is two hairline rectangles that read as outlines rather than as a control \u2014 on the one control WCAG 2.2.2 requires to be findable. `Icon` already carries the fill axis, so this is one prop on one glyph, not a new component. The contrast RATIO is unchanged; the mark\u2019s weight is what changes" },
      { kind: "Changed", text: "147 EMPTY WHITE PLACEHOLDER RECTANGLES REMOVED from the SAMAVESH library \u2014 every page carried one or more frames named \u201cFrame header\u201d holding nothing at all, up to 1,890px wide. Three were kept because they genuinely had content, which is why each was checked for children, fills, strokes and effects rather than deleted by name" },
      { kind: "Fixed", text: "THE FIGMA PANEL SHOWED TWO AND A HALF ROWS AND NO EDGE FADE. The viewport was a flat 176px at every breakpoint against a documented `rows` default of 4, and the 20px fade the CSS applies was drawn nowhere. Each viewport is the MEASURED height of its own first four rows now \u2014 280px desktop, 300px tablet, 360px mobile, three different numbers precisely because rows wrap, which is the same reason the component measures rather than multiplying a nominal row height. The fade is on desktop and tablet in all four motion variants and on no mobile variant, because the CSS gates it on `data-scroll` rather than play state: pausing keeps it, and a phone never scrolls so it never earns one" },
      { kind: "Fixed", text: "THE TICKER PANEL CLIPPED ITS NOTICE TITLES AT TABLET AND MOBILE. `Track` is absolutely positioned inside `Viewport` \u2014 it has to be, since translating it is what scrolls the list \u2014 and it had been left FIXED at the desktop 1376px in every breakpoint while the viewport itself correctly filled. Tablet overflowed its frame by 542px and mobile by 986px, so titles ran off the right edge and were cut mid-word, which is the exact failure the no-truncation rule exists to prevent. Each track is sized to its viewport with a STRETCH constraint now, and one stray row pinned at 632px was unpinned" },
      { kind: "Fixed", text: "THE MOBILE PANEL DREW A PAUSE CONTROL OVER A LIST THAT CANNOT MOVE. Below 640px the panel is a still list \u2014 there is no hover to stop it with \u2014 and the rule is that nothing which cannot move may show a control governing motion, because a pause button on something still advertises motion a citizen may be trying to escape. The four mobile panel variants hide it now, matching what the component actually renders" },
      { kind: "Fixed", text: "THREE SURFACES DOCUMENTED A ROW THE COMPONENT HAD STOPPED RENDERING. design.md, the docs page and the Storybook notes all still described the panel row as a bold lead-in before a colon with the sentence after it \u2014 the shape that was replaced by title-over-subtitle when the ticker met the real notice list. design.md carried BOTH descriptions, in adjacent paragraphs, contradicting each other. Two of the three also still named primaryScale/500 and /700 as the ground and plinth, which the contrast fix had already moved to /600 and /800, and design.md still said the mark defaults to the Material `campaign` glyph it no longer uses" },
      { kind: "Fixed", text: "THE TICKER\u2019S FIGMA DESCRIPTION HAD COMPOUNDED ITS OWN ESCAPING SIX LEVELS DEEP. Reading Figma\u2019s `description` returns HTML-escaped text; writing that text back escapes it again, so a read-modify-write cycle walks an apostrophe to &#39;, then &amp;#39;, then &amp;amp;#39; \u2014 in the very text the Figma MCP server hands an agent as its briefing. Writing FRESH text is fine; it is the round-trip that does the damage, and one naive rewrite was enough to reproduce it. Ticker and Ticker / Mark were rewritten through `descriptionMarkdown`, which round-trips unchanged" },
      { kind: "Added", text: "THE TICKER HAS A FIGMA DOCUMENTATION PAGE, built to the house pattern the Chatbot page set: a hero with six at-a-glance figures, nine numbered sections \u2014 shapes, row anatomy, contrast, states, motion, height, accessibility, tokens, prototype \u2014 and a separate Component record frame carrying what is still open. 257 nodes, audited at 100% bound on fills, padding, gaps and radii, 172 of 172 text nodes on a published style, zero UNACCOUNTED and nothing under the 11px floor" },
      { kind: "Changed", text: "THE TICKER FIGMA PAGE IS ORGANISED INTO SECTIONS instead of six sets loose on the canvas. `1 \u00b7 Ticker` holds the published 24-variant set, `2 \u00b7 Ticker Parts` the four nested interaction parts, `3 \u00b7 Ticker Mark` the drawn megaphone \u2014 the same convention the Chatbot page uses, so the two pages can be read the same way" },
      { kind: "Fixed", text: "SIX FIGMA DESCRIPTIONS CARRIED COMPOUNDED ESCAPING, AND THE FIRST COUNT OF THEM WAS WRONG. Tabs / More (19 chained entities), Tabs / Tab (17), Tabs (16), AccessibilityBar (7), Navbar/NavItem (4) and Navbar/NavDropdown (2) are decoded and rewritten once each, so all 119 components in the library now scan clean. The earlier note claiming 53 components and 216 entities counted SINGLE-level entities too \u2014 which are Figma\u2019s normal projection of an apostrophe and render correctly \u2014 and named four components (Chatbot, Buttons, Inputs, Accordion, Card) that were never damaged while missing Navbar and AccessibilityBar, which were. Content was compared word-for-word either side of every rewrite" },
      { kind: "Added", text: "THE ESCAPING TRAP IS WRITTEN DOWN WHERE IT WILL BE READ, in component-authoring \u00a712b, corrected to describe the CYCLE rather than the field: reading `description` returns escaped text and writing it back escapes it again, proved by a naive read-modify-write that reintroduced the damage on the spot. Writing fresh text is fine. Two traps are recorded with it \u2014 audit for two-or-more `amp;` and not for any entity, or every healthy description looks broken; and never read `descriptionMarkdown` as your source without checking it is non-empty, because it is empty on anything authored through `description` and a sweep that missed that called 53 damaged components clean" },
    ],
  },
  {
    version: "v0.57.0",
    date: "2026-08-25",
    changes: [
      { kind: "Fixed", text: "THE TICKER WAS FAILING WCAG 1.4.3 ON ITS OWN GROUND, AND ITS DOCUMENTATION ASSERTED IT WAS FINE. White on primaryScale/500 (#0373df) measures 4.64:1 \u2014 it clears the 4.5:1 floor by four hundredths \u2014 and the moment secondary text is dimmed it fails outright: 90% is 4.06:1, 80% is 3.52:1. The bar shipped an 80% description line on that ground while design.md and the docs page both claimed 6.3:1, a number that was never computed. The ground is primaryScale/600 now, where the primary line is 6.36:1 and 80% is 4.66:1 \u2014 which is what makes a dimmed second line possible at all. The plinth moved to /800 with it, holding the tonal step at 1.77:1 against the 1.85:1 the two had before, so the shape is unchanged" },
      { kind: "Fixed", text: "PAUSE RESET THE SCROLL INSTEAD OF HOLDING IT. `canScroll` (a fact about the content) and `isPlaying` (the citizen\u2019s choice) were one flag, so pressing pause removed the `animation` property outright, unmounted the duplicated copy that makes the loop seamless, and re-sliced the first \u2014 the track snapped to translateY(0) and resumed from the top. A pause that loses your place is not a pause, it is a reset with the wrong label. The DOM and the animation are identical either side of the press now and only `animation-play-state` moves" },
      { kind: "Fixed", text: "NOTHING IN THE TICKER IS TRUNCATED. Both lines clipped to an ellipsis, and on a notice list that is a loss of meaning rather than a compromise: two of the department\u2019s notices both open \u201cExtension of Application Submission Date for Financial Adviser (FA) Post at\u2026\u201d and clipped to the SAME visible string \u2014 two links reading identically and leading to different pages. Text wraps; the bar has a minimum height rather than a fixed one, and the panel MEASURES its window with a ResizeObserver because wrapped rows have different heights and no arithmetic on a nominal row height can be right" },
      { kind: "Added", text: "`Ticker` \u2014 recent announcements in TWO SHAPES from one data model. `horizontal` is the bar, one message at a time; `vertical` is the panel, the same items stacked as rows scrolling upward under a header. The panel is built from the bar\u2019s own parts \u2014 the same navy plinth, the same ground, the same ink, rows in the bar\u2019s own title role \u2014 so side by side they read as one component in two shapes, which is what they are. It replaced 90 lines of hub-local Tailwind that nothing else in the estate could reach" },
      { kind: "Added", text: "EVERY ROW CARRIES A MARKER, AND THE TEXT HANGS OFF IT. Without one every line in the rail starts at the same x, so nothing but a vertical gap says whether a line begins a notice or continues the one above \u2014 32px between rows against 20px inside a wrapped title, a ratio of 1.6 that is thin enough to misread WHILE THE LIST IS MOVING. The hanging indent is the point, not the dot: set in its own grid column the marker is the only thing at the outer edge, so item starts are pickable out of the left margin without reading a word" },
      { kind: "Added", text: "`TickerMark` \u2014 a bespoke animated megaphone whose arcs pulse while the strip moves and STOP when it is paused, so the mark is a readout of the strip\u2019s state rather than decoration looping over a stopped list. It replaced a 32px white rounded tile that read as a sticker pasted onto the navy plinth, its border/neutral/subtle hairline having been drawn for a white tile on a LIGHT ground and so invisible on navy. Drawn rather than an <Icon> for two reasons a font glyph cannot meet: it animates in parts, and it answers to state" },
      { kind: "Added", text: "EIGHT PLAYWRIGHT REGRESSION TESTS, and each one was proved to catch its defect rather than merely pass. Reintroducing the pause-reset bug fails the suite; restoring the fix passes it. They pin the behaviours that broke: pause holding its position, the duplicated copy staying out of the tab order and the accessibility tree, hover stopping the scroll without touching the button\u2019s own state, nothing truncating, the marker hanging, no underline on hover, list semantics with no live region, and the phone case standing still with its motion controls gone and no horizontal overflow" },
      { kind: "Changed", text: "THE WEBSITE\u2019S LATEST UPDATES MOVED OUT OF THE HERO\u2019S WAY. As a full-bleed strip it stacked a third full-width coloured band beneath the saffron SAMAVESH bar and the hero, and put a second pause control 65px below the carousel\u2019s own \u2014 two controls on one screen that both stop something, neither saying what. It is the right-hand rail of Our Offerings now, showing six notices where the strip showed one. The card grid reflows 3-up to 2-up at EXACTLY the same 408px card width, so the rail costs the cards nothing" },
      { kind: "Changed", text: "ROWS NO LONGER UNDERLINE ON HOVER. WCAG 1.4.1 asks that a link be distinguishable from the text AROUND it, and in a list where every row is a link there is no surrounding text to confuse it with \u2014 so the underline carried no accessibility weight, struck through both lines of a wrapped notice, and fought the subtitle for the same few pixels. The row\u2019s background wash and the cursor carry the affordance; focus keeps its 2px ring, which is what 2.4.7 actually requires" },
      { kind: "Changed", text: "HOVER-TO-PAUSE IS GATED BEHIND (hover: hover) AND (pointer: fine); FOCUS IS NOT. On a touch screen `:hover` sticks after a tap, which would freeze the list for the rest of the visit on the one device that cannot un-hover. `:focus-within` stays unconditional because a keyboard user has no pointer to withdraw and a row that scrolls away takes the focus ring with it" },
      { kind: "Changed", text: "THE MESSAGE\u2019S ENTRY CURVE WAS MATERIAL\u2019S cubic-bezier(0.4, 0, 0.2, 1), which eases IN as well as out \u2014 so the first third of the travel is slow, exactly when a citizen is looking hardest at a message that just changed. It is a strong ease-out at 320ms now. The marquee stays linear, because any easing makes it hesitate at the loop point. Reduced motion keeps a 200ms fade rather than killing every animation: a line of text that swaps with no transition at all is the jarring change the setting exists to prevent" },
      { kind: "Added", text: "THE PARITY LEDGER KNOWS THE COMPONENT EXISTS. `Ticker` is `mapped`; its four interaction parts \u2014 Control, Action, Message and Row \u2014 are declared internal sub-parts with reasons rather than showing as four components somebody forgot to build. Regenerating also picked up roughly forty code exports the ledger had drifted behind, which is drift this change did not cause and does not fix beyond recording it" },
    ],
  },
  {
    version: "v0.56.0",
    date: "2026-08-25",
    changes: [
      { kind: "Fixed", text: "THE BUTTON DOCS PAGE CONTRADICTED ITSELF ABOUT TOUCH TARGETS, IN THE ACCESSIBILITY CHECKLIST. Three places said size sm meets 44px; two others correctly said it is 32px and only lg reaches 44. sm measures 32. The checklist entry is the worst place to be wrong, and it also miscited the criterion \u2014 2.5.8 is 24\u00d724 at AA; 44\u00d744 is 2.5.5 at AAA. UX4G\u2019s own document makes the same misquote, which is exactly why standards-precedence.md warns about it. Also corrected: one \u201cWCAG 2.1\u201d where the estate targets 2.2, a claim that disabled buttons expose aria-disabled (a native <button> does not need it, and an <a> never gets it), and a checklist entry crediting a loading state the component does not have" },
      { kind: "Added", text: "A FULL AUDIT OF THE ESTATE\u2019S MOST-USED COMPONENT \u2014 565 consumers, measured rather than eyeballed. Three defects ship today: `disabled` is inert on a link-button (<a disabled> is not a valid attribute \u2014 measured focusable, clickable, no aria-disabled); a fixed height clips the label at 200% text, failing WCAG 1.4.4; and five variant/appearance pairs miss the 3:1 non-text contrast requirement (all four tonals, 1.21\u20131.52, plus neutral outlined at 2.15). Text contrast, by contrast, is clean on all sixteen pairs. Written up in docs/design-system/components/button-audit.md with a companion cleanup brief" },
      { kind: "Added", text: "`Button \u2014 Documentation` REBUILT IN FIGMA, AND THE FIRST `Component record` IN THE LIBRARY. The old page was a 1400px template (the house style is 1680) with 16 text nodes, ZERO on a published style, describing a component that does not exist \u2014 \u201cPrimary \u00b7 Secondary \u00b7 Outlined \u00b7 Ghost\u201d, where the set has Type \u00d7 Sub-type. It also claimed Figma and code properties were 1:1 and listed properties neither surface has. The replacement is 74 text nodes, 74 on published styles, 100 fills bound, 22 radii bound, seven numbered sections built from LIVE component instances. figma-code-sync.md records that no page in the library had a Component record; this is the first" },
      { kind: "Changed", text: "THE FIGMA NUMBERS ARE COUNTED, NOT SAMPLED. All 1,440 padding bindings on the Button set sit on the TYPE collection (Font Size/6 \u00d7640, /3 \u00d7640, /1 \u00d7160) and none on Space \u2014 so changing the type scale silently re-paddings every button in the estate. 900 of 1,956 colour bindings reach Tier-1 ref/*; only the Neutral variants added the day before bind Tier 3. Radius is the one clean axis at 720/720 on shape/8. None of it is fixed here: restructuring the set changes every instance in the estate and belongs in its own change" },
      { kind: "Added", text: "STORYBOOK NOW RENDERS THE DEFECTS INSTEAD OF DESCRIBING THEM. A KnownDefects story shows the live disabled link-button beside a correctly disabled button, the clipped label at 200%, and the four edgeless tonals; InverseIgnoresVariant shows inverseOutlined painting all four variants identically; LoadingIsConsumerSupplied shows the pattern to copy. A design system that only demonstrates its happy path teaches the happy path" },
      { kind: "Added", text: "`docs/design-system/components/button.md` \u2014 the estate\u2019s most-used component had no repo spec, while the far-less-used Chatbot did" },
    ],
  },
  {
    version: "v0.55.0",
    date: "2026-08-25",
    changes: [
      { kind: "Added", text: "`Type=Neutral` IN FIGMA — 180 VARIANTS, THE COMPLETE MATRIX. The Button set had Primary/Success/Danger \u00d7 4 sub-types \u00d7 5 states \u00d7 3 icon positions and no way to express \u201cquiet, no semantic charge\u201d, so the chatbot\u2019s reset was authored in the estate\u2019s rejection red. A partial Type is a trap \u2014 a designer picking Neutral+Tonal+Hover has to find something \u2014 so it is complete or it is nothing: 720 masters, 180 per Type. `clone()` silently drops `componentPropertyReferences`, so all 180 lost their text and icon-swap wiring and had to be re-linked from their Primary source; the label read \u201cButton\u201d until that was found" },
      { kind: "Fixed", text: "A COMPLETE TIER-3 ACTION FAMILY EXISTED AND NOTHING CONSUMED IT. `cmp/action/neutral/*` \u2014 72 variables in Figma, 464 `--sa-cmp-action-*` in tokens.css across four intents \u2014 had exactly two references in the whole estate, both inside comments, one of which said Tier 3 \u201cis the eventual home for this; restructuring the Button onto it is a separate change\u201d. That deferral is what the chatbot bug came out of: a neutral action family existed on both sides and the author still reached for `danger`, because Button bound none of it. `variant=\u201cneutral\u201d` now binds Tier 3 and the new Figma variants are built from the same tokens. Primary/Success/Danger still reach into Tier-1 `ref/color/*/source` and are recorded as open, not swept in" },
      { kind: "Changed", text: "NEUTRAL TERTIARY INK IS 800, NOT THE MATRIX DEFAULT 700 \u2014 a neutral-only override, and the reason is specific to that ramp. On brand, success and destructive, 700 is a saturated hue nobody could mistake for prose. On the NEUTRAL ramp 700 IS `text/muted`, the exact ink disclaimers and secondary prose use, so a chrome-less control painted in it disappears into the paragraph beside it. Holding it constant across default/hover also matches button.css, where `--_color` never changes and the state is carried entirely by the background wash. Nothing consumed these two tokens, so the blast radius was zero and this was the only free moment to correct them. Pushed to the library, read back, and `figma-live.json` re-recorded \u2014 the gate refuses a record updated without a verified read" },
      { kind: "Changed", text: "THE CHATBOT\u2019S RESET IS A `Button` INSTANCE IN FIGMA TOO. It was a hand-drawn frame with a 1px error stroke and a text child \u2014 the same defect as the ~40 lines of hand-rolled CSS, authored twice, once per surface. All three panel states now instance `Size=Small, Type=Neutral, Sub-type=Text` and read \u201cStart over\u201d" },
      { kind: "Changed", text: "THE FIGMA PANEL SHOWS THE HEIGHT THE CODE RENDERS. `State=Greeting` drops 719 \u2192 396 and `State=Typing` 719 \u2192 252, because both were drawn at a ceiling the component never sits at on open \u2014 the same 45% void the code carried. `State=Transcript` stays at 719 deliberately: it is the state that demonstrates the cap and the scroll, so the set now shows both truths instead of one" },
      { kind: "Fixed", text: "NINE STALE CLAIMS ON THE FIGMA DOCUMENTATION AND COMPONENT RECORD. \u00a702 still described \u201cEnd chat as a BORDERED BUTTON\u201d, \u00a707 called 719 a height, the a11y section credited the error ink at 9.10:1 for a control that no longer carries one, and the record described a stroke on a node that no longer has one. Every numeric and behavioural claim is pinned in `claims.json`, and the gate caught one of MY assertions going stale mid-change \u2014 which is the gate working" },
    ],
  },
  {
    version: "v0.54.0",
    date: "2026-08-25",
    changes: [
      { kind: "Added", text: "`neutral` JOINS `ButtonVariant`, and its absence had a measurable cost. There was no way to say “quiet, no semantic charge”, so an author who needed a control to look different from the paragraph beside it reached for `danger` — and the chatbot’s reset shipped OUTLINED IN THE ESTATE’S REJECTION RED for what is housekeeping, making the least-used control the loudest thing in a footer whose only filled control is disabled at rest. On a portal where red means “your application was rejected”, spending it on a reset devalues the signal. Its ink is `text/neutral/base`, not `subtle`: quiet is the job of the APPEARANCE, and `subtle` resolves to the same `text/muted` as body copy, which came out the exact colour of the disclaimer it sits beside" },
      { kind: "Changed", text: "“END CHAT” IS “START OVER”, AND IT NO LONGER CLOSES THE PANEL. It used to clear the transcript AND close, which made it a second way to close sitting next to a ✕ already labelled “Minimise chat” — two controls that both close, one of which destroys the conversation, is how somebody loses a conversation reaching for the wrong one. It also made the label untrue whichever of its two words you trusted. The two are now separated by outcome: ✕ closes and KEEPS, Start over keeps the panel and CLEARS. A controlled consumer must now re-seed its greeting in `onEndChat`, because the close that used to trigger the replay is gone" },
      { kind: "Changed", text: "THE PANEL SIZES TO ITS CONTENT; 719 IS A CAP, NOT A HEIGHT. Pinned at 719 the opening state was a 531px log holding 96px of greeting — 435px of white, about 45% of the panel, and because the log is floor-anchored it all collected under the header, which is where a panel looks unfinished. Two tempting fixes were both wrong: top-aligning the first bubble strands it at the ceiling (the stylesheet had already rejected that, correctly), and a welcome block duplicates the header, which carries the mark, the name and the Devanagari 40px above. So: do not fill the space, do not have it. It opens at ~420px and grows with the conversation" },
      { kind: "Fixed", text: "HALF THE COMPOSER DID NOTHING WHEN TAPPED, AND ITS TARGET WAS UNDER THE WCAG MINIMUM. The pill renders 42px tall; the `<input>` had no height, so it was 20px — its line box — floated in the middle by `align-items: center`. Everything above and below belonged to the FORM, which focuses nothing: measured with `elementFromPoint`, a click 4px under the top border returned `.ds-chatbot__composer`. That also put the real target at 20px, under the 24px WCAG 2.2 (2.5.8) minimum, on a government service where that is a compliance line. `align-self: stretch` resolves it to the pill’s 32px content box. Invisible to typecheck, lint and every token gate — pinned in `e2e/chatbot/composer-target.spec.ts`" },
      { kind: "Fixed", text: "FOUR MASCOTS WERE BOBBING AT ONCE. The float was on every `.ds-chatbot-mascot__figure`, which is every mark the component renders — the 84px launcher, the 40px brand mark in the header, and a 40px avatar beside every run of bot messages — each at its own phase, because each started when its element mounted. The justification on record (2.5px on a 55px mascot is below the threshold of drawing the eye) is a claim about ONE mark on an otherwise still page; it does not survive being multiplied down a scrolling transcript. Gated to the launcher, with `.ds-chatbot-mascot--float` as the opt-in for a specimen" },
      { kind: "Fixed", text: "EXPANDING ANIMATED `width` AND `height`; RESTORING SNAPPED. Both are layout properties, so every frame of the 240ms re-ran layout and re-wrapped every bubble — at the one moment the panel is guaranteed to be full of text. And because both lived only in the `[data-expanded]` rule, the two halves of one gesture behaved differently. The box now changes in one step, both ways. A cross-fade to cover it was written and deleted: keyed off `[data-expanded]` vs `:not(...)` it keeps the same `animation-name` on both sides, so the browser never restarts it and it plays once, on mount" },
      { kind: "Fixed", text: "A COMMENT CLAIMED A LINT GATE THAT IS SWITCHED OFF, AND THE DEFECT IT WARNED ABOUT HAD ALREADY RECURRED. v0.51.0 below says `.ds-chatbot__end` being declared twice was “found only because stylelint’s `no-duplicate-selectors` blocks any commit that touches the file”. That rule is `null` in `.stylelintrc.js` — deliberately, with a documented rationale — so nothing was blocking anything, and `.ds-chatbot__end:active` was subsequently declared twice, ten lines apart, with `npx stylelint` passing clean over it. The entry below is left as written because a changelog is a record; this is the correction. A comment asserting a guard that does not exist is worse than no comment" },
      { kind: "Changed", text: "THE CHATBOT’S RESET IS A DS `Button`, NOT A HAND-ROLLED ONE. ~40 lines re-implementing padding, radius, focus ring, press scale and hover wash are gone. The component sits INSIDE the design system and broke its own design-system-first rule four times over — `__end`, `__reply`, `__send`, `__icon-btn` — and being styled by hand is exactly why it landed on its own weight scale instead of the system’s. The other three remain, and are recorded as open" },
    ],
  },
  {
    version: "v0.53.0",
    date: "2026-08-24",
    changes: [
      { kind: "Changed", text: "THE LOOPS MOVED OFF THE SPECIMEN SLIDE AND ONTO THE COMPONENTS. All three were keyframed on one standalone Figma frame, which gave them that frame\u2019s single 10s duration \u2014 so the 1200ms typing wave pulsed once and then held still for 9.3 seconds, and the 5s float ran once and stopped. A main component ACCEPTS keyframes (only an instance sublayer refuses them), so the wave now lives on `State=Typing` and the float on `Chatbot Mascot`, and an instance dropped in any frame carries its own motion. A track plays inside the HOST frame\u2019s timeline, so the durations are chosen to tile: 30s is the least common multiple of 1.2, 5 and 10 \u2014 25 waves, 6 floats, 3 turns of the seal" },
      { kind: "Changed", text: "THE MOTION SPECIMEN IS PART OF THE DOCUMENTATION NOW, as section 09, and two of its three stages hold LIVE INSTANCES rather than detached copies. The seal stays a declared specimen because its turn is opt-in and the master must never spin \u2014 which is the same reason the code has no trigger for it but `spin`. The old standalone frame is deleted; its stale copy of the seal was still showing the pre-6.5 wordmark" },
      { kind: "Added", text: "`layout/chatbot/*` EXISTED IN THE LIBRARY AND NOWHERE ELSE. Five variables \u2014 width 400, height 719, launcher 84, bubbleMax 246, typingDot \u2014 with the master\u2019s panel and launcher genuinely bound to them, while @mosje/tokens defined none of them and chatbot.css restated the three geometry values as literals. The documentation could therefore claim a token family the stylesheet had no way to name. They are in semantic.json now and the stylesheet resolves them. typingDot was ALSO wrong: the library said 6 while the master and the stylesheet both drew 5, because nothing was bound to it \u2014 it is 5, and the four dots are bound to it" },
      { kind: "Changed", text: "END CHAT IS A BUTTON, SO IT LOOKS LIKE ONE. An underlined word in error ink reads as a link \u2014 something that navigates \u2014 and this is the control that ends the conversation and clears the transcript. It is now an outline button (1px `border/status/error/base`, `shape/8`, `padding/6` \u00d7 `padding/12`, 83\u00d734) sharing the note\u2019s row and sitting hard right. Outline rather than filled so it stays quiet beside the note and does not compete with Send, which is the panel\u2019s one filled control" },
      { kind: "Fixed", text: "TEN STALE OR SELF-CONTRADICTING CLAIMS ON THE FIGMA DOCUMENTATION PAGE. \u00a703 said the states were \u201cdrawn as separate frames because a static file cannot show motion\u201d while \u00a708 said the master is an interactive component; \u00a704 pointed at a specimen frame that no longer exists; \u00a707 described a token family that had never reached code. Three sections also carried remediation history \u2014 a deployment anecdote, a naming post-mortem \u2014 which `figma-documentation-style.md` \u00a75 puts in the Component record, not on the page a reader is deciding from. Two stale comments in chatbot.css went the same way: both still described the seal turning on hover" },
      { kind: "Added", text: "FOUR MORE PINNED CLAIMS in figma-doc-parity, including one that derives all five `layout/chatbot/*` values from the tokens \u2014 so a token that moves without the page moving fails the build. Each new assertion was proved to FAIL before it was trusted" },
    ],
  },
  {
    version: "v0.52.0",
    date: "2026-08-23",
    changes: [
      { kind: "Fixed", text: "THE SUGGESTION ROW WAS CROOKED, AND IT WAS NOT FLEXBOX. `.ds-prose li + li { margin-top: 8px }` \u2014 a docs rule for VERTICAL prose lists \u2014 landed on the chatbot\u2019s horizontal reply row, pushing every pill after the first down 8px and stretching the first one to the taller line. The list reset its own margin and padding; the ITEM never did. A row of controls is not a prose list, so it opts out rather than hoping no consumer has list styling" },
      { kind: "Changed", text: "SUGGESTIONS PACK RIGHT NOW, ON THE USER\u2019S SIDE. Every one of them is a sentence the CITIZEN is about to say, and pressing one puts those exact words into a user bubble, which lands hard right. Packed left, the suggestion and the message it became sat on opposite sides of the column and the transcript read as if the assistant had proposed and then answered itself. Still wrapping rather than one-per-row, so two short suggestions share a line" },
      { kind: "Changed", text: "END CHAT IS A SIBLING OF THE NOTE, NOT A WORD INSIDE IT. It lived in the paragraph, separated by a space, which cost two things: the 28px control stretched the note\u2019s last line box from 16px to 28px and broke the footer\u2019s rhythm, and the only way out of the conversation moved horizontally with the text wrap, so its position depended on how long the disclaimer happened to be. A control people reach for should be somewhere they can learn" },
      { kind: "Fixed", text: "FOUR HAND-DRAWN <svg> PATHS REPLACED BY THE ICONS THE MASTER ACTUALLY USES. Expand, minimise, the launcher\u2019s close and send were bespoke geometry, which `component-authoring.md` \u00a72 forbids outright, and they had drifted from Figma\u2019s Material Symbols Rounded Light glyphs \u2014 `open_in_full` / `close_fullscreen`, `close`, `send`. Passing `size` rather than sizing the box in CSS is the point: `size` drives the `opsz` optical-size axis, so the glyph is DRAWN for the size it is displayed at, where a width/height rule sets the box and leaves the drawing wrong" },
      { kind: "Changed", text: "THE SEAL WORDMARK IS RE-EXPORTED FROM THE SAMAVESH MASTER, not from the WIP file it had been frozen against. The old cut read `Samajik Sahayak ~ \u0938\u093e\u092e\u093e\u091c\u093f\u0915 \u0938\u0939\u093e\u092f\u0915` with a TILDE where the master sets a middle dot. viewBox moves from `0 0 74 77` to the flattened node\u2019s true ink bounds `0 0 76.56 75.43`, and the ring insets are recomputed from the same measurement \u2014 the two are one number written twice, so they move together or not at all" },
      { kind: "Fixed", text: "TWO STALE `@default` CLAIMS IN THE PUBLIC API. `title` and `launcherLabel` both documented `\u201cChat with us\u201d` while the implementation had moved to the assistant\u2019s own name; the props table on this site repeated it, and so did the Figma master and the Identity section of its documentation page. All five now agree: the seal has the name written round the ring, so a widget that introduces itself as something else is a defect, not a personality" },
    ],
  },
  {
    version: "v0.51.0",
    date: "2026-08-23",
    changes: [
      { kind: "Changed", text: "THE AVATAR SITS BESIDE THE BUBBLE NOW, NOT ABOVE IT. `.ds-chatbot__turn` was a COLUMN, so every bot turn stacked a robot on top of its message and spent a whole avatar\u2019s height of vertical room doing it. The master has always drawn it as a row \u2014 and the rule two blocks below in the same stylesheet, \u201cbot bubbles point up-left AT THEIR AVATAR\u201d, only means anything if the avatar is to the left. A user turn has no avatar, so it moves to `justify-content: flex-end`; `align-items: flex-end` in a row would have bottom-aligned it" },
      { kind: "Changed", text: "THE AVATAR IS 40, NOT 37. 37 is on no scale in this system, and the mark it sits under in the header is 40. The CSS rule that set it was also DEAD: the size arrives through the `size` prop, which writes an inline custom property, so a class rule could never win. Both said 37, the prop was the one doing the work, and the rule is deleted rather than left as a decorative duplicate" },
      { kind: "Changed", text: "MESSAGE TEXT IS REGULAR, NOT 500. `Body/body-1` is a Regular style and the master uses it as one; the extra weight made every message read a shade heavier than the system\u2019s own body text, which is the opposite of what a transcript wants" },
      { kind: "Fixed", text: "TWO FIGMA-SIDE GAPS, WHERE THE CODE WAS RIGHT. End chat was `Body/body-3` at 12px against the code\u2019s `Label/label-1` at 14 \u2014 and at 12px with no padding its target box was 16px, under the WCAG 2.2 minimum the code had already been corrected to clear. The send glyph was 20px against the code\u2019s 16; 16 in a 32px disc is the standard ratio and is already on the icon scale" },
      { kind: "Removed", text: "A DUPLICATE \u201c06 \u00b7 Motion specimen\u201d frame floating above the page with a single test instance in it, and the Component record frame\u2019s overlap with the documentation \u2014 it sat at y=4800 inside a frame spanning 0\u20135412. The prototype stage\u2019s three skeleton blocks were also 100px wide inside a 1200px row, the same resize-then-FILL ordering bug that had already been fixed once in the motion specimen" },
    ],
  },
  {
    version: "v0.50.0",
    date: "2026-08-23",
    changes: [
      { kind: "Changed", text: "THE LAST THREE NAVY LITERALS ARE GONE, so the assistant now re-tones with the portal it sits on \u2014 launcher disc, close disc, send button, user bubble and the unread ring all bind `bg/brand/primary/bolder`. The disc was the interesting one: it was deliberately fixed on the argument that a mascot is ARTWORK and artwork does not re-tone, the same reasoning that keeps the National Emblem un-recoloured. That argument does not survive contact with the object \u2014 the artwork is the ROBOT, and the disc is the surface it is mounted on, which is ordinary brand chrome. Navy mode resolves the token to #003366, so it renders byte-identical there; only blue changes, and only because blue was wrong" },
      { kind: "Fixed", text: "THE FIGMA SEAL WAS 6.6px OFF-CENTRE, WHICH IS WHY THE DOTS WERE HALF DOTS. Its path circle was centred on (35.4, 35.4) while the disc is centred on (42, 42), so the glyphs at the top overran the 84px frame and the `\u00b7` separators were cut in half at the boundary \u2014 the semicircle that was reported. Centred, and tracked to 2.59px so the wordmark closes the circle exactly instead of covering ~60% of it and leaving a gap down the left" },
      { kind: "Fixed", text: "THE FIGMA MASCOT WAS DRAWN FAR TOO SMALL. `chatbot.css` sizes the figure at 71.5% of the mark without the ring and 66% with it, positioned off three more non-round percentages; Figma had 54.8% and 39.3%, and one variant\u2019s figure was pinned CENTER/CENTER where every sibling SCALEs. Both are now derived from the CSS values and the image\u2019s own crop aspect rather than eyeballed" },
      { kind: "Added", text: "THE CHATBOT IS AN INTERACTIVE COMPONENT. Its variants are wired to each other, so an instance dropped into ANY frame walks the lifecycle on its own \u2014 launcher opens, the bot composes for 900ms, the greeting lands, a suggestion is answered, close returns it \u2014 with no frame-level prototyping at all. The five prototype frames stay, because the two answer different questions: the component shows how the widget BEHAVES anywhere, the frames show what it looks like ON A PAGE growing out of the corner, which variant-switching in place cannot show" },
      { kind: "Fixed", text: "AN INLINE CHATBOT WAS CLAIMING THE CORNER AND THE WALL. `data-sa-corner-occupant` and `data-sa-wall-occupant` were emitted regardless of `placement`, and this documentation page renders an inline specimen. Nothing actually moved \u2014 the rail\u2019s own guards rejected it twice, for being outside the corner zone and for being 811px tall against a 200px ceiling \u2014 but a widget saved by someone else\u2019s guard is a defect waiting for the day a specimen happens to be small and bottom-right. Both attributes are now gated on `fixed`" },
      { kind: "Added", text: "A RULE FOR WHERE FLOATING ELEMENTS GO, because the answer was correct but scattered across four files and none of them answered \u201cI am adding a back-to-top button, where does it go?\u201d. Two rails only \u2014 the bottom-right corner and the right wall \u2014 and the corner stacks BY PERMANENCE: the accessibility widget anchors it, the chatbot sits above, and anything that appears and disappears on scroll goes on TOP. The reason is movement, not importance: put a back-to-top at the bottom of the stack and the two controls that most need to be findable by muscle memory slide up and down the page as the citizen scrolls" },
    ],
  },
  {
    version: "v0.49.0",
    date: "2026-08-23",
    changes: [
      { kind: "Fixed", text: "THE CITIZEN\u2019S OWN WORDS WERE THE ONE THING ON THE PANEL THAT IGNORED THE BRAND. `--sa-color-brand-navy` is a LITERAL, not a themed slot, so the user bubble painted #003366 in every mode \u2014 including the DEFAULT blue one, where everything around it is #0373DF. It now takes `bg/brand/primary/bolder` and its `on/` pair: white measures 6.36:1 in blue, and navy mode resolves to #003366, so that mode renders byte-identical. Only the mode that was wrong changes" },
      { kind: "Fixed", text: "THE SEND BUTTON HAD BOTH DEFECTS AT ONCE and was not in the original audit \u2014 it turned up while fixing the others. Same hard-coded navy, plus a raw 32px box. Both bound, matching what the Figma master already carried. The two places the navy literal STAYS are the mascot disc and the close disc, because those are artwork: the National Emblem is not recoloured per portal either, and neither is the assistant\u2019s face" },
      { kind: "Fixed", text: "THE PANEL HEADER HAD NO HIERARCHY. Title and subtitle both resolved to the same muted ink, so the Devanagari name read as equal in weight to the invitation above it. The title takes `text/default` now and the subtitle keeps `text/muted`. Separately, the header controls were a raw 32px carrying an 18px glyph \u2014 and 18 is not on the icon scale at all, it was a number rather than a size. Now `icon/size/32` and `icon/size/20`" },
      { kind: "Changed", text: "THE TYPING INDICATOR SITS IN A BUBBLE, AND THAT IS WHAT MAKES THE ANSWER ARRIVE PROPERLY. Bare dots beside an avatar read as the whole panel loading. In a bubble they occupy the exact place the reply will occupy, so the reply GROWS OUT OF the indicator instead of appearing next to it \u2014 the same spatial-continuity argument that makes the panel grow out of the launcher. It hugs, so it is a small pill on the bot bubble\u2019s own tail geometry rather than an empty slab of the full bubble width" },
      { kind: "Fixed", text: "END CHAT WAS 16px TALL, UNDER THE WCAG 2.2 TARGET MINIMUM, AND EVERY DOCUMENT SAID OTHERWISE. `.ds-chatbot__end` was declared TWICE in one stylesheet \u2014 once in the header block with the padding and type that made it a 24px box, and again 450 lines later with `padding: 0` and `font: inherit`. The second one won, so the control rendered 49\u00d716 while the accessibility checklist on its own documentation page claimed a 24px box, and the negative margin left behind was pulling the label out of the gutter to compensate for padding that no longer existed. Merged into one rule in the footer section where the control lives; now 73\u00d728. Found only because stylelint\u2019s `no-duplicate-selectors` blocks any commit that touches the file \u2014 the defect had been invisible to every other gate" },
      { kind: "Removed", text: "THE SEAL NO LONGER TURNS ON `data-thinking`, BECAUSE NOBODY COULD SEE IT. The widget only ever thinks while it is OPEN, and while it is open the launcher has already crossfaded the mark to the close \u00d7 at opacity 0 \u2014 so the one loop specified as the thinking cue was animating behind the thing that replaced it. Moving it to the 40px avatar was considered and rejected on legibility: the wordmark is already small on an 84px launcher and at 40px it is a grey smudge, and a seal you cannot read is not a seal. `--spin` stays and is now the only thing that starts it; `data-thinking` stays as a state hook for consumers and tests. The cue that actually reads is the typing indicator, which is in the log where the reply is coming from" },
    ],
  },
  {
    version: "v0.48.0",
    date: "2026-08-23",
    changes: [
      { kind: "Added", text: "THE CHATBOT IS A WORKING PROTOTYPE NOW, wired on INSTANCES of the master rather than a redraw of it \u2014 so a fix to the component shows up in the flow without anyone re-linking anything. Five 1440\u00d7900 frames, launcher \u2192 thinking \u2192 greeting \u2192 asked \u2192 answered, with both the minimise control and the close disc returning from anywhere. The instance is anchored BOTTOM-RIGHT, which is the whole trick: Smart Animate then grows the panel out of the launcher and shrinks it back into the same place, instead of sliding it down from the top corner. Every timing is read from `chatbot.css` \u2014 240ms enter, 160ms exit, 260ms turn-in on cubic-bezier(0.23, 1, 0.32, 1), and the 900ms `typingDelayMs` beat as a frame delay" },
      { kind: "Added", text: "MOTION, AS KEYFRAMES, AND THE SPLIT IS NOT A STYLE CHOICE. State changes are TRANSITIONS because they must interrupt and retarget when someone double-clicks the launcher; only the genuinely endless parts are keyframed, because there is nothing to retarget and they have to run forever. That is the same division `chatbot.css` already makes. Three loops authored: the four-dot typing wave (1200ms, 110ms apart, opacity .35\u21921 with a 3px rise at 30% of the cycle), the 5s mascot float, and the 10s seal turn. Verified by rendering the timeline and sampling frames, not by assertion \u2014 which is how the rotation direction was confirmed clockwise" },
      { kind: "Added", text: "FOUR TEXT AND TWO BOOLEAN COMPONENT PROPERTIES, bound across all three open variants: Title, Subtitle, Note and Placeholder map to `title`, `subtitle`, `note` and `composerPlaceholder`; Show composer maps to `composer`. Show subtitle has no code equivalent on purpose \u2014 passing an empty string is how the component suppresses it, and the boolean is that same decision made switchable in Figma" },
      { kind: "Changed", text: "TYPING COMES BEFORE GREETING, WHICH IS THE OPPOSITE OF WHAT v0.47.0 SAID. The opening effect turns typing on a beat after open and only then, `typingDelayMs` later, replaces it with the greeting \u2014 so the dots are what the greeting arrives OUT OF, not something that follows it. `State` now reads Closed \u2192 Typing \u2192 Greeting \u2192 Transcript on canvas, in the Code Connect mapping and in both documentation surfaces. It also explains the frame that looked like a mistake: Typing is drawn with an EMPTY LOG because that is the one moment it is the only thing on screen" },
      { kind: "Fixed", text: "THE MASCOT ARTWORK LANDED AND BROUGHT THREE DEFECTS WITH IT. The dashed ARTWORK SLOT placeholders were still in the master, rendering as real borders on every instance. The pasted figure was pinned CENTER/CENTER where every sibling SCALEs, so at the 40px header avatar it stayed 46\u00d752 and overflowed its own disc by 3px a side. And it filled 87% of the small ring disc against 62% on the large one. Separately, the launcher now carries the CLOSE GLYPH it trades places with \u2014 a full-bleed navy disc with a white \u00d7, hidden at rest and shown in every open state, so the crossfade the stylesheet describes is finally representable" },
      { kind: "Fixed", text: "THE TYPING INDICATOR WAS A 246\u00d796 SLAB, which was the bot bubble\u2019s geometry copied over, holding three 6px dots where the code ships four at 5px. It is now a hugging 56\u00d729 pill with four dots. Keeping the bubble is a deliberate departure from the code, which draws the dots bare: the pill occupies the position the answer will occupy, so the answer GROWS OUT OF IT rather than appearing beside it \u2014 the same spatial-continuity argument that makes the panel grow out of the launcher" },
      { kind: "Fixed", text: "THE CODE CONNECT PARITY GATE WAS PASSING VACUOUSLY ON ALL 19 TEMPLATES. Check 4 matched props indented EXACTLY two spaces and no template in the repo indents that shallowly, so it verified nothing; and the interface lookup required `{` straight after the name, so any Props interface with an `extends` clause \u2014 Chatbot, Button, TabDef \u2014 was skipped with a note nobody read. Both fixed, and the gate was proved to bite by feeding it a prop that does not exist" },
    ],
  },
  {
    version: "v0.47.0",
    date: "2026-08-23",
    changes: [
      { kind: "Added", text: "CHATBOT IS IN THE SAMAVESH LIBRARY, and that is what unblocked Code Connect for the whole package. It lived only in MoSJE (WIP) as four 475\u00d7852 SCREEN MOCKUPS \u2014 not a component \u2014 in a file the publishing token cannot write, and `code connect publish` uploads every template in ONE request, so that single file had been failing all 19 templates until it was excluded. The exclude is gone, `chatbot.figma.ts` points at `<SAMAVESH>`, and a fixture is captured, so the mapping is verified rather than merely present" },
      { kind: "Changed", text: "THE VARIANT AXIS WAS ACTIVELY MISLEADING and renaming it was the point of the promotion, not a side effect. `Property 1 = Open | 1 | 2 | 3`, where `Open` meant CLOSED, is now `State = Closed | Greeting | Typing | Transcript` \u2014 the widget\u2019s own lifecycle order. Only `Closed` maps to a prop (`defaultOpen`); the other three are transient internals the component walks through at roughly 260ms, 1160ms and 1480ms after opening, and giving them props would let a consumer freeze the widget in a state it is meant to pass through" },
      { kind: "Added", text: "FIVE TOKENS RATHER THAN FIVE LITERALS \u2014 `layout/chatbot/width` (400), `height` (719), `launcher` (84), `bubbleMax` (246) and `typingDot` (6), plus `ref/brand/samavesh/navy` (#003366) for the mascot disc. The navy is deliberately NOT a themable surface: the disc is artwork, so it stays navy in blue, navy and dbim alike, the same way the National Emblem is not recoloured per portal. Both masters audit to ZERO unbound fills, strokes, paddings, gaps, radii and authored dimensions" },
      { kind: "Added", text: "DOCUMENTATION AT BOTH ENDS. A `Chatbot \u2014 Documentation` frame in the house style \u2014 hero, six counted stats, seven numbered sections \u2014 with a sibling `Component record` frame carrying the open work, which `figma-documentation-style.md` asks for and no page in the library had. The web page moves off the auto-generated stub onto the docs-kit pattern the data-display components adopted yesterday: a live playground, `DoDont`, `A11yChecklist`, `Callout` and a 19-row `PropsTable`" },
      { kind: "Fixed", text: "THREE TOKEN DEFECTS THE PROMOTION SURFACED, corrected in Figma and recorded against the code. The user bubble is hard-coded `--sa-color-brand-navy`, so in the DEFAULT blue brand it paints #003366 while everything around it is #0373DF \u2014 it ignores `data-brand` entirely. Panel title and subtitle both resolve to #404040, leaving the header with no hierarchy. And the icon button is a raw 32px carrying an 18px glyph, where 18 is not on the icon scale at all" },
    ],
  },
  {
    version: "v0.46.0",
    date: "2026-08-22",
    changes: [
      { kind: "Changed", text: "THE SOCIAL MARKS SIT IN A CHIP NOW, WHICH IS THE PART SIZING COULD NOT DO. v0.44.0 normalised them and got the ink spread from 2.37\u00d7 to 1.52\u00d7, and the rail still looked unbalanced \u2014 because the objection was never to the measurement. A letterform, a bare X, a hollow camera, a filled slab and a bubble are five different SILHOUETTES, and no amount of scaling makes them siblings. One repeating circle makes the circle the unit the eye reads and the marks its contents, and the variance stops mattering. This reverses an earlier note in the stylesheet, which argued against five hard shapes in the quietest part of the footer \u2014 correctly, but about OUTLINED circles, which is what it was arguing against. A filled tint is ground, not line" },
      { kind: "Changed", text: "THE TUNING BELONGS TO THE CONTAINMENT, NOT THE MARK. Bare, the corrections pulled hard toward equal ink \u2014 YouTube down to 0.86 \u2014 because with nothing to compare against the eye judges a mark by how dark it is. Inside a chip the frame is constant, the eye reads EXTENT instead, and that correction left YouTube visibly undersized in its circle. Retuned light: YouTube 0.94, Instagram 0.98, the rest 1.0. Marks fill 47\u201350% of the 40px chip; at 55% the chip stops being a frame and becomes a tight collar" },
      { kind: "Fixed", text: "THE CHIP IS A PROPORTION OF THE GROUND, NOT A RAMP RUNG. Rung 700 on the 800 ground looked right in blue, but the lift it gives depends on how each brand spaces its ramp \u2014 measured across the eight modes it ran from 1.13:1 in navy, where the chip is simply invisible, to 1.39:1. `color-mix` of 12% ink into the ground gives 1.30\u20131.36 everywhere. The rung stays as a fallback OUTSIDE an @supports block, because a custom property is substituted lazily: an unsupported color-mix does not fall back to an earlier declaration, it makes the using property invalid and the chip disappears" },
      { kind: "Fixed", text: "THE RAIL\u2019S NEGATIVE MARGIN, which existed to pull the first GLYPH\u2019s optical edge out to the text above it while its 40px box was invisible. With a visible chip the same offset pushed the row 8px PAST the address it should line up with. Removed \u2014 rail and address now both start at 104px" },
    ],
  },
  {
    version: "v0.45.0",
    date: "2026-08-22",
    changes: [
      { kind: "Added", text: "`ActionBanner` GAINS A `card` VARIANT beside its full-width `banner`. Same content model, one axis changed \u2014 and the card STRETCHES to its grid cell and pins the action to the bottom, so a row of cards is one height and every button lands on one line however long the descriptions run. That single rule is most of what makes a card grid look composed rather than assembled. Variants rather than two components because a second component is a second thing to keep in step" },
      { kind: "Fixed", text: "THE FOOTER WAS SHIPPING TWO SUPPORT CTAs. An `ActionBanner` above the footer and a `supportStrip` band inside it, both saying roughly the same thing and both linking to Contact Us \u2014 two sessions solving the same problem and meeting at a merge. `supportStrip` is gone from `SiteFooter` entirely: a call to action is page content, the footer is statutory chrome, and the estate already has one component for the job. The light band above the footer also answers the open question about tone \u2014 the two registers now read as different at a glance" },
      { kind: "Changed", text: "`ActionBanner` NOW ANSWERS TO `data-brand`. It painted `bg-gradient-to-r from-blue-50 to-indigo-50` with a `blue-100` border and `neutral-900/600` text. Indigo is not a SAMAVESH colour and appears nowhere else in the estate; a literal palette cannot follow the brand mode, so the panel stayed blue in navy, burgundy and green \u2014 the same defect the footer had when it painted `bg-navy`; and grey text on a coloured ground reads as washed out, where the secondary line should be a deeper shade of the same tint. The gradient went too: two near-identical tints a fraction apart is not a gradient anyone perceives, it is a second colour to keep in sync for no visible return" },
      { kind: "Changed", text: "THE BANNER\u2019S TITLE IS A REAL HEADING (`as`, default `h3`), so a CTA appears in the document outline instead of being a styled paragraph. The action never shrinks either \u2014 a long sentence wraps instead, because the button is the point of the component and is the last thing that should give way" },
    ],
  },
  {
    version: "v0.44.0",
    date: "2026-08-22",
    changes: [
      { kind: "Added", text: "`BrandGlyph` \u2014 THIRD-PARTY BRAND MARKS, OPTICALLY NORMALISED. Five companies draw their logos to five containment rules, and dropping them into one row at 24px does not make them a set. Measured on the artwork the estate was shipping: optical heights ran 16.9 \u2192 24 and ink coverage 31.5% \u2192 62.4% \u2014 a 2\u00d7 spread in visual weight in a monochrome rail with nothing else to tell the marks apart. The component adds the one thing no vendor supplies: a shared optical size. Ink spread is now 1.52\u00d7, every mark still the vendor\u2019s own artwork" },
      { kind: "Fixed", text: "FACEBOOK WAS THE WRONG ASSET, NOT THE WRONG SIZE. It was supplied as the app BADGE \u2014 the \u201cf\u201d already inside a filled disc \u2014 while the other four marks were bare. A solid blob beside four open marks is a different KIND of object and no amount of scaling fixes it. On the homepage social feed the same badge was being drawn inside a pale tinted chip: a disc inside a disc. Now the bare \u201cf\u201d in both places" },
      { kind: "Changed", text: "MARKS ARE SIZED BY LONGEST SIDE, then corrected by a hand-tuned optical factor \u2014 not by bounding box and not by area. YouTube is wide and short, so area-matching inflates it past the box edge; that is measured, not assumed, because the first attempt here made the spread WORSE (2.37\u00d7 \u2192 2.51\u00d7) before the metric was fixed" },
      { kind: "Removed", text: "THE SAME FIVE PATH STRINGS WERE DUPLICATED ACROSS `SiteFooter` AND THE HOMEPAGE SOCIAL FEED, free to drift apart, and the feed also carried three hand-rolled icon components. `SiteFooterSocial.icon` now takes a NAME instead of a `d` attribute, so a footer\u2019s content file no longer carries a kilobyte of vendor artwork. 28 lines out of the feed, and one source of truth for the marks" },
    ],
  },
  {
    version: "v0.43.0",
    date: "2026-08-22",
    changes: [
      { kind: "Added", text: "`SiteFooter` GAINS A `portal` VARIANT, and that is why it is a variant rather than a second component. The statutory bar \u2014 lineage, policies, related links, credits, copyright, last-updated \u2014 is the half that must stay DBIM 5.6 compliant, and a separate portal footer is a second thing to keep compliant that will drift. `variant=\"portal\"` renders that bar alone at 217px: no navigation columns, no social rail, no address, no support strip, because a portal has its own navigation and a citizen mid-application does not need a sitemap. `columns` / `social` / `address` / `supportStrip` are ignored rather than erroring, so one content object drives both variants" },
      { kind: "Changed", text: "THE DS ALREADY SHIPPED A SLIM PORTAL `Footer` AND NO PORTAL EVER ADOPTED IT \u2014 smile-admin, pm-ajay and nmba each rolled their own. So the variant replaces a component nobody used rather than adding a third. `Footer` stays exported and storied; new work uses `variant=\"portal\"`" },
      { kind: "Added", text: "THE SUPPORT STRIP IS BACK AS AN OPT-IN BAND, absent from the DOM when the `supportStrip` prop is omitted. It was folded into the identity column in v0.42.0, where the button ended up orphaned \u2014 a naked CTA between an address and a row of social marks, with the sentence that explained it deleted. A call to action needs that sentence. Omitting the prop is how a site switches it off; there is no empty state and no placeholder" },
      { kind: "Removed", text: "THREE LINKS THAT WENT WHERE ANOTHER LINK ALREADY WENT. \u201cVision & Mission\u201d pointed at `/website/about-us`, the same page as \u201cAbout Ministry\u201d directly above it; \u201cContact Us\u201d and \u201cHelp & Support\u201d were both `/website/contact-us`; \u201cSitemap\u201d appeared in the Support column AND the policy row. A second label on the same destination promises somewhere new and returns the reader where they were. 36 links to 33. The \u201cHelp\u201d wording was kept over \u201cContact Us\u201d deliberately \u2014 DBIM 5.6 names Help as a required element, and satisfying it by inference from a column heading is the kind of thing an auditor is right to reject" },
      { kind: "Changed", text: "ONE GROUND, ONE HAIRLINE \u2014 INCLUDING THE SUPPORT STRIP, reversing the exception this entry originally carved out for it. The footer ran two near-identical navies for its two bands; at a 1.28:1 step that is not a distinction, it is a smudge. Recessing the strip to rung 900 was tone doing category\u2019s job, and it is wrong twice: visual weight should INCREASE downward, so the darkest value at the top made the footer read as floating on a foreign shelf; and the strip is the most inviting element in the component, so pushing it back says \u201cfurther away\u201d, which is the opposite of its job. It needed a boundary and room, not a colour. One ground, hairlines at the register changes" },
      { kind: "Changed", text: "THE LINEAGE SENTENCE STOPS AT \u201cGOVERNMENT OF INDIA\u201d, which is where DBIM 5.6 prescribes it stops. It had been running on to \u201c\u2026developed and maintained by Digital India Corporation, MeitY\u201d directly beside the credit marks \u2014 the same fact twice, once in prose and once in logos \u2014 and it named ONE of the two organisations credited, so a sentence crediting Digital India ran immediately before a Digital India logo with NeGD unmentioned. Attribution is a separate element and 5.6 already has one for it: hyperlinked logos. Both marks now carry their own label (\u201cDeveloped & maintained by\u201d NeGD, \u201cPowered by\u201d Digital India); previously one had a label and one did not, which read as \u201cPowered by [NeGD] [Digital India]\u201d" },
    ],
  },
  {
    version: "v0.42.0",
    date: "2026-08-20",
    changes: [
      { kind: "Added", text: "`SiteFooter` \u2014 THE PUBLIC-WEBSITE FOOTER, NOW A DESIGN-SYSTEM COMPONENT. Two bands: the working footer (identity, address, one CTA, social, four link columns) and the statutory bar (lineage, policies, related links, credits, copyright, last-updated). It is structural, not content-bound \u2014 every label, href and sentence arrives as a prop \u2014 so a second site in the estate gets the same footer by passing its own content. Distinct from `Footer`, which is the slim app-shell strip under an authenticated portal workflow; the two answer to different clauses and must not be merged" },
      { kind: "Fixed", text: "THE FOOTER COULD NOT ANSWER TO `data-brand`, AND NOTHING SAID SO. It painted `bg-navy`, a Tailwind utility whose bare name is declared TWICE in the hub\u2019s globals.css \u2014 once in `@theme inline` bound to `--sa-color-brand-navy`, and again in the later portal-only `@theme` as the LITERAL `#13366b`. The literal won every time, so the first declaration was a lie the estate read as truth: `bg-navy` looked token-bound and mode-reactive and was neither. The dead declaration is removed and the surviving one is documented as portal-owned; 113 of the 130 bare-navy call sites are portal surfaces that genuinely want that rung, so flipping it would have been the wrong fix" },
      { kind: "Changed", text: "THE FOOTER\u2019S COLOUR IS NOW THREE LOGICAL BANDS ON ONE MODE-AWARE FAMILY. Ground = `primaryScale` rung 800, statutory bar = rung 900, hairlines = rung 600, and the ink ladder is rungs 100 and 200 plus `on/bg/brand/primary/boldest`. In `dbim` the ground resolves to `#162F6A` \u2014 DBIM\u2019s own published Blue shade 1 \u2014 so [DBIM 5.6] (\u201cfooter background = the key colour, darkest shade\u201d) is satisfied by construction rather than by coincidence, across all seven brand modes" },
      { kind: "Changed", text: "INK ON THE FOOTER IS SAME-HUE, NEVER WHITE-ALPHA. `rgba(255,255,255,.8)` on a coloured ground desaturates to a dirty grey; the ramp\u2019s own rungs stay in the family and measure BETTER. Worst case across all seven modes is 5.88:1 against the 4.5:1 AA asks for, and the previous build\u2019s weakest footer text was 4.27:1 \u2014 a real failure nobody had measured" },
      { kind: "Added", text: "`VisitorCounter` \u2014 the \u201cTotal Visits\u201d figure, WITH ITS MOCK NATURE MADE STRUCTURAL. There is no analytics backend here, so the figure is DERIVED (baseline counted at a fixed date, extrapolated per-day, ticking while the page is open) rather than printed as an invented constant. A fixed number on a government footer is a fabricated record; a derived one at least declares its own arithmetic. It is deliberately not a live region \u2014 a figure re-announcing every twelve seconds talks over the page \u2014 and it stops ticking under `prefers-reduced-motion`" },
      { kind: "Removed", text: "THE \u201cNEED SUPPORT?\u201d STRIP. It spanned the full width to carry one heading, one sentence and one button, and cost roughly 120px to say what the button says alone. Folded into the identity column. With the rest of the tightening the footer went 831px \u2192 574px at desktop and 2035px \u2192 1239px at mobile, with nothing removed but the strip" },
    ],
  },
  {
    version: "v0.41.0",
    date: "2026-08-20",
    changes: [
      { kind: "Added", text: "WIDGET VISIBILITY IS ADMIN CONFIGURATION, NOT A DEPLOY FLAG. `/admin/portals` now carries three master switches beside the registry \u2014 demo dock, assistant, cookie banner \u2014 each stored in its own settings row, cache-tagged, saved by the one Save button. The demo dock previously had only `NEXT_PUBLIC_DEMO_TOOLS`, on the assumption it was scaffolding to strip from a real deployment. That is backwards for this estate: the demo IS the product, and what is actually needed is hiding it for one audience \u2014 a ministry walkthrough, a screenshot, a recording \u2014 and putting it straight back without a redeploy" },
      { kind: "Changed", text: "THE COOKIE BANNER IS OFF BY DEFAULT WHILE IT IS REDESIGNED, and the compliance reasoning is recorded next to the constant rather than left implicit. Its copy cites GIGW 3.0 and DBIM, so hiding it is a compliance decision, not only a visual one. Two conditions make it defensible \u2014 the estate is a gated prototype, and it stores only `mosje_cookie_consent`, a first-party functional flag, not tracking. If either stops being true the banner goes back up BEFORE that happens, redesigned or not" },
      { kind: "Added", text: "A SHARED `settings/toggle` MODULE, written when the SECOND on/off setting arrived rather than speculatively for the first. It exists to stop two non-obvious rules being copy-pasted and then diverging: `enabled` must be a REAL boolean, because `Boolean(\"false\")` is `true` and that coercion is exactly what makes an admin toggle look like it does nothing; and an unknown `version` is refused outright rather than half-read. The assistant\u2019s config is deliberately NOT built on it \u2014 it carries a per-surface map as well as a master switch, so it is a different shape rather than a generic one bent to fit" },
      { kind: "Changed", text: "THE TWO TOGGLES FAIL IN OPPOSITE DIRECTIONS, ON PURPOSE. An unreadable store degrades the demo dock to VISIBLE and the cookie banner to HIDDEN. Hiding the dock on a database blip would strip the thing this prototype exists to show; showing a banner the team deliberately took down would be the surprising outcome. Both directions are pinned by tests so neither can be \u201ctidied\u201d into the other" },
      { kind: "Changed", text: "`NEXT_PUBLIC_DEMO_TOOLS=false` SURVIVES AS A BUILD-TIME HARD OFF above the setting, and the precedence is one-way: a deployment built without demo tooling must not be able to acquire it from a database row. Only the exact string `false` counts \u2014 `False`, `0`, `no` and a padded `  false  ` all defer to the setting, which is pinned by test because a loose check here would disable the dock on a typo" },
    ],
  },
  {
    version: "v0.40.0",
    date: "2026-08-20",
    changes: [
      { kind: "Fixed", text: "EVERYTHING PAINTED OVER AN OPEN CONVERSATION. Measured on the live website, the assistant was the LOWEST of the four fixed layers: demo dock 2147483000, UX4G accessibility panel 999999, Important Links 1002, assistant 1000. A panel the citizen deliberately opened was being covered by two persistent rails and a piece of demo scaffolding" },
      { kind: "Changed", text: "THE LAYER IS IN TWO PARTS, NOT ONE BLANKET MAXIMUM. Closed, the launcher is an 84px control the wall and corner rails already keep clear of everything, so it needs only to beat product chrome and sits at 1010 \u2014 deliberately UNDER the accessibility panel, because that is a statutory control and a chat launcher has no business on top of it. Open, the panel is the surface the citizen summoned and nothing decorative may cover it, so it clears the highest layer by one" },
      { kind: "Fixed", text: "THE OPEN VALUE IS GROTESQUE ON PURPOSE, AND THE REASON IS RECORDED. It is 2147483001 because the demo dock claimed (2^31-1)-647 for demo tooling. The honest fix is the dock coming down to a sane layer rather than product UI climbing past it; the dock is outside this component\u2019s scope, so the number is a reaction rather than a choice and says so in the source. The UX4G widget signals \u201copen\u201d only through `right: -530px -> 0px` with no class or attribute, so there is no CSS hook to yield to it \u2014 which is exactly why the closed state stays underneath instead" },
    ],
  },
  {
    version: "v0.39.0",
    date: "2026-08-20",
    changes: [
      { kind: "Fixed", text: "ON PRODUCTION, THE ASSISTANT PANEL\u2019S HEADER WAS OFF THE TOP OF THE SCREEN \u2014 name, expand and close all unreachable. Measured on the live site at panel `y = -117`, header `bottom = -43`. The cap read `100dvh - launcher - gap*3`, which silently assumes the launcher is at its RESTING offset. It is not: the corner rail lifts it clear of whatever occupies the corner, and the website\u2019s cookie bar pushes it to 261px. The panel is anchored to the launcher, so every pixel the rail lifts was a pixel the panel ran off the top. The cap now derives from `--sa-corner-rail-bottom`, the same custom property the launcher is positioned by, so the two cannot disagree again. Verified at lifts of 32, 150, 261 and 400px: the panel shrinks to 719, 605, 494 and 355 and the header stays visible at every one" },
      { kind: "Fixed", text: "THE EXPANDED HEIGHT CARRIED THE SAME ASSUMPTION and would have reintroduced the overflow the cap exists to prevent. Corrected in the same expression" },
    ],
  },
  {
    version: "v0.38.0",
    date: "2026-08-20",
    changes: [
      { kind: "Fixed", text: "THE ASSISTANT WAS WEARING SOMEBODY ELSE\u2019S NAME. It introduced itself as \u201cNoddy\u201d while the seal it renders reads \u201cSamajik Sahayak ~ \u0938\u093e\u092e\u093e\u091c\u093f\u0915 \u0938\u0939\u093e\u092f\u0915\u201d twice around the ring, its header said \u201cChat with us\u201d, and the live assistant on dosje.gov.in \u2014 which is a national myScheme/GovAI embed, not a DoSJE build \u2014 is called Samajik Sahayak. Three names, one widget. It is now Samajik Sahayak everywhere, in both scripts. \u201cNoddy\u201d was also a British children\u2019s character: somebody else\u2019s property, and the wrong register for a Government of India service" },
      { kind: "Fixed", text: "THE ONLY VISIBLE CONTROL IN THE PANEL WAS A DESTRUCTIVE ONE. \u201cEnd Chat\u201d sat alone in the header, in red, in the top-right \u2014 the exact position every user on earth reads as a harmless dismiss \u2014 and it wiped the transcript. There was no minimise at all; closing without losing the conversation meant knowing about Escape or the launcher. The header now carries EXPAND and MINIMISE, matching the live panel, and end-chat is a quiet text action in the footer where destructive things belong" },
      { kind: "Added", text: "IDENTITY THAT DOES NOT SCROLL AWAY. The greeting was the only statement of who was answering, and it leaves the viewport after two exchanges. The header now holds the mascot, the name and the Devanagari subtitle permanently \u2014 which is also what the live assistant does, for the same reason" },
      { kind: "Added", text: "A COMPOSER THAT DOES NOT PRETEND TO BE A LANGUAGE MODEL. The live assistant has a text field and a citizen arriving from it expects one, so there is one. `onSubmit` answers a typed question; with no handler the bot says plainly that it can only help with a few things and re-offers the suggestions. Silence and a fabricated answer are both worse than an honest limit. The note under it is deliberately NOT live\u2019s \u201ccan make mistakes\u201d wording \u2014 that describes a generative model, and copying it would be cargo-culting the shape of a disclaimer without its meaning" },
      { kind: "Fixed", text: "THE OPEN STATE HAD A THICK WHITE BAND AROUND A SMALL BLUE DISC, and it read as a sticker rather than a control. That band is 12px because it CARRIES THE WORDMARK \u2014 at rest the width is earned, there is bilingual type running around it. Insetting the close disc left the same band holding nothing, and a band with nothing on it is not a ring. The disc is full bleed now and a single 1px hairline does the separating. The hairline is deliberately LIGHT, which is what makes it read against the blue masthead, navy portal chrome and the orange band; the shadow carries the one case a hairline cannot, white on white" },
      { kind: "Fixed", text: "THE WIDGET COULD DISAPPEAR INTO THE PAGE, IN TWO DIFFERENT WAYS. The white ring belonged to the MASCOT, and the mascot fades to nothing when the panel opens \u2014 so the open state was a bare navy disc, which vanishes against the website\u2019s blue masthead and the navy portal chrome. The ring and the hairline now belong to the LAUNCHER, so they survive both states. Separately, a white panel on a white page has no visible top edge no matter how much shadow sits under it; it carries a 1px bound border now, which is the only thing that works on white-on-white" },
      { kind: "Changed", text: "ELEVATION IS `toast` LAYERED OVER `raised`, BECAUSE AMBIENT ALONE READS AS FOG. `toast` is 18% alpha at 48px blur with -12px spread \u2014 built for a card on a calm page, and simply not visible over the website\u2019s photographic hero or its orange band. Figma drew `0 12px 6px rgba(31,41,55,.32)`: four times denser, eight times tighter. The fix is not to adopt that literal but to give the object BOTH shadows a real one casts, a wide ambient and a tight contact, and the ladder already has rungs for both" },
      { kind: "Fixed", text: "THE SEAL NO LONGER RESPONDS TO HOVER AT ALL, and that is what was still \u201ca little off\u201d. An animation snapping from stopped to full speed and back cannot be eased in CSS \u2014 a hover-driven keyframe is abrupt by construction. Hover is now carried entirely by the lift and the shadow, which are transitions and interrupt and reverse cleanly. The seal turns for exactly one reason, the assistant thinking, at 10s rather than 6 \u2014 six was an overcorrection that read as spinning, wrong for a government seal, and a slow rate is affordable now it is on screen for seconds rather than the half-second a hover lasts" },
      { kind: "Fixed", text: "THE FLOAT IS CONSTANT, NOT MODULATED. Transitioning its amplitude was smoother than adding and removing it, but not smooth ENOUGH: the keyframe and the amplitude were two independent clocks, so how it looked depended on where in the cycle the pointer happened to arrive \u2014 exactly the reported \u201cnot smooth sometimes\u201d. 2.5px over 5s, always, everywhere. One clock, no phase to get unlucky with" },
      { kind: "Fixed", text: "THE PANEL WAS 402x721, NOT 400x719. The new border was being ADDED to the specified size because the package sets no global reset and the element defaulted to `content-box` \u2014 which also pushed the expanded height 2px past the viewport cap it is meant to respect. The component states its own box model now" },
      { kind: "Changed", text: "THE HEADER MARK IS 40px, MATCHING THE TYPE IT LABELS. Title (16/24) over subtitle (12/16) is 40px of text, so a 30px mark sat visibly short and made the header look top-heavy. Optical alignment, not a magic number" },
      { kind: "Fixed", text: "EVERY EM-DASH IS GONE FROM USER-VISIBLE COPY \u2014 the note, the launcher\u2019s accessible name, the unread announcement and five scripted answers. Caught by the anti-slop pass, not by review: the em-dash is the single most reliable tell that a string was machine-written, and a government assistant is the last place that should read as generated" },
      { kind: "Fixed", text: "THE HOVER JERKED ON MOUSE-LEAVE, AND IT WAS THREE SEPARATE FAULTS STACKED \u2014 all three invisible in the source, because every line of it was correctly token-bound and looked like an ordinary hover rule. (1) BOX-SHADOW STACKS OF DIFFERENT LENGTHS DO NOT INTERPOLATE. Rest was `elevation/toast`, one layer; hover added `elevation/raised`, two more. CSS cannot ease 1 layer into 3, so it SNAPPED, on the way in and again on the way out. The deeper shadow is now an `::after` faded by opacity, which always interpolates. (2) CHANGING `animation-duration` ON A RUNNING ANIMATION JUMPS \u2014 the browser recomputes progress against the new duration, and the seal\u2019s 14s/8s/4s meant two jumps in and two out. One duration now, 6s, and only the play-state moves; moving-versus-still was always the signal, never the rate. (3) REMOVING A TRANSFORM ANIMATION SNAPS IT HOME \u2014 dropping the float returned the mascot to centre from wherever in its cycle it was, up to the full 3px, instantly" },
      { kind: "Changed", text: "THE FLOAT NOW ALWAYS RUNS AND ITS AMPLITUDE TRANSITIONS. `--ds-chatbot-lift` is a registered `@property` with a `<length>` syntax, which is what makes a custom property interpolable at all; the keyframe reads it, so 0px at rest moves nothing and 3px engaged floats. Both directions continuous, and no moment where a transform is switched on or off" },
      { kind: "Changed", text: "HOVER HAS ITS OWN DURATION AND CURVE \u2014 200ms, symmetric `ease` \u2014 because it is REVERSIBLE. The strong ease-out used everywhere else covers most of the distance immediately and then crawls, which is right for something arriving once and being watched. Played backwards on mouse-leave it leaves the element loitering near the hovered position and then dropping, which is precisely what \u201cabrupt on leave\u201d is. 140ms was also a press duration doing a hover\u2019s job: a press is a confirmation and wants to be instant, a hover is an invitation and 140ms reads as twitchy" },
      { kind: "Changed", text: "THE SEAL NOW TURNS TO MEAN SOMETHING. It rotated on hover at 14s a revolution, which is invisible: a one-second hover moves the wordmark about 25 degrees, most of it after the eye has moved on \u2014 the flask defect again, real motion below the threshold of being seen. It is STILL at rest and turns at ONE speed whenever the widget is engaged \u2014 hovered, focused or thinking \u2014 so it reports work in progress even when the panel has scrolled the typing dots out of view. Rotation that encodes state beats rotation that decorates; a rotation RATE encodes nothing anybody reads, and trying to make it cost a visible jump" },
      { kind: "Added", text: "THE GENIE FLOAT, 3px OVER 4.5s. The mascot is a 3D render of a legless robot drawn mid-hover, and a still picture of a floating thing is subtly wrong \u2014 the artwork promises motion the interface refuses and the eye notices without being able to say why. It runs wherever the widget is engaged and unconditionally on the avatar inside the panel, which is a surface the citizen chose to open. It does NOT run on a dormant launcher, for the same reason the seal does not" },
      { kind: "Added", text: "AN ARRIVAL, ONCE PER PAGE LOAD \u2014 320ms, a rise and settle, never repeated. The launcher is the last thing to mount on a page that has already painted, so with no transition it popped into a finished layout and read as a late-loading advert. AND NO SOUND, deliberately: unsolicited audio is hostile on the shared terminals and common service centres where these pages are actually used, and browsers block it before the first interaction anyway, so it would fire inconsistently even if it were wanted" },
      { kind: "Added", text: "EXPAND, because a 400px column is a wall of text for a scheme answer carrying eligibility and a document list. The live panel offers it and already trains the gesture; ours caps at 560x820 and at the viewport, so it can never grow past what is on screen" },
      { kind: "Fixed", text: "THE TRANSCRIPT WOULD HAVE PUSHED THE COMPOSER OFF THE PANEL. A flex item refuses to shrink below its content without `min-height: 0`, so a long conversation would have shoved the whole footer past the bottom edge. Caught while adding the composer, not after someone reported it" },
    ],
  },
  {
    version: "v0.37.0",
    date: "2026-08-20",
    changes: [
      { kind: "Added", text: "THE ASSISTANT IS ON THE WEBSITE, AND WHERE IT APPEARS IS CONFIGURATION. `ConditionalChatbot` is mounted ONCE in the hub root layout \u2014 the same reason `DemoDock` is, because the hub natively hosts every portal and a per-portal mount is something each new portal has to remember and would eventually be forgotten or doubled. On by default on `/website` and nowhere else; a new portal arrives switched OFF. The stored value is a SPARSE PATCH over the code defaults, which is what stops a blob saved today from dictating the answer for a path that did not exist when it was saved" },
      { kind: "Changed", text: "ONE PAGE, NOT TWO. The assistant briefly had a settings page of its own and that was wrong: two tables listing the same 22 surfaces, each with one control. Its switch now sits ON THE REGISTRY ROW at `/admin/portals`, beside that entry\u2019s status, so everything about a portal is in one place. The UI is merged; the STORAGE deliberately is not \u2014 `proxy.ts` reads the registry row on every request to enforce the hidden-entry block, so a malformed assistant config must never be able to reach that path. One Save button, two settings rows, registry written first because it is the one that governs reachability" },
      { kind: "Added", text: "THE CORNER RAIL IS BACK, FOR THE REASON IT WAS RETIRED FOR. `useCornerRailOffset` was removed in v0.34.0 with the note that it was one file in git history away if a chatbot ever wanted that corner \u2014 it does, so it was recovered rather than rewritten. The assistant\u2019s bottom offset is MEASURED, never chosen: the corner is shared with the UX4G accessibility widget\u2019s floating button, which is `display: none` on every page carrying an `AccessibilityBar` and visible on every page that is not, so a constant would be wrong on half the estate" },
      { kind: "Fixed", text: "THE COOKIE CONSENT BAR COVERED THE LAUNCHER, and it was found by measuring rather than by looking \u2014 the bar is z-1050 over the assistant\u2019s z-1000 and full width, so it hid the bottom 54px of an 84px control on a desktop viewport and ALL of it below ~600px, where the bar wraps to 176px. One attribute fixed it: the bar marks itself `data-sa-corner-occupant` and the rail lifts anything in that corner clear while it is up. Deliberately that way round \u2014 consent comes before a chat widget on a government site, so the widget yields, not the bar" },
      { kind: "Changed", text: "THE CORNER RAIL\u2019S OCCUPANT GUARD IS 200px, RAISED FROM 140. The guard exists to tell a LAUNCHER from an open PANEL \u2014 its own example is a 480px conversation panel, and 200 is nowhere near that. What 140 excluded was the consent bar at its measured 176px wrapped height, at which point the rail silently stopped treating it as an occupant. The ceiling is derived from this constant, so raising it moved the ceiling in step rather than leaving the two to disagree" },
      { kind: "Fixed", text: "THE REGISTRY ROW CRUSHED ITS OWN NAME at narrow widths. `flex-1` with no basis let the name column shrink toward zero to keep the controls on one line, clipping a portal to \u201cLiv\u2026\u201d and \u201c/\u2026\u201d \u2014 worse once the assistant toggle became a third control in that row. A `basis-56` gives the name a floor to defend, so the controls wrap to the next line instead" },
      { kind: "Added", text: "AN ADMINISTRATION LINK IN THE HUB FOOTER, which is the one piece of chrome every hub page already carries \u2014 so it is where the door belongs, rather than a new floating control or a URL people memorise. Exposing the link is not exposing the panel: `/admin` sits OUTSIDE the site gate by design (it is the recovery path when the gate password is lost) and is guarded on its own by `requireAdmin()`. An unauthenticated visitor following it reaches a password form, exactly as they would by typing the path" },
      { kind: "Added", text: "WHAT THE ASSISTANT SAYS IS CODE, NOT CONFIGURATION, and that is a deliberate boundary. The admin panel decides WHERE it appears; it does not decide what it claims about government schemes. Every scripted answer is a ROUTE rather than a ruling \u2014 which portal to use, what to have ready, who decides \u2014 and none states an eligibility outcome, because this widget is not the authority on one and a citizen acting on a wrong answer has a real cost" },
    ],
  },
  {
    version: "v0.36.0",
    date: "2026-08-20",
    changes: [
      { kind: "Changed", text: "THE FLASK\u2019S LIQUID MOVES PERMANENTLY. A still flask reads as a picture of a flask; a moving one reads as a thing that is running. Affordable where a permanent animation usually is not \u2014 a single compositor-only translateX on one path inside a 26px box, no layout, no paint, no JavaScript, and slow enough at 3.7s a cycle to sit below the threshold of drawing the eye. The BUBBLES stay gated, so idle and engaged are still legibly different, and `prefers-reduced-motion` stops all of it: permanent motion is precisely what that preference exists to switch off" },
      { kind: "Changed", text: "THE WOBBLE ANSWERS A HOVER OF THE WHOLE RAIL, not of the flask alone \u2014 the rail is one object and the flask is its face, so hovering any part of it, doors included, swings the flask. This REVERSES the removal two releases ago, which cut the hover rock on frequency grounds; it is the widget\u2019s signature and it is what makes hovering the thing feel answered. The reversal brings back the second SVG wrapper, for the reason it first existed: an animation cannot be RESTARTED by re-matching its name on the same element, so an entrance swing and a repeatable hover swing cannot share one node. Retiring it was premature \u2014 it was dead structure only for as long as the hover rock stayed gone" },
      { kind: "Changed", text: "THE ROCK IS PASSED AS AN `animation` SHORTHAND, not a boolean. A flag cannot restart an animation \u2014 the name is already applied, so re-matching changes nothing \u2014 whereas handing the consumer the whole shorthand means its `:hover` rule sets a name where there was `none`, which is a genuine start. It also lets the consumer retime the swing without the icon knowing" },
      { kind: "Fixed", text: "THE DOCUMENTATION DESCRIBED A DOCK THAT NO LONGER EXISTS. design.md, the placement rule, the Storybook story and the changelog all still carried the vertical wordmark and the 52x105 -> 52x153 fold, three releases after the wordmark was removed and the flask learned to become a cross. The design-system rule calls docs drift a defect rather than a follow-up, and this was drift in four places at once. All four now describe the 52x56 tab, the tooltip, the cross and the active-tab indicator" },
      { kind: "Fixed", text: "A CHANGELOG ENTRY ASSERTED SOMETHING WE HAD ALREADY DISPROVED \u2014 that the middle of the right wall is empty on every surface in the estate. The website\u2019s Important Links tab sits at top-42% and the dock landed on top of it, which is why the wall is measured now. The claim is corrected in place rather than deleted: a changelog that quietly edits away a wrong statement teaches nobody why it was wrong" },
    ],
  },
  {
    version: "v0.35.0",
    date: "2026-08-20",
    changes: [
      { kind: "Added", text: "CHATBOT \u2014 \u201cNoddy\u201d, the assistant surface, built from the Figma design at 2175:79096. A launcher that folds open into a quick-reply conversation panel. Two modes that must not be mixed: give it a `greeting` and `quickReplies` and it runs the whole scripted opening itself \u2014 panel, typing, greeting, then the suggestions cascading \u2014 and `onQuickReply` may return a `ChatbotReply` so it answers with no backend; pass `messages` and it renders exactly that and runs no sequence of its own, because two things writing one transcript is how a chat surface starts double-posting" },
      { kind: "Added", text: "THE PANEL IS NON-MODAL, WHICH IS THE OPPOSITE OF `Modal` AND DELIBERATE. It never traps focus and never blocks the page behind it \u2014 a help widget that hostages the keyboard is worse than no help widget. Escape closes it and returns focus to the launcher, verified with a trusted key event and `document.hasFocus()` asserted first, per the lesson the DemoDock fold recorded: a programmatic `focus()` fires no focus events at all in a tab without OS focus, so a synthetic check reports green on a broken keyboard path" },
      { kind: "Changed", text: "THE MOCK\u2019S \u201cEnd Chat\u201d RED FAILS AA AND DID NOT SHIP. `#ff0004` measures 4.00:1 on white against the 4.5:1 normal-text threshold; `text/status/error/base` is the same signal at 9.10:1. The quick-reply chips moved too: nothing in the ramp resolves near the mock\u2019s `#EFE8FF` lavender, and the pale brand tint is the same ROLE, so the design moves to the system rather than the system growing a one-off variable for one component. Both divergences are recorded in `design.md`, not silently absorbed" },
      { kind: "Added", text: "MOTION AUTHORED FROM FIRST PRINCIPLES, BECAUSE THERE WAS NONE TO IMPORT \u2014 `get_motion_context` on the node returns an empty set. The panel is ORIGIN-AWARE, growing from `bottom right` so it visibly comes out of the launcher (a modal stays centred precisely because it is anchored to nothing). Enter 240ms, exit 160ms, both on a strong ease-out; `ease-in` appears nowhere, because it withholds movement at the moment the eye is watching hardest and reads as lag at an identical duration. Only transform, opacity and filter animate. Transitions for anything interruptible, keyframes only for the genuine loops" },
      { kind: "Added", text: "THE BILINGUAL SEAL IS STATIC AT REST, AND THAT IS THE DESIGN DECISION, not an omission. The launcher rides on every page of the estate all day, so a perpetually turning wordmark would be the most-seen animation we ship and the least useful \u2014 and would pull WCAG 2.2.2 in for nothing. It turns on hover and focus only; the unread nudge pulses three times and stops, for the same reason. The mascot and the close glyph cross-fade THROUGH A 3px BLUR, which is what stops the swap reading as two objects overlapping" },
      { kind: "Added", text: "THE WORDMARK IS ONE PATH, WHICH IS WHY IT CAN ROTATE. Figma exports it as 56 separate glyph outlines; merged (they share a fill and carry no strokes, so the union is loss-free) and rounded to 2dp it went 64 kB \u2192 41 kB and, more usefully, became a rigid body. Outlines rather than a live `textPath` because the Devanagari half needs correct shaping and an outline is the same on every platform. The mascot is a 3D render with no vector original, so it is WebP \u2014 8 kB against 31 kB for a PNG of identical output. Both inlined, because this package ships as source with no asset pipeline" },
      { kind: "Fixed", text: "THE LAUNCHER\u2019S SHADOW WAS A RAW `drop-shadow()` AND DID NOT NEED TO BE. The reasoning behind it was wrong: the button\u2019s own border box IS the circle at `shape/full`, so a `box-shadow` casts a circle here and binds to `elevation/toast` cleanly. The first fix attempt declared a `ds-exempt` instead \u2014 which is the wrong resolution whenever a bound alternative exists, and exactly the near-miss literal that stops matching the day elevation moves" },
      { kind: "Changed", text: "`placement=\"fixed\"` MARKS ITSELF `data-sa-wall-occupant`, so the demo dock\u2019s rail measures around the chatbot instead of landing on top of it. That one attribute is the entire contract \u2014 the same one `ImportantLinks` carries. Note what this is NOT: `useCornerRailOffset`, retired in v0.34.0, is still retired. The dock lives on the right WALL now, not in the corner, so a chatbot arriving needs the wall contract and not the corner hook that was written in anticipation of it" },
    ],
  },
  {
    version: "v0.34.0",
    date: "2026-08-19",
    changes: [
      { kind: "Changed", text: "THE DEMO DOCK IS A FOLDING RAIL ON THE RIGHT WALL, not a FAB in a corner. At rest it is a tab \u2014 a 26px flask in a tinted cell plus a vertical wordmark that says what it is without being touched. Engaged, the wordmark collapses and it unfolds DOWNWARD into three doors, 52x105 -> 52x153. This is the third placement and each move fixed the previous one\u2019s real defect: bottom-left collided with PortalLoginShell\u2019s strip; bottom-right inherited a stack above a widget that is `display: none` on every page with an AccessibilityBar, so the FAB floated 108px above an empty corner. SUPERSEDED THE SAME WEEK: the claim that the middle of the right wall is empty on every surface was FALSE \u2014 the website\u2019s Important Links tab sits at top-42%, and the dock landed on top of it. The wall is measured now, not assumed" },
      { kind: "Fixed", text: "THE FLASK WAS 16px AND ITS OWN ANIMATION WAS INVISIBLE. The bubbles are 0.55\u20130.78 units in a 24-unit viewBox \u2014 under one device pixel at that size \u2014 and the 11\u00b0 rock moved the neck about 1.5px. The motion was real and below the threshold of being seen. It is 26px now, in a tinted cell: the cell is what buys the size, because a 26px flask on a bare white field reads as an oversized glyph rather than as an instrument" },
      { kind: "Added", text: "THREE DOORS, EACH OPENING THE PANEL PRE-SELECTED. That is the win over one FAB: Colour drops from two clicks to one, and re-toning the page live is the most-used action in a demo \u2014 the \u2318\u2325C shortcut exists to work around exactly that cost. They target tabs BY ID, never by index, because the tab list changes shape: Sign in is prepended on login routes, so Colour is at index 2 there and index 1 everywhere else. Verified on /portals/nmba/admin/login, which is the only place an index-based door would have been wrong" },
      { kind: "Changed", text: "SIGN IN DELIBERATELY GETS NO DOOR. It exists only on login routes, so a fourth door would appear and vanish by route \u2014 the same \u201crelocates between routes\u201d defect the bottom-left opt-in boolean was removed for. Three doors, fixed, so the rail is something you can build muscle memory for. Sign in already leads inside the panel on those routes, which is enough" },
      { kind: "Fixed", text: "HOVER AND FOCUS WERE ONE FLAG, AND IT BROKE BOTH WAYS ROUND. Focus a door then move the mouse away, and `pointerleave` COLLAPSED THE RAIL OUT FROM UNDER A KEYBOARD USER with their focused control still inside it; blur while still hovering, and it collapsed with no fresh `pointerenter` coming to reopen it. They are two independent holds now, each with its own latch, and the rail is open while either holds. The finding that matters more than the fix: a programmatic `element.focus()` fires NO focus events at all in a tab without OS focus, so every synthetic test had reported this green. It failed on the first real click-then-move. Verify focus behaviour with trusted events, and assert `document.hasFocus()` before believing the result" },
      { kind: "Fixed", text: "THE PANEL RAN 22px OFF THE LEFT EDGE below ~590px wide. Its cap was `100vw - 40px`, written when it was anchored at `right: 0`; moving it out to `right: 62px` without re-deriving the cap left it that much too generous. Nothing about the rule looks wrong until you subtract, and the panel is only that wide on a narrow viewport \u2014 found by measuring the rendered rect, not by reading the CSS" },
      { kind: "Fixed", text: "THE FLASK MUST NOT MOVE ACROSS THE FOLD, and in the prototype it did. The container is anchored by its `top` (centred, it would slide the flask UP as the drawer grew) and its WIDTH IS CONSTANT \u2014 the first build grew 48 -> 58 on unfold, which slid the flask 5px LEFT, because a right-anchored box that widens pushes its centred children inward. The width change bought a faint \u201cengaged\u201d signal and cost the anchor the whole design rests on; the shadow carries that signal instead. Measured at 0.00px on both axes" },
      { kind: "Removed", text: "`useCornerRailOffset`, RETIRED BY ITS OWN SUCCESSOR the same day it merged. It stacked floating widgets above whatever occupied the bottom-right corner; with the dock on the right wall nothing consumes it, and a shared primitive with no consumer reads as governance while governing nothing \u2014 the same rule this system applies to an orphaned Tier-3 token. It is one file in git history away if a chatbot ever does want that corner; do not re-add it speculatively" },
    ],
  },
  {
    version: "v0.33.0",
    date: "2026-08-19",
    changes: [
      { kind: "Fixed", text: "THE ACCESSIBILITYBAR DOCUMENTATION PAGE HAD TEN WRONG CLAIMS, and none of them were visible to a reader \u2014 which is the point, because a documentation page is believed precisely BECAUSE it looks authoritative. The label had moved to `body-2` (14/20), a change made in Figma and synced into the code, and the master\u2019s description followed; the documentation page still said `Label/label-2 (12/16)` in two places. It described the link as underlined when there is no resting underline. It listed `inline/s \u00b7 inline/m \u00b7 inline/l \u00b7 padding/m \u00b7 padding/2xl` and `shape/xxs \u00b7 shape/xs` \u2014 SEVEN NAMES THAT CEASED TO EXIST in the value-naming migrations. It called the divider `Inverse` after the bar standardised on `Inverse subtle`. It claimed WCAG 2.1 where the estate targets 2.2. Target size and the 24\u00d724 / 28\u00d728 hit areas appeared nowhere, though the master documented them" },
      { kind: "Fixed", text: "THE PAGE CONTRADICTED ITSELF FOUR SECTIONS APART. \u00a708 said focus \u201cuses focus/ring\u201d while \u00a704 correctly explained that focus/ring is #0373DF, measures 1.37:1 on this bar\u2019s #005EB9 fill, and fails WCAG 1.4.11 \u2014 inverse ink is 6.36:1. \u00a704 was right. The frame was also renamed to the house style `AccessibilityBar \u2014 Documentation`; Divider and Navbar already used the em dash and this page was the outlier" },
      { kind: "Fixed", text: "THE WEB PAGE CARRIED THE SAME ROT, in four of its seven token-table rows: `--sa-type-label-2-size` (the bar is body-2), `--sa-border-neutral-inverse` (the rule moved into the Divider component), `--sa-shape-full` (radii were unified on shape/4) and `--sa-focus-ring` (rejected at 1.37:1). Its \u201cdo\u201d specimen DEMONSTRATED the focus ring in the very colour the component refuses, and its accessibility checklist asserted the same wrong token. Every one of those names exists in tokens.css, so no dangling-var check could ever have seen them" },
      { kind: "Added", text: "A FIGMA\u2194CODE PARITY GATE. `check:figma-docs` pins each factual claim to the code that backs it: a NUMBER to the token it must equal (resolved through aliases, rem\u2192px, clamp caps read at their ceiling), a BEHAVIOURAL claim to the line that implements it. \u201cFocus is inverse ink, never focus/ring\u201d is pinned to the ABSENCE of --sa-focus-ring in the bar\u2019s stylesheet, so the claim cannot outlive the code. Proven by breaking it four ways \u2014 moving the type token caught the drift in three places at once, exactly the failure that occurred" },
      { kind: "Added", text: "A TOKEN-TABLE CHECK, because three of the four web defects were one kind: the page named a token the component does not reference. Each `token:` row on a component\u2019s documentation page must appear in that component\u2019s stylesheet, or be declared in `via` as reached through another component \u2014 as `--sa-border-neutral-inverse-subtle` is, through Divider. Verified by reintroducing all three original stale rows; each is now caught. The reverse direction (someone edits Figma, the snapshot rots) needs the API, so `check:figma-docs:live` re-fetches and diffs, and `:sync` adopts \u2014 both secret-guarded, degrading to a skip rather than a failure" },
    ],
  },
  {
    version: "v0.32.0",
    date: "2026-08-19",
    changes: [
      { kind: "Fixed", text: "SIXTEEN SIDEBAR LINKS WENT NOWHERE \u2014 nearly half the nav\u2019s anchor links. Each named an id no page renders, which is invisible by construction: the link resolves, returns 200, and silently drops the reader at the TOP of a page about something else. The badge page documents Badge alone, yet the nav promised Alert, Modal, Toast, Loader, Stepper, Empty State and Avatar there; card promised Data Table, Metric Card and Charts; input promised Checkbox, Toggle, Search and Chip; header promised Footer" },
      { kind: "Removed", text: "APP SWITCHER, WHICH HAS NOT EXISTED SINCE DEMODOCK REPLACED IT, was still advertised in three places \u2014 the sidebar, the component index on the design-system home page, and the Navbar page\u2019s Related list, the last pointing at a `components/app-switcher` route that never existed at all. Only `AppSwitcherPanel` survives, as content inside DemoDock, which is demo tooling rather than a product component and does not belong in the index. The Navbar\u2019s Related list now points at the Accessibility Bar, which is what actually sits directly above it" },
      { kind: "Fixed", text: "THE COLOUR MODE LINK POINTED AT `#color-modes`, A SECTION THAT HAS NEVER EXISTED on the Colour page. The section is `#brands` \u2014 \u201cTwo brands, and exactly what a swap changes\u201d. Verified live: the anchor now lands at 80px, clear of the 56px sticky header" },
      { kind: "Added", text: "A DOCS-LINK GATE \u2014 `check:dangling-vars` for pointers. `check:docs-links` resolves every internal `/design-system` link: the ROUTE against pages, `route.ts` handlers AND static files under `public` (an earlier pass that knew only about pages called `llms.txt`, `tokens.json` and two SVGs broken \u2014 a checker that cries wolf gets ignored), and the ANCHOR against the ids its page actually renders. Constructed paths are skipped. Proven by breaking it three ways: a new orphan anchor, a missing route, and a baseline entry that is no longer broken all exit 1" },
      { kind: "Changed", text: "THE 15 REMAINING ORPHANS ARE DECLARED DEBT, NOT SILENCED. They live in `tools/docs-links/orphan-anchors.json`, which MAY ONLY SHRINK \u2014 the ratchet the storybook-coverage and icon-scale gates already use \u2014 and each records where its component IS documented today. Fourteen are in Storybook; Toast is documented NOWHERE, because `ToastProvider` sits in `NOT_RENDERABLE_IN_STORYBOOK` as a context provider, so the toast\u2019s visual appearance has no home. Deleting the nav entries would hide components people need to find, and writing 15 doc sections is not a bug fix, so the resolution is left to a human and the gate stops a sixteenth appearing quietly" },
      { kind: "Fixed", text: "THE PROJECT BRAIN DESCRIBED A REPO THAT NO LONGER EXISTS. `CLAUDE.md` still routed readers to `apps/dosje/`, `apps/portals/` and `apps/docs/` \u2014 all gone in the single-origin consolidation, and contradicted by its own Commands section forty lines below. It also claimed \u201c17 atoms\u201d against 90 exported components, named the colour modes `blue-light` / `blue-dark` when the axis is `data-brand` with `blue` and `navy`, and said the repo was \u201cnot yet an npm workspace\u201d when every `-w` command in the same file depends on it being one" },
    ],
  },
  {
    version: "v0.31.0",
    date: "2026-08-19",
    changes: [
      { kind: "Added", text: "A DANGLING-VAR GATE, because CSS does not warn \u2014 it drops the declaration. `check:dangling-vars` walks every stylesheet and inline style object in the design system, config, hub and Storybook, and fails when a `var(--sa-*)` names a token nothing declares. It runs FIRST in `npm run check`, ahead of the gates it usually explains. Proven by breaking it deliberately: a bogus reference exits 1, a clean tree exits 0, and an unreadable scope or a scan that finds zero declarations exits 2 rather than reporting a false clean \u2014 the failure mode the first ds-linkage checker actually shipped with" },
      { kind: "Fixed", text: "47 SPACING REFERENCES ACROSS 4 FILES POINTED AT NAMES THAT NO LONGER EXIST, left behind by the value-naming migration exactly as 12 radius references had been. `tabs.css` (13), the Divider docs page (15), the changelog page (11) and the Tabs docs page (8). The mapping was recovered from the pre-migration `tokens.css` in git history rather than guessed, which matters because THE FAMILIES ARE NOT ONE RAMP: `stack/s` was 12 where `inline/s` was 8, and `l` meant 16, 24, 20 and 56 in the four families. A single map would have silently moved values instead of preserving them" },
      { kind: "Fixed", text: "THE TOAST WARNING ICON WAS PAINTED BY A FALLBACK, NOT A TOKEN. `status/warningStrong` is the one absent member of a four-token Strong family \u2014 its three siblings exist \u2014 so the reference beside it had never resolved, and the second half of the pair was doing the work. It cannot simply be authored: the naming grammar rejects the whole `color/status/*` shape, and the legacy allow-list that grandfathers the other eleven is CLOSED AND MAY ONLY SHRINK. Bound instead to `text/status/warning/base`, which is what the fallback already resolved to \u2014 identical pixels (#704b00, 7.00:1 on the warning surface), one fewer dangling name. A darker icon would need a token the grammar forbids, which is a design decision rather than a defect fix. The error icon's fallback went too: its token IS declared, so the fallback never fired and only disagreed with it" },
      { kind: "Fixed", text: "THE GATE GREETED ITS OWN MERGE BY FLAGGING THE CORNER RAIL, and it was the gate that was wrong. `useCornerRailOffset` declares its property name as a DESTRUCTURING DEFAULT and calls `setProperty` with the variable seventy lines later, so the literal is nowhere near the call a `setProperty(\u2026)` pattern watches. A custom-property name sitting in VALUE position \u2014 after `=` or `:` \u2014 is a name being plumbed somewhere, never a reference, because references live inside `var(\u2026)`. Recognising that also retired two more advisory entries. Re-verified after the loosening that a bogus reference still fails, in CSS and in TypeScript both: widening what counts as a declaration is exactly the change that can quietly turn a gate off" },
      { kind: "Fixed", text: "THE GATE'S OWN FALSE POSITIVES, before it was wired in \u2014 95 findings over 34 names reduced to 47 real ones. Three classes were noise, all of them ways a name can be declared or mentioned without a stylesheet saying so: prose inside comments and changelog strings, names CONSTRUCTED at runtime (`var(\u0060--sa-${token}\u0060)`), properties set through `setProperty`, and React's computed-key form `[\"--hero-speed\" as string]:`, whose `as string]` between the quote and the colon is why a plain `\"--x\":` pattern misses it. A gate that cries wolf is a gate that gets silenced" },
    ],
  },
  {
    version: "v0.30.0",
    date: "2026-08-19",
    changes: [
      { kind: "Fixed", text: "THE DEMO FAB FLOATED 108PX ABOVE AN EMPTY CORNER, on the majority of the estate. It hardcoded \u201c24px UX4G offset + 70px UX4G height + 14px gap\u201d as its CSS fallback and refined it by measuring `#uw-widget-custom-trigger`. But that trigger is `display: none` on every page carrying an AccessibilityBar \u2014 the website masthead, the whole design-system documentation surface, scw, tg, nhapoa, nmba and smile-admin \u2014 so the measure loop found the element, failed its height check thirty times, gave up, and left the fallback in place. The number LOOKED deliberate, which is why no page-level review ever caught it" },
      { kind: "Added", text: "THE BOTTOM-RIGHT CORNER RAIL \u2014 `useCornerRailOffset`, one shared answer to where a floating widget sits so two of them never land on top of each other. It rests 32px from the bottom when the corner is empty and stacks 16px above whatever is actually there, measured live. A widget joins by calling the hook; a widget that merely OCCUPIES the corner \u2014 a chatbot launcher, when one exists \u2014 sets `data-sa-corner-occupant` on its own element and everything above it moves up, with no change to any other file. Occupancy is re-measured on late injection, on the AccessibilityBar\u2019s root flag flipping, and on resize, all funnelled through one rAF-debounced read" },
      { kind: "Added", text: "A LAUNCHER MOVES THE RAIL; AN OPEN PANEL DOES NOT. Only occupants under 140px tall count. Without that guard a chatbot\u2019s 480px conversation panel opening in the same corner would shove the dock to the top of the viewport \u2014 far worse than the overlap it avoids. The rest offset is a FLOOR, not a default, so a low occupant can never pull the rail down below it, and the ceiling is DERIVED from the corner zone plus the launcher cap rather than picked, because an independent number there would either be unreachable and untestable or would silently contradict them" },
      { kind: "Added", text: "FLASKICON \u2014 the DemoDock FAB\u2019s glyph, hand-drawn and animated. `<Icon>` (Material Symbols Rounded) remains the icon system and the right answer everywhere else; this one is bespoke because it has to MOVE ITS OWN INSIDES to report state, and a font glyph is a single indivisible shape. Idle is still, hover/focus starts the bubbles, and `aria-expanded=\u201ctrue\u201d` speeds them up and raises the liquid \u2014 so the FAB says whether the dock is open even while the panel covers the screen above it. Geometry is computed, not eyeballed: the neck meets the bulb at the exact tangent (\u221a(5.6\u00b2\u22121.7\u00b2) = 5.34), recomputed rather than nudged when the neck was narrowed" },
      { kind: "Added", text: "THE ICON ANIMATES NOTHING BY ITSELF. It ships its animations permanently PAUSED and exposes four custom properties \u2014 `--ds-flask-play`, `--ds-flask-bubbles`, `--ds-flask-cycle`, `--ds-flask-level` \u2014 for the consumer to drive, because only the consumer knows its own markup. That is what lets one file serve both the FAB and the panel\u2019s header badge, which sit in different subtrees and answer to different state. Pausing rather than switching the animation on also means \u201coff\u201d is genuinely invisible (0% is `fill-opacity: 0`) and \u201con\u201d resumes with the three bubbles already out of phase, instead of restarting them in lockstep \u2014 the tell that gives a loop away" },
      { kind: "Fixed", text: "TWO ANIMATION BUGS NO STATIC FRAME COULD SHOW, both found by rendering the icon at 180px rather than trusting it at 16. The bubbles rose 6.5 units and SURFACED INTO THE EMPTY NECK, reading as three dots hovering outside the liquid; travel is now bounded by the liquid, not by the flask. And the wave path spanned only three wavelengths \u2014 enough to cover the bulb at rest, so every paused frame looked right, but a fraction of a cycle in its right edge exposed bare glass down the side of the bulb. Four wavelengths now, verified by freezing the slosh at four points across its cycle" },
      { kind: "Changed", text: "COLOUR COMES FROM `currentColor` THROUGHOUT, the liquid and bubbles via `fill-opacity`. No token of its own, no prop to thread, and correct in all seven brand modes. Under `prefers-reduced-motion` nothing moves but STATE SURVIVES \u2014 a raised level stays raised and the bubbles hold a static frame, so \u201crunning\u201d still looks different from \u201cidle\u201d. Only the movement between them is traded away" },
    ],
  },
  {
    version: "v0.29.0",
    date: "2026-08-19",
    changes: [
      { kind: "Fixed", text: "TWELVE RADIUS REFERENCES POINTED AT TOKENS THAT NO LONGER EXIST, and every one was silently rendering radius 0. The radius value-naming migration renamed the ladder (`shape/xs` \u2192 `shape/4`) but did not follow it into COMPONENT CSS \u2014 `accessibility-bar.css` (3), `tabs.css` (7), and two docs pages. This is not new drift from a merge: main carried the same dangling names. It surfaced as a visible defect \u2014 the bar's font-size buttons had square corners because `--sa-shape-xs` resolved to nothing. Worth noting the gap: the tokens contract test checks dangling `var()` in GENERATED css only, so component stylesheets have no such gate" },
      { kind: "Fixed", text: "THE BAR'S THREE SEPARATORS SAT HIGH \u2014 y=7 in a 46px bar where every control around them was centred at 13. `align-self: stretch` is right for a rule with no length and ACTIVELY WRONG for one with a length: a flex item with a definite cross size treats stretch as flex-start. It fails silently, because the rule is the right size and the right colour, just in the wrong place \u2014 any check measuring the divider alone passes it. Divider now sets `align-self: center` whenever an explicit length is given" },
      { kind: "Fixed", text: "HOVER WAS A SQUARE, A CIRCLE AND A PILL. Five controls in one row, sharing one set of states, carried THREE corner treatments \u2014 and the master had the same split: shape/4 on the steppers and reset, shape/full on the accessibility button and Language, shape/8 on the States specimen chip. Both sides are now shape/4; 42 nodes across the 9 Figma variants were rebound. The defect originated in Figma, not the code" },
      { kind: "Fixed", text: "THE HOVER UNDERLINE RAN THROUGH THE LAUNCH GLYPH. `text-decoration` on an anchor is drawn across every inline descendant, and `text-decoration: none` on the child CANNOT remove a line the parent is drawing \u2014 the only fix is to decorate the label span instead of the anchor" },
      { kind: "Fixed", text: "THE DOCUMENTATION SPECIMENS HAD NO FLAG. `flagSrc` is optional and not one of the three previews passed it, so the docs page demonstrated a bar the estate never actually ships \u2014 every real consumer passes the emblem chip. All three now reference the one national emblem asset rather than copying it" },
      { kind: "Changed", text: "THE LAST 8 HAND-ROLLED RULES ARE STANDARDISED ON inverse-subtle (design decision). They were brand-surface rules at THREE different opacities \u2014 white at 20, 25 and 30% \u2014 none of them chosen, just whatever was nearest. All 22 separator rules in the estate now render <Divider>; the 23rd was never a divider, it is a saffron accent under a login heading. This one MOVES PIXELS \u2014 those rules become slightly more present \u2014 which is exactly why it was held for a human call instead of being folded in with the greys" },
      { kind: "Fixed", text: "TWO GAPS IN THE BAR WERE WRONG BY 10PX AND 4PX, and only a measured side-by-side found them. The master nests the Government-of-India label with its launch glyph in a `Link` frame (gap 2) and the language glyph with its label in a `Label` frame (gap 4); the code used ONE uniform gap on the row, so the launch glyph sat 10px from the word it belongs to and the caret 4px too far. The code now mirrors the master's nesting. Gov block 206 \u2192 196, exactly Figma's; language 120 \u2192 116 against 117" },
      { kind: "Fixed", text: "\u201cSELECTION LAYER\u201d NO LONGER SELECTS ANYTHING \u2014 renamed to `reset hit-area` across all 9 variants. The centre stopped carrying the lit state when it moved to the direction buttons; a layer name describing a highlight that no longer exists is the same class of stale artifact as a wrong comment" },
      { kind: "Added", text: "FULL FIGMA\u2194CODE VERIFICATION OF THE BAR, measured rather than eyeballed: bar 1440\u00d746, fill #005EB9, flag 33\u00d722, font-size group 96 with 8/8 gaps, steppers 24\u00d724, icon buttons 28\u00d728, dividers 1\u00d720, all seven glyphs, type 14/20 at weights 400/400/500 \u2014 every one matching. States checked against \u00a704 and resolved live: Default transparent, Hover #ffffff14 = white 8%, Active #ffffff29 = white 16%, and Focus-visible confirmed with a REAL Tab press at 2px solid rgb(255,255,255) offset 2px" },
    ],
  },
  {
    version: "v0.28.0",
    date: "2026-08-19",
    changes: [
      { kind: "Fixed", text: "THE ACCESSIBILITY BAR RENDERED 12PX WHERE THE MASTER SAYS 14, and the parity table claimed a \u2713 on it. The 2026-08-12 audit checked one text node and generalised; the master actually mixes two roles \u2014 Body/body-2 14/20 Regular on both links, Label/label-1 14/20 Medium on the language label. Caught by a 2\u00d7 side-by-side against Device=Desktop, Layout=Fluid, then confirmed by reading the text styles rather than eyeballing widths. 14 is also the better end of the argument: 12px sits well under the 16px body floor the guidelines set, and this bar carries the SKIP LINK \u2014 a WCAG 2.4.1 bypass mechanism, not decoration" },
      { kind: "Fixed", text: "CLICKING A\u2212 OR A+ HIGHLIGHTED THE CENTRE, ALWAYS \u2014 reported from use, and it was right. The lit state now marks the DIRECTION: A\u2212 lights below the default size, A+ above it, neither at it. Press A+ and A+ lights. The centre is purely the reset and never lights. The old behaviour was wrong twice over: the highlight sat on a button nobody had pressed, so it read as \u201cthe centre is selected\u201d, and one indicator cannot express direction \u2014 90% and 120% were visually identical. Verified across all four steps; boundary disabling unchanged" },
      { kind: "Changed", text: "STILL NO `aria-pressed` ANYWHERE IN THE STEPPER. A\u2212, A and A+ are all ACTIONS, not toggles \u2014 announcing a pressed/unpressed state would describe a control that does not exist. The state a screen-reader user needs is the current size, and it rides on the centre\u2019s accessible name: \u201cText size: 100% (default)\u201d at rest, \u201cReset text size to default \u2014 currently 110%\u201d when deviated" },
      { kind: "Changed", text: "THE GOVERNMENT-OF-INDIA LINK LOSES ITS RESTING UNDERLINE (design decision) and drops from weight 500 to Regular, both matching the master. The affordance is not lost \u2014 it underlines on hover AND on focus, which is what the skip link beside it already did, so the bar's two links finally behave identically instead of one shouting and one whispering" },
    ],
  },
  {
    version: "v0.27.0",
    date: "2026-08-19",
    changes: [
      { kind: "Changed", text: "BREAKING — THE RADIUS LADDER IS VALUE-NAMED, matching the spacing ladder. `shape/none|xxs|xs|sm|md|lg|xl|2xl|3xl|4xl|5xl` -> `shape/0|2|4|6|8|12|16|20|24|32|40`, and `ref/radius/*` with it. This REVERSES a call made earlier the same day. The argument for keeping T-shirt names was that they carry a ROLE — `sm` = input, `md` = button — that a number does not; it fails on inspection, because the role layer ALREADY EXISTS at Tier 3 as `control/radius` and `cmp/*/radius`. A button binds `cmp/button/radius`, never `shape/md`, so role-naming was never Tier 2’s job. `full` deliberately keeps its name: it is a sentinel meaning fully rounded, not a measurement, and S7 asserts it is the ONLY permitted non-numeric rung. 22 renames were declared in RENAMES and PROVEN value-preserving against the un-regenerated fixture before it was rebaselined, 279 call sites were codemodded across 81 files, and 22 Figma variables were renamed with codeSyntax AND description rewritten in the same pass — a rename updates neither, and the spacing rename lost 51 codeSyntax entries exactly that way" },
      { kind: "Added", text: "CHECK:RADIUS-LINKAGE — radius had NO gate of any kind, while colour had six contract tests, spacing had a ratchet and icons had a per-file one. Spacing had been censused three times; radius, zero. The first census of the live library shows what that cost: of 51,844 authorable radius properties, 2.87 % sat on a correct Tier-2 token, 4,902 on a HIDDEN `ref/radius/*` primitive, 5,599 raw, 548 on ANOTHER library’s vocabulary and 132 on a Font Size or Line Heights variable. The gate has two halves — SOURCE invariants that need no Figma access, and a per-page census ratchet that refuses a census not captured one page per invocation, because batching returns partial, non-deterministic trees" },
      { kind: "Fixed", text: "THREE PUBLISHED VARIABLES POINTED AT A PRIMITIVE DESIGNERS CANNOT SEE. `control/radius`, `cmp/button/radius` and `cmp/card/radius` each aliased `{radius.md}` — a HIDDEN Tier-1 — so the published role tokens bypassed Tier 2 entirely. Caught by the new gate’s S5 invariant, the one that shipped with the change that introduced it. Fixed in code and in Figma, value-preservingly: all three still resolve to 8px and tokens.css changed only two alias paths" },
      { kind: "Fixed", text: "RADIUS DEBT SPENT TO ZERO, ACROSS ALL 69 PAGES. 2.87 % -> 97.90 % on a correct Tier-2 token, and every defect class — Tier-1, cross-family, foreign-library and ghost bindings — is now ZERO. The Colour documentation page, the reference every other page copies, went 0 % -> 100 % (2,960 of 2,960). The rebind rule was PROVABLE rather than approximate: an exact rung match binds that rung; a value >= half the shorter side binds `shape/full`, because it ALREADY renders fully rounded; a Tier-1 binding is promoted to the Tier-2 rung of identical value; anything else was reported, not guessed. 116 fractional radii (4.80, 3.90, 3.20, 2.925…) bound to their nearest rung, every move under 1.1px. The per-page ratchet is now frozen at zero, which is the strongest state it can hold — any new raw radius on any page fails the build" },
      { kind: "Changed", text: "BREAKING — CARDS ARE 12px. `cmp/card/radius` said 8px while `shape/12` is the rung published as “cards and panels” and design.md asked for 12. The more useful finding is that the token was ORPHANED: `--sa-cmp-card-radius` appeared only in tokens.css and no component consumed it, so Card drew a raw `--sa-shape-8` and MetricCard a raw `--sa-shape-12` — two card surfaces already 4px apart, with the component token that existed to prevent exactly that sitting unused. Both now bind it. A Tier-3 token nothing binds is worse than no token: it reads as governance while the thing it governs drifts underneath" },
      { kind: "Added", text: "THE STROKE/* BORDER-WIDTH LADDER — `stroke/0 1 2 3 4`. There was NO semantic border-width token at all: the only 1px width a designer could bind was a deprecated, hand-made `ref/border-width/hairline`, which is why 79 documentation-frame borders were sitting on it with nowhere else to go. They could not be forced onto `control/border/width`, which means the border of an interactive CONTROL. Named `stroke` and not `border` for the same reason corner radius is called `shape` — Style Dictionary merges the primitive and semantic namespaces, so a Tier-2 `border/width/*` would self-reference the Tier-1 it aliases — and it is also Figma’s own word for the property. Scoped STROKE_FLOAT + WIDTH_HEIGHT, the second because a rule drawn as a RECTANGLE, which is how Divider is built, sizes its thickness with width/height rather than a stroke. All 79 rebound at the same 1px with zero visual change, and `ref/border-width/hairline` was deleted after verifying zero node bindings" },
      { kind: "Fixed", text: "THE FIGMA EXPORTER SILENTLY DROPPED TOKENS. `collectionFor()` ended in `return null`, which meant “omit from the export” — no warning, no count, no failure — and Tier-3 was routed by COMPONENT NAME against a hardcoded set, so a component reached designers only if someone had remembered to add its name. It now routes by TYPE (colour -> Color, dimension -> Space) and THROWS on anything it cannot place, naming the token. Exercised by feeding the build an untyped probe. The legacy carve-out stays first, because Figma refuses to move a variable between collections and re-routing an existing one would orphan it" },
      { kind: "Added", text: "A SHAPE PAGE IN FOUNDATION — the web half of the Radius documentation, which did not exist. Seven sections, wired into NAV so llms.txt picks it up automatically. Every swatch is drawn with its own token rather than a hardcoded number, VERIFIED in the browser by reading computed styles: `shape/0` -> 0px through `shape/40` -> 40px and `shape/full` -> 999px, all twelve exact. The single raw value on the page is the 9px in the “don’t” card, declared `ds-exempt(specimen)` because it IS the demonstration" },
    ],
  },
  {
    version: "v0.26.0",
    date: "2026-08-18",
    changes: [
      { kind: "Changed", text: "BREAKING — THE SPACING LADDER IS VALUE-NAMED. `padding/m` is now `padding/16`, and 16px in every other family too. The rungs were t-shirt labels and the SAME LABEL CARRIED A DIFFERENT VALUE IN EACH FAMILY: `l` was 16 in inline, 24 in stack, 20 in padding and 56 in section. Seven of eleven labels collided, and the inverse was as bad — 24px answered to four different names. That is inherited verbatim from UX4G 3.0, whose published contract has `--ux4g-inline-l` at 16 beside `--ux4g-stack-l` at 24; standards-precedence.md puts UX4G at authority tier 4, so quality wins and the divergence is recorded. UX4G conformance is untouched, because the `--ux4g-*` layer is emitted independently and never reads our names. 50 renames were declared in RENAMES and PROVEN value-preserving against the un-regenerated fixture before it was rebaselined, 6,149 code references were rewritten by codemod, and 51 Figma variables were renamed — safely, since Figma binds by id" },
      { kind: "Added", text: "EVERY FAMILY NOW CARRIES THE SAME LADDER — 0 2 4 6 8 12 16 20 24 32 40 48 56 64 72 80, plus 120 and 360 in padding for UX4G parity, and section from 24. Previously six values existed in only one family and 72px in none, so a 20px gap and a 40px padding were UNEXPRESSIBLE and work reached past the layer to a hidden `ref/space/*` primitive. 6px is new: UX4G ships it as space-3 and 400 padding properties in the library needed it. 27 variables were created to close the gaps" },
      { kind: "Fixed", text: "38,799 SPACING PROPERTIES WERE BOUND TO A RADIUS VARIABLE — 68% of every binding in the library, and the largest defect the audit found. `ref/radius/none` is scoped CORNER_RADIUS and resolves to 0, so nothing looked wrong; that is exactly how it survived. It is now ZERO on all 54 content pages. Correct semantic spacing went 6.96% → 69.12%. The Colour documentation page — the house-style reference — had 1,284 spacing properties and none bound; it is now 100%" },
      { kind: "Added", text: "CHECK:SPACE-LINKAGE — space had no guardrail of any kind while colour had six contract tests and icons had a ratchet. Twelve assertions now run in Design System Quality: a per-page ratchet over a census of the live library that fails on growth AND on an un-rebaselined improvement, plus two invariants that were impossible before value-naming — a rung's NAME is its pixel value, and no label carries two values across families. All failure modes were exercised by deliberately breaking them" },
      { kind: "Added", text: "A SPACING PAGE IN FOUNDATION. Space was the most-bound token family in the Figma library and the only foundation with no documentation page. It is built to the house style, 100% bound on fills, padding, gaps, radii and text, with the ladder drawn true to scale and a sibling record frame carrying the open gaps" },
    ],
  },
  {
    version: "v0.25.0",
    date: "2026-08-18",
    changes: [
      { kind: "Fixed", text: "THE FONT SIZER NOW ACTUALLY RESIZES TEXT. Until today the A\u2212/A/A+ control wrote `--sa-font-scale` to the document root and NOTHING READ IT — the buttons moved a variable and not one pixel of type. A single rule now consumes it: `:root[data-sa-font-scale] { font-size: calc(100% * var(--sa-font-scale, 1)) }`. Scaling the ROOT is the mechanism rather than multiplying each type token, because the whole scale is authored in `rem` — including the min and max of every fluid `clamp()` — so one change carries the entire ramp plus rem-based spacing, control heights and icons. The rule is armed by the ATTRIBUTE, never by the variable's fallback, so a page with no bar keeps the reader's own browser zoom untouched" },
      { kind: "Added", text: "THE CHOICE PERSISTS. The scale is written to `localStorage` and restored on mount, wrapped in try/catch so a blocked store (private mode, cookie policy) degrades to the default instead of throwing. A text-size control that forgets on every navigation is not an accessibility feature. Verified live: A+ twice \u2192 root 16px \u2192 19.2px, navigate to another page, still 19.2px" },
      { kind: "Changed", text: "THE MASTHEAD REVERSES ITS `fontSize={false}`. The old rationale — \u201cthe UX4G widget is the single mechanism; a second stepper doubles up\u201d — rested on a premise that was never true in practice: the stepper was INERT, so it was not a competing mechanism, it was a dead control. Now that it works, the bar is the direct, visible way to resize text and the widget's floating button is hidden wherever the bar offers the same entry. One door, not two. AND THE LIBRARY ALREADY AGREED \u2014 the \u201cconsequence\u201d this entry first claimed was a phantom. It said 13 nested instances were set OFF to match the old code and would have to be flipped; audited against Figma the same day, all 39 nested AccessibilityBar instances (Navbar 21, Portal Login Template 14, Accessibility Bar 4) already have Font size ON. The CODE was the outlier, so turning it on CLOSED a divergence. Verify a claim like that before repeating it \u2014 this one had been carried forward unchecked through two documents" },
      { kind: "Fixed", text: "THE VENDOR'S FLOATING BUTTON IS HIDDEN, NEVER UNMOUNTED, AND THE DISTINCTION IS LOAD-BEARING. The bar's accessibility icon opens the panel by dispatching a click on `#uw-widget-custom-trigger` — the FAB itself. Remove it and the icon silently stops working, because the thing it clicks no longer exists. `display: none !important` keeps the element and the vendor's listeners alive, and a dispatched click still reaches a hidden element because that is programmatic dispatch, not hit-testing. Scoped to a flag the bar sets ONLY while mounted AND offering the entry, so a page without the bar gets the floating button back rather than losing the widget entirely. Verified in the browser: FAB `display: none`, rect 0\u00d70, and the panel still opens (`right: -530px \u2192 0px`)" },
      { kind: "Fixed", text: "THE HIDE FLAG IS REFCOUNTED. Set-on-mount / delete-on-unmount was wrong on any page with more than one bar — the documentation page renders several previews, and the first one to unmount cleared the attribute while other bars were still live, un-hiding the floating button underneath them. Only the last bar out clears it now" },
      { kind: "Fixed", text: "RESTORE-THEN-APPLY RUNS IN ONE EFFECT. Two effects looked equivalent and were not: on mount the apply effect ran FIRST with the default index, so it wrote scale 1 to the root and wrote \u201c1\u201d OVER the reader's stored preference before the restore effect's setState had re-rendered. A reader who had chosen A++ saw the page snap to 100% and then to 120%, and an unmount caught in between persisted the wrong value" },
      { kind: "Changed", text: "TWO GLYPHS FOLLOW THE FIGMA MASTER, which the designer changed since the last sync: the font-size pill's literal \u201cA\u201d becomes `font_download`, and the language globe `language` becomes `translate_indic`. The second is the better call on its own merits — it is the Devanagari-aware translate glyph, and this estate's language switch is English\u2194Hindi, not a generic locale picker. FLAGGED FOR A DECISION: `font_download` draws its own rounded box, so on the already-boxed pill it reads as a box inside a box, which the component spec had previously recorded a decision AGAINST" },
      { kind: "Added", text: "`skipLabel` — the skip link's text is now a prop, mirroring Figma's `Skip label`. It was hardcoded English, which on a bilingual estate is a defect rather than a design: a Hindi surface needs \u201c\u092e\u0941\u0916\u094d\u092f \u0938\u093e\u092e\u0917\u094d\u0930\u0940 \u092a\u0930 \u091c\u093e\u090f\u0901\u201d and could not have it. The Code Connect template mapped `Skip label` to nothing and said so honestly; all 9 Figma properties are now mapped and none are omitted" },
      { kind: "Added", text: "`cmp/accessibilityBar/iconButtonSize` (`size/28`) — the accessibility and language buttons carried a raw `28px` in the component stylesheet, the one unbound dimension left on the bar. Measured after binding: stepper 24\u00d724, icon buttons 28\u00d728 — at and above the WCAG 2.2 AA 2.5.8 floor of 24\u00d724. That is the AA minimum; 44\u00d744 is 2.5.5 Enhanced at AAA and is not claimed" },
      { kind: "Fixed", text: "`letter-spacing` WAS A RAW `0.1px` THAT DISAGREED WITH THE MASTER, where the bound value is `type/label/tracking` = 0. A literal that no token backed and that shifted every character in the bar by a tenth of a pixel against the design" },
      { kind: "Changed", text: "13 OF THE ESTATE'S 23 HAND-ROLLED RULES NOW RENDER <Divider>, plus one exact inverse match \u2014 14 in total, across the design system, five portals, the website and the reports zone. The neutral greys normalise onto the token: #e2e8f0 (Tailwind slate-200, 9 sites) and #e5e7eb (gray-200, 1) become #dcdee1. Near-identical pale greys, and the Tailwind defaults had no business in a government design system" },
      { kind: "Changed", text: "NINE WERE DELIBERATELY NOT CONVERTED, and they are listed rather than quietly forced. Eight are brand-surface rules at white 20 / 25 / 30% \u2014 three opacities Divider does not model, and pushing them to 40% would visibly change their prominence, so they need a decision: standardise on inverse-subtle, or add tones. The ninth is a 56px saffron flourish under a login heading, which is a decorative accent and not a separator at all. Converting everything would have moved pixels on eighteen sites; that is a redesign, not a refactor" },
      { kind: "Added", text: "DIVIDER IS DOCUMENTED ON BOTH SURFACES, to the same standard as the Navbar. FIGMA: a `Divider \u2014 Documentation` frame in the house style \u2014 1680 wide, hero with a six-stat at-a-glance card, and six numbered sections whose titles are claims rather than nouns (\u201cA master with no code is only a suggestion\u201d, \u201cThe 20px on this page is a specimen, not a default\u201d). Audited at 100% bound: 126 fills, 159 spacing values, 20 radii, 98 text nodes on published styles, ZERO unaccounted. WEB: /design-system/components/divider, registered in the nav so llms.txt picks it up automatically" },
      { kind: "Fixed", text: "THE DOCUMENTATION PAGE HIT THE TWO SNAPPING DECISIONS THE TABS PASS ALREADY RECORDED, and follows them rather than re-litigating: the house style's 88px hero padding becomes 80 (there is no 88 token, and the two house-style rules contradict each other on this), and its 28px panel padding becomes 32 (padding has no 28 step \u2014 the ramp changes cadence there). Layout-only containers carry NO fill: `createAutoLayout` hands out a default white one, which counted as 25 raw fills until they were cleared" },
      { kind: "Added", text: "DIVIDER IS A REAL COMPONENT NOW. The SAMAVESH library has had a `Divider` master since the AccessibilityBar was built \u2014 6 variants, Orientation \u00d7 Tone \u2014 and the design system had NO code counterpart at all, so every consumer hand-rolled its own rule: the bar with a styled <span>, others with a bordered <div>. That is how one 1px hairline ends up with several slightly different colours across one estate. `<Divider orientation tone length decorative>` ships from the barrel, with a Storybook story, a Code Connect template and a design.md entry" },
      { kind: "Changed", text: "TONE FOLLOWS THE SURFACE: `default` on light, `inverse` (white) for a rule separating SECTIONS on a dark surface, `inverse-subtle` (white @ 40%) between CONTROLS inside a brand surface \u2014 at full strength a rule competes with the thing it separates, which is exactly why the AccessibilityBar uses the subtle one. `length` is optional and usually wrong to set: omit it and the rule stretches, vertical via `align-self: stretch` and NOT `height: 100%`, which resolves against a parent with no height and collapses to nothing" },
      { kind: "Fixed", text: "`decorative` DEFAULTS TO TRUE, AND THAT IS AN ACCESSIBILITY DECISION, not laziness. A rule between toolbar controls is presentation \u2014 announcing \u201cseparator\u201d between every pair of buttons in the accessibility bar is noise \u2014 so the default is aria-hidden with no role. A genuine thematic break passes `decorative={false}` and renders a real <hr>, which already carries role=\"separator\". Figma cannot express this, so the Figma description says so in words" },
      { kind: "Added", text: "`border/neutral/inverse` AND `border/neutral/inverse-subtle` WERE LIBRARY-ONLY \u2014 Figma had carried them since the bar was built and the code had no name for them, which is why the bar hand-rolled a white rgba. Both are authored in code now, dropping the recorded Colour gap from 6 library-only variables to 4" },
      { kind: "Changed", text: "BOTH WERE RENAMED IN PLACE IN THE LIBRARY \u2014 `border/neutral/inverse` \u2192 `border/neutral/inverse/default` and `border/neutral/inverse-subtle` \u2192 `border/neutral/inverse/subtle` (VariableIDs preserved, so every binding followed rather than breaking). The hyphen inside a segment violates grammar RULE 1 \u2014 a hyphen is the flattening delimiter, not part of a name \u2014 and the round-trip test caught it the moment the code tried to author it. The library also contradicted itself: border/neutral/bolder already nests as bolder/default and bolder/hover" },
      { kind: "Removed", text: "THE BAR'S THREE PRIVATE DIVIDER TOKENS ARE RETIRED from code AND Figma \u2014 cmp/accessibilityBar/{dividerWidth,dividerHeight,dividerColor}. A rule's width and colour are Divider's business. Zero consumers was VERIFIED before deleting (a grep returned only stale .next artefacts), the Figma three were unpublished and unbound, and the values did not vanish: width \u2192 cmp/divider/width, colour \u2192 border/neutral/inverse/subtle, length \u2192 the consumer's call. Declared in REMOVED with that evidence, because a token that disappears without a declared reason is indistinguishable from an accident" },
      { kind: "Fixed", text: "ADOPTING DIVIDER NEARLY BROKE THE MOBILE COLLAPSE. The Device=Mobile rule selects `.sa-abar__sep`, so swapping the spans for <Divider> would have left three white rules visible on mobile where the whole right cluster is meant to disappear. The class survives as a HOOK carrying no paint, and the collapse was re-verified in the browser at 375px: dividers hidden, font-size hidden, SKIP LINK STILL VISIBLE \u2014 the deliberate WCAG 2.4.1 divergence intact" },
      { kind: "Fixed", text: "A WHOLE COMPONENT TOKEN TIER WAS FALLING OUT OF THE FIGMA PAYLOAD SILENTLY. `collectionFor()` in the Figma exporter had no case for `accessibilityBar`, so it returned null and ALL TWELVE `cmp/accessibilityBar/*` tokens were dropped \u2014 the THIRD instance of the exact bug that file's own `layout/*` and `badge/dotSize` comments already describe, and the reason the library carried `layout/bar/height` and `layout/flag/width` by hand with nothing in the build defining them. Routed by TYPE, because the tier is mixed: 9 measurements to Space, 3 inverse state layers to Colour" },
      { kind: "Added", text: "ALL 12 NOW EXIST IN THE LIBRARY, AND EVERY ONE IS AN ALIAS \u2014 to layout/bar/height, layout/flag/width, ref/size/{12,20,22,24,28,32}, ref/border-width/hairline, border/neutral/inverse-subtle and overlay/on-brand/{hover,pressed}. Not one duplicates a literal, so a component token cannot drift from the semantic it mirrors. The value-parity gate refuses a record that was not pushed first (\u201cDo NOT just update the record\u201d), so they were created in Figma and the checksums re-recorded against a live read \u2014 counts read, not inferred" },
      { kind: "Fixed", text: "TWO TOKENS DEFINED THE SAME THING TWICE IN CODE: `layout/bar/height` and `cmp/accessibilityBar/height` each carried their own 46px literal, as did `layout/flag/width` and `cmp/accessibilityBar/flagWidth` at 33px. Two independent definitions of one measurement \u2014 move one and the other silently stays. The component tokens now ALIAS the layout ones; resolved values are unchanged at 46 and 33" },
      { kind: "Added", text: "`check:code-connect` \u2014 THE DRIFT GATE PROMPT 11 \u00a7PHASE D ASKS FOR, WHICH DID NOT EXIST. `.figma.ts` files are EXCLUDED from the design-system tsconfig, so nothing type-checked them: a template could name a renamed React prop or a deleted Figma property and every gate stayed green while Dev Mode served a snippet that does not compile. It now fails on six things \u2014 a prop the Props interface does not declare, an unmapped variant value, a Figma property the master does not have, a property silently dropped, a missing source file, and the v1 `figma.connect()` format. All six were verified by deliberately breaking the template and confirming each was caught" },
      { kind: "Changed", text: "CODE CONNECT IS CONNECTED, AND THE REMAINING HALF IS HONESTLY NAMED. All 9 AccessibilityBar variants resolve in Dev Mode. The RICH templated snippet \u2014 props filled from the selected instance \u2014 needs `figma connect publish`, which needs a FIGMA_ACCESS_TOKEN; the MCP push accepts a `template` argument but the published record still returns `hasTemplate: false`, verified twice. `@figma/code-connect` v2 is now a devDependency with `figma:connect` / `figma:connect:check` scripts, and the dry run validates all 7 templates" },
      { kind: "Fixed", text: "TWO CODE CONNECT CONFIG DEFECTS, BOTH CAUGHT BY ACTUALLY RUNNING THE CLI: `figma.config.json` declared `\"parser\": \"react\"`, the v1 parser-based setting, which CLI v2 refuses to run with even though every template here is parserless; and `accessibility-bar.figma.ts` hardcoded its Figma URL while the other six used the `<SAMAVESH>` substitution. CLAUDE.md and the readiness doc both still said Code Connect was BLOCKED with zero mappings and no dependency in the repo \u2014 false on all three counts, and corrected" },
      { kind: "Changed", text: "THE FONT-SIZE PILL NOW LIGHTS ON DEVIATION, NOT AT THE DEFAULT (design decision, 2026-08-18). It lit at 100% before, which said nothing useful and actively failed the returning reader: the scale PERSISTS now, so someone who chose 120% last visit came back to a bar that looked identical to an untouched one \u2014 three of the four steps were visually indistinguishable. Lighting on deviation makes the highlight mean \u201cthis page is not at the default size, and this is the control that undoes it\u201d. An unlit pill means 100%. The master's nine `Selection layer` frames were cleared to transparent in the same pass, so Figma's resting state matches" },
      { kind: "Fixed", text: "`aria-pressed` IS GONE FROM THE RESET BUTTON. The middle A is a reset ACTION, not a toggle, and announcing it as pressed/unpressed described a control that does not exist. The state a screen-reader user actually needs is the current size, so the accessible name carries it instead \u2014 \u201cText size: 100% (default)\u201d at rest, \u201cReset text size to default \u2014 currently 110%\u201d when deviated. It stays ENABLED at the default even though resetting is then a no-op: disabling it on reset would destroy focus at the exact moment the reader activated it" },
      { kind: "Fixed", text: "THE MASTER'S CONTROLS WERE 20\u00d720 AND WOULD HAVE FAILED WCAG 2.5.8 \u2014 now closed (design decision, 2026-08-18). A\u2212, A+, the accessibility button and the language pill were bare glyphs with NO hit-area frame; only the middle A had one. All 8 variants that carry the cluster gained explicit `hit-area` frames: 24\u00d724 steppers (`shape/4`), 28\u00d728 accessibility, 28-tall language (`shape/full`), with width, height and all four radii BOUND to variables rather than typed. Figma now matches the code, which already shipped these sizes. Nothing moved visually \u2014 the frames are transparent, every variant is still 46px tall, and the right cluster grew ~35px on a 1200 container" },
      { kind: "Fixed", text: "THE BAR HAD NO PRESSED STATE AT ALL, and its hover was an invented value. \u00a704 of the Figma documentation page defines four states \u2014 Default, Hover (white 8%), Focus-visible (2px ring), Active (white 16%) \u2014 and the code implemented three: hover was hardcoded at 12% against the published `overlay/on-brand/hover` 8%, and `:active` did not exist. Both are fixed and every clickable control on the bar is covered. `:active` is declared AFTER `:hover` at equal specificity, because a pointer is almost always hovering the control it presses and the reverse order makes the pressed state unreachable" },
      { kind: "Changed", text: "ACTIVE MEANS TWO THINGS, AND THE PAGE SAYS SO: \u201cpressed, or the current selection (e.g. the middle A)\u201d. The light rectangle behind the middle A is that ACTIVE layer showing the current selection \u2014 its Figma layer is literally named `Selection layer` \u2014 and pressing A\u2212 or A+ now shows the same layer, which it did not before. Both senses bind to ONE token rather than two look-alike values" },
      { kind: "Fixed", text: "THE DOCUMENTATION PAGE PRESCRIBED A FOCUS RING THAT FAILS WCAG, and it has been corrected IN FIGMA. \u00a704 specified `focus/ring \u00b7 2px`; `focus/ring` is #0373DF, which on the bar's #005EB9 fill measures 1.37:1 \u2014 far below the 3:1 that WCAG 1.4.11 / 2.4.11 require, i.e. close to invisible exactly where a keyboard user needs it. Inverse ink measures 6.36:1. The code already used inverse ink and was right; the Figma specimen and its card were rebound to match. A ring's contrast is a property of what it lands on, not of its name" },
      { kind: "Fixed", text: "THE \u201c13 NESTED INSTANCES ARE SET OFF\u201d CLAIM WAS FALSE, and it had been carried forward unchecked through two documents and a commit message. Audited against Figma: all 39 nested AccessibilityBar instances \u2014 Navbar 21, Portal Login Template 14, Accessibility Bar 4 \u2014 already have `Font size` ON. The CODE was the outlier, so turning the masthead's stepper on CLOSED a divergence rather than opening one. `navbar.md`'s Anatomy, which asserted the same thing in the present tense, is corrected" },
      { kind: "Changed", text: "THE MASTER BOARD NOW MATCHES THE NAVBAR PAGE'S ORGANISATION \u2014 a grey `1 \u00b7 Accessibility Bar` section (renamed from `01 \u00b7 \u2026`), holding a white card at radius 12 with padding 28 and gap 16, opening with a `head` frame that carries the component name in Title/title-2 and a one-line purpose in Label/label-3, then the component set at padding 32 / gap 40. Every fill is bound to the same variables the Navbar cards use, not to matching literals" },
      { kind: "Fixed", text: "TWO UNBOUND VALUES ON THE BAR: `letter-spacing` was a raw 0.1px that disagreed with the master's `type/label/tracking` of 0, and the icon buttons' hit area was a raw 28px, now `cmp/accessibilityBar/iconButtonSize`. Measured after binding \u2014 stepper 24\u00d724, icon buttons 28\u00d728, at and above the WCAG 2.2 AA 2.5.8 floor of 24\u00d724 (44\u00d744 is 2.5.5 Enhanced at AAA and is not claimed)" },
      { kind: "Changed", text: "OPEN, AND IT IS AN ACCESSIBILITY GAP IN THE MASTER: in Figma the A\u2212, A+, accessibility and language glyphs are bare 20\u00d720 text nodes with NO hit-area frame \u2014 only the middle A has one, the 32\u00d732 `Selection layer`. As drawn those targets are 20\u00d720 and would fail WCAG 2.5.8. The code does not reproduce that (24\u00d724 and 28\u00d728, measured live). Figma should gain matching hit frames; flagged rather than changed, because it alters the master's structure" },
      { kind: "Fixed", text: "THE CHANGELOG HAD TWO ENTRIES FLAGGED `current` — v0.24.0 and v0.23.0 — the exact merge artefact `CLAUDE.md` warns about on this file. Exactly one is current again" },
      { kind: "Changed", text: "MEASURED, AND IT IS NOT THE HAPPY NUMBER: on the live homepage only 38 of 272 text elements (14%) actually resize, because the website's markup is authored in hardcoded px (`text-[15px]`, `text-[13px]`, `text-[11px]`) that no root change can reach. The token-driven documentation surface scales 80.4% and a portal 29.3%. The MECHANISM is correct and the token scale is honest; the consuming pages are not. Recorded rather than hidden, because a citizen pressing A+ on the homepage today sees almost nothing move" },
    ],
  },
  {
    version: "v0.24.0",
    date: "2026-08-18",
    changes: [
      { kind: "Fixed", text: "THE OVERFLOW ROW LOOKED BROKEN, AND A SCREENSHOT SHOWED IT WHERE MEASUREMENTS HAD NOT. A native scrollbar ran THROUGH the filled segmented control, \u201cOverview\u201d rendered as \u201crview\u201d and \u201cSkill development\u201d as \u201cSkill d\u201d, and the selected pill was sliced at its rounded corner. Three fixes: the native scrollbar is hidden — ONLY in overflow mode, where the menu is an alternative affordance, never when it is the sole one; tabs `scroll-snap` to the start edge so the row always rests on a whole tab; and — on `track=\"none\"` — measured per-direction edge fades (`mask-image`, not an overlay, because an open row sits on a surface the component cannot know) make the trailing cut read as \u201cmore this way\u201d instead of damage. The trigger\u2019s gap went from 2px to 8px so it stops reading as part of the track" },
      { kind: "Fixed", text: "THE EDGE FADE WAS DISSOLVING THE TRACK, NOT THE TABS. On `track=\u201cenclosed\u201d` the border, the fill and the corner radius are all painted by the SCROLLING element, and a mask applies to everything an element paints — so the fade ate the container. At 200% zoom the track lost its rounded corner and read as a smudge. The fade is now scoped to `track=\u201cnone\u201d`, where nothing else explains the cut; an enclosed row clips crisply against its own border, which is the call Material and Carbon also make for a bounded scrollable row. Caught by screenshotting the two side by side, not by reading the rule that justified the mask" },
      { kind: "Fixed", text: "THE MASK STOPS WERE RAW `#000`, and the token-lint and ds-linkage gates both failed on them — correctly, even though a mask never paints its hue. They are `currentColor` now, which states the true constraint: a mask reads ALPHA only, so any opaque value serves. Binding a colour TOKEN would have been worse, implying the fade re-themes when it cannot. Both gates run only in CI, which is why every local check was green when this was pushed" },
      { kind: "Fixed", text: "THE OVERFLOW TRIGGER WAS RENDERING 20\u00d720 INSTEAD OF THE MASTER\u2019S 44\u00d744 — a WCAG 2.5.8 Target Size failure at Level AA, on a citizen-facing government portal. `--ds-tabs-pad-y` is defined by `.ds-tabs--{size}`, and the trigger is a SIBLING of the tablist rather than a descendant, so it inherited no padding at all and collapsed to the bare glyph. The size class now sits on the wrapper too. Found by auditing the rendered element, not by reading the CSS" },
      { kind: "Fixed", text: "RTL WAS INVERTED. Chromium and Firefox report `scrollLeft` as a NEGATIVE offset in RTL (measured: -461), so the edge detection claimed \u201cat the start\u201d while scrolled to the end and put the fades on the wrong sides. It now uses `Math.abs(scrollLeft)`, and the two single-edge masks swap under `[dir=rtl]` because a CSS gradient has no logical direction keyword. `scroll-padding-inline` and `scroll-snap-align` were already logical" },
      { kind: "Changed", text: "THE MENU NOW LISTS EVERY TAB, not just the hidden ones, and marks the current one with `role=\"menuitemradio\"` + `aria-checked`. Listing only what was out of view meant the same menu gave different contents at different scroll positions — surprising, and something no shipped system does. `aria-checked` on a radio item replaced `aria-current`, which is a global attribute with patchy menu support where this is the documented pattern. The ResizeObserver callback is also coalesced into a single `requestAnimationFrame`: one entry per tab plus the list meant N+1 layout reads per font swap on the low-end Android these portals actually run on" },
      { kind: "Added", text: "THE TABS / MORE OVERFLOW TRIGGER IS BUILT — the last master with no code counterpart. Set `overflow` on `<Tabs>` and it appears ONLY when tabs are actually hidden, never as permanent chrome, listing the ones currently scrolled out of view. There is no `<TabsMore>` export and there must not be: the trigger needs the tablist beside it to have anything to talk about" },
      { kind: "Added", text: "IT IS A MENU BUTTON, NOT A TAB — `role=\"button\"`, `aria-haspopup=\"menu\"`, `aria-expanded`. `role=\"tab\"` would promise a panel that does not exist and tell a screen-reader user there are more sections than there are. It renders OUTSIDE the `role=\"tablist\"` for that reason, and being outside is also what keeps it pinned while the tabs scroll past. Keyboard: Enter/Space/Down opens and focuses the first enabled item, Up/Down and Home/End move and SKIP disabled items exactly as the tablist does, Escape closes and returns focus to the trigger, Tab leaves. The menu is portalled and fixed, because the tablist sets `overflow-x: auto` and an in-flow menu would be clipped by the very scroll container it exists to resolve" },
      { kind: "Added", text: "IT DOES NOT REMOVE TABS FROM THE TABLIST. Every tab stays rendered, focusable and arrow-reachable; the menu is a POINTER shortcut to what is out of view, and its contents recompute on scroll because \u201cwhat you cannot see\u201d is what it means. The other common model — moving tabs into the menu — costs them their `role=\"tab\"`, their `aria-controls` and their place in the roving tabindex, which is a worse trade than the scrolling it saves. Choosing an item selects that tab AND scrolls it into view; without that it would select something still off-screen and appear to do nothing" },
      { kind: "Changed", text: "`overflow` IMPLIES CONTENT-WIDTH TABS, AND IT HAS TO. `flex: 1 1 0` makes every enclosed tab share the track equally, so the row can NEVER overflow — add tabs and they simply get narrower and truncate harder, the trigger never appears, and the labels get less readable. This was caught in the browser, not in review: the first build rendered seven tabs squeezed into 460px with every label truncated to two characters and no trigger in sight. Enabling `overflow` now stops the tabs shrinking, so labels stay fully readable and the truncation tooltip has nothing to do there. `overflow` is off by default because switching it on wraps the tablist in a positioning element" },
      { kind: "Fixed", text: "A CONSUMER TRAP WORTH KNOWING: a tablist inside a flex or grid item needs `min-width: 0` on that item. Without it the item refuses to shrink below its content, the row never overflows, and the trigger correctly never appears — which reads exactly like a broken feature. Documented on the props table and in the story that first hit it" },
    ],
  },
  {
    version: "v0.23.0",
    date: "2026-08-17",
    changes: [
      { kind: "Added", text: "TABS NOW MATCHES THE REBUILT FIGMA MASTERS. `<Tabs>` gains `indicator` (underline · rail · pill), `size` (s · m · l → 36 / 44 / 48), `track` (none · enclosed) and `orientation` (horizontal · vertical); `TabDef` gains `icon`, `badge` and `disabled`. Every default preserves what shipped before — pill in an enclosed horizontal track at size m — so no existing call site moves" },
      { kind: "Added", text: "INDICATOR AND TRACK PAIR, AND ONLY TWO OF THE SIX COMBINATIONS ARE CORRECT: `enclosed` takes `pill`; `none` takes `underline` when horizontal and `rail` when vertical. The Storybook story renders the two wrong pairings beside the two right ones, because a pill floating on an open list and an underline drawing a second edge inside a track are both easier to recognise than to describe. `size` and `indicator` stay LIST props even though Figma carries them per tab — a list whose tabs disagree about size is a defect" },
      { kind: "Added", text: "A DISABLED TAB STAYS IN THE TABLIST. It keeps `role=\"tab\"` and is marked `aria-disabled`; the arrow keys step over it, and Home/End land on the first and last ENABLED tab. The native `disabled` attribute is deliberately not used: it drops the button out of the accessibility tree, so a screen-reader user would lose the fact that the section exists at all" },
      { kind: "Added", text: "TOKENS: `text/brand/primary/bolder` and `icon/brand/primary/bolder` (both `color/primaryScale/600`), `layout/tab/indicator` (2) and `layout/tab/track` (4). The brand key colour is chosen to be recognisable, not readable — #0373DF measures 4.07:1 on `bg/neutral/subtler` and 4.19:1 on `bg/brand/primary/base`, failing WCAG 1.4.3 AA on exactly the tinted surfaces brand text sits on; the 600 rung measures 6.36 / 5.57 / 5.74. All four already existed in the library, so this was CODE catching up to Figma, not the usual direction — each was read back live from the Tabs masters before the parity record moved" },
      { kind: "Fixed", text: "`focus/ring` DROPS ITS 48% ALPHA in Blue (#0373DF) and Navy (#003366). Composited it measured 2.01:1 on white, 1.92:1 on a tab track and 1.16:1 on a selected brand pill — all below the 3:1 floor of WCAG 1.4.11 and 2.4.11, and worst exactly where a keyboard user needs it most. An alpha ring cannot be assessed once and trusted, because its contrast is a property of whatever it happens to land on" },
      { kind: "Fixed", text: "THE SLIDING INDICATOR WAS 1PX OFF ON THE ENCLOSED TRACK, for as long as the track has had a border: `getBoundingClientRect` reports the border box, but the indicator is positioned against the PADDING box. It now subtracts `clientLeft`/`clientTop` — measured, not guessed — and adds `scrollLeft`/`scrollTop`, without which it drifted by exactly the scroll distance once a horizontal list overflowed" },
      { kind: "Fixed", text: "EQUAL-WIDTH TABS LET A LONG LABEL PAINT OVER ITS NEIGHBOUR — \"Application details\" ran through \"Documents\" in a 720px track, because `flex: 1 1 0` hands every tab the same slot and `white-space: nowrap` does not care. The label now ellipsises. That is a fallback, not the intent: the Figma masters define no truncation treatment, so it stays flagged as an open gap" },
      { kind: "Added", text: "`inline/xl` = 24. `inline` was the ONLY spacing family without a 24 step — `stack/l` and `padding/xl` both have one — so a 24px horizontal gap was the one common measurement with no semantic name, and every use had to reach past the layer to `ref/space/2xl`. A t-shirt ramp has no slot between `l` and `xl`, so inserting one pushes the steps above it up a name: the old `inline/xl` (32) is now `inline/2xl`, renamed IN PLACE in Figma so every existing binding followed it rather than breaking, with 2 code call sites migrated. It was proven value-preserving before rebaselining — the contract fixture showed the old name's old value equalling the new name's new value across all 24 selector contexts, and nothing else moved" },
      { kind: "Changed", text: "THE MATCHING 28 STEP FOR `padding` WAS ATTEMPTED AND ABANDONED, and the reasons are worth keeping. Three independent constraints refuse it and all three are correct: `padding` already uses ALL ELEVEN canonical rung names (`none`…`4xl`) that `space-linkage.test.mjs` recognises, so there is no slot to insert into; `space` has no 28 primitive because its ramp deliberately changes cadence there, running in 4s to 24 and in 8s from 32; and that same gate requires every semantic space token to alias a primitive, so a literal is refused too. Aliasing `{size.28}` was tried and reverted — `size/*` is a rem scale, and 1.75rem sitting in a px ramp INVERTS the scale below a 16px root (at 13px it resolves to 22.75px, under `padding/xl`'s 24). The system was saying 28 is not a padding step, so the documentation moved onto 32 instead: 53 nodes bound to `ref/size/28` are now `padding/2xl`, and 49 gaps moved from `ref/space/2xl` to the new `inline/xl`" },
      { kind: "Added", text: "THE CLIPPED TAB LABEL IS NOW SOLVED FOR EVERY INPUT, not by one affordance. MOUSE: the label clips and a real `Tooltip` shows the full text on hover. KEYBOARD: the same tooltip opens INSTANTLY on focus — the `title` attribute it replaces never opened on focus at all, which was the largest hole in the old behaviour — and Escape dismisses it without moving focus (WCAG 1.4.13). SCREEN READER: nothing to rescue, because the clipping is CSS and the full string is already the button\u2019s accessible name; the bubble is `aria-hidden` and carries no `aria-describedby`, without which the name is announced TWICE. TOUCH: the label is NOT CLIPPED AT ALL — under `@media (hover: none)` enclosed tabs stop sharing the width equally, size to their content, and the row scrolls, because a tooltip is unreachable where there is no hover" },
      { kind: "Changed", text: "THE MEDIA QUERY IS `hover: none`, DELIBERATELY NOT `pointer: coarse` — what decides the treatment is whether the RESCUE works, not how precise the finger is, and a stylus reports `hover: none` with `pointer: fine`. VERTICAL LISTS NOW WRAP RATHER THAN TRUNCATE: a column\u2019s items size independently and the rail is measured at runtime, so a wrapped label costs nothing structurally and hides nothing on any input. That is why \u201cnever wrap\u201d is a rule about ROWS, and why the documented 36 / 44 / 48 heights describe a single-line tab" },
      { kind: "Fixed", text: "MEASUREMENT MOVED FROM A `resize` LISTENER TO A `ResizeObserver`, on the list AND on every tab. Both things Tabs measures depend on the CONTAINER, not the viewport: whether a label is clipped is a function of the width it was given, and the indicator is placed from the selected tab\u2019s rect. A collapsing sidebar, a sibling growing, a panel opening or a webfont swapping in changes those without the window moving — and the symptom is silent, because a stale indicator still looks like an indicator. Caught in the browser: narrowing a container left the rail at 44px against a tab that had wrapped to 64px, and only a window resize corrected it" },
      { kind: "Fixed", text: "TOOLTIP CLOBBERED ITS TRIGGER\u2019S REF — a latent defect that would have broken Tabs outright. It clones the child to attach its own ref, and `cloneElement(child, { ref })` silently OVERWRITES whatever ref the consumer already put there; Tabs uses its per-tab refs to move focus and to place the indicator, so the tooltip would have killed keyboard navigation on exactly the tabs that needed a tooltip. Refs are now merged through a shared `mergeRefs` util. Note for React 19: the ref is `element.props.ref` and reading `element.ref` is REMOVED — doing so logs an error and costs you the whole tooltip, which is how the first attempt failed" },
      { kind: "Added", text: "`Tooltip` gains `duplicatesTriggerName` for the case where the bubble repeats the trigger\u2019s own accessible name. It drops `aria-describedby` and hides the bubble from assistive technology. Without it a truncated label is announced twice, which is worse than the visual problem the tooltip was added to solve — so it is opt-in and `Tabs` is currently the only caller" },
      { kind: "Added", text: "THE LONG-LABEL SOLUTION IS NOW DOCUMENTED WHERE THE SPEC LIVES — `docs/design-system/components/tabs.md` §3a, which had said only \"no label truncation\" in a list of open gaps. It now carries the six rules, the four-step escalation (shorten the label → `track=\"none\"` and scroll → the overflow menu → and only then the ellipsis), and the mechanics: the clipping is CSS-only so the accessible name is never truncated, a clipped tab gains a `title` set only where the text actually overflows, and `title` is named as the weak affordance it is because it does not work on touch. It also records WHY the behaviour exists — `flex: 1 1 0` plus `white-space: nowrap` meant a long label painted over its neighbour — and that Figma still defines no truncation treatment, so the visual remains an open design question" },
      { kind: "Fixed", text: "THE TABS FIGMA DOCUMENTATION PAGE WAS 25% BOUND AND SAID IT WAS 100%. Its own hero carried a stat reading \"0 unbound values — every fill, space and radius resolves to a token\" while the frame held 222 raw paddings, 92 raw gaps and 34 raw radii. That is the exact failure `.claude/rules/documentation-ds-linkage.md` was written after: documentation that drifts from the system it documents is worse than none, because people trust it. All three surfaces on the page — the documentation frame, the maintainers' component record, and the display chrome around the masters — are now 100% bound: 223/223 text on published styles, 281 fills, 298 paddings, 111 gaps, 168 radii, zero raw, zero unaccounted. The hero's claim is now true" },
      { kind: "Changed", text: "TWO VALUES WERE SNAPPED RATHER THAN GIVEN A TOKEN, because minting one per off-scale value is the palette inflation the linkage rule warns against: the hero's 88px top padding → 80 (`section/2xl`, which also resolves a contradiction between our own two house-style rules — one says 88, the other says 80), and twelve 6px stat gaps → 8. The only unbound values left on the page are the 5px corner radii on the three COMPONENT_SET frames, which are Figma's own variant-container chrome and never render in a product. Binding also exposed two real gaps in the semantic scales, left for a decision rather than filled: nothing is 28 (`padding/*` jumps 24 → 32, yet 28 is the house style's own panel padding) and `inline/*` has no 24 (it jumps 16 → 32), so both had to bind to `ref/*` primitives" },
      { kind: "Added", text: "THE TAB LABEL RULES — seven of them, written into `design.md`, the Storybook doc and all three Figma component descriptions, because a label is content and no amount of CSS fixes a badly written one. The load-bearing rule is that TWO TABS MUST NEVER TRUNCATE TO THE SAME VISIBLE STRING: \"Application details\" and \"Application status\" both become \"Application…\", and the row stops being navigation. Front-load the distinguishing word instead. The `LabelsThatCollide` story renders both halves at a pinned 420px — pinned deliberately, since whether a label truncates depends on the available width, and a story that relies on the reader's viewport demonstrates the failure on some screens and nothing at all on others" },
      { kind: "Added", text: "TRUNCATION IS NOW RECOVERABLE. A clipped tab gains a `title` carrying its full label, set only where the text actually overflows so every other tab is spared a redundant tooltip. The ellipsis stays CSS-only and never JavaScript: shortening the string in code would rewrite the accessible name too, turning a visual compromise into a real loss. Escalation order is documented — shorten the label, then move to `track=\"none\"` and let the row scroll, then add the overflow menu, and only then accept the ellipsis" },
      { kind: "Added", text: "`cmp/badge/dotSize` (6) AND `cmp/badge/dotSizeLg` (`size/8`) — the estate's status dot now has ONE definition instead of one per component. `.ds-badge__dot` and the new Tabs badge were independently hardcoding 6px, which is the same shape of drift as the code palette once hand-rolled in three places. 6 and not the on-grid 8 is deliberate: beside 14px label text an 8px dot reads as a bullet rather than a signal, and 6 is what the Figma masters render and what Material and Carbon both publish. Both variables were CREATED IN THE FIGMA LIBRARY in the same change, so this is not a code-only addition" },
      { kind: "Fixed", text: "A ROUTING BUG IN THE FIGMA EXPORTER, surfaced by those two tokens: `badge` is a COLOUR root, so a DIMENSION underneath it was filed into the Color collection purely because of whose namespace it sat in. That is the same class of mistake the exporter already carries targeted fixes for at `focus/width` and `icon/size` — 'route the measurement by what it IS, not by whose namespace it sits in', as its own comment puts it. Caught on the run that introduced it; the dots now sit in Space with a WIDTH_HEIGHT scope" },
      { kind: "Fixed", text: "THE ENCLOSED TRACK'S GAP WAS 4, NOT THE MASTER'S 2. Measured off the Figma container, whose three tabs sit at 0 / 104.67 / 209.33 in a 312 slot. It now binds `inline/2xs` — `inline` and not `stack`, because it is horizontal rhythm between siblings, and it stays 2 in the vertical track so the segments read as one control rather than as a stack of separate pills" },
      { kind: "Added", text: "CODE CONNECT TEMPLATES for the container (`tabs.figma.ts`) and the tab (`tab.figma.ts`). Two files because a template can only read a child's properties through that child's own template — which is how `Indicator` and `Size`, stored per tab in Figma, reach `<Tabs>` in code. `Tabs / More` is deliberately UNMAPPED: the overflow trigger has no React counterpart, a horizontal list simply scrolls, and mapping it to a guess would emit an API that does not exist. The Tab master is now MAPPED AND LIVE in Figma (all 36 variants). The container and `Tabs / More` could not be: `add_code_connect_map` rejects an unpublished component, and both were rebuilt today and have not been published since. Publish the library and they can be pushed" },
    ],
  },
  {
    version: "v0.22.0",
    date: "2026-08-17",
    changes: [
      { kind: "Removed", text: "THE `darpan` AND `aadhaar` AUTH MODES NEVER EXISTED. A full read of the MoSJE Portal Handoff — 69 auth screens across 10 of its 12 pages — found no NGO DARPAN screen and no Aadhaar e-KYC screen in any portal. Both were invented from a written brief before the design file was available. `PortalAuthMode` is now `password | otp | digilocker`, the two render blocks are gone, and `LoginSubmitPayload.credentials` no longer carries `darpanId` / `aadhaarNo`. The matching Figma axis was retired the same day: `PortalLoginTemplate` went from Device × Auth Method (5) = 10 variants to Device × Step = 8, updated IN PLACE so the component key and every instance link survived" },
      { kind: "Added", text: "AUTH PARTS — seven small components mirroring the `Auth / *` sets in the Figma library: AuthDivider, ConsentLine, ResendTimer, MaskedContactRow, SSOButton, AccountPrompt, SigningIntoBar. Nine portals were each hand-rolling the same fragments with different rules; the rules now live in one place. Two carry the ones most often got wrong: SSOButton must be hidden for the Officer audience (officers hold no DigiLocker account), and ResendTimer goes active IMMEDIATELY on an incorrect-OTP error rather than waiting out the cooldown" },
      { kind: "Added", text: "PASSWORDSTRENGTHMETER and CAPTCHAFIELD. The meter takes a zxcvbn score, is advisory rather than a gate, and is for passwords being CREATED — never beside one being entered, where it tells an attacker how close a guess is. CaptchaField ships with the warning attached: WCAG 2.2 SC 3.3.8 is Level AA, so a cognitive-function test with no alternative is a conformance failure, not hardening. One surface in the estate uses it" },
      { kind: "Added", text: "PORTALAUDIENCE — `citizen · officer · organisation`, one taxonomy for the estate. NMBA's 'Patient Monitoring', SMILE-Transgender's 'Garima Greh' and SCW's 'SAGE Organisation' are all `organisation`, renamed via the tab label. There were five bespoke taxonomies across nine portals and no way to write a rule that held in more than one of them" },
      { kind: "Added", text: "TOKENS: `layout/login/{hero.width 922, panel.width 518, panel.gutter 64, content.width 390}` and `text/neutral/subtler`. The login group had existed in Figma since 16 August and in code not at all — that drift is now closed, with both sides read back live before the parity record moved. `text/neutral/subtler` was first authored as `text/neutral/placeholder` and renamed in place: the naming grammar has no placeholder slot, and adding one to STATE would have let `bg/*/placeholder` parse too, so the token is named for its rung and its use is in the description" },
    ],
  },
  {
    version: "v0.21.0",
    date: "2026-08-17",
    changes: [
      { kind: "Added", text: "PORTALLOGINTEMPLATE — a login page described by a CONFIG OBJECT rather than assembled by hand. It renders the role tabs, the login-method selector and the right fields for each of the five auth modes (password, otp, digilocker, darpan, aadhaar), and returns one `LoginSubmitPayload` from `onSubmit`. It exists because those shapes kept being re-typed per portal. `PortalLoginShell` remains the right choice when a login is genuinely bespoke — forcing a one-off through a config object produces a worse page than composing it" },
      { kind: "Changed", text: "IT SHIPS MID-RESCOPE, DELIBERATELY, AND IS NOT YET ADOPTED — no page renders it; both E-Anudaan logins still use PortalLoginShell. `components/auth/LOGIN-SYSTEM-ANALYSIS.md` supersedes `FIGMA-SPEC.md` §9: the MoSJE Portal Handoff carries 69 auth screens across 10 pages, not the 25 the designer's own index frame counts. The component covers the original reading; the config shape is expected to grow, so it is marked Beta rather than Stable" },
      { kind: "Fixed", text: "SIX DEAD TOKEN NAMES IN THE NEW TEMPLATE, verified dead by reading the live page rather than by grep: `--sa-color-danger-base` and `--sa-text-neutral-default` both resolve to EMPTY in the browser. The semantic layer is `status/error`, not `danger`, so the `var(--token, fallback)` fallbacks were what actually painted — Tailwind #ef4444 on six required-field asterisks, #fef2f2/#fca5a5/#991b1b on the error banner, #bfdbfe/#eff6ff on the DigiLocker panel: off-brand and unreachable by the blue/navy brand switch. `--sa-text-neutral-default` appeared ~20 TIMES WITH NO FALLBACK AT ALL, so that text rendered with no colour" },
      { kind: "Changed", text: "THIS IS THE THIRD RECURRENCE IN THE SAME COMPONENT FAMILY — v0.18.1 records seven dead `--color-*` names, all 16 usages in PortalLoginShell. The ds-linkage gate cannot see the no-fallback form (there is no hex for it to flag), which is why it keeps coming back. A gate that fails on any `--sa-*` name absent from tokens.css would close it. Rebindings were made by RESOLVED VALUE and measured, not assumed: error ink on tint 10.52:1, asterisk on white 9.10:1, DigiLocker heading 11.41:1, body on tint 7.70:1 — all AA, with the banner taking the system's own measured `on/bg/status/error/subtler` pairing rather than a hand-picked ink" },
      { kind: "Fixed", text: "A HYDRATION FAILURE ON THE E-ANUDAAN NGO SIGN-IN, introduced by an earlier fix for a lint error: `useState(() => generateCaptcha())` runs Math.random() on the SERVER as well as the client, so the two rendered different captchas. The captcha is now generated on mount, client-only. One lint error had been traded for one runtime error; the console is clean on a fresh load" },
      { kind: "Fixed", text: "The captcha refresh icon was `size={18}`, off the 16/20/24/32/40/48/64 scale — now 20, confirmed rendering at 20px with `opsz 20` so the optical axis matches the box. A CSS class sets the box but never opsz, which is why the size prop is the only correct lever" },
    ],
  },
  {
    version: "v0.20.0",
    date: "2026-08-14",
    changes: [
      { kind: "Added", text: "AccessibilityBar is now a real code component, not just a Figma master. `@mosje/design-system` exports `AccessibilityBar` — the government top utility bar (UX4G / GIGW): the Government of India link plus skip-to-content, a working A−/A/A+ font-size stepper, the accessibility entry and the language selector. It mirrors the SAMAVESH Figma *Accessibility Bar* component, is fully tokenised (`--sa-*`, zero raw hex/px), and passes AA (white on the brand fill at 6.36:1). Storybook story, hub documentation page, and design.md entry ship with it" },
      { kind: "Added", text: "The font-size control drives a `--sa-font-scale` CSS variable (and a `data-sa-font-scale` attribute) on the document root, so content sized in `rem` reflows with the reader's choice; `onFontScaleChange` persists it. `layout` (narrow 720 / wide 1200 / fluid full-bleed) reproduces UX4G's per-breakpoint padding with one content-container mechanism" },
      { kind: "Changed", text: "SITEHEADER IS MIGRATED ONTO THE SHARED COMPONENT, and its own hand-rolled Tier-1 bar is deleted — the `.ds-hdr-util*` block is gone from header.css, leaving AccessibilityBar as the single implementation. PUBLIC-SITE BEHAVIOUR IS UNCHANGED: the masthead renders `fontSize={false}`, so the A−/A/A+ stepper is NOT surfaced there and the UX4G widget remains the estate's single mechanism for text size and contrast, per the accessibility-consolidation spec §3" },
      { kind: "Fixed", text: "THAT `fontSize={false}` HAD TO BE PASSED EXPLICITLY, because the prop defaults to true — so the migration silently turned the stepper ON at first. It is the second time this exact default has bitten: navbar.md v2.3.0 records setting Font size off across all 13 nested AccessibilityBar instances in the library because it had already 'silently reverted to the component default' there. A default that is wrong for the estate's only consumer of the component is worth revisiting" },
      { kind: "Changed", text: "COLOUR IS A MODE, NOT A PROP — the `tone` prop is removed from AccessibilityBar. SiteHeader now wraps the bar in `data-brand=\"navy\"`, which re-resolves bg/brand/primary/bolder to the navy ramp (#003366), the same value the retired prop hardcoded" },
      { kind: "Changed", text: "The bar's type is bound to `--sa-type-label-2-*` (12/16) from the Figma master's own variable defs, rather than the 14/20 the deleted utility bar hand-set. The two surfaces cited the same UX4G Navbar spec with different numbers; this takes the audited Figma value. Flagged for confirmation against the published spec" },
    ],
  },
  {
    version: "v0.19.0",
    date: "2026-08-13",
    changes: [
      { kind: "Fixed", text: "THE MASTHEAD NO LONGER MISALIGNS WITH THE PAGE. SiteHeader defaulted to a hardcoded maxWidth of 1320 while all 22 website section files capped at 1280, so above a 1320px viewport the header's content column ran 20px wider on each side than the content beneath it and the National Emblem did not line up. Verified fixed in the running hub: header and page both measure 1200 from x=150 at a 1500px viewport, and both measure 1320 from x=240 at 1800px" },
      { kind: "Changed", text: "THE ESTATE ADOPTS UX4G 3.0'S TWO-STEP CONTENT CONTAINER — 1200 at desktop, 1320 at desktop XL, per UX4G's 'Grid and layout' section. Four widths had been shipping at once: UX4G specified 1200, container/content said 1280 with no consumer anywhere, 22 files hardcoded that same 1280 as a literal beside it, SiteHeader defaulted to 1320, and PortalLoginShell's chrome used 1536. container/content is now 1200 and container/contentXl (1320) is new" },
      { kind: "Added", text: "ref/breakpoint/desktopXl (1768) — the anchor where the container widens. UX4G names the desktop-XL step but publishes no breakpoint for it, so this takes the estate's own desktop-XL design frame rather than inventing a number. It is NOT part of the type curve: clamp() still interpolates between mobile and desktop and tops out at 1280, so no glyph resizes" },
      { kind: "Added", text: "foundations/layout.css ships .sa-container — the centred content column, with the cap AND the responsive side margin (grid/margin 16/24/32) in one place. It replaced 22 hand-copied `mx-auto max-w-[1280px] px-4` strings. The one unavoidable literal is the 1768px media-query threshold, because a CSS media query cannot read a custom property; it is duplicated FROM ref/breakpoint/desktopXl and says so" },
      { kind: "Changed", text: "SiteHeader's maxWidth prop is now an override rather than a default. Left unset it binds --sa-container-page, the same variable the page content uses, which is what makes the two columns provably identical rather than coincidentally equal" },
      { kind: "Added", text: "PAGE-SKELETON TOKENS — layout/bar/height (46), layout/flag/width (33), layout/masthead/minHeight (72), layout/chrome/minHeight (118) and layout/sidebar/width (300), measured from the portal Handoff at 1440x1068. Only genuinely fixed measurements are tokens: the brand row, the page header and every card HUG their content, so they carry a minimum at most. A shell must never be sized by subtracting a chrome height from the viewport — AppShell is three grid rows, auto/auto/1fr, and layout/chrome/minHeight is for sticky offsets only" },
      { kind: "Added", text: "THE LAYOUT COMPONENTS SHIP — Container, Grid, GridItem, Band, PageHeader, AppShell and SiteLayout. Primitives compose the content column; templates compose the page. AppShell is a grid whose chrome rows are auto and whose body row is 1fr, so nothing subtracts a chrome height from the viewport; its `pending` prop replaces the `return null` that flashed a blank page while a portal hydrated, and below the tablet anchor its sidebar becomes a drawer rather than a narrowed column. All are presentational — no store, no router, no redirect" },
      { kind: "Added", text: "FOUR LAYOUT GRID STYLES — Grid/Mobile, Grid/Tablet, Grid/Desktop and Grid/Desktop XL, publishable in Figma so a designer applies one to a frame instead of typing column counts. Twelve tracks at every breakpoint: a child spans more of them on a phone rather than the count changing. design.md's claim of a 4-column mobile grid was stale and contradicted grid/columns, which has always been 12" },
      { kind: "Fixed", text: "A LAYOUT GRID STYLE IN THE LIBRARY WAS MISLABELLED. 'Container Fixed/940px' held 12 columns of 56 at a 24 gutter — which sums to 936, the inner width of a 960 container, and 940 is not a value in UX4G's ladder (540/720/960/1140/1320). The columns were right and the name was wrong, so it is renamed to 960px and its edge marker corrected from 916 to 936. 'Container Fixed/720px' had a marker describing a 752 container; also corrected. An exact duplicate differing only in 'Col' vs 'Cols' is deprecated by rename, and a 1200px style was added for the estate cap" },
      { kind: "Added", text: "container/2xl (1320) completes UX4G's container ladder — the one step that was missing, which is why contentXl had to restate the literal. contentXl now ALIASES it, in code and in the library, so the estate cap and UX4G's ladder cannot drift apart" },
      { kind: "Changed", text: "PortalLoginShell's chrome rows use the estate container instead of max-w-screen-2xl (1536), so the emblem lines up with the same column as every other page. That was the last of the four widths" },
      { kind: "Fixed", text: "The Figma exporter dropped the entire layout/* group — the tokens emitted to CSS but never reached the variables payload, so the library could only have carried them by hand. That is exactly how layout/bar/height and layout/flag/width came to exist in the published library with nothing in the build defining them. figma-variables.mjs now routes layout/* to Space alongside grid/* and target/*" },
    ],
  },
  {
    version: "v0.18.2",
    date: "2026-08-12",
    changes: [
      { kind: "Fixed", text: "ICON IS NOW DECORATIVE BY DEFAULT, which closes an accessibility gap at 533 of 718 call sites. A Material Symbols glyph is real text content, so an unmarked <Icon name=\"arrow_back\" /> is announced by a screen reader as the stray word \"arrow back\". The Iconography page has always stated the rule — every icon is either hidden from assistive technology or given a label, with no third option — but as an unenforced convention depending on 533 separate acts of memory, that is what it converged to. The component now decides: aria-label given means meaningful, so role=\"img\" and announced; otherwise aria-hidden. An explicit aria-hidden={false} still wins. Editing 533 call sites would have fixed today's instances and none of tomorrow's" },
      { kind: "Changed", text: "THE ICONOGRAPHY DOCS PAGE IS SYNCED WITH FIGMA — four sections against Figma's eight, so the two surfaces documenting one system said different things. It now mirrors Icons — Documentation section for section: 01 How it works, 02 The catalogue, 03 Sizes, 04 Weight, 05 Colour, 06 Bespoke marks, 07 Accessibility, 08 Handoff. The prose is the Figma prose; the numbers come from code" },
      { kind: "Added", text: "The 223-icon starter set is GENERATED from Figma section 02 into icon-catalogue.data.ts rather than hand-listed, so the web catalogue cannot drift from the sheet. On the web it filters and copies on click — Figma's own section states the problem it is solving (\"a text property cannot be browsed visually, so this sheet is the browser\") and a static grid of 223 tiles is a weak answer to it" },
      { kind: "Changed", text: "Section 05 documents all NINE --sa-icon-* colour roles, not the six Figma swatches. info, disabled and inverse ship in the system with no swatch; documentation that under-reports what ships is how a colour gets reinvented as a literal. The gap is recorded as Figma-side drift rather than quietly matched" },
      { kind: "Fixed", text: "SIX HAND-DRAWN DUPLICATES OF MATERIAL SYMBOLS WERE LIVING IN THE DESIGN SYSTEM'S OWN DOCS CHROME — the strictest case under the documentation-linkage rule, which requires documentation to be BUILT from the system it documents. terminal-code.tsx drew its own tick and clipboard, docs-header.tsx its own X, hamburger and magnifier, cmd-search.tsx another magnifier. All six are now the Icon component, and each landed ON the size scale (14 → 16, 18 → 20) rather than preserving an off-scale value" },
      { kind: "Added", text: "tools/icon-audit/check.mjs — a repeatable audit of whether the estate uses the system the page documents. It reads the size scale and the catalogue from their sources rather than holding copies, so it tracks them. Reports, does not gate: 213 of 762 call sites are off the seven-step scale, and moving them changes rendered size in live portals, which is a human decision. Full findings in docs/design-system/icon-audit.md" },
    ],
  },
  {
    version: "v0.18.1",
    date: "2026-08-12",
    changes: [
      { kind: "Removed", text: "THE TEN DEAD [data-theme=\"dark\"] RULES ARE GONE. The appearance axis was retired earlier — tokens.css emits no [data-theme] block and nothing anywhere sets the attribute — but ten hand-written companion rules outlived the token layer they depended on, in globals.css, design-system.css, hero.css and skeleton.css. They could never match, so they were not styling a dark mode; they were describing one that does not exist" },
      { kind: "Changed", text: "DELETED rather than rewired to the UX4G widget's .dark-mode, which was the other option. The accessibility consolidation spec (§3) is explicit: the widget owns citizen-facing accessibility dark, tokens own product brand, and the two must not be wired together without a deliberate decision and QA, because both applying at once double-darkens. Re-pointing these ten rules would have re-added a capability that was removed on purpose. The spec now carries a status note saying the caveat is moot, since the design-side half no longer exists" },
      { kind: "Fixed", text: "A stale nested apps/storybook/.storybook/.storybook/ was found and removed. Storybook loads .storybook/preview.tsx; this second copy was never loaded and had drifted — it still declared the retired Theme toolbar (light/dark/hc), still used the pre-rename brand ids Blue-Light and Blue-Dark, and was missing the icons.css import. It was the only place in the repo that still set data-theme, which is what made the dead rules look live" },
      { kind: "Fixed", text: "Two documentation claims corrected against the code. website.css said the hub's attribute-based dark variant still stands — it does not exist, and saying so invited someone to write a dark: utility that would silently never apply. The Accessibility page said .dark-mode is separate from the design system's data-theme token theming, which now reads as though data-theme is live; it names data-brand instead, and states that .dark-mode is the estate's only dark appearance" },
      { kind: "Changed", text: "Skeleton keeps its history without the dead rule. The comment explaining why the shimmer must never key off a brand id — [data-color-mode$=\"-dark\"] once caught blue-dark and ux4g-dark, which render on LIGHT surfaces — is worth more than the rule was, so it stays as a note for whoever reintroduces a dark surface" },
      { kind: "Fixed", text: "THE DESIGN-SYSTEM PACKAGE IS NOW GATED TOO — 472 unbound values to zero, and packages/design-system is no longer an advisory scope in tools/ds-linkage/config.json. The stylelint gate had covered raw hex in component CSS; it could not see inline style objects, var() fallbacks, or padding and gap literals, which is where nearly all of this lived" },
      { kind: "Fixed", text: "SEVEN LEGACY --color-* NAMES WERE DEFINED NOWHERE, so their hardcoded fallbacks were what actually painted: --color-navy #13366b, --color-saffron #ec6a1f, --color-line #e2e8f0, --color-ink-muted/#475569, --color-ink-hint #94a3b8, --color-surface-muted #f5f7fb. All 16 usages sat in PortalLoginShell — the login page every portal renders — so that screen was painted in Tailwind-default slate and an off-brand navy that no token and no brand switch could reach" },
      { kind: "Fixed", text: "FOUR WCAG AA FAILURES IN THAT SAME LOGIN SHELL, all found by measuring rather than looking. Hint text 2.56:1 (now 10.88:1). Bold saffron on the dark bar 4.93:1, and briefly 3.13:1 when it was first bound to the on-white step of the ramp — the correction to the light step measures 6.63:1 live. The SIGNING INTO label 3.53:1 (now 9.58:1). The Beta badge 3.56:1 (now the system's own bg/ink pair). Three text nodes also sat at 10px, under the 11px floor the type scale declares" },
      { kind: "Fixed", text: "231 var(--token, fallback) FALLBACKS DROPPED ACROSS 47 FILES, 128 of which disagreed with their token. Nearly all the type ones were stale px copies left behind when the scale went fluid: --sa-type-title-1-size fell back to 16px against a clamp() that resolves to 18px and up; --sa-shape-lg to 16px against 12px; --sa-shape-xs to 2px against 4px" },
      { kind: "Changed", text: "85 declarations snapped onto the published spacing scale. The library had been writing 3, 5, 6, 7, 10, 11, 13, 14 and 18px directly — off the 8px grid its own Spacing page teaches — in chips, tooltips, chart legends and metric cards. Ties round UP, so tap targets grow rather than shrink. Where a value had no step in its own family but an exact one in a sibling (padding 40 = stack/2xl, 48 = section/m, gap 20 = padding/l) the exact cross-family token wins over an inexact same-family one, so those pixels did not move at all" },
      { kind: "Fixed", text: "Every remaining scrim, shadow and glass edge in lightbox, side-sheet, skeleton, media inputs, data-table and the inverse Button now resolves through a token via color-mix or --sa-color-transparent-white-*, instead of a hand-written rgba" },
      { kind: "Added", text: "A GATE FOR THE DOCUMENTATION-LINKAGE RULE (tools/ds-linkage/check.mjs, wired into npm run check and the Design System Quality workflow). The rule said documentation must be BUILT from the design system; the only check was a grep in a markdown file, and the surface drifted back to raw values anyway. The grep was wrong in both directions — it flagged every prose mention of the 8px grid, and missed inline style objects, Tailwind arbitrary values, .css files entirely, and bare numerics like fontSize: 13, which React silently turns into px and no px-grep can ever see" },
      { kind: "Added", text: "code/* — the code and terminal specimen palette, 13 tokens. It did not exist, so the SAME six hexes had been hand-rolled independently in THREE places (home page, contributing page, playground), with two different terminal blacks (#12141c, #1e2130) that were never meant to differ. Brand- and theme-invariant on purpose: a terminal that flips to a light surface stops reading as a terminal. Every foreground role clears AA against code/bg, measured, with comment the floor at 5.09:1" },
      { kind: "Added", text: "TerminalCode gained CodeBlock and Syn.Comment / Syn.Keyword / Syn.Str / Syn.Builtin in docs-kit, so a page never colours a token of code by hand again" },
      { kind: "Fixed", text: "42 var(--token, fallback) FALLBACKS DROPPED, AND 15 OF THEM DISAGREED WITH THEIR TOKEN. --sa-shape-sm fell back to 4px against a real 6px; --sa-color-status-danger fell back to a bright #DC2626 against a real #8b1f18; --sa-shape-xl to 20px against 16px. A fallback is a second, stale copy of a value that no build ever checks. Treat it as a defect, not a safety net" },
      { kind: "Fixed", text: "TWO ACCESSIBILITY DEFECTS FOUND BY MEASURING RATHER THAN LOOKING. The terminal titlebar sat at rgba(255,255,255,0.4) = 4.1:1, below AA; it is now 0.45 = 4.52:1. A WCAG badge in the a11y checklist was set at 10px, under the 11px floor the Typography page itself declares; it is now label-3" },
      { kind: "Fixed", text: "The docs header height 56px was written out in SEVEN files (shell grid, two scroll-margins, three heading offsets, an IntersectionObserver rootMargin), so changing the header silently broke anchor scrolling in six of them. One declaration now, --docs-header-h, read from JS via getComputedStyle where CSS cannot reach" },
      { kind: "Fixed", text: "The roadmap's later column used --sa-color-text-muted as a BACKGROUND — a text role standing in for a surface role, which is why it had no on- companion and carried a hardcoded white. Each tone now carries its own ink: 11.67:1, 4.64:1 and 9.73:1. Binding it by NAME to on-bg-neutral-bold would have put dark ink on a dark chip at 1.48:1; the arithmetic caught it, the name did not" },
      { kind: "Fixed", text: "Stale documentation corrected against the code it describes: the contributing page still told people to run npm run dev:website / dev:smile / dev:pm-ajay / dev:docs on :3000 — none of which have existed since the single-origin consolidation — and a comment claimed the mono token is IBM Plex Mono, which the token's own description records as tried briefly and reverted. It is a system stack" },
      { kind: "Removed", text: "The last three vectors that duplicated a Material glyph are gone — external-link, language-switch and syllabus. They were held back because instances existed; by this point only two remained, both syllabus inside Tab components, and both were MIGRATED to the Icon component at auto_stories before the components were deleted. Nothing broke. Six bespoke marks remain on the page and NONE of them resolves to a Material ligature: Aadhaar, certificate, facebook, indian-flag, Indian Country Code, x" },
      { kind: "Changed", text: "Iconography page reorganised so every group is a PEER of the others. It had four different treatments — a bare component set with no label, a raw frame, and two sections — which is why it read as clutter. Now four numbered sections, one shell each: 1 The component, 2 Bespoke marks, 3 Emblems logos & misc marks, 4 Organisation logos, then the documentation. Same card, same padding, same heading-plus-note, so grouping is unambiguous rather than implied by proximity" },
      { kind: "Fixed", text: "THE ICON MASTER COMPONENT HAD SIX OF ITS SEVEN VARIANTS STACKED AT 0,0. Only Size=24 had a position, so the set rendered as one overlapping blob 264x24 — a 64px variant inside a 24px box. Caused by an earlier reorder loop that appended each variant without repositioning it. The variants now sit in a baseline-aligned row, 16 through 64, and the set is sized to contain them: the component reads as a size ladder, which is what a variant set is for" },
      { kind: "Fixed", text: "Emblems and org logos restructured into labelled cards — State emblems, Union Territory emblems, Government & programme marks, the org-logo set — each with a heading and a one-line note, each grid filling its card and wrapping wide instead of sitting in a 340px column inside a 1520px card. The `Sample Brand Logo` is now named as the PLACEHOLDER it is, so nobody reads it as an MoSJE mark" },
      { kind: "Fixed", text: "THE 223-ICON CATALOGUE WAS RENDERING AS 223 EMPTY SLIVERS. Every cell had collapsed to 10px tall with no icon and no label — resize() was called AFTER setting layoutSizingVertical to HUG, and resize resets sizing back to FIXED. The previous entry claimed the catalogue was rebuilt; structurally it was, visually it was blank. Rebuilt with width set before the sizing modes, which is the documented order" },
      { kind: "Fixed", text: "Iconography page reflowed to a single 1680px column with even gaps: Icon component, bespoke marks, emblems and logos, org logos, documentation. The bespoke frame was still a 3061px single row sized for the 89 marks it used to hold and now wraps at column width for the 9 that remain; a 17x30 orphan group left behind by the logos-page merge is gone" },
      { kind: "Added", text: "FIGMA: seven Icon TEXT STYLES — Icon/16 through Icon/64, Material Symbols Rounded Light, with fontSize and lineHeight BOUND to icon/size/*, so a change to the scale reaches every style. This was the right answer to `is a type style the correct way to implement font icons`: a glyph IS text, and a text node with no style is an unbound node. Applying them took the Icons documentation from 62% to 97.8% of text on a published style, turning 140 declared exemptions into real bindings. The component still carries the size variants and the icon property and remains the normal way to place an icon" },
      { kind: "Changed", text: "Reviewed all 228 icon components in the MoSJE Portal DS file. 155 resolve to a Material glyph and need no vector at all; only 7 are genuinely bespoke — Aadhaar, the Indian flag, the X logo, Facebook, Indian Country Code, State Public Service Commissions and a certificate placeholder. Crucially about 100 of them are `name/active` + `name/default` or `filled` + `outline` PAIRS: that is one icon on Material's FILL axis, not two icons, so roughly half the library is a duplicate of the other half" },
      { kind: "Changed", text: "The catalogue is now 223 icons — the estate's own set unioned with every Material icon the Portal DS calls for — each rendered live from the component. It is a starting point, not a limit: any Material name works" },
      { kind: "Changed", text: "FIGMA: one Iconography page. `Logos and Misc Icons` and `Org Logos` merged in as labelled sections — state and UT emblems, the org-logo set — and both pages deleted. The page now reads top to bottom: Icon component, bespoke marks, emblems and logos, org logos, documentation. figma.ts drops the now-dangling logosIcons id, which had no callers" },
      { kind: "Removed", text: "39 more bespoke vectors, on the evidence already sitting in the file: every mark carries an MI: annotation naming its Material counterpart, and where that name resolves to a real glyph the vector is a duplicate — gavel for Court Judgments, quiz for FAQ, home for the navigation home pair. 42 of 48 tested redundant; 39 had zero instances and are gone. external-link, language-switch and syllabus are superseded but STILL STANDING because instances of them exist — deleting a component breaks every instance, so each carries a migrate-then-delete note instead" },
      { kind: "Fixed", text: "USAGE COUNTS ARE OUT OF THE DOCUMENTATION. `358 uses`, `713 icon usages across 238 files` and the like are snapshots of the codebase, not properties of the system — stale the next time anyone writes an icon, with nothing to regenerate them. Each size now states what it is FOR, which does not rot. The same reasoning that moved the type scale and the icon scale out of hand-typed lists" },
      { kind: "Changed", text: "Success and danger filled buttons move to their bg bolder rungs, finishing what v0.18.0 started for primary: #004220 to #00542B, and #8B1F18 to #AA2F25. Note the direction — for these two the change LOWERS contrast, 11.67:1 to 9.12:1 and 9.10:1 to 6.68:1, where primary's raised it. Both stay far clear of the 4.5:1 floor. The trade is systemic consistency, not accessibility: a fill is painted from the bg slot in every family, or the rule is not a rule" },
      { kind: "Fixed", text: "The filled Button hardcoded its label colour to primary's ink — success and danger painted their text with a foreground measured for a different fill. All three resolve to #ffffff today, which is why nothing looked wrong and nothing caught it. Each variant now declares its own --_on, so the measured pairing cannot silently diverge if any of those inks ever moves" },
      { kind: "Changed", text: "Peer actions now read as peers. A primary, success and danger button sitting in one row previously drew from two different ladders — one fill slot and two ink slots — so success and danger were visibly darker than primary for no reason a reader could infer" },
    ],
  },
  {
    version: "v0.18.0",
    date: "2026-08-12",
    changes: [
      { kind: "Removed", text: "FIGMA: the `Icons — Material Symbols` page is gone. A hand-built v0.9 sheet — font spec, code usage and about 40 icons drawn as plain frames — with ZERO components and ZERO instances, and unreferenced from code. Every part of it is covered by the Icons documentation: the font spec by sections 04 and 05, the code usage by section 08, and its 40-icon sheet by a 117-icon catalogue rendered live from the component rather than redrawn" },
      { kind: "Fixed", text: "The Icons page is down to three nodes in reading order — the Icon component, `Bespoke marks — no Material Symbols equivalent` (52), and the documentation. Cleared out: 37 display frames still holding artwork for components deleted in the previous pass, an empty `Rounded` section, a stray vector `search` component Material already draws, a misnamed `Filled` section stranding the `ministry` mark, and one exact duplicate of `Department of Personnel & Training` — all with zero instances" },
      { kind: "Added", text: "tokens.ts now exports iconSize, READ OUT OF THE STYLESHEET rather than listed in the generator — so adding or removing a step needs no edit anywhere else. The iconography docs page renders the scale from it instead of hand-typed numbers, which is what let that page sit on 16/20/24 while the tokens had grown to seven steps. Same failure the typography data table had before it was generated" },
      { kind: "Fixed", text: "Iconography docs rebuilt against the real scale: all seven sizes, each tagged DBIM or Interface, with its token name, its live area where DBIM defines one, and what it is for. Zero hardcoded values left in the page's style objects. Figma section 01 still told designers to pick 16, 20 or 24 — corrected" },
      { kind: "Changed", text: "FILLED PRIMARY BUTTONS MOVED ONE RUNG DEEPER, from #0373DF to #005EB9. White-on-primary goes 4.64:1 to 6.36:1, and Navy 8.77:1 to 12.61:1. 36 solid fills across the estate plus the DS Button. The ladder already said this should be so: a bg rung sits one step deeper than the ink of the same family precisely because a fill carries white text while an ink sits on the page. The button had been reaching past its own system" },
      { kind: "Fixed", text: "The DS Button painted its filled background from a TEXT token. Its single --_color drove background, border AND outlined ink at once, so there was no way to move the fill without dragging outlined text with it. Split into --_fill and --_color: filled uses the bg slot, outlined and text keep the ink at 4.64:1 against the page, which is where that value is correct" },
      { kind: "Changed", text: "What did NOT move, and why. Success and danger keep ink as their fill — their bg bolder rungs are different values (#00542b, #aa2f25), so moving them is a separate design decision rather than a consequence of this one. Gradients and color-mix washes keep the lighter value deliberately: a wash is not a fill and does not carry text. Links, borders and icons keep #0373DF, measured against the page" },
      { kind: "Changed", text: "design.md sections B and C rewritten onto canonical token names. They still spelled every token in the --ds-* vocabulary retired earlier the same day — the values had been corrected but the names had not, which is its own kind of wrong in the file agents read first" },
    ],
  },
  {
    version: "v0.17.0",
    date: "2026-08-12",
    changes: [
      { kind: "Fixed", text: "ICON SIZES 16, 20 AND 40 ARE BACK. Earlier the same day the scale was narrowed to DBIM 3.4's four and those three deleted, reading section 3.7.i as exclusive. Withdrawn: 3.4 publishes a downloadable ASSET BANK in four sizes, it does not forbid a smaller inline glyph — and 16px beside 14px body text is 358 of 713 call sites because it is the right size there. Narrowing would have visibly enlarged icons in every dense table, button and form row to make a checklist go green. The scale is 16/20/24/32/40/48/64: DBIM's four, all present, plus the three steps interface work needs" },
      { kind: "Added", text: "A precedence rule, so this does not recur: current design-craft standards first, then DBIM, GIGW and UX4G fitted in wherever they do not hamper quality. When a standard specifies a set, ADD what is missing and never DELETE what quality needs — a standard's list is a floor, not a ceiling. Accessibility is the one thing never traded against, because accessibility IS quality. .claude/rules/standards-precedence.md, referenced from CLAUDE.md" },
      { kind: "Changed", text: "ICON SIZES ARE NOW DBIM'S, NOT OURS. DBIM 3.0 section 3.4 (Figure 9) defines exactly four icon sizes — 24, 32, 48, 64 — and section 3.7.i makes them mandatory. The scale held 16, 20, 24, 32, 40: three sizes DBIM does not sanction, and two of its four missing entirely. Now exactly the four, named for their pixel value (--sa-icon-size-24 …) rather than t-shirt letters, because a letter can quietly come to mean a different number — which is exactly how the retired --ds-text-title-* family shipped a name that resolved to another role" },
      { kind: "Changed", text: "Both sides were pushed and then read back independently rather than asserted: the built payload and the live Figma library each check to baa9e208:85. md and lg were renamed in place to 24 and 32 (same values, so ids and any bindings survived); 16, 20 and 40 were removed with evidence of zero consumers — the only occurrences of icon-size- in source were the five declarations in tokens.css itself, and Icon takes a raw size number that never read the scale" },
      { kind: "Fixed", text: "Deleting a variable is what strands a canvas node on a ghost, so the ghost sweep was re-run rather than assumed: all 68 pages, 46,710 nodes, 89,483 variable references, plus local styles and every variable-to-variable alias. The three removed ids are bound NOWHERE, and no new ghost appeared — 25 distinct local ghosts seen, every one already in the record" },
      { kind: "Added", text: "Figma: one Icon component replaces the icon library. A text property takes any Material Symbols name, so all ~3,000 icons are reachable from a single component; 44 vector duplicates of icons Material already draws were deleted, and the 54 bespoke MoSJE marks Material has no equivalent for (Aadhaar, SHe-Box, Swachh Bharat, indian-flag, the navigation/* pairs) stay as vectors. New 10-section Icons documentation page. OPEN: 656 of 713 call sites still render at off-DBIM sizes" },
      { kind: "Removed", text: "THE LEGACY --ds-* VOCABULARY IS GONE. All 341 tokens, retired in one pass. Nothing emits them: zero occurrences in tokens.css, tokens.ts, tailwind-preset.cjs, ux4g.css or the Figma payload, and zero references in source. 3,561 call sites across 162 files were moved to the canonical --sa-* token each legacy name already resolved to, so the migration was value-preserving BY CONSTRUCTION rather than by inspection — 0 of 192 distinct mappings change a rendered value" },
      { kind: "Changed", text: "The proof is the re-baselined ux4g contract fixture: 4,433 removals, every single one a --ds-* name, 208 additions (the new Tier-2 tokens), and ZERO changed values across all 13 selector contexts — every brand, every axis block. Brand swap, brand-invariance of secondary and accent, and the six DBIM previews were re-verified in the browser after the migration and all still behave" },
      { kind: "Added", text: "Three Tier-2 groups had to exist first, because 320 usages had no canonical home and were binding Tier-1 primitives by proxy: --sa-shape-* (corner radius, 12 steps), --sa-font-latin/display/mono alongside the existing devanagari, and --sa-stack-2xl (40px). It is called shape rather than radius because Style Dictionary merges the primitive and semantic namespaces, so a Tier-2 radius group self-references the Tier-1 scale it aliases — the build fails with 12 reference errors" },
      { kind: "Fixed", text: "About 40 references that RENDERED NOTHING, found because the migration had to resolve every name. --ds-space-2 through -12, --ds-line, --ds-border-control, --ds-leading-label-2 and --ds-ink-hint were never declared anywhere, so the declarations carrying them were dropped as invalid CSS: the markdown docs pages had no vertical rhythm and two form controls had no border. Nobody had noticed, because a dropped declaration looks like a design decision" },
      { kind: "Fixed", text: "The Figma library was telling designers, in Dev Mode, to use tokens that no longer exist — 73 font/role variables published var(--ds-type-*) as their codeSyntax. Repointed, and 16 new variables pushed with their GENERATED descriptions. Six of eight collections now agree with the payload byte-for-byte; Palette and Type differ by exactly the documented orphan set (24 x 2 modes and 13 x 6 modes), now recorded with the arithmetic so anything that is not a whole orphan reads as real drift" },
      { kind: "Removed", text: "Two test files deleted rather than repaired: tier2-parity and type-alias-parity existed solely to prove the canonical and legacy layers agreed, and there is no longer a second layer. legacy-snapshot.json goes with them. A test whose subject no longer exists is not coverage" },
      { kind: "Changed", text: "The --ds-text-* alias trap retires with the vocabulary that carried it: --ds-text-title-1 resolved to the headline-2 role, which caused four production bugs. The only surviving record of the mismatch is the TYPE_TOKEN table in build/generate-ts-mirror.mjs, which preserves it so the mirror's named exports keep their values" },
    ],
  },
  {
    version: "v0.16.2",
    date: "2026-08-11",
    changes: [
      { kind: "Fixed", text: "THE SHADOWS WERE TINTED TOWARD A COLOUR THE SYSTEM NO LONGER HAD. The ramp's own description says it keeps \"the SAMAVESH convention of tinting toward ink rather than UX4G's flat black\" — and it was not: five rungs plus the modal scrim carried rgba(31, 36, 40), hand-written from a neutral/800 the ramp rebuild moved to #1e2124. Derived from neutral/800 now, so it cannot drift again. Geometry stays authored; only the ink is generated" },
      { kind: "Changed", text: "Nobody could see it, which is exactly why it needed deriving rather than a one-time correction. Composited over white the old ink and the new differ by dE 0.14 to 0.54 across the ramp's alphas — under the ~1.0 just-noticeable threshold at every rung. A wrong value you can see gets fixed the first time somebody looks at it; a wrong value you cannot see survives every review and is still wrong" },
      { kind: "Fixed", text: "A REAL BUG THIS SURFACED, shipped in v0.16.0: text/disabled fell back to the blue-grey in all six DBIM modes while text/default correctly resolved to Deep Earthy Brown — enabled text brown, disabled text blue-grey, in modes whose whole claim is conformance. The DBIM preprocessor synthesises those modes by remapping token REFERENCES, so an rgba() literal was invisible to it and its hand-written override was deleted on the way past. text/disabled and the modal scrim now carry real brand ids and follow the ink in every brand" },
      { kind: "Changed", text: "Shadows stay BRAND-INVARIANT, and the reason is structural rather than taste. Blue and Navy composite to dE 0.00 — identical — so the two shipping brands gain nothing. DBIM differs by up to 1.63 at the xl rung, marginally over threshold; but a shadow is composite and elevation/* inlines its resolved value instead of aliasing it, so a per-brand override repaints --ds-shadow-* and leaves --sa-elevation-* behind. Two names for one shadow disagreeing by brand is worse to ship than a uniform sub-threshold difference. Revisit if elevation/* ever aliases" },
      { kind: "Fixed", text: "Two places assumed \"has any colorModes\" means \"varies by brand\": the Figma exporter's brandOwner and the round-trip gate's axis check. Figma's Palette models exactly one brand axis, [Blue, Navy], so a token whose only overrides are the code-only dbim-* modes has nothing brand-varying to say there. Giving the scrim DBIM inks promoted it out of single-mode Color into Palette as a NEW variable whose two modes were identical — a structural change to a shared library bought for a Blue-vs-Navy difference of exactly zero. Both now key on navy specifically" },
      { kind: "Changed", text: "Figma: the five elevation/* effect styles retinted and overlay/neutral/boldest updated, every offset, radius, spread, alpha and blend mode preserved. The six Shadows/shadow-* styles were deliberately left alone — they are the pre-existing set on flat #212121 that has never matched the token source and is annotated as such. Focus States/Neutral needed nothing: it is variable-bound to color/transparent/neutral/48 and had tracked the rebuild on its own, which is the argument for binding rather than copying, made by a style that was" },
    ],
  },
  {
    version: "v0.16.1",
    date: "2026-08-11",
    changes: [
      { kind: "Fixed", text: "THE DIVERGING CHART SCALE NOW FOLLOWS THE RAMPS instead of carrying copies of them. chart/div/* was seven hardcoded hexes, and they had already gone stale: negSoft and mid were copies of danger/100 and neutral/200 taken before those ramps were rebuilt, and the positive end was Material green, which this estate retired. They are references now — the same treatment chart/trend/* always had, and for the same reason: \"negative\" here means exactly what \"error\" means everywhere else, so a scale keeping its own red is a chart saying that bad news is a different kind of bad news" },
      { kind: "Changed", text: "The rungs were chosen for SYMMETRY, measured, not for nearest match. A diverging scale whose wings differ in lightness encodes one sign as louder than the other, and the old set was 15.8 L* asymmetric across its three matched pairs — negative reading heavier than positive. The new set is 2.1, with neg and pos landing on the same L* 64.4. div/neg does not move at all: #ec5042 turned out to be exactly danger/400, the ramp anchor, which is where the copy came from" },
      { kind: "Changed", text: "In Figma the same seven stopped being unlinked literals and became ALIASES onto the Palette collection — the identical defect the font-size variables had in v0.15.1, in a different corner of the library. A palette change now reaches the diverging scale there too, instead of drifting until somebody measures it" },
      { kind: "Fixed", text: "TWO CATEGORICAL SERIES WERE RETIRED BRAND COLOURS. cat/3 was Material green #2E7D32 sitting dE 3.2 from cat/9 — two \"mutually distinguishable\" series a reader cannot actually separate, which is the one defect a categorical palette exists to prevent. India Green #046A38 puts them 9.0 apart and lifts the set's worst pair from 3.2 to 5.8" },
      { kind: "Fixed", text: "cat/2 was the retired saffron #F97316 at 2.80:1 against the page — under the 3:1 WCAG 1.4.11 asks of a graphical object you need in order to read the chart, and the only one of the twelve below it. It is now secondaryScale/500 at 3.79:1, so the whole categorical set clears 3:1 for the first time. Bright India Saffron was tried and rejected by measurement at 2.91:1, and rung 600 was rejected too — 4.97:1 but only dE 3.3 from cat/6, trading a contrast failure for a collision" },
      { kind: "Changed", text: "cat/* and seq/* stay LITERAL on purpose and now say so in their own descriptions, because the next person to read them will otherwise \"fix\" them. A categorical palette is tuned as a SET, and a reference would let one member drift out of that tuning on a brand swap — point cat/1 at primaryScale and a Blue-to-Navy switch slides it toward cat/8, which is already a dark blue. A sequential ramp needs even PERCEPTUAL steps so equal data differences read as equal, which is a different constraint from primaryScale's contrast rungs" },
      { kind: "Fixed", text: "One limit recorded rather than papered over: div/mid follows the neutral ramp but does not re-tint between Blue and Navy, because color.neutral.* is the blue ramp and the navy override lives on each consuming token. Every other chart neutral — grid, axis, tooltipBg, regionEmpty, trend/flat — already behaved that way, so this is the chart layer's existing convention, and the two neutrals differ by ~9 degrees of hue at chroma 0.015, which is not visible on a gridline. It does follow a DBIM mode, where the whole neutral family is remapped" },
    ],
  },
  {
    version: "v0.16.0",
    date: "2026-08-11",
    changes: [
      { kind: "Added", text: "DBIM CONFORMANCE IS NOW SOMETHING YOU CAN SEE. All six of DBIM's published primary groups — Blue, Burgundy, Purple, Green, Chrome Yellow, Cinnamon Red — are selectable in the DemoDock's Colour tab, under their own heading with a DEMO ONLY tag and a plain statement of what they are. DBIM's rule is that an organisation picks exactly one group; MoSJE's is Blue, and the other five are there so the alternatives can be looked at instead of argued about" },
      { kind: "Added", text: "FULL conformance, not a repainted primary ramp — the difference matters enough that the UI says which claim it is making. Selecting a group also swaps all four status colours to DBIM's own (Liberty Green #198754, Mustard Yellow #FFC107, Coral Red #DC3545, DBIM Blue #0D6EFD), replaces the brand-tinted greys with DBIM's pure ones, and moves body text to Deep Earthy Brown #150202, which is not a neutral step at all" },
      { kind: "Fixed", text: "WHAT THE PREVIEW FOUND, which is the reason to build it: DBIM's own palette does not always meet DBIM's own rule 4, \"colour usage must ensure accessibility of digital platform\". The Green group's shade 2 (#2D8686) lands on the filled rung that carries white text at 4.32:1 — below AA. On Cinnamon Red the brand primary is 2 degrees and dE 0.9 from the error status, which is to say indistinguishable from it. On Chrome Yellow the primary sits 10 degrees from the warning status. All three are recorded with their measurements rather than corrected — a conformance palette that has been quietly fixed demonstrates nothing" },
      { kind: "Changed", text: "The hue-separation ledger is now scoped per brand (dbim-cinnamon-red::error|primary, not error|primary). An unscoped entry silences a pair in EVERY brand, so recording a DBIM finding the old way would have hidden a genuine C-02 regression in the shipping Blue and Navy brands behind it" },
      { kind: "Fixed", text: "The same gate's brand-discovery pattern excluded hyphens, so all six dbim-* brands were invisible to it — a gate whose own comment is about exactly this failure mode was reporting green while three quarters of the brand axis went unchecked. A discovery pattern that silently matches nothing is worse than a hardcoded list, because a hardcoded list is at least obviously incomplete" },
      { kind: "Changed", text: "The shape rule does NOT apply to the DBIM ramps, and the exemption is written down rather than left as a silent difference. Reproducing DBIM's exact hexes and holding a 4-16 L* ladder are mutually exclusive: a search over every assignment of five published shades to eleven rungs found no configuration satisfying both for five of the six groups. A transcription is exempt by construction; the accessibility gates still bind" },
      { kind: "Changed", text: "The single dbim brand added earlier today is renamed dbim-blue now that the other five groups exist and a bare dbim would be the odd id out. Both the old CSS selector and a persisted cookie keep working, via the alias mechanisms the estate already had for blue-dark and ux4g-light" },
      { kind: "Added", text: "DBIM's palette lives in the PRIMITIVE layer, not a brand pack — DBIM's colours are DBIM's whichever brand this estate wears, and a re-skin does not get its own Burgundy. It also means brands/_starter still resolves every DBIM mode instead of failing the build on 75 missing references" },
    ],
  },
  {
    version: "v0.15.3",
    date: "2026-08-11",
    changes: [
      { kind: "Fixed", text: "THE LAST VALUE DIFFERENCE WITH FIGMA IS CLOSED, and it was closed in the EXPORTER rather than the library. ref/font/weight/* projected CSS numbers (400/500/600/700) into a payload whose live counterparts are STRING font-STYLE names (Regular/Medium/SemiBold/Bold). Figma has no numeric weight at all — a text style selects a cut by fontName.style — so the library was already right and the PAYLOAD was the wrong side. All eight collections are now byte-identical to the library on values" },
      { kind: "Changed", text: "This one could not have been fixed by pushing, which is why it sat as a recorded exception. A variable resolvedType is fixed at creation, so a FLOAT 700 cannot be written into a STRING variable — and forcing it would have broken every text style bound to the name. The fix had to be on the code side or nowhere" },
      { kind: "Added", text: "figmaFontStyle maps the full 100-900 ladder, not just the four weights in use, so adding light or black to the source cannot silently reintroduce a FLOAT. An unmapped value falls back to its own string rather than a guess: a visible 350 in the picker is easier to diagnose than a plausible-but-absent Book. Same shape of projection as primaryFontFamily, one axis over" },
      { kind: "Fixed", text: "A bug in the value-parity gate itself, found immediately by using it: the stale-exemption ratchet read the $-prefixed metadata key inside knownDifference as a collection name, and since neither side has a checksum for it the two compared equal and it reported ITSELF stale. It now skips $-prefixed keys, which is the convention the rest of the snapshot file already uses. Re-verified that it still catches a genuinely stale exemption" },
      { kind: "Changed", text: "Nothing renders differently. CSS still emits --sa-ref-font-weight-* as 400/500/600/700; only the Figma payload changed, and only in the type it declares for four variables" },
    ],
  },
  {
    version: "v0.15.1",
    date: "2026-08-11",
    changes: [
      { kind: "Fixed", text: "THE LIBRARY DISAGREED WITH THE CODE ON 80 VALUES, all under the correct names, which is why nothing had noticed. 13 component tokens were bound to the WRONG palette rung: cmp/action/destructive/primary/* still on danger 700/800/900 after the code moved to 600/700/800, and cmp/action/brand/tonal/* still on primary 100/200/300 — stale since v0.13.0. ref/font/family/mono still held the webfont the code had deliberately reverted. And 54 fluid-type variables carried the OLD tablet curve (body/1/size at 15 where the code says 14.89; body/1/para at 16 where it says 13.77). All pushed" },
      { kind: "Changed", text: "12 font size/lineHeight variables were unlinked LITERALS in Figma where the payload declares aliases into Space — same numbers, but a change to the Space step would not have followed. Now aliased. Every pair was verified value-identical first, so nothing renders differently" },
      { kind: "Added", text: "A VALUE parity gate (test/figma-value-parity.test.mjs), because every existing check compared NAMES. reference/figma-live.json lists names, figma-roundtrip checks names, and the per-collection checksums were over names — all of it blind to a token bound to the wrong rung. One checksum per collection over name|mode|value now fails the build when a value moves without the library record being refreshed. Mutation-tested: changing one hex digit fails it, naming the collection and both checksums" },
      { kind: "Fixed", text: "NOT pushed, and correctly so: ref/font/weight/* is the only value difference left. Figma holds these as STRING font-STYLE names (Regular/Medium/SemiBold/Bold) because that is what a text style binds; the payload declares FLOAT 400/500/600/700 because that is what CSS needs. resolvedType cannot change after creation, so the number cannot be pushed — and pushing it would break every text style bound to the string. Recorded as a knownDifference with a ratchet that fails if it ever stops being a real difference" },
    ],
  },
  {
    version: "v0.15.0",
    date: "2026-08-11",
    changes: [
      { kind: "Fixed", text: "THE SYSTEM HAS NO WCAG AA SHORTFALL LEFT, in any brand. The last two were bg/status/error/bolder at 4.40:1 and bg/status/warning/bolder at 4.46:1 — the fills that carry white text on a destructive button and a warning chip. Both now measure 6.68:1 and 5.68:1. The known-below-AA list that pinned them is empty for the first time" },
      { kind: "Changed", text: "Both closed the same way, and it is the rule this system keeps re-learning: AN ANCHOR BELONGS AT THE RUNG ITS LIGHTNESS SAYS, not at 500 by convention. #ec5042 is L* 64 and the warning amber is L* 76. Forced to 500, the rung below them lands inside the dead zone — roughly L* 59-66, where a fill is too dark for dark ink and too light for white and NEITHER ink reaches 4.5:1, so that rung cannot be made accessible at all. Anchored at 400 and 300 the ramps clear it comfortably" },
      { kind: "Changed", text: "dangerScale, warningScale, infoScale and neutralScale are now GENERATED from anchors like every other ramp. They were the four the 2026-08-11 rebuild did not reach, and they still carried what the audit measured: danger/400 and danger/500 were 1.8 L* apart (one colour wearing two names), warning/500 was darker AND duller than 400 (which is what made it read muddy), info/400 and /500 were another near-duplicate pair. All eight ramps now step 4-16 L* apart, monotonic, hue held within ~6 degrees" },
      { kind: "Changed", text: "The warning ramp's anchor MOVED IN HUE, which no other ramp's did, because that ramp disagreed with itself: steps 50-200 sat at hue 75 and steps 300-950 at hue 65. A ramp cannot have two hues. 75.9 wins because it is the hue of the rungs people see most, because it is what amber means rather than orange, and decisively because 65.9 is only 25 degrees from India Saffron — under the hue-separation gate's 30-degree floor. Locking the dark end's hue would have traded one defect for a harder one" },
      { kind: "Changed", text: "THE GREYS ARE DELIBERATELY TINTED, which they always were — the defect was that nobody had chosen the tint. neutral/400 measured hue 256, neutral/500 hue 245 and neutral/950 hue 264, with chroma DROPPING between 400 and 500 while lightness fell. Nobody picked those numbers; independent 8-bit rounding of a nearly-grey colour did. Hue is now locked to the brand's own primary for the whole ramp, with chroma on one arc peaking ~0.016 in the mid-tones. Material 3, Radix and Tailwind's slate all tint their neutrals the same way" },
      { kind: "Changed", text: "VISIBLE CHANGE, expect it: the neutral ladder was re-cut. It used to put four steps inside its lightest 7.7 L* and then cross the middle in two jumps of 15+, which is why there was exactly one grey between a light surface and a mid grey, and why components kept reaching for a one-off hex the system had no name for. Borders and subtle surfaces are now genuinely visible — --sa-border-neutral-subtle moves from #f1f3f5 to #dcdee1, --sa-border-neutral-base from #e2e6ea to #c6c9cd" },
      { kind: "Changed", text: "0 and 1000 stay EXACTLY #ffffff and #000000. They are achromatic, which is the whole reason they live on the neutral ramp and on no chromatic one, and tinting them would contradict that. The arc reaches zero at both ends so the steps beside them are true greys too" },
      { kind: "Removed", text: "The destructive button's step override. It shifted the filled progression up to 700/800/900 for one reason — white on dangerScale.600 was 4.40:1 — and that reason is gone. Destructive now uses the same 600/700/800 table as every other intent. A workaround kept past its cause is just an inconsistency nobody can explain" },
      { kind: "Fixed", text: "The ux4g and ux4gdeep demo brands overrode the four status FOREGROUNDS to UX4G's palette but left the TONAL backgrounds on SAMAVESH's ramps, so UX4G's teal #006d75 sat on SAMAVESH's blue infoScale/100 — a mismatched pair that met AA by luck and stopped the moment the info ramp moved. Both halves now come from the same UX4G ramp" },
      { kind: "Fixed", text: "NOT fixed, and worth recording because the old note claimed otherwise: error|secondary stays on the separation ledger. It said the remedy was a deeper, redder danger ramp. When that ramp was actually regenerated the claim was tested by sweeping the whole parameter space, and the best separation reachable at the subtler rung was dE 11.3 against a threshold of 12 — and only by starting the ramp at L* 90, which makes an error banner a saturated pink. At the pale end of two adjacent hues there is no ramp shape that pulls them apart, because nearly-white is one colour. A real fix has to change what one of the two families IS" },
    ],
  },
  {
    version: "v0.14.2",
    date: "2026-08-11",
    changes: [
      { kind: "Added", text: "Descriptions on the six Focus States/* effect styles, which had none — surfaced by the coverage check added with the elevation work. Each now says which intent it belongs to, and that reaching for a ring whose colour disagrees with its control makes the control read as a different component. Effect-style description coverage is now 18/18, matching the 909/909 the variables already had" },
      { kind: "Fixed", text: "Recorded a geometry divergence worth knowing before someone 'fixes' it: each focus style is a SINGLE FLUSH 4px spread, while the build renders a 2px ring held 2px off the control (--sa-focus-width 2px, --sa-focus-offset 2px). The 4px total footprint agrees; the transparent gap does not exist in Figma. That is a limitation, not drift — a drop shadow cannot leave a transparent gap without painting the backdrop, and faking it means painting the inner 2px in the page colour, which is right on the default surface and wrong on every other" },
      { kind: "Changed", text: "Unlike the Shadows/* styles, these needed no correction: every one already BINDS its colour to color/transparent/<family>/48, so they follow the brand and cannot rot into literals. Worth stating plainly, because the previous two releases found the opposite everywhere they looked" },
      { kind: "Added", text: "The focus record is gated alongside the elevation parity check — the six styles and the variable each binds to must stay recorded, and the caveat must keep its reason. A caveat that loses its reason reads as a bug, and the next person corrects it into a two-layer style that only works on one background" },
    ],
  },
  {
    version: "v0.14.1",
    date: "2026-08-11",
    changes: [
      { kind: "Added", text: "ELEVATION REACHED FIGMA — as effect STYLES, not variables. A shadow is a composite value and Figma variables hold only COLOR/FLOAT/STRING/BOOLEAN, so shadow.* and elevation.* could never be variables; an effect style is the only Figma primitive that fits. elevation/{flat,card,raised,dropdown,modal,toast} are generated from shadow.{none,xs,sm,md,lg,xl} exactly, including the two-layer shadows and their negative spreads" },
      { kind: "Fixed", text: "A CORRECTION to the previous entry, which said designers had no shadow tokens at all. They did: six Shadows/shadow-* effect styles already existed in the library, unknown to the code — which is worse than none, because nothing was checking them. NOT ONE of the six matches the token source. All six use flat #212121 where the tokens use the tinted rgb(31,36,40), and shadow-s and shadow-md also differ in GEOMETRY (0/1/3/+1 plus 0/4/4/0 against the token's 0/4/6/-1 plus 0/2/4/-2; 0/2/8/-2 plus 0/6/8/-2 against 0/8/12/-3 plus 0/3/5/-2), so a design using either does not match what ships" },
      { kind: "Changed", text: "The six pre-existing styles were LEFT AS THEY ARE, not corrected. This is a published library and they are applied in consumer files that cannot be enumerated from inside it, so changing their geometry would restyle other people's work unannounced. Each now carries a description naming the elevation/* style that supersedes it and the exact divergence, so a designer choosing between the two can see which is authoritative. Shadows/shadow-2xl is marked ORPHAN — no token stands behind it and the code ramp stops at shadow.xl" },
      { kind: "Added", text: "A drift gate (test/elevation-parity.test.mjs), because effect styles are invisible to every existing check — the payload, the checksums and the round-trip test all see variables only, which is exactly how six mismatched shadow styles sat in the library unnoticed. It asserts the ELEVATION map matches semantic.json, that the recorded library state still matches what build/shadow.mjs produces from primitive.json, and that the pre-existing divergence stays recorded until it is resolved. Mutation-tested: changing shadow.md's blur by 2px fails it with both values named" },
      { kind: "Added", text: "build/shadow.mjs — one CSS-box-shadow parser shared by the sync that WRITES the styles and the test that CHECKS them. Two parsers would eventually disagree, which is the same defect class as the stale codeSyntax. It splits layers on commas outside brackets (so rgba() survives), treats spread as optional, and throws on a layer with no colour rather than defaulting one" },
    ],
  },
  {
    version: "v0.14.0",
    date: "2026-08-11",
    changes: [
      { kind: "Added", text: "THE LAYOUT GRID, which did not exist. The only grid in the system was chart/grid — a chart GRIDLINE COLOUR. grid/columns (12), grid/gutter (24px) and grid/margin/{mobile,tablet,desktop} (16/24/32px) are now first-class tokens, so a designer can bind a Figma layout grid and a developer can read the same numbers from CSS" },
      { kind: "Changed", text: "The grid's GEOMETRY is UX4G 3.0/Bootstrap exactly — 12 columns, 1.5rem/24px gutter — because UX4G is the Government of India's own system and this repo holds a parity contract with it. A designer moving between the two libraries must not find a different grid. What is NOT UX4G's is the shape: UX4G expresses its grid as Bootstrap CSS classes, which is exactly why none of it ever reached Figma. These are tokens instead, on IBM Carbon's model" },
      { kind: "Changed", text: "Page margin is RESPONSIVE (16/24/32px), not Bootstrap's flat 12px half-gutter. 12px either side of a government page on a 1280px desktop is too thin; Carbon uses 32px and GOV.UK 30px. Every value aliases an existing space step — no new literals" },
      { kind: "Added", text: "A tap-target LADDER, because 44px is not the only truth and is not even the AA floor. 44 is WCAG 2.5.5 Target Size (Enhanced), Level AAA. The AA floor is WCAG 2.2 SC 2.5.8 at 24x24 CSS px, carrying a spacing exception. And GIGW 3.0 binds this estate to WCAG 2.1 AA + IS 17802 — WCAG 2.1 AA has NO target-size criterion at all, because 2.5.8 arrived in 2.2 and 2.5.5 is AAA. So target/min (24, AA), target/comfortable (44, AAA + Apple 44pt), target/spacious (48, Material 48dp) and target/spacing (8, Material separation) each name their authority, and every one is a deliberate choice ABOVE the mandate" },
      { kind: "Changed", text: "Targets alias the rem-based size scale, so CSS emits 1.5/2.75/3/0.5rem and Figma receives 24/44/48/8px. rem is deliberate, not incidental: a citizen who raises their browser font size gets proportionally larger targets, which a hardcoded px floor would deny them" },
      { kind: "Fixed", text: "NOT changed, and the reasons are worth recording. breakpoint/* was NOT extended to UX4G's six — those three are the fluid TYPE ramp's clamp anchors (the generator throws without mobile/desktop), not layout media queries, so layout breakpoints are a separate axis and a separate decision. container/2xl (1320px) was not added because the estate's content width is 1280px and a wider container would contradict it. Shadow/elevation still has no Figma representation and cannot as a variable — Figma holds only COLOR/FLOAT/STRING/BOOLEAN, and a shadow is composite; it needs effect STYLES" },
    ],
  },
  {
    version: "v0.13.2",
    date: "2026-08-11",
    changes: [
      { kind: "Fixed", text: "DEV MODE WAS LYING. 61 Figma variables published a codeSyntax naming a CSS custom property that does not exist — a developer who opened Dev Mode, copied var(--sa-bg-brand-primary-strong) and pasted it got a declaration that silently does nothing. The prominence ladder was renamed to base/subtler/subtle/bold/bolder/boldest and codeSyntax was never re-pushed, so every rung name in it (default/soft/emphasis/strong/stronger) plus the old ink rungs (text-neutral-primary/secondary) went stale; one, --color-border-neutral-inverse, had the wrong prefix entirely. Verified by resolving all 91 semantic entries against dist/tokens.css: 62 dead" },
      { kind: "Added", text: "codeSyntax on every code-owned variable — 487 had none at all, so Space, Type, Radius, Motion, Density and Static showed a developer nothing but the Figma name. All 863 now carry the exporter's own emitted name, proven rule-derivable against the payload (863/863, 0 mismatches) before a single write" },
      { kind: "Fixed", text: "The 18 accent variables added earlier carried ALL_SCOPES, because the push that created them never set scopes — so a brand fill offered itself in the corner-radius and font-size pickers. They now mirror their primary/secondary siblings (FRAME_FILL+SHAPE_FILL for fills, SHAPE_FILL+TEXT_FILL for foregrounds, ALL_FILLS+STROKE_COLOR+EFFECT_COLOR for the alpha washes). The 17 that remain ALL_SCOPES are z-index, breakpoints and motion, which Figma has no property to scope to" },
      { kind: "Fixed", text: "Twelve descriptions. Five ref/border/width/* shipped BLANK — primitivePointer had no `border` case, and a missing case returns null which the exporter writes as an empty string. The seven legacy cmp/button|card tokens described a Light/Dark mode structure the Color collection has not had since the theme axis was removed. Description coverage is now 900/900" },
      { kind: "Added", text: "A gate: no variable may ship to Figma with an empty description. It guards the sync as much as the docs — descriptions are pushed from the payload, so an empty string does not merely omit guidance, it ERASES whatever the library already had" },
    ],
  },
  {
    version: "v0.13.1",
    date: "2026-08-11",
    changes: [
      { kind: "Added", text: "v0.13.0 reached the Figma library. 36 variables created: the accentScale ramp (11 steps), its transparent washes (6), the 950 step on all seven chromatic ramps, and the accent semantics bg/brand/accent/* (6) + on/bg/brand/accent/* (6). The library now carries 899 variables across 8 collections" },
      { kind: "Added", text: "Set equality between the build payload and the live library is now PROVEN per collection by checksum rather than inferred from counts. Six of the eight collections — including Color, all 472 of it — hash identically; Palette and Type differ only by the 36 orphans now itemised in reference/figma-live.json under $orphans. Counting had been hiding two-way drift: the previous snapshot recorded a Component Options collection that no longer exists, and every earlier check looked only for what was MISSING, never for what the library carried that the source does not define" },
      { kind: "Fixed", text: "The on/* coverage gate asserted a floor (found.length >= 40) and therefore passed while six bg/brand/accent/* fills had no foreground at all — a brand colour wired as far as the palette and stopped. It now derives the expected set from the fills themselves, so a fill that carries content and has no measured ink fails by name. Mutation-tested by deleting a pairing" },
      { kind: "Added", text: "The six accent foregrounds, each chosen BY MEASUREMENT across both brands rather than assumed from the rung name. Accent flips to white ink at bolder, and its bold rung measures 4.60:1 with dark ink — margins that differ enough from primary that inheriting primary's flip point would have shipped an unreadable pairing" },
      { kind: "Fixed", text: "The neutral ramp's own $description still advertised a 0-1100 range and --sa-color-neutralScale-0…1100 after the 2026-08-11 renumbering retired the 1100 step. Stale prose in the token source, describing a token that no longer exists" },
      { kind: "Changed", text: "color/neutralScale/1100 no longer exists under that NAME — it was renamed, not deleted, and no binding was detached. An earlier entry recorded this as an unaccounted hard delete with unrecoverable bindings; that was a fair reading of a missing name but it is wrong. The renumbering was applied as a rename CHAIN (1000 -> 950 first to free the name, then 1100 -> 1000), which is the only safe way to renumber in a published library: renaming preserves the variable id, so every binding follows. Verified against ids captured before the write — VariableID:3791:8969 (was 1000) is now 950, VariableID:3791:8970 (was 1100) is now 1000, both re-read by id afterwards and both resolve. A missing name does not imply a missing variable; only an id lookup can tell the two apart" },
    ],
  },
  {
    version: "v0.13.0",
    date: "2026-08-11",
    changes: [
      { kind: "Fixed", text: "THE COLOUR COLLISION (audit C-02). In the Navy brand, bg/brand/secondary/bold (#66c99b) and bg/status/success/bold (#81c784) measured 0.3 L* and 16 degrees apart — a contrast ratio between them of 1.00:1, so a secondary-action chip and a saved-state chip were the same object on screen. The brand axis was rotating secondary's hue about 100 degrees into territory the success semantic already owned. Secondary and accent are now brand-INVARIANT: only primary changes with data-brand" },
      { kind: "Added", text: "A hue-separation gate (test/hue-separation.test.mjs) — every colour-family pair, at every matched rung, in every brand. A pair passes on hue OR perceptual distance, because hue alone is both too weak (Blue and Navy are 9 degrees apart and told apart by lightness) and too strong (red and orange are adjacent and can never separate in hue). Verified by reintroducing C-02 and watching it fail" },
      { kind: "Added", text: "The two SAMAVESH logo colours as first-class brand ramps: secondary = India Saffron #FF671F, accent = India Green #046A38. Both read from the logo, identical in the Figma handoff file and Assets/SAMAVESH Logo.svg. Both brand-invariant, because a logo colour is a constant of the identity, not a variant of it" },
      { kind: "Changed", text: "The Navy brand's key colour is now the DBIM colour #162F6A, replacing #003366. The DBIM Compliance Audit fails the estate on this twice — checkpoint 3 (icons) and checkpoint 5.6 (footer must be the key colour's darkest shade). The two colours measure deltaE 1.9 apart, so the brand does not visibly move; it just becomes compliant" },
      { kind: "Changed", text: "Success is now the same green as the accent. Two greens nine degrees apart is a defect whichever token owns them, and a citizen seeing the ministry's own green on a success state is better brand than the leftover Material green it replaced" },
      { kind: "Added", text: "Ramps are GENERATED, not hand-picked (build/ramp.mjs + build/brand-ramps.mjs). Only the anchors are authored; the other ten steps are derived to even L*, held hue and a single chroma arc. The Navy ramp used to fall 27.4 L* in one step and then crush four rungs into fifteen points — nobody chose that, it is what happens when ten values are picked one at a time. It is now 8.6-11.0 throughout" },
      { kind: "Changed", text: "Anchors sit at the rung their LIGHTNESS says, not at 500 by convention. #162F6A is L* 32 (a shade) and sits at 600 — the rung that paints the primary button, so the brand's most prominent surface IS the DBIM colour. #FF671F is L* 70 and sits at 400: forced to 500 it put rung 600 at L* 62.6, inside the dead zone where a fill is too dark for dark ink and too light for white and NEITHER reaches 4.5:1" },
      { kind: "Fixed", text: "bg/brand/secondary/bolder went from 3.94:1 to 4.97:1 and left the known-below-AA ledger; bg/status/success/bold now clears 3:1. Both fell out of the ramp rebuild rather than being chased" },
      { kind: "Added", text: "Step 950 on every chromatic ramp — 11 steps, matching UX4G 3.0, which we were one short of. 950 is the near-black shade a footer or a boldest fill wants without falling back to pure black" },
      { kind: "Changed", text: "The neutral endpoints renumbered to match UX4G: 1000 -> 950 and 1100 -> 1000, so pure white is 0 and pure black is 1000. Pure white and black are achromatic and therefore belong to the neutral ramp ONLY — a 'pure black red' is just black. Both renames were proven value-preserving in every selector context before being baselined" },
      { kind: "Fixed", text: "The alpha overlay tiers are now DERIVED from the ramps. They were 42 hand-written rgba() literals and had rotted exactly as you would expect — still carrying the retired saffron, the retired Material green and the retired navy after the rebuild. Nothing caught it, because a literal has no reference to break. An accent tier was added and secondary is brand-invariant like its ramp" },
      { kind: "Fixed", text: "accentScale exported only 6 of its 11 steps to Figma — it was missing from RAMP_FOLDER, so it reached the Palette collection only at the rungs the prominence ladder happens to alias. A designer would have found 11 steps of every other ramp and 6 of that one" },
      { kind: "Changed", text: "gov- dropped from every colour name: gov-blue/gov-blue-dark/gov-blue-tonal/gov-navy/gov-yellow became primary/primary-dark/primary-tonal/navy/yellow, across 330 call sites in 78 files. gov-blue and primary were already two names for one value, so that pair was a merge rather than a remap. A colour is named for what it DOES, not for who owns the system" },
      { kind: "Changed", text: "The legacy Tier-2 allowlist went 150 -> 35, by NAMING RULES rather than widening exemptions: the palette-scale shape (color/<x>Scale/<step>, 73 entries) and the alpha-tier shape (color/transparent/<family>/<pct>, 42) now parse natively. Of the 35 left, 10 are deliberately-retained source for the revivable dark/hc axis and the other 25 are internal plumbing no app code references" },
      { kind: "Added", text: "brand/accent as a fourth brand variant in the grammar, and the whole bg/text/border/icon accent family it generates. accent names a specific identity colour rather than a rank, which is why it joined the ordinal words primary/secondary/tertiary instead of becoming a fourth ordinal" },
      { kind: "Fixed", text: "brand/tonal shifted to rungs 50/100/200 for the brand intent only. The Navy ramp runs darker by design, which made primaryScale.300 a MID tone there rather than a tint: navy 900 on navy 300 measured 4.33:1. The other three intents are brand-invariant and keep their headroom" },
    ],
  },
  {
    version: "v0.12.3",
    date: "2026-08-10",
    changes: [
      { kind: "Added", text: "icon/size/* — five steps, md=24px being the default <Icon> already ships with. Every component needed an icon size and none had a token to bind, so each hardcoded its own and they drifted" },
      { kind: "Added", text: "focus/width and focus/offset. The focus ring's COLOUR was tokenised long ago and its geometry never was, which left WCAG 2.4.7's most-regulated affordance two-thirds hardcoded. All three parts are now tokens" },
      { kind: "Added", text: "container/* (5), including the 1280px content max-width that CLAUDE.md mandates estate-wide and that existed only as a literal in CSS — a rule no build could read is a rule that drifts" },
      { kind: "Added", text: "elevation/* — six semantic levels over the raw shadow ramp, so a card versus a dropdown versus a modal is chosen by WHAT THE SURFACE IS rather than by how deep the shadow looks. CSS only: Figma models shadows as effect styles, not variables, and a test now asserts that exclusion stays explained rather than becoming a silent gap" },
      { kind: "Added", text: "motion/{enter,exit,emphasis} — each pairing a duration with the easing that belongs to it. Entering decelerates and may take its time; leaving accelerates and gets out of the way. Binding a bare duration loses the pairing, which is the actual decision, so a test asserts each intent keeps both halves" },
      { kind: "Added", text: "control/radius and control/border/width. Density already moved a control's SIZE while its SHAPE stayed hardcoded per component — one radius is what keeps buttons, inputs and selects reading as a family" },
      { kind: "Fixed", text: "Figma routing is now TYPE-AWARE. focus, icon and control each own both a colour and a measurement — focus/ring is a colour, focus/width is a number — so routing by the path root alone put five FLOATs in the colour collection. Route the measurement by what it IS, not by whose namespace it sits in" },
    ],
  },
  {
    version: "v0.12.2",
    date: "2026-08-10",
    changes: [
      { kind: "Added", text: "Usage guidance on every semantic token — what it is FOR, alongside what it is WORTH. Descriptions carried a measured contrast ratio and at best a two-word label (“Hovered rows, quiet panels”), which is rigorous and nearly useless when choosing between two neighbouring tokens. UX4G's Figma library does the opposite (“Use when the tonal button's action is not available”): worse evidence, much better guidance. They were never alternatives, and both halves now ship. 417 variables had no description at all; 7 still do, and each of those is deliberate" },
      { kind: "Changed", text: "The vocabulary lives in ONE module derived from the token path, not 400 hand-written strings. Guidance written per-token drifts — the same rung ends up described three ways in three namespaces and nobody notices, because no two descriptions ever sit next to each other. One module means a whole rung can be reworded in a single edit, and a new token cannot ship with nothing to go on" },
      { kind: "Fixed", text: "The Tier-2 generator had been UNRUNNABLE. The ordinal-ladder rename updated its OUTPUT by hand and left five put() calls on retired rung names; the generator validates every path and exits before writing, so it failed silently every time. src/system.generated.json said “GENERATED — do not edit” while in fact being hand-maintained, and the only symptom was a message nobody ran. Both generators are now gated by a test that runs them and diffs the result against the committed file" },
      { kind: "Fixed", text: "Two prose bugs caught before they reached 800 variables — “the outline of a the primary brand colour control”, and borders described as “decorative fills” by wording that belongs to the fill ladder. Also fixed the contrast sentence for a boundary at the canonical rung, which read as if a divider were a fill" },
      { kind: "Changed", text: "The Figma push computed guidance in-plugin rather than shipping 108KB of text, then diffed itself against the build — which caught it dropping the measured-contrast half of every description, because the live text was still in the older “Guarantees ≥” format the reconstruction could not find. Those measurements were re-applied. A residual 1–4 characters per token of prose drift remains on ~330 variables; the build is authoritative and a faithful re-push is queued. The verification is the point: a copy that is diffed is a copy that cannot drift silently" },
    ],
  },
  {
    version: "v0.12.1",
    date: "2026-08-10",
    changes: [
      { kind: "Changed", text: "Type primitives are VALUE-NAMED, adopted from UX4G. --sa-ref-font-size-400 told you nothing; --sa-ref-font-size-16 cannot be misread. It is UX4G's convention and Tailwind's and Spectrum's, and it was the one naming decision where they were plainly better than us. Renamed in place in Figma, so every binding followed" },
      { kind: "Added", text: "size/* — a general numeric scale we simply did not have, carrying UX4G's 20 steps at their exact rem values plus 22 and 44 as a SAMAVESH superset our own type scale needs. Font sizes now ALIAS it, exactly as UX4G's fs-16 points at size-16, so a px value has one definition instead of one per namespace. That relationship is gated: a font step carrying its own literal fails the build" },
      { kind: "Added", text: "breakpoint/* — and this one matters less as a token than as a fix. 360, 768 and 1280 were restated as literals in TWO build files, so the estate had three copies of its own viewport anchors and no way to notice a drift. The fluid type curve and Figma's Type modes now both read the token, and a test fails if a literal comes back — a token that merely duplicates a constant is worse than none, because it is a second place to be wrong" },
      { kind: "Added", text: "blur/* (8) — UX4G's ramp and its four semantic names, verbatim. We had none" },
      { kind: "Changed", text: "NOT adopted: UX4G's 116 utility-value tokens. --ux4g-object-fit-cover: cover is a CSS keyword with a variable wrapped round it, not a design decision. Copying them would have moved our count past theirs while buying nothing a stylesheet does not already do — worth naming so the 925-vs-755 comparison is not misread as breadth" },
      { kind: "Fixed", text: "The raw type steps moved from px to rem as a side effect of aliasing the size scale. That is a UNIT change, not a rename, and is recorded as one rather than laundered through the rename ledger — which asserts old and new resolve identically, and 16px to 1rem does not. It is safe and an improvement: nothing renders from those tokens, and their one consumer is the UX4G parity layer, where --ux4g-line-height-16 IS 1rem in UX4G's own contract. All 755 conformance names still emit" },
    ],
  },
  {
    version: "v0.12.0",
    date: "2026-08-10",
    changes: [
      { kind: "Added", text: "The four scales the architecture promised and never built. border/width (5) and opacity (14) carry UX4G's exact values, bound by name so they cannot drift. The z ladder (8) is UX4G's verbatim — Bootstrap's numbers, which UX4G inherited and which third-party CSS in the estate already assumes, so diverging would put our modals under someone else's. layer/* (8) is Carbon's nestable-surface model, so a card inside a card inside a page resolves by depth instead of by eye. Until now all four were listed in the spec as advantages over UX4G while three of them were things UX4G had and we did not" },
      { kind: "Added", text: "on/* — 40 guaranteed foregrounds, one per fill that carries content, so nobody has to guess the label colour on a status chip. Every pairing was chosen BY MEASUREMENT: the ink that clears AA on that fill in the WORST brand, because a pairing is only as accessible as its least accessible brand. Three of the forty cannot reach AA — no ink rescues a surface that close to its own background — and they are the SAME three tokens as the prominence shortfall ledger, reached by an independent measurement. The test asserts that agreement, so if the two ledgers ever diverge the build fails" },
      { kind: "Added", text: "on-pair-contrast.test.mjs — spec §9.3, written together with the namespace it checks. A gate for a namespace nobody built is worse than no gate, because it reads as coverage" },
      { kind: "Removed", text: "The dead fixed 5-role type scale. It shadowed the fluid 21-role scale under a friendlier name (--sa-type-display-size beside --sa-type-display-1-size), aliased the raw size steps so it could never respond to surface or breakpoint, and had zero consumers anywhere — verified by exact-match grep including the generated sheet. In Figma it was RENAMED to deprecated/* rather than deleted: this is a published library and a binding in a consuming file cannot be ruled out from inside it. Type went 111 to 96" },
      { kind: "Changed", text: "Density is an axis again — 1 variable to 8. It now moves control padding, control gap, row height and row padding rather than a single control height, which is what a compact mode has to change for a data-dense portal table" },
      { kind: "Added", text: "A Static collection in Figma for the unitless scales. This is the one place spec §8.4's tier-per-collection design was actually buildable, and only because those tokens are new — Figma forbids MOVING a variable between collections, but nothing is bound to these yet, so creating them in the right home costs nothing" },
      { kind: "Fixed", text: "267 variables scoped, so a colour is no longer offered for corner radius and a duration no longer appears in the font-size picker. ref/z/* is hidden from publishing — it has no canvas property to bind to and was created in the same pass, so it is provably unbound. Blanket-hiding ref/* was deliberately NOT done: ref/color/ink/dark alone carries 1,143 bindings, and un-publishing a bound variable strands it in every file that uses it" },
      { kind: "Fixed", text: "A race that had been making the test suite non-deterministic. brand-contrast rebuilds dist/ under a different brand pack to prove a re-skin passes contrast, and node --test parallelises across files — so other tests were reading dist/tokens.css while it held the wrong brand's output. Two non-reproducing failures were traced to this. The suite now runs serially, and the reason is written where the rebuild happens" },
    ],
  },
  {
    version: "v0.11.13",
    date: "2026-08-10",
    changes: [
      { kind: "Fixed", text: "The entire component tier ignored the brand axis. All 296 --sa-cmp-* shipped as frozen hexes, so --sa-cmp-action-brand-primary-default-bg was #025fb8 under Blue and #025fb8 under Navy — the primary button never changed brand. The CSS format handed var() chains only to system.generated.json, and Tier 3 fell through to the resolved literal. The source was never at fault: Tier 3 is 196 references plus 92 deliberate literals (white-alpha inverse variants and transparent fills, which are correctly brand-invariant). 101 component tokens now repaint under Navy, up from zero" },
      { kind: "Fixed", text: "Re-assertion inside an axis block is now TRANSITIVE, and it had to be. An alias re-declared in a block becomes a changed source for anything pointing at it, so one pass only reaches depth 1. That was invisible while Tier 3 emitted literals; with the chains restored, cmp → bg/brand/primary/bolder → color/primaryScale/600 is three deep, and a single pass would have re-asserted the middle link while leaving the component token resolving against :root — brand-blind in exactly the way the fix exists to remove" },
      { kind: "Fixed", text: "Code and Figma had silently disagreed about this layer. Figma held the same tokens as ALIASES, where 85 of them did repaint under Navy, against zero in code — a divergence in the layer that describes buttons, which nothing detected because figma-roundtrip validates the payload against itself. 16 live variables were rebound onto the brand-bearing Palette; both sides now report 101 repainting and 195 brand-invariant, exactly" },
      { kind: "Fixed", text: "The gate that missed all of this. action-contrast.test.mjs resolved only :root — it checked the Blue brand and called that coverage. That was harmless while Navy rendered identically (which was itself the bug), but it would have left all 101 newly-live Navy values unverified. It now runs the full 48-combination matrix per brand; Navy passes AA" },
      { kind: "Added", text: "Two gates so this cannot return: no --sa-cmp-* whose SOURCE is a reference may be emitted as a literal (compared against the source, so the 92 legitimate literals are not flagged), and the component tier must measurably repaint between brands — a chain that bottoms out in something brand-invariant would pass the first check and fail the second" },
      { kind: "Changed", text: "Nothing that renders today changes. :root is byte-identical; every value that moved is inside [data-brand=navy], and the one component token anything currently consumes — --sa-cmp-badge-beta-bg, the yellow accent — is correctly unchanged. The layer is now correct for whenever it is adopted" },
    ],
  },
  {
    version: "v0.11.12",
    date: "2026-08-10",
    changes: [
      { kind: "Fixed", text: "Every page in the estate was loading at 110% zoom with letter-spacing and line-height bumped, and three accessibility features showing as switched on that nobody had switched on. The cause was our own workaround for a null dereference in the UX4G v1.15 widget: it seeded the widget's settings key BEFORE the script ran, which pushed loadSettings() down its restore branch instead of the no-op path a fresh visitor should take — and that branch ends in updateWidgetToggles(), which calls the widget's own CLICK handlers, each of which advances a counter unconditionally. Seeding a neutral object was therefore indistinguishable from clicking three controls once per page load. The squeezed National Emblem in the header was the same bug, not a broken asset" },
      { kind: "Changed", text: "The UX4G accessibility widget is upgraded from accessibility-beta-v1.15 to accessibility-v3.28 — the build ux4g.gov.in itself serves. Both defects behind the above are fixed upstream: detectRouteChange() null-checks its read and settings moved to a cookie, so the seeding workaround is deleted rather than corrected. UX4G document the click-handler bug in v3.28's own source. New capabilities come with it: disability profiles, reading guides and masks, saturation controls, and a Ctrl+F2 shortcut" },
      { kind: "Changed", text: "The brand skin covers v3.x's palette. v1.15 exposed a single --color-dark-blue-1 hook; v3.28 hardcodes ~13 literal violets across roughly 25 declarations that the variable never reaches, so overriding it alone left the panel half brand-blue and half violet — the Reset button, view toggle, profile cards and submit button all stayed purple. Those now resolve to action-primary / -hover / -tonal. Rules are prefixed with #uw-main for SPECIFICITY, not scoping: the widget injects its stylesheet after ours and marks several of these !important, so at equal specificity source order would win" },
      { kind: "Fixed", text: "The reskin's font rule was matching a class the upgrade renamed. v3.x namespaced every class it ships (the trigger became .ux4g-accessibility-uw-widget-custom-trigger) but left element ids alone, so the rule is keyed on #uw-widget-custom-trigger — the hook that actually survived the upgrade" },
      { kind: "Added", text: "An analytics prop on UX4GAccessibilityWidget, defaulting to OFF. v3.28 beacons the full URL, pathname, referrer, user agent, language, screen resolution and a session id to audit360.ux4g.gov.in on load, then tracks panel opens and feature toggles — telemetry v1.15 had none of. On authenticated portals a full URL can carry application and beneficiary identifiers, so it is off unless a property opts in. There is no documented opt-out, but ANALYTICS_CONFIG is exposed by reference as window.UX4G_Analytics.config and the send path re-checks it, so flipping it suppresses every event including the initial load beacon" },
      { kind: "Fixed", text: "Two icons stay violet and are documented rather than patched: the Accessibility Profile square and the language icon carry fill='#613AF5' inside an SVG data: URI, which no CSS colour property can reach. Re-emitting the URI would hardcode a brand hex where no token can follow it, and this estate is multi-brand — so the residue is the better trade, and it is written down in the stylesheet" },
      { kind: "Fixed", text: "The keyboard shortcut is platform-correct on macOS. v3.28 hardcodes Ctrl+F2 into the trigger and binds exactly that, which on a Mac is wrong twice: Ctrl+F2 is a RESERVED macOS shortcut (move focus to the menu bar) that the OS consumes before the page sees it, and F2 is a media key unless the user has enabled standard function keys. A shortcut advertised on the button and then not working is worse than none — especially on an accessibility control, whose users are the keyboard users. Macs now get ⌘⌥A, with the trigger relabelled and the shortcut appended to its aria-label, since the aria-label overrides the visible text and would otherwise announce nothing. Windows and Linux are untouched and keep Ctrl+F2" },
      { kind: "Changed", text: "The Mac binding is a BRIDGE, not a reimplementation: it dispatches the synthetic Ctrl+F2 the widget already listens for, so open/close/focus stays vendor behaviour. ⌘⌥A was chosen specifically to avoid ⌃⌥, which is VoiceOver's modifier — binding that on an accessibility widget would collide with the screen reader its users are most likely running. Cmd also suppresses Option's character substitution, so it cannot type a stray å. Verified: ⌃⌥A, ⌘A, ⌘⌥⇧A and ⌘⌥S are all correctly ignored" },
      { kind: "Fixed", text: "The key match reads e.code OR e.key, not e.code alone. e.code is the correct layout-independent signal on real hardware — Option rewrites e.key — but it is not guaranteed: the first version silently dropped every press because the observed event arrived as {key: 'a', code: '', meta: true, alt: true}. Synthetic events, remote-input paths and some assistive tools deliver an empty code, which is precisely the population this shortcut exists for" },
    ],
  },
  {
    version: "v0.11.11",
    date: "2026-08-10",
    changes: [
      { kind: "Changed", text: "The prominence ladder is renamed and now actually ORDERS. UX4G's base / soft / subtle / emphasis / strong / stronger did not: subtle sat louder than soft while reading quieter, and base was the quietest of all while reading like the default. A ladder whose names do not sort is not a ladder, it is six adjectives. It is now base / subtler / subtle / bold / bolder / boldest for fills — Atlassian's shipped pattern rather than a private scale" },
      { kind: "Fixed", text: "The primary/secondary/tertiary overload is finally dissolved. Those words were ink rungs AND brand variants at once, separated only by the parser's greedy order — the mechanism that put “body and heading text” on sixteen backgrounds. Ink now uses the same ordinal words as fills, so the three are variants and nothing else and the collision cannot be spelled. The slot-disjointness guard is what forced the stale entries out of its own pinned list rather than leaving an exemption behind" },
      { kind: "Changed", text: "The contrast contract is now keyed per LADDER, not per word — and sharing one vocabulary is what forced it. subtle on a fill is a quiet tonal chip that need only be distinguishable (≥3:1, WCAG 1.4.11); subtle on ink is a caption, which is still text and still owes AA (≥4.5:1, 1.4.3). A single flat table could only ever have been right about one of them. A test fails if the two ladders ever stop differing, at which point the split has stopped earning its keep" },
      { kind: "Changed", text: "base is the CANONICAL value, not a loudness, so it sits at a different rung on each ladder — the ordinary fill is the quietest thing on the page, the ordinary ink is mid-way between a caption and a max-contrast heading. There is deliberately no subtlest: base already occupies that rung for fills, and adding it would mean two names for one value" },
      { kind: "Changed", text: "Figma collection names dropped their tier-number prefixes — Palette, Color, Space, Type, Radius, Motion, Density. The numbering came from a spec written when tier was going to BE the collection split; tier now lives in the variable path (ref/ … cmp/), so the prefix was redundant, and six of the seven read “2 ·” anyway, which conveys nothing" },
      { kind: "Fixed", text: "Nothing renders differently. 34 names moved and every one resolves byte-identically — dist/tokens.css is a 230-line diff with 230 insertions and 230 deletions, no additions and no removals, pinned by visual-contract.test.mjs" },
    ],
  },
  {
    version: "v0.11.10",
    date: "2026-08-10",
    changes: [
      { kind: "Changed", text: "The Figma library is canonical end to end. All 691 variables were renamed in place so a variable's NAME is its token path — bg/neutral/subtle, ref/space/md, cmp/action/brand/primary/hover/bg — and the collections are tier-ordered: 1 · Palette, 2 · Color, 2 · Space, 2 · Type, 2 · Radius, 2 · Motion, 2 · Density. 435 of 669 names previously differed from the path they came from, and 179 carried a hyphen inside a segment, which is the exact defect RULE 1 exists to remove" },
      { kind: "Changed", text: "RENAME ONLY, never recreate — and that was a finding, not a preference. Figma refuses to move a variable between collections (variableCollectionId is get-only; probed rather than assumed), so the six-collection tier split in spec §8.4 would have meant deleting and recreating ~500 variables. This is a PUBLISHED library whose consumers are other files, and a plugin running inside it cannot see their bindings — so a delete could not be shown to be safe, only hoped to be. Renaming preserves the variable id, so every binding in every consuming file followed automatically. Tier now lives in the NAME (ref/ … cmp/), which Figma's picker navigates exactly like a collection" },
      { kind: "Added", text: "40 tokens got a Figma home for the first time: the 38 data-visualisation tokens and the two Devanagari type tokens. The Devanagari pair had been unreachable because font-family/devanagari collided with the Tier-1 family of the same name — canonicalisation gave them distinct paths and the collision simply stopped existing" },
      { kind: "Fixed", text: "Source paths were canonicalised BEFORE projecting to Figma, so the rename would not bake in our own naming debt: spacing/* became space/* (the grammar's own group dictionary says space), color/chart/* moved to chart/* (spec §11.4 decided this and it had never been done), and the brand ramps' light/dark keys became blue/navy — they are two palettes, not two appearances, which is the whole reason §4.2 renamed the axis" },
      { kind: "Fixed", text: "The grammar allowlist is itemised instead of rooted. It was four ROOTS (color, type, spacing, density), and the check skipped every path beginning with one — so it exempted 188 existing tokens AND every token anyone might write under those roots in future. A brand-new ungrammatical color/… landed green, and the freeze criterion could never be met. It is now 150 explicit paths with two ratchet tests: a stale entry fails, and an entry that now parses fails. Three of the four roots turned out not to need exempting at all" },
      { kind: "Fixed", text: "Two repo pointers sent syncs to the wrong Figma file. Both .claude/commands/sync-figma.md and the sync handoff doc named older SAMAVESH copies that still contain variables — so a run against either looks successful and reaches nobody. Both now name the canonical file from GOVERNANCE.md, with the variable count to check against" },
      { kind: "Changed", text: "Renaming type/title1 to type/title/1 was attempted and REVERTED: it flattens onto a name the fluid font/role scale already owns, silently merging a fixed 22px semantic role with a clamp(). The visual-contract test caught it, and the two namespaces now stay spelled differently on purpose, with the reason written down where the next person will look" },
    ],
  },
  {
    version: "v0.11.9",
    date: "2026-08-11",
    changes: [
      { kind: "Changed", text: "DemoDock's FAB moves from bottom-left to bottom-right, docked directly above the UX4G accessibility widget's own trigger — one coordinated utility rail (same right edge, a fixed gap) instead of two FABs in unrelated corners at different sizes and offsets. The gap above the widget is measured live off its real geometry rather than hardcoded, so an upstream resize of the widget can't silently reopen an overlap" },
      { kind: "Removed", text: "The per-registry-entry boolean that raised the FAB above PortalLoginShell's \"Signing Into\" strip on NMBA's two login routes is gone, with no replacement flag — moving the FAB off bottom-left eliminates that collision at the source, so every route now renders the FAB in the identical position, nothing to opt into and nothing to forget" },
      { kind: "Fixed", text: "The FAB no longer visibly relocates between routes (bottom-left at rest vs. raised on NMBA's login pages) — it now holds one fixed position, bottom-right, on every page" },
    ],
  },
  {
    version: "v0.11.8",
    date: "2026-08-11",
    changes: [
      { kind: "Changed", text: "DemoDock's Colour tab: the plain swatch row plus live Button/Badge/Alert preview block is replaced by a wrapping grid of fixed-size (~72×48px) motif tiles — a miniature header bar, content surface, accent mark and button shape per mode. Each tile renders in that mode's own palette via a nested data-brand island on the tile itself, so a Navy tile looks navy even while the app is in Blue, with no hardcoded hex" },
      { kind: "Removed", text: "The live Button/Badge/Alert preview block under the Colour tab's swatch list is gone. The motif tiles are the preview now, and — being fixed-size regardless of mode count — give the tab a height that no longer grows with a live component block or a longer mode list" },
      { kind: "Changed", text: "Selected state keeps the tick plus a visible ring (still not colour alone — WCAG 1.4.1); touch targets remain ≥44px (AAA, WCAG 2.5.5)" },
    ],
  },
  {
    version: "v0.11.7",
    date: "2026-08-10",
    changes: [
      { kind: "Changed", text: "DemoDock redesign: real visual hierarchy in the header (a badge mark, a tonal wash separating it from the body), a token-driven open/close animation and a swatch-selection animation, both collapsing to instant under prefers-reduced-motion. The footer disclaimer row (\"Demo tooling — not part of the product\") is gone — the dock is unambiguous demo chrome by context, and the row was pure noise" },
      { kind: "Fixed", text: "Switching to the Colour tab used to collapse the panel dramatically, because that tab had almost no content and nothing gave the body a floor. The body now carries a min-height calibrated to the Apps tab's typical unscrolled footprint, so no tab switch visibly resizes the panel" },
      { kind: "Removed", text: "ColorModeSwitcher is gone from the design system entirely — deleted from foundations/, the public barrel, and Storybook. The Colour tab is now a plain row of brand-palette swatches reading useColorMode() directly: no \"Colour mode\" label, no pill-track background, just swatches that apply on click. An app that still wants a standalone brand-mode control builds one from useColorMode() the same way" },
      { kind: "Changed", text: "The Sign in tab now renders — and leads the tab order — only when the current path IS a login route (isLoginRoute: ends in /login, /login-otp or /sign-in), not merely somewhere under a portal that has one. Previously it appeared on every page under a portal's prefix, including dashboards with no login form to fill. Apps and Colour keep their relative order behind it" },
    ],
  },
  {
    version: "v0.11.6",
    date: "2026-08-10",
    changes: [
      { kind: "Added", text: "DemoDock — the estate's single floating demo console (Apps / Colour / Sign in tabs behind one FAB), replacing three widgets that used to compete for the bottom-left corner: the AppSwitcher FAB, its hand-rolled colour swatches, and DemoFab mounted separately on 8 login pages. Mounted exactly once, by the hub root layout via ConditionalDemoDock, and gated estate-wide by NEXT_PUBLIC_DEMO_TOOLS — absent or anything but the literal string \"false\" keeps it visible, so the deployed review site (the one place stakeholders actually need it) shows it by default" },
      { kind: "Removed", text: "AppSwitcher is gone — no longer exported. The colour-mode swatches it carried were a hand-rolled duplicate of ColorModeSwitcher; they're deleted outright rather than ported, and DemoDock's Colour tab renders the real ColorModeSwitcher instead. ColorModeSwitcher also came out of SMILE Admin's access bar for the same reason: one colour control, not two" },
      { kind: "Added", text: "AppSwitcherPanel and DemoAccountsPanel, extracted as the reusable content behind DemoDock's Apps and Sign in tabs. AppSwitcherPanel owns the search box and the grouped destination list with no position:fixed and no scroll box of its own — whatever mounts it owns the one scrollbar. DemoAccountsPanel is now the single credentials table shared by DemoDock and DemoFab, so the two can no longer drift apart the way the old per-page copies did" },
      { kind: "Changed", text: "Demo credentials moved out of 8 per-page consts into one pathname-keyed registry, DEMO_ACCOUNTS in packages/design-system/demo/demo-accounts.ts, resolved via findDemoAccounts (longest-prefix match). This is a direct consequence of the dock mounting once, above every page — a page can no longer hand its accounts down as a prop to something already mounted above it in the tree. Where a path matches nothing in the registry (the website, a dashboard), the Sign in tab is not rendered at all rather than shown empty. .claude/rules/portal-login-demos.md now points at the registry as the source of truth instead of being one" },
      { kind: "Added", text: "AppEntry.group gained \"Reports\", carrying the two QC report pages (SCW Design QC, E-Utthan Admin QC) into the Apps tab's search alongside Website / Portals / Resources" },
      { kind: "Fixed", text: "AppSwitcherPanel's CSS lived only in the old AppSwitcher shell's stylesheet. Once the panel could be mounted by something else, DemoDock's Apps tab would have rendered completely unstyled — caught before it shipped, and the styles now live in their own app-switcher-panel.css so they load wherever the panel is used" },
      { kind: "Fixed", text: "4 of the 8 login pages (NMBA admin, NMBA treatment-centre, PM-AJAY, SMILE Admin) drove DemoFab's onFill prop directly and had no demo:fill listener at all. DemoDock only ever dispatches that event — without a listener, pressing Use on those four pages would have silently done nothing. A listener was added to each" },
      { kind: "Fixed", text: "The NMBA treatment-centre login — a Project Id + OTP flow, not the admin mobile-number form — was matching the broader /portals/nmba prefix and would have inherited the admin accounts. Gave it its own /portals/nmba/treatment-centre registry entry so the longest-prefix match resolves it correctly" },
      { kind: "Fixed", text: "npm test -w @mosje/design-system had never run in CI — the Design System Quality workflow only ever executed the tokens package's tests, so a broken design-system unit test could merge unnoticed. Added it as a CI step, and widened the package's own tsconfig include, which had stopped at index.ts/tokens.ts/components/** and left demo/, foundations/ and utils/ silently untypechecked" },
      { kind: "Fixed", text: "The Figma library was publishing 322 WCAG contrast guarantees that nothing had measured. They came from a substring scan of the token path, so any name containing a rung word got a class: 192 sat on Tier-3 Action/* variables, which have no prominence slot at all; Background/Brand/Primary/Base read “Guarantees ≥4.5:1, body and heading text” because primary is a brand VARIANT that happens to spell an ink rung; and motion/duration-base — a number — read “no contrast guarantee, decorative fills only”. Of the 41 claims that were measurable, 23 were false" },
      { kind: "Changed", text: "A contrast class is now a MEASUREMENT, not an assertion. Every applicable token is resolved through its alias chain, composited if translucent, and measured against its own surface across every brand — worst case wins, because a token is only as accessible as its least accessible brand. The description states the number it measured, and adds the permission sentence only where the threshold is actually met, so the true half is unconditional and the claim is conditional" },
      { kind: "Fixed", text: "Text and icon tokens are never silent. The ink ladder has no rung for the canonical value, so text/brand/primary/base and text/link/brand/default — brand body text and the text link, the two tokens that most obviously must be AA — fell through it entirely while a decorative fill got a paragraph. Where the ladder says nothing, WCAG 1.4.3 for text and 1.4.11 for icons apply, attributed to WCAG rather than to a rung" },
      { kind: "Added", text: "prominence-contract.test.mjs — spec §9.2, which had never been written. The only test on the subject asserted that the threshold TABLE was sorted; nothing had ever compared a claim to a colour. It now fails if any published sentence is unmeasured, disagrees with its own number, claims a threshold it misses, or lands on a non-colour, Tier-3 or disabled token. Hand-written ≥N:1 claims in authored prose are checked too. Watched failing on both original defects before being trusted" },
      { kind: "Added", text: "A shortfall ledger of 19 tokens whose rung name promises more than the token delivers — published in the Figma description rather than suppressed, and pinned in CI so it may only shrink. 17 are Background/* tonal chips measured against the page, where the fill ladder's ≥3:1 is the wrong requirement (WCAG 1.4.11 governs boundaries that identify a control, not quiet fills); 2 are Border/Neutral, where ≥3:1 genuinely is the bar. The likely fix is the ladder, not the colours — that decision is now visible instead of hidden behind a false guarantee" },
      { kind: "Changed", text: "The WCAG maths moved to build/wcag.mjs and test/lib/contrast.mjs re-exports it, so the build and the gates share one implementation rather than two copies staying accidentally identical. Nothing renders differently: this is a description-only change, and visual-contract.test.mjs proves no token moved" },
    ],
  },
  {
    version: "v0.11.5",
    date: "2026-08-10",
    changes: [
      { kind: "Changed", text: "The appearance axis is gone. `data-theme` (light/dark/hc) no longer exists: Figma's Theme collection is single-mode and tokens.css emits no [data-theme] block at all. The UX4G accessibility widget is the estate's single canonical dark and high-contrast mechanism \u2014 it applies its own .dark-mode class to <html> and never read data-theme, so this was a second parallel mechanism nothing consumed. Removing it removes confusion, not capability" },
      { kind: "Fixed", text: "Verified a true no-op: three selector contexts disappeared and ZERO tokens changed value in any surviving context. [data-theme=light] went too, correctly \u2014 it existed only to re-assert base values when returning from dark, and :root already holds them" },
      { kind: "Removed", text: "Three switches that would now toggle nothing: the gate-chrome light/dark toggle, the design-system docs-header toggle and the playground theme control \u2014 plus both theme modules, the no-flash init script and the orphaned CSS. Storybook's theme picker is gone, and its brand labels are corrected from the pre-rename \u201cBlue \u00b7 Light\u201d / \u201cBlue \u00b7 Dark\u201d to \u201cBlue\u201d / \u201cNavy\u201d" },
      { kind: "Fixed", text: "Figma alpha values: 33 were stored as 8-bit n/255 steps (0.47843137 = 122/255) while their siblings used clean percentages \u2014 one role, two conventions. All normalised to two decimals; RGB untouched, largest shift 0.16 percentage points. The other 156 fractional-looking alphas are NOT a defect \u2014 that is float32 storage, and rewriting them would change nothing" },
      { kind: "Changed", text: "The obsolete contrast test was replaced rather than deleted: mode-contrast.test.mjs now asserts the axis STAYS removed, and says that if it is ever wanted back, the contrast sweep must return in the same change. Deleting it would have let a future re-add ship an untested accessibility mode" },
    ],
  },
  {
    version: "v0.11.4",
    date: "2026-08-10",
    changes: [
      { kind: "Fixed", text: "The Figma exporter had a silent catch-all: anything under font/ that it did not recognise was filed as font-family/{name}. That labelled 13 px-valued numbers as font-family/size-100, font-family/lineHeight-100 and so on \u2014 a font-family folder full of sizes. font/size/* and font/lineHeight/* now map to font-size/ and line-height/, and the fallback THROWS instead of guessing. A catch-all that renames what it does not recognise is worse than a crash, because it ships" },
      { kind: "Added", text: "Figma library: created the 61 variables the code emitted but the library never had \u2014 Spacing 15 to 49 (the inline/stack/padding/section semantic scale) and Typography 79 to 106 (font families, the raw size and line-height scale, and the type/* role aliases). Literals are created before the aliases that reference them, and every variable gets an explicit scope rather than ALL_SCOPES" },
      { kind: "Changed", text: "Figma library: 28 in-place renames so it matches the shipped grammar \u2014 27 in Theme (prominence /Default to /Base, Text/Link/Default to Text/Link/Brand) and easing-in-out to easing-inOut in Motion. Renamed, never recreated, so every variable id survived and any binding follows" },
      { kind: "Fixed", text: "Color 149 to 141: retired 8 leftovers the code does not emit and nothing uses. Text/Secondary was NOT retired \u2014 it is bound on 12 nodes, and the pre-delete check caught that after an earlier scan reported it unused. Figma's findAll(predicate) proved unreliable, returning 0 and 12 for the same query minutes apart, so every deletion now needs three independent traversals to agree" },
      { kind: "Changed", text: "The 24 remaining Color names beyond the payload are deliberate: they are Figma-native primitives designers bind to directly (Text/Dark alone has 1,143 bindings) and the exporter withholds them on purpose. Five type/*-weight variables stay absent from Figma because Figma models font weight as a STRING style name and the code as a numeric FLOAT \u2014 Figma rejects an alias across resolved types, so that is a modelling decision, not a gap to paper over" },
    ],
  },
  {
    version: "v0.11.3",
    date: "2026-08-10",
    changes: [
      { kind: "Added", text: "Storybook now documents all 69 exported components, up from 10. The 59 entries in apps/storybook/coverage-baseline.json are paid off and the backlog array is empty, so the coverage ratchet is at 100% — a new component without a story now fails the build outright rather than being absorbed as declared debt" },
      { kind: "Added", text: "Every story is written against real MoSJE content — scheme names, districts, beneficiary counts, the three-tier approval chain — and each file's doc comment leads with the distinction that decides whether the component is the right one, not a restatement of its props. Modal vs SideSheet is about keeping list context visible rather than size; Skeleton vs Loader is about whether the shape is known; SlaProgressIndicator is not a Progress bar because a paused clock must render neutral" },
      { kind: "Added", text: "check:storybook:parity — the coverage gate only asks whether a story EXISTS, which misses the two ways it goes stale afterwards. Parity fails when a prop is added that no story mentions, and when a story still references an export the barrel no longer has. A story documenting a renamed or deleted component is worse than a missing one: a reviewer reads it and believes it" },
      { kind: "Added", text: "check:storybook:smoke — mounts every story in Chromium and fails on a thrown error, a console error, or an empty canvas. A story that satisfies the counter but throws in the canvas is worse than no story, because the gate then reports green while the documentation is blank. It reuses the Playwright already installed for test:e2e rather than adding @storybook/test-runner, which would bring Jest and a second Playwright pin for one page visit per story" },
      { kind: "Added", text: "Both new checks run in the Design System Quality workflow, and both were watched failing before they were trusted — a prop added to Badge and mentioned nowhere, an EmptyState renamed in the barrel with its story left behind, and a story that throws. A check nobody has seen fail is indistinguishable from one that cannot" },
      { kind: "Fixed", text: "Writing the stories surfaced 23 props across 12 components that no story mentioned — Button's href and icon slots, Card's variant and orientation, Radio's card variant, Alert's action and timestamp, SiteHeader's skipTo and accessibility-statement props among them. All are now documented; the parity gate is what found them and is what stops them recurring" },
      { kind: "Fixed", text: "Storybook's preview now loads icons.css. An app imports it once in its root layout, so without it every <Icon> in a story rendered its name as literal text — the component looking broken when only the harness was" },
    ],
  },
  {
    version: "v0.11.2",
    date: "2026-08-10",
    changes: [
      { kind: "Fixed", text: "Figma library: four variable names existed in both the Color and Theme collections, left over from an earlier hand-migration. All 504 live bindings were rebound onto the Theme copies and the Color leftovers removed. Focus/Ring stays in both deliberately \u2014 it is a brand-source companion that the appearance layer consumes" },
      { kind: "Fixed", text: "Two of those leftovers were mislabelled: Color's Background/Brand/Primary/Subtle held ramp step 50, which the prominence ladder calls `base`, not `subtle`, and Strong held Source rather than 600. The Theme copies already carried the correct ladder values, so retiring the leftovers brings 90 elements on the Accessibility Bar onto the values the code has always emitted. Figma and dist/tokens.css now agree token-for-token" },
      { kind: "Fixed", text: "That correction improves contrast rather than costing it: white text on the brand bar goes from 4.64:1 to 6.30:1 in Blue and 12.61:1 to 14.22:1 in Navy, both comfortably past WCAG 2.1 AA" },
      { kind: "Fixed", text: "The figma-live.json snapshot recorded the Theme collection as empty while the live library held 374 variables, so every Theme variable was reported as `new` in the import delta. Refreshed against the live library: the delta is now 27 new / 347 existing" },
    ],
  },
  {
    version: "v0.11.1",
    date: "2026-08-10",
    changes: [
      { kind: "Changed", text: "`default` now means exactly one thing. It had been sitting in three slot dictionaries at once \u2014 prominence, state and the link variant \u2014 so the token parser bound it greedily to the first one and never reached the others: text/link/visited/default parsed as a PROMINENCE and silently threw away the state it was spelling. The prominence canonical is now `base` (--sa-bg-neutral-base, --sa-text-status-error-base) and the link variant is now `brand` (--sa-text-link-brand-default). `default` stays the canonical state" },
      { kind: "Changed", text: "Nothing renders differently. This was a rename, not a redesign: all 27 moved token names resolve to byte-identical values in all 7 selector contexts (:root, light/dark/hc, brand, density, portal surface). The --ds-* names your code actually uses did not change at all \u2014 they were retargeted at the new canonical names" },
      { kind: "Added", text: "A visual-contract test that resolves every var() chain in the generated CSS down to a literal, per selector block, and pins all 8,393 of them. A rename must be declared, and the test then proves the old name's old value equals the new name's new value \u2014 so a rename that changes what renders cannot pass quietly" },
      { kind: "Fixed", text: "The Tailwind v4 @theme export (@mosje/tokens/tailwind-v4) aliased 111 custom properties that do not exist. It hand-rolled its target names as --sa-{path}, which drops the tier marker, so every Tier-1 entry pointed at --sa-color-* while the sheet declares --sa-ref-color-* \u2014 every colour utility built on them resolved to nothing. Names now come from toCssName/tierOfFile like everywhere else. No utility name changed; nothing in the estate imports this file yet, which is why it went unnoticed since v0.11.0" },
      { kind: "Added", text: "A UX4G parity contract that pins the combined tokens.css + ux4g.css sheet. ux4g.css emits --sa-* overrides inside its colour-mode blocks from a hand-typed name table \u2014 that is how the UX4G palette repaints SAMAVESH tokens \u2014 and nothing pinned them, so a rename that forgot that table would have left the UX4G colour modes silently failing to repaint --sa-bg-neutral-base while every other test stayed green" },
      { kind: "Added", text: "A theming-axis invariant: a page setting data-brand and data-theme together must resolve to a value one of those axes actually declares. 41 properties are declared by both axes, so neither single-axis test can say who wins. It holds today, so combinations are asserted rather than pinned" },
      { kind: "Added", text: "A slot-disjointness guard that fails the build if any word becomes reachable in two slots of the same token path. Two pre-existing ambiguities are pinned rather than fixed (primary/secondary/tertiary as both a brand variant and an ink prominence; visited as both a link variant and a state) \u2014 both are recorded in the spec and cost a token rename to resolve" },
    ],
  },
  {
    version: "v0.11.0",
    date: "2026-08-07",
    changes: [
      { kind: "Changed", text: "Brand axis renamed: data-color-mode \u2192 data-brand, and blue-light/blue-dark \u2192 blue/navy (ux4g-light/ux4g-dark \u2192 ux4g/ux4gdeep). The old ids read as light and dark THEMES \u2014 they never were: both brands render on light surfaces and differ only in palette. data-color-mode still matches as a deprecated alias selector and old ids are normalised on read, so existing markup and persisted cookies keep working" },
      { kind: "Fixed", text: "Skeleton shimmer darkened itself under the navy brand on a LIGHT surface, because a [data-color-mode$=\"-dark\"] suffix selector caught a brand id. It now keys off data-theme alone \u2014 a bug the old naming directly caused" },
      { kind: "Added", text: "Tier markers in every token name: --sa-ref-* (reference, banned in app code), --sa-* (system), --sa-cmp-* (component). Tier derives from the source file and is enforced by tests, not convention" },
      { kind: "Added", text: "Canonical Tier-2 namespace on the new grammar \u2014 111 tokens including 9 icon roles and a 6-token link set with visited, neither of which existed before. Values are identical to the legacy layer, proven in every theme and brand block" },
      { kind: "Added", text: "Generated Action matrix (288 component tokens) replacing filter: brightness() for hover and active. Naming each state made them testable, which found two live WCAG AA failures on the danger ramp" },
      { kind: "Added", text: "Figma exporter emits 898 variables across 5 collections with modes, types and 1,456 preserved alias edges, replacing the flat value dump; a round-trip test now fails the build if code and Figma drift" },
    ],
  },
  {
    version: "v0.10.0",
    date: "2026-08-07",
    changes: [
      { kind: "Added", text: "Tooltip — hover/focus hint that meets WCAG 1.4.13 (Escape dismisses, the bubble is hoverable, it never times out). Portalled, so a Card or DataTable's overflow can't clip it" },
      { kind: "Added", text: "Skeleton, SkeletonText and SkeletonRow — the loading placeholders design.md already required but the system didn't ship" },
      { kind: "Added", text: "Label — standalone form label for controls that aren't wrapped in FormField" },
      { kind: "Added", text: "LiveRegion + useLiveRegion — announces async results (\"12 records exported\") to screen readers; re-announces a repeated message instead of going silent" },
      { kind: "Added", text: "SectionTitle — the shared eyebrow/heading/count/actions row, so section headers stop being hand-rolled per page" },
      { kind: "Added", text: "Input gains leftIcon and rightIcon; a bare Input still renders with no wrapper, so existing layouts are untouched" },
      { kind: "Changed", text: "The estate is off lucide-react entirely — every icon is now Material Symbols Rounded through <Icon>, as CLAUDE.md and design.md always specified. 668 icons across 239 files; the dependency is gone" },
      { kind: "Changed", text: "SidebarNavItem.icon is a Material Symbols name, not a component — nav configs stay serialisable data" },
      { kind: "Changed", text: "shadcn and Radix are fully removed: 13 Radix packages plus class-variance-authority dropped, and smile-admin's 18-component island retired onto the design system" },
      { kind: "Fixed", text: "CardTitle rendered at 32px — it referenced the Headline 1 alias while its own fallback said 20px. Now bound to the canonical Title 1 role" },
      { kind: "Fixed", text: "Icon accepts a style prop, merged after its own font-size and variation axes" },
      { kind: "Fixed", text: "The AppSwitcher's design system and Storybook entries moved from the dev-only \"Dev\" group to \"Resources\" and are now visible everywhere — gating them on NODE_ENV hid them from the BAs, QAs and designers who most need them. AppSwitcher's devMode prop is deprecated and inert" },
      { kind: "Fixed", text: "/storybook no longer dead-ends on a bare DNS_HOSTNAME_RESOLVED_PRIVATE 404 in production. Storybook is not part of the deployment, so unless ZONE_DS_URL points somewhere public the route now serves the \"app not running\" page instead of proxying to loopback" },
    ],
  },
  {
    version: "v0.9.0",
    date: "2026-08-06",
    changes: [
      { kind: "Added", text: "SLA Progress Indicator — Right to Service Act deadline tracking" },
      { kind: "Added", text: "Identity inputs — Aadhaar, OTP and PAN, with per-format masking and validation" },
      { kind: "Added", text: "ApprovalTimeline, DeclarationCheckbox and GeoPhotoInput" },
      { kind: "Added", text: "PasswordInput — password field with a reveal toggle; use it for every password field rather than an Input plus a hand-rolled eye, which is how the submit-on-toggle bug and the missing accessible name get reintroduced" },
      { kind: "Changed", text: "Buttons now give on press (scale 0.97, reduced-motion aware) — colour alone told you the button noticed, not that it was listening" },
      { kind: "Added", text: "UX4G 3.0 parity layer — semantic spacing roles, display font family, shadow ramp" },
      { kind: "Changed", text: "Status colours moved to the 700 ramp step: success, warning and danger text on their tonal chips now meet WCAG AA (all four pairs previously shipped between 2.6:1 and 3.8:1)" },
      { kind: "Changed", text: "The brand contrast gate now covers status-on-tonal pairings, so this class of regression fails the build" },
      { kind: "Changed", text: "Type sizes are expressed in rem so they honour the browser's font-size setting" },
      { kind: "Fixed", text: "Contrast swept across every colour mode and theme, not just the default" },
    ],
  },
  {
    version: "v0.8.0",
    date: "2026-07-16",
    changes: [
      { kind: "Added", text: "NHAPOA (SAMBAL) portal built on the shared system — all 8 roles" },
      { kind: "Changed", text: "Token-lint gate made real: recursive glob, and 48 raw hex values tokenised" },
      { kind: "Fixed", text: "--ds-* aliases re-resolved inside [data-color-mode] blocks" },
    ],
  },
  {
    version: "v0.7.0",
    date: "2026-06-25",
    changes: [
      { kind: "Added", text: "White-label brand packs, gated by an automated WCAG contrast test" },
      { kind: "Added", text: "Data-visualisation layer — categorical, sequential and diverging chart tokens" },
      { kind: "Added", text: "Lightbox, MediaGalleryInput, FormCard and Tabs" },
      { kind: "Added", text: "Page patterns documentation" },
      { kind: "Changed", text: "Upgraded to Tailwind v4 with Turbopack" },
    ],
  },
  {
    version: "v0.6.0",
    date: "2026-06-13",
    changes: [
      { kind: "Added", text: "Portal component variants — Badge v2, Chip dropdown, Radio Card, Search sm" },
      { kind: "Added", text: "Info palette, corrected neutrals, responsive type scale" },
      { kind: "Changed", text: "Portal DS merged into SAMAVESH tokens; t-shirt size scale adopted" },
      { kind: "Changed", text: "design-system package reorganised by functional category" },
      { kind: "Fixed", text: "Portal-wide spacing — the --ds-space-* tokens were never defined" },
    ],
  },
  {
    version: "v0.5.0",
    date: "2026-06-12",
    changes: [
      { kind: "Added", text: "AppSwitcher with searchable panel, color-mode swatches, dev-mode toggle" },
      { kind: "Added", text: "FormField, Input, Textarea, Select with full accessibility wiring" },
      { kind: "Added", text: "Badge, Chip, Card, Button, Alert, EmptyState, Avatar, Loader" },
      { kind: "Added", text: "SAMAVESH documentation portal (this site)" },
      { kind: "Added", text: "Interactive Playground with react-live and prop controls" },
      { kind: "Changed", text: "tokens.css is now generated by @mosje/tokens Style Dictionary — never hand-edit" },
      { kind: "Fixed", text: "CSS comment syntax breaking token vars" },
    ],
  },
  {
    version: "v0.4.0",
    date: "2026-06-10",
    changes: [
      { kind: "Added", text: "ZoneSwitcher / AppSwitcher cross-zone navigation component" },
      { kind: "Added", text: "Multi-zone hub routing (dosje, smile-admin, pm-ajay, eutthan-admin)" },
    ],
  },
  {
    version: "v0.3.0",
    date: "2026-06-08",
    changes: [
      { kind: "Added", text: "Form atoms (Input, Textarea, Select, FormField, Checkbox, Radio, Toggle)" },
      { kind: "Added", text: "ColorModeProvider + ColorModeSwitcher for brand-axis theming" },
      { kind: "Added", text: "Storybook 8 + a11y addon + theme/density toolbar" },
    ],
  },
  {
    version: "v0.2.0",
    date: "2026-06-07",
    changes: [
      { kind: "Added", text: "@mosje/tokens DTCG token pipeline with Style Dictionary v4" },
      { kind: "Added", text: "3-tier token model (Primitive / Semantic / Component)" },
      { kind: "Added", text: "Light/dark/high-contrast themes + comfortable/compact density" },
      { kind: "Added", text: "--ds-* backward-compatible CSS var contract with 50-token snapshot test" },
    ],
  },
  {
    version: "v0.1.0",
    date: "2026-06-06",
    changes: [
      { kind: "Added", text: "Initial design system package with Button, Card, Badge, Chip, Avatar" },
      { kind: "Added", text: "Brand tokens (gov-blue, saffron, navy, gov-yellow) in tokens.css" },
    ],
  },
];

const KIND_COLOR: Record<ChangeEntry["kind"], string> = {
  Added: "var(--sa-color-status-success)",
  Changed: "var(--sa-color-status-info)",
  Fixed: "var(--sa-color-brand-saffron)",
  // Taking something away is worth its own badge: a reader scanning for "why did that control
  // disappear" should not have to read a paragraph filed under "Changed" to find out.
  Removed: "var(--sa-color-status-danger)",
  Breaking: "var(--sa-color-status-danger)",
};

export default function ChangelogPage(): React.JSX.Element {
  return (
    <>
      {/* ── Page header ───────────────────────────────────────── */}
      <header className="docs-page-header">
        <div className="docs-page-header__text">
          <h1 className="docs-page-header__title">Changelog</h1>
          <p className="docs-page-header__desc">
            A complete, dated history of the SAMAVESH design system. Each entry is
            tagged <strong>Added</strong>, <strong>Changed</strong>, or{" "}
            <strong>Fixed</strong> so you can see at a glance what a release means
            for your work. Versions follow{" "}
            <a href="/design-system/resources/governance#semver">semantic versioning</a>.
          </p>
        </div>
      </header>

      <section className="docs-section">
        <Callout type="info" title="How to read this">
          While we are pre-1.0, minor versions (the middle number) may include
          breaking changes. Watch the <strong>Changed</strong> entries — those are
          the ones most likely to need action when you upgrade.
        </Callout>

        <div
          style={{
            marginTop: "var(--sa-stack-24)",
            display: "flex",
            flexDirection: "column",
            gap: "var(--sa-stack-32)",
          }}
        >
          {RELEASES.map((release) => (
            <article
              key={release.version}
              id={release.version.replace(/\./g, "-")}
              style={{ scrollMarginTop: "var(--docs-anchor-offset)" }}
            >
              {/* Release heading */}
              <div
                style={{
                  display: "flex",
                  alignItems: "baseline",
                  gap: "var(--sa-stack-12)",
                  flexWrap: "wrap",
                  paddingBottom: "var(--sa-padding-12)",
                  borderBottom: "1px solid var(--sa-border-neutral-subtle)",
                  marginBottom: "var(--sa-stack-16)",
                }}
              >
                <h2
                  style={{
                    fontSize: "var(--sa-type-headline-2-size)",
                    fontWeight: 700,
                    color: "var(--sa-text-neutral-base)",
                  }}
                >
                  {release.version}
                </h2>
                <span
                  style={{
                    fontSize: "var(--sa-type-body-2-size)",
                    color: "var(--sa-text-neutral-subtle)",
                  }}
                >
                  {release.date}
                </span>
                {release.current ? (
                  <span
                    style={{
                      fontSize: "var(--sa-type-body-3-size)",
                      fontWeight: 600,
                      color: "var(--sa-on-bg-brand-primary-bolder)",
                      background: "var(--sa-bg-brand-primary-bolder)",
                      padding: "var(--sa-padding-2) var(--sa-padding-8)",
                      borderRadius: "var(--sa-shape-6)",
                    }}
                  >
                    Current
                  </span>
                ) : null}
              </div>

              {/* Change list */}
              <ul
                style={{
                  listStyle: "none",
                  margin: 0,
                  padding: 0,
                  display: "flex",
                  flexDirection: "column",
                  gap: "var(--sa-stack-12)",
                }}
              >
                {release.changes.map((change, i) => (
                  <li
                    key={i}
                    style={{
                      display: "flex",
                      gap: "var(--sa-stack-12)",
                      alignItems: "flex-start",
                    }}
                  >
                    <span
                      style={{
                        flexShrink: 0,
                        minWidth: 64,
                        textAlign: "center",
                        fontSize: "var(--sa-type-body-3-size)",
                        fontWeight: 700,
                        color: "var(--sa-on-bg-brand-primary-bolder)",
                        background: KIND_COLOR[change.kind],
                        padding: "var(--sa-padding-2) var(--sa-padding-8)",
                        borderRadius: "var(--sa-shape-6)",
                      }}
                    >
                      {change.kind}
                    </span>
                    <span
                      style={{
                        fontSize: "var(--sa-type-body-2-size)",
                        color: "var(--sa-text-neutral-base)",
                        lineHeight: "var(--sa-type-body-2-lh)",
                      }}
                    >
                      {change.text}
                    </span>
                  </li>
                ))}
              </ul>
            </article>
          ))}
        </div>
      </section>
    </>
  );
}
