// StatsCard was a local duplicate of the design-system MetricCard. It now
// aliases the shared component so there is one definition across all portals.
export { MetricCard as StatsCard } from "@mosje/design-system";
