{
  "$description": "Declarative source for the Tier-3 Action matrix. Expanded into DTCG tokens by build/generate-component-tokens.mjs — NEVER hand-write the resulting token names. Spec §7.3.",
  "$notes": [
    "Ramp steps, not filter: brightness(). button.css currently computes hover/active with",
    "filter: brightness(0.95) — which means those colours cannot be contrast-tested, cannot be",
    "expressed as Figma variables, and drift unpredictably under dark/HC themes. Naming the",
    "step makes each state a designed, testable value.",
    "",
    "`success` is an intent here but not in UX4G's Action matrix. SAMAVESH's Button ships one,",
    "so the matrix describes the component we have (spec §7.3, corrected by the Phase 1 slice)."
  ],
  "intents": {
    "brand": {
      "scale": "primaryScale",
      "onFill": "{color.neutralScale.0}"
    },
    "success": {
      "scale": "successScale",
      "onFill": "{color.neutralScale.0}"
    },
    "destructive": {
      "scale": "dangerScale",
      "onFill": "{color.neutralScale.0}"
    },
    "neutral": {
      "scale": "neutralScale",
      "onFill": "{color.neutralScale.0}"
    }
  },
  "$stepNotes": {
    "fill": "600 not 500 — 500 fails 4.5:1 against white for the label on several ramps. See test/prominence-contract.test.mjs.",
    "disabled": "A flat neutral fill, NOT the intent colour at reduced opacity: a washed-out red still reads as an error affordance."
  },
  "variants": {
    "primary": {
      "$comment": "filled",
      "default": {
        "bg": "600",
        "text": "onFill",
        "border": "600"
      },
      "hover": {
        "bg": "700",
        "text": "onFill",
        "border": "700"
      },
      "active": {
        "bg": "800",
        "text": "onFill",
        "border": "800"
      },
      "disabled": {
        "bg": "neutral200",
        "text": "neutralDisabled",
        "border": "neutral200"
      }
    },
    "secondary": {
      "$comment": "outlined",
      "default": {
        "bg": "transparent",
        "text": "700",
        "border": "300"
      },
      "hover": {
        "bg": "50",
        "text": "700",
        "border": "400"
      },
      "active": {
        "bg": "100",
        "text": "800",
        "border": "500"
      },
      "disabled": {
        "bg": "transparent",
        "text": "neutralDisabled",
        "border": "neutral200"
      }
    },
    "tertiary": {
      "$comment": "text",
      "default": {
        "bg": "transparent",
        "text": "700",
        "border": "transparent"
      },
      "hover": {
        "bg": "50",
        "text": "700",
        "border": "transparent"
      },
      "active": {
        "bg": "100",
        "text": "800",
        "border": "transparent"
      },
      "disabled": {
        "bg": "transparent",
        "text": "neutralDisabled",
        "border": "transparent"
      }
    },
    "tonal": {
      "$comment": "soft fill, dark label",
      "default": {
        "bg": "100",
        "text": "800",
        "border": "100"
      },
      "hover": {
        "bg": "200",
        "text": "800",
        "border": "200"
      },
      "active": {
        "bg": "300",
        "text": "900",
        "border": "300"
      },
      "disabled": {
        "bg": "neutral200",
        "text": "neutralDisabled",
        "border": "neutral200"
      }
    }
  },
  "$overrideNotes": [
    "Per-intent step overrides. The four ramps are NOT perceptually equivalent at equal steps,",
    "so one step table cannot serve all of them. Entries here are found by",
    "test/action-contrast.test.mjs, not by eye.",
    "",
    "REMOVED 2026-08-11 — the whole `destructive` block, because the defect it worked around is",
    "gone. It read: white on dangerScale.600 (#d64539) is 4.40:1, just under AA, so the filled",
    "progression shifts up one step to 700/800/900; and dangerScale.800 on 200 is 4.02:1, so",
    "the tonal hover text shifts to 900. Both were symptoms of a danger ramp anchored at the",
    "wrong rung — #ec5042 is L* 64, inside the dead zone where no ink reaches 4.5:1. Re-anchored",
    "at 400 (see build/brand-ramps.mjs) the same rungs measure 6.68:1 and 6.10:1, so destructive",
    "now uses the SAME step table as every other intent, which is what a shared table is for.",
    "A workaround kept past its cause is just an inconsistency nobody can explain.",
    "",
    "  brand/tonal/* — added 2026-08-11 with the regenerated ramps. The Navy brand runs DARKER",
    "    than Blue by design (the two are only 9 degrees apart in hue, so lightness is the only",
    "    thing telling them apart), which makes primaryScale.300 a MID tone in Navy rather than a",
    "    tint: navy 900 on navy 300 measured 4.33:1. The tonal progression therefore shifts down a",
    "    step to 50/100/200 for the brand intent only — the other three ramps are brand-invariant",
    "    and keep their headroom at 100/200/300.",
    "",
    "  neutral/tertiary/{default,hover}/text — added 2026-08-25. On every OTHER ramp, 700 is a",
    "    saturated hue that could not be mistaken for body copy. On the neutral ramp 700 IS",
    "    `text/muted` (#3a3d41) — the exact ink disclaimers and secondary prose use. A chrome-less",
    "    control painted in the muted body ink is indistinguishable from the paragraph beside it,",
    "    which is precisely what happened in the chatbot footer: the reset control came out the",
    "    same colour as the note it sits next to. 800 is the body-text ink, so the control sits a",
    "    step ABOVE muted instead of level with it. Holding text constant across default/hover",
    "    also matches button.css, where `--_color` never changes and the state is carried",
    "    entirely by the background wash."
  ],
  "overrides": {
    "neutral": {
      "tertiary": {
        "default": {
          "text": "800"
        },
        "hover": {
          "text": "800"
        }
      }
    },
    "brand": {
      "tonal": {
        "default": {
          "bg": "50",
          "border": "50"
        },
        "hover": {
          "bg": "100",
          "border": "100"
        },
        "active": {
          "bg": "200",
          "border": "200"
        }
      }
    }
  },
  "$inverseNotes": [
    "For a button sitting on a solid brand surface (e.g. a navy page header, the ticker bar).",
    "Modelled as a qualifier so it composes with the variants above instead of needing new",
    "variant words.",
    "",
    "THE FILL SIDE IS WHITE-ALPHA AND STAYS THAT WAY: a white fill works on any solid",
    "surface, and the intent rides on the LABEL ink (brand #004b96 vs destructive #8b1f18),",
    "which is why an inverse filled button never lost its signal.",
    "",
    "THE OUTLINED SIDE USED TO BE WHITE-ALPHA TOO, AND THAT WAS TWO BUGS IN ONE VALUE.",
    "A flat white40 border was identical for all four intents, so `danger` was",
    "indistinguishable from `brand` (finding #8) — and at 2.25:1 on the ticker bar and",
    "1.91:1 on gov-blue it was not a findable edge at all (WCAG 1.4.11). It cleared 3:1 on",
    "navy alone, which is the one brand surface the 2026-08-25 audit measured.",
    "",
    "So the border now takes the INTENT'S OWN SCALE at its lightest usable rungs:",
    "100 at rest, 50 on hover, white when pressed — brighter as the control is engaged,",
    "which is the correct direction on a dark ground. Measured on the ticker bar (#005eb9):",
    "brand 4.49, success 4.19, danger 4.14, neutral 4.72 — all clear of 3:1 with room,",
    "not by a hundredth.",
    "",
    "The TEXT deliberately stays white. A 100-rung tint as the label measures 4.49:1 on that",
    "same bar, which misses 1.4.3's 4.5 by a hundredth — so the border carries the signal",
    "and the label keeps the readability. Two jobs, two values."
  ],
  "inverse": {
    "primary": {
      "default": {
        "bg": "white",
        "text": "700",
        "border": "white"
      },
      "hover": {
        "bg": "white92",
        "text": "800",
        "border": "white92"
      },
      "active": {
        "bg": "white84",
        "text": "900",
        "border": "white84"
      },
      "disabled": {
        "bg": "white40",
        "text": "white64",
        "border": "white40"
      }
    },
    "secondary": {
      "default": {
        "bg": "transparent",
        "text": "white",
        "border": "100"
      },
      "hover": {
        "bg": "white10",
        "text": "white",
        "border": "50"
      },
      "active": {
        "bg": "white16",
        "text": "white",
        "border": "white"
      },
      "disabled": {
        "bg": "transparent",
        "text": "white40",
        "border": "white24"
      }
    }
  }
}
