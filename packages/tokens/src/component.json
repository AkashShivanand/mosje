{
  "$description": "Tier 3 — component tokens. Resolve to semantic tokens only.",
  "button": {
    "$type": "color",
    "primary": {
      "bg": {
        "$value": "{color.action.primary.default}"
      },
      "bgHover": {
        "$value": "{color.action.primary.hover}"
      },
      "text": {
        "$value": "{color.text.onPrimary}"
      }
    },
    "radius": {
      "$type": "dimension",
      "$value": "{control.radius}",
      "$description": "A button IS a control, so it follows control/radius rather than restating a rung. Value unchanged at 8px."
    }
  },
  "card": {
    "$type": "color",
    "bg": {
      "$value": "{bg.neutral.base}"
    },
    "border": {
      "$value": "{border.neutral.subtle}"
    },
    "radius": {
      "$type": "dimension",
      "$value": "{shape.12}",
      "$description": "Use for the card surface radius. shape/12 is the rung published as \"cards and panels\", which is what a card is. Raised from 8px on 2026-08-18: the token had contradicted its own role, and Card (8px) and MetricCard (12px) had already drifted apart because neither bound this token."
    }
  },
  "badge": {
    "$type": "color",
    "beta": {
      "bg": {
        "$value": "{color.brand.yellow}",
        "$description": "BETA badge background — ad-hoc accent (#ffd323), NOT a brand or status colour. Correctly-named entry point for the beta-badge colour."
      }
    },
    "dotSize": {
      "$type": "dimension",
      "$value": "6px",
      "$description": "The status/unread DOT — ONE definition for the whole estate, not one per component. Bound by the Badge's dot and by Tabs' badge; when this moves, every dot moves. It is 6 and not the on-grid 8 on purpose: beside 14px label text an 8px dot reads as a bullet rather than a signal, and 6 is what Material and Carbon both publish. A literal rather than an alias because no ref/size step is 6, and inventing one would add a scale step for a single affordance — the same reasoning layout/bar/height (46) and layout/flag/width (33) already follow. Authored in Figma first; reconciled into code 2026-08-18."
    },
    "dotSizeLg": {
      "$type": "dimension",
      "$value": "{size.8}",
      "$description": "The dot beside 16px body text — Badge size=lg, and Tabs size=L. A dot is sized against the TYPE it annotates, not against its container, which is why this steps with the type scale and not with the control height."
    }
  },
  "accessibilityBar": {
    "$description": "AccessibilityBar (UX4G/GIGW top utility bar). Component-scoped dimensions and inverse state layers that mirror the Figma master 1:1. They live in Tier 3 because they are the bar's own geometry — a 46px bar height and a 33px flag chip are not shared scale steps, and the inverse overlays only make sense on this bar's brand fill.",
    "height": {
      "$type": "dimension",
      "$description": "The bar's fixed height. Aliases layout/bar/height rather than restating 46px — the literal was a second copy of a semantic value that nothing kept in step.",
      "$value": "{layout.bar.height}"
    },
    "flagWidth": {
      "$type": "dimension",
      "$description": "The flag mark's fixed width. Aliases layout/flag/width rather than restating 33px, for the same reason as height.",
      "$value": "{layout.flag.width}"
    },
    "flagHeight": {
      "$type": "dimension",
      "$value": "{size.22}"
    },
    "dividerWidth": {
      "$type": "dimension",
      "$description": "Hairline divider width (Figma ref/border-width/hairline).",
      "$value": "{border.width.sm}"
    },
    "dividerHeight": {
      "$type": "dimension",
      "$value": "{size.20}"
    },
    "dividerColor": {
      "$type": "color",
      "$description": "Divider ink on the brand fill (Figma border/neutral/inverse-subtle, #ffffff66 = 40%). Pre-composited so consumers never hand-roll an opacity.",
      "$value": "rgba(255, 255, 255, 0.4)"
    },
    "pillSize": {
      "$type": "dimension",
      "$description": "Selected font-size pill (Figma ref/size/32).",
      "$value": "{size.32}"
    },
    "pillBg": {
      "$type": "color",
      "$description": "Selected-step pill on the bar. Aliases overlay/brand/active.",
      "$value": "{overlay.brand.active}"
    },
    "hoverBg": {
      "$type": "color",
      "$description": "Hover wash on the bar. Aliases overlay/brand/hover rather than restating the rgba — the structure the designer used in Figma, now real and grammar-conformant in code.",
      "$value": "{overlay.brand.hover}"
    },
    "stepSize": {
      "$type": "dimension",
      "$description": "A- / A+ step button hit area.",
      "$value": "{size.24}"
    },
    "launchIconSize": {
      "$type": "dimension",
      "$description": "The GoI link's launch glyph (Figma: 12px).",
      "$value": "{size.12}"
    },
    "iconButtonSize": {
      "$type": "dimension",
      "$value": "{size.28}",
      "$description": "The icon button's box. Authored in Figma first, on 2026-08-18, because the exporter was silently dropping this component and .sa-abar__icbtn was drawn at a hardcoded 28px. Reconciled into code the same day."
    }
  },
  "divider": {
    "$description": "The Divider component. Only the THICKNESS is component-scoped; the tones bind straight to border/neutral/*, because a rule's colour is a shared semantic — that reasoning is the designer's, from the hand-made Figma variable this replaces.",
    "width": {
      "$type": "dimension",
      "$value": "{border.width.sm}",
      "$description": "Divider thickness — the hairline. Bound to the component's WIDTH or HEIGHT depending on orientation, not to a stroke, which is why it needs a WIDTH_HEIGHT scope that control/border/width does not carry."
    }
  }
}
