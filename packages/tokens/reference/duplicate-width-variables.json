{
  "$schema": "binding sweep for the two duplicate 1px width variables in the SAMAVESH Figma library",
  "capturedAt": "2026-08-18",
  "fileKey": "3FF5l0SMNIwdpZrKkeyPTm",
  "method": "one-page-per-invocation",
  "coverage": {
    "pagesWalked": 65,
    "pagesProvenEmpty": 4,
    "libraryPages": 69,
    "note": "The four separator pages report children: 0, so they can hold no bindings. Every other page was walked individually — batching returns partial trees, and a partial tree here is a FALSE NEGATIVE on a delete decision."
  },
  "canonical": {
    "name": "ref/border/width/sm",
    "id": "VariableID:54801:712",
    "collection": "Static",
    "codeSyntax": "var(--sa-ref-border-width-sm)",
    "value": 1,
    "note": "The real one. Code-defined, exported by the pipeline, correct grammar. Prefer `control/border/width`, which aliases it and says what the edge is for."
  },
  "duplicates": [
    {
      "name": "ref/border-width/hairline",
      "id": "VariableID:55073:700",
      "collection": "Space",
      "codeSyntax": null,
      "hidden": true,
      "value": 1,
      "bindings": 0,
      "verdict": "DELETED",
      "why": "All 242 bindings cleared: 163 followed the Divider mains automatically, 9 were rebound directly on the Divider page, and the final 79 — every one a documentation-frame BORDER (strokeTop/Bottom/Left/RightWeight on Do/Don't cards, doc `row` frames and one `close` frame) — moved to the new Tier-2 stroke/1. Verified at 0 node bindings AND 0 variable-level aliasers before deletion; a post-delete ghost check on Layout Grid found none.",
      "boundOn": {
        "Layout Grid": 51,
        "Navbar": 12,
        "Portal Login Template": 8,
        "Accessibility Bar": 8
      },
      "howToRetire": "Done."
    },
    {
      "name": "cmp/divider/width",
      "id": "VariableID:55677:37371",
      "collection": "Space",
      "codeSyntax": null,
      "hidden": false,
      "value": 1,
      "aliases": "ref/border-width/hairline",
      "bindings": 0,
      "verdict": "RESOLVED — deleted and replaced by a code-backed token of the same name",
      "why": "Nothing in the library binds it — zero hits across all 69 pages. It was created on 2026-08-18 (id 55677:*, among the newest in the file) and never wired up: the Divider component binds `ref/border-width/hairline` directly instead. It is PUBLISHED, so it does appear in designers' pickers as a plausible-looking choice that no code token backs.",
      "alternativeToDeleting": "If a Divider component token is actually wanted, define `cmp/divider/width` in packages/tokens/src/component.json and let the exporter own it — the exporter now routes cmp/* by type, so it would land in Space and upsert onto this same id, preserving it."
    }
  ],
  "resolvedAt": "2026-08-18",
  "outcome": {
    "cmp/divider/width": "DELETED (id 55677:37371, the hand-made orphan, 0 bindings) and RECREATED as a code-backed token (id 55703:695). It is now defined in packages/tokens/src/component.json, owned by the exporter, and carries var(--sa-cmp-divider-width) with WIDTH_HEIGHT|STROKE_FLOAT scope. `divider` was registered in grammar.mjs COMPONENT — which failed LOUDLY, unlike the exporter's old silent drop.",
    "Divider variants": "All six COMPONENT variants (Orientation x Tone) plus 3 page instances rebound from ref/border-width/hairline to cmp/divider/width. Same value, 1px, zero visual change.",
    "instancePropagation": "233 of the 242 hairline bindings were Divider INSTANCES. Rebinding the six mains carried 163 of them automatically — Tabs 3/3, Side Sheet 2/2, Accessibility Bar 39/47, Navbar 48/60, Portal Login 62/70. Instances follow their main unless they carry an explicit override, which is exactly why the mains are done first.",
    "whyControlBorderWidthWasRejected": "The original recommendation was to rebind to control/border/width. On inspection that was wrong twice: it is scoped STROKE_FLOAT only, while the Divider binds its thickness to WIDTH/HEIGHT (it is a rectangle, not a stroke); and it means 'the border width of an interactive control', which a divider is not.",
    "borderWidthToken": "Added `stroke/{0,1,2,3,4}` — the Tier-2 border-width ladder that did not exist, which is why 79 borders were stranded on a deprecated hand-made variable. Value-named like space and radius, aliasing ref/border/width/*. Named `stroke` not `border` for the same reason corner radius is called `shape`: a Tier-2 `border/width/*` would self-reference the Tier-1 `border.width.*` it aliases. Scoped STROKE_FLOAT + WIDTH_HEIGHT, because a rule drawn as a rectangle sizes its thickness with width/height rather than a stroke."
  },
  "status": "CLOSED — both duplicates retired"
}
