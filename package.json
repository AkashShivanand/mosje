{
  "name": "mosje",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "apps/portals/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "prepare": "husky || true",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "kill-ports": "bash scripts/kill-dev-ports.sh",
    "predev": "npm run kill-ports",
    "dev": "concurrently --restart-tries 3 --restart-after 2000 --prefix-colors \"blue,cyan\" -n \"hub,storybook\" \"bash -c 'cd apps/hub && exec ./node_modules/.bin/next dev --turbopack -p 3000'\" \"bash -c 'cd apps/storybook && exec env NODE_PATH=$(pwd)/node_modules ../../node_modules/.bin/storybook dev -p 6006 --ci'\"",
    "dev:hub": "bash -c 'cd apps/hub && exec ./node_modules/.bin/next dev --turbopack -p 3000'",
    "dev:storybook": "bash -c 'cd apps/storybook && exec env NODE_PATH=$(pwd)/node_modules ../../node_modules/.bin/storybook dev -p 6006'",
    "check": "npm run check:hub",
    "check:hub": "npm --prefix apps/hub run typecheck",
    "lint": "npm run lint:hub",
    "lint:hub": "npm --prefix apps/hub run lint",
    "test": "npm test -w @mosje/design-system && npm test --prefix apps/hub"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.7",
    "stylelint": "^16.26.1"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "puppeteer-core": "^25.3.0"
  }
}
